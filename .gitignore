.claude/
doc/
MS_connect/

# 요청 백로그 (로컬 전용 — backlog Skill, .claude/skills/backlog/)
.backlog/

# 비밀값·환경 (CLAUDE.md §1·§11-A)
.env
.env.*
!.env.example
*.key
*.pem
appsettings*.json
secrets/

# 의존성·빌드 산출물
node_modules/
dist/
build/
.next/

# Python 캐시 (_build_html.py 실행 시 생성)
__pycache__/
*.pyc

# ERP 분석 출력 (실전표 데이터 — 로컬 전용, 커밋 금지)
10_ERP_DB연계/etl/_analysis_out/

# 배포·인프라 도구
.vercel/
supabase/.temp/
supabase/.branches/

# MCP 로컬 설정 (Supabase PAT 포함 — 커밋 금지)
.mcp.json

# 그룹웨어 화면 자동화 증적 스크린샷 — 직원 정보가 찍히므로 커밋하지 않는다
10_ERP_DB연계/etl/_gw_shots/
