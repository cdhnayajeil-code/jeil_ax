# 03. 변경관리 CHANGELOG + 문서 수명주기

> **관리: 최동혁** · 프로젝트 전체의 의미 있는 변경을 한 줄씩 기록한다.
> 기록 단위: **일자 · 범위 · 대상 · 내용**. 코드 커밋과 별개로, "무엇이 왜 바뀌었나"를 비개발자도 읽을 수 있게 남긴다.
> 세부 코드 변경은 git 이력, 운영현황 변경은 `이관/00_현황스냅샷 §9`, 협력사 요구사항은 `08/00_종합정리_요구사항이력`에 둔다(중복 금지 — 여기는 프로젝트 레벨 요약).

---

## 1. 문서 수명주기 정책

각 문서는 `00_문서대장`에서 아래 상태로 관리한다.

| 상태 | 의미 | 갱신 빈도 |
|---|---|---|
| **Draft** | 작성 중·검토 전 | 잦음 |
| **Active** | 현행 기준·운영 중 | 변경 시 |
| **Maintained** | 지속 갱신 대장(현황·진행·CHANGELOG) | 수시 |
| **Reference** | 결정 완료·참고용(변경 드묾) | 드묾 |
| **Archived** | 대체됨·보존만 | 없음 |

- 문서를 **이동·이름변경·폐기**하면 `00_문서대장` + 본 CHANGELOG를 함께 갱신한다.
- 폐기 대신 **Archived 표시 + 대체 문서 링크**를 우선한다(이력 보존).

---

## 2. 변경 이력

| 일자 | 범위 | 대상 | 내용 | 작성 |
|---|---|---|---|---|
| 2026-08-27 | 회계·ERP연계 | `public.gl_template`(데이터 정리 1행) | **`TPL-0001` 의 ERP 케이스 코드 `TEST` 제거(관리자 지시).** 화면에서 케이스 코드 입력칸을 걷어낸 뒤 라이브 점검에서 드러난 잔여값 — 「(주)쿠콘 {YY}년{M}월분 국세청 스크래핑외」(부서 공용·42회 사용)에 `erp_case_cd='TEST'` 가 손입력돼 있었다. 이 칸은 **반복전표 분석이 붙이는 패밀리 ID** 자리이므로(`10_ERP_DB연계/10` F8) `TEST` 는 아무 데도 연결되지 않는 쓰레기 값이고, 남겨 두면 뒤에 올 일괄 등재의 케이스 대사를 흐린다. `src_family_key is null` 을 조건에 걸어 **일괄 등재로 붙은 값은 건드릴 수 없게** 한 뒤 `tpl_id=1` 한 행만 `null` 로 정리. 결과 — 케이스 코드가 남은 템플릿 **0건/4건**, 편집기 제목줄이 「템플릿 편집 · TPL-0001」 로 정상 표시(케이스 표시 사라짐). 스키마 변경 없음. **남은 사항**: 화면에는 케이스 코드를 지우는 수단이 없다(읽기전용 표시만) — 앞으로 같은 잔여값이 생기면 DB 정리가 필요하다 | 최동혁(+Claude) |
| 2026-08-27 | 회계·ERP연계 | `pages/자금_결의전표_입력_2026.html` | **월 토큰을 「외워서 적는 것」에서 「눌러서 넣는 것」으로 — 라이브 실측으로 잡은 결함 대응.** 앞선 토큰 치환 수정을 라이브에서 확인하다가 진짜 원인을 찾았다: 등재된 `TPL-0001` 의 제목 틀이 **`(주)쿠콘 YY년M월분 국세청 스크래핑외`** — **중괄호 없이** 저장돼 있었다. `{YY}` 가 아니라 `YY` 라 치환 대상이 아니었고, 적요에 「YY년M월분」이 그대로 찍혔다. 코드가 아니라 **입력 방식의 문제**였다 (토큰 표기를 사람이 외워서 손으로 적어야 했다). 중괄호 없는 `YY`·`M` 을 토큰으로 인정하는 것은 **해결책이 아니다** — 「3M」·「M&S」 같은 평범한 글자를 숫자로 바꿔 버린다. **조치 4종** — ①**토큰 버튼** — 제목 틀·기본 적요·관리항목 고정값 입력칸 아래에 `{YYYY}`·`{YY}`·`{MM}`·`{M}` 버튼을 두고 **커서 자리에 삽입**한다. 버튼마다 이번 달 기준 값(2026·26·08·8)을 함께 띄워 무엇이 들어갈지 보인다. ②**결과 미리보기** — 입력칸 바로 아래에 「2026-08 기준으로 찍히는 값 — …」 을 실시간 표시(월 표기 기준을 바꾸면 같이 움직인다). ③**중괄호 누락 감지 + 원클릭 교정** — **한글 년·월 바로 앞에 붙은** `YYYY`·`YY`·`MM`·`M` 만 후보로 보고(`/(YYYY|YY)(?=년)|(MM|M)(?=월)/gi`, `{…}` 안은 건드리지 않음 — 오탐 0) 경고 + **[{ } 씌워서 고치기]** 버튼 한 번으로 교정한다. ④**목록 배지** — 템플릿 관리 목록에서 문제 건에 `⚠ 월 토큰 YY·M` 배지 + 상단에 건수 안내. **어떤 템플릿이 잘못됐는지 몰라 못 고치던 상태를 없앤다**(토큰이 먹는 자리 전부를 본다 — 제목 틀·기본 적요·라인 적요·관리항목 고정값). **운영 데이터는 손대지 않았다** — `TPL-0001`(부서 공용·40회 사용)은 관리자가 화면에서 직접 고치도록 남겼다. 라이브 실측 부수 확인: 부서 검색 팝업에서 「인사」→2건 좁힘·선택 시 코스트센터 `C013150` 자동 연동, 그리드 코스트센터 **92건 검색형**·차대(2)·금액규칙(4)·프리필잠금(2) 네이티브 유지, 대상 부서 기본값 총무팀(3200) 자동 선택, `TPL-0001` 의 케이스 코드가 **"TEST"** 로 손입력돼 있어 이 칸이 왜 입력용이 아니었는지도 확인됨. 검증: jsdom 회귀 **34건 신규 전건 통과**(누락 감지 7 · 자동 교정 5 · 도구 UI 6 · 교정 흐름 5 · 커서 삽입 3 · 기준월 연동 1 · 목록 배지 4 · 기존동작 3), 기존 77건 포함 **111건 전건 통과** | 최동혁(+Claude) |
| 2026-08-27 | 회계·ERP연계 | `pages/자금_결의전표_입력_2026.html` | **전표 입력의 「고르는 칸」을 검색형으로 통일 + 템플릿 분류·부서 기본값 + 월 토큰 결함 수정.** ①**선택칸 텍스트 검색(화면 공통 기본세팅)** — 부서·코스트센터처럼 선택지가 수십 개인 칸을 스크롤로만 찾던 것을, 클릭·Space·글자 입력 시 **검색 팝업**(계정과목·거래처와 같은 컴포넌트)이 열리도록 바꿨다. `<select>` 가 여전히 값의 단일 출처라 기존 `.value` 코드는 그대로 동작하고, **문서 전체 위임(capture)** 으로 걸어 그리드·서식처럼 나중에 그려지는 칸도 자동 적용된다(앞으로 만드는 전표 입력칸도 동일). 선택지 6개 미만은 네이티브 유지, `data-search="on|off"` 로 강제·해제. ERP 마스터 코드 목록(부서·코스트센터)은 적재 건수와 무관하게 항상 검색형. 그리드 셀의 Enter·↑↓ 는 줄 이동 몫이라 뺏지 않는다. ②**템플릿 분류를 자유 입력 → 선택 + 새 분류 입력** — 자유 입력은 「경비」·「경비 」처럼 같은 뜻의 분류가 갈라져 칩이 지저분해졌다. 등록된 분류(비공개 템플릿 것 포함)에서 고르고, 없을 때만 `+ 새 분류 직접 입력`으로 만든다. ③**템플릿 대상 부서 기본값 = 로그인 사용자 부서**(헤더 부서와 같은 `myDeptCd()` 를 공유). ④**ERP 케이스 코드 입력칸 제거** — 사람이 손으로 적을 값이 아니라 반복전표 분석이 붙이는 패밀리 ID(`10_ERP_DB연계/10` F8)라 손입력은 아무 데도 연결되지 않았다. 입력칸을 없애고 연결된 값이 있을 때만 편집기 제목줄에 읽기전용 표시, 저장 시 원본 값을 그대로 되돌려 보내 일괄 등재분이 지워지지 않게 했다(`vat_mode`·`src_family_key` 와 같은 처리). ⑤**월 토큰 결함 수정** — `{YYYY}`·`{YY}`·`{M}` 치환이 **월반복 회차의 제목 틀에만** 걸려 있어, 일반 템플릿을 [적용]하면 `{YY}년{M}월분` 이 적요에 그대로 찍혔다. 이제 **적요·라인 적요·관리항목 고정값**까지 같은 규칙으로 치환하고, 기준 월은 결의일자(없으면 오늘)다. **`{MM}`(두 자리 월) 추가**, `{YY}` 도 두 자리로 채움(2005→05), 대소문자·`{ M }` 공백 표기 허용. 적용 전 미리보기·편집기 서식에도 치환 결과를 함께 보여준다. 검증: jsdom 회귀 **77건 신규 전건 통과**(토큰 10 · 적용 6 · 분류 10 · 부서 3 · 케이스 5 · 검색칸 24 · 그리드셀 12 · 월반복 5 · 기존동작 3) | 최동혁(+Claude) |
| 2026-08-27 | 회계·ERP연계 | `이관/sql/32_gl_draft_no_rule.sql`(신규) · 마이그레이션 `gl_draft_no_rule_v1` · `supabase/functions/jeil-gl-draft/index.ts`(v5.10) · `pages/자금_결의전표_입력_2026.html` · `10_ERP_DB연계/09`(C-4 개정·C-16 신설)·`02` | **참조번호 채번규칙 재정립 + 결의전표 화면 용어 정리(결정 C-16).** 종전 초안번호 `DRAFT-YYYYMMDD-nnnn` 은 **날짜별 일련번호처럼 보이지만 아니었다** — 전역 시퀀스라 날짜가 바뀌어도 이어져, `DRAFT-20260824-0014` 의 `0014` 는 그날 14번째가 아니라 개설 이래 14번째다. 작성자 정보도 없어 번호만 들고는 누가 만든 건인지 알 수 없었고, `DRAFT-` 접두는 ERP 표기 관례 밖이었다(이 번호는 그대로 ERP `A_BATCH.REF_NO`·`A_TEMP_GL.REF_NO` 로 들어가는 멱등키다). **새 규칙은 `AX{YYMMDD}-U{작성자코드}-{순번}`**(예 `AX260827-U001-003`, 18자) — ERP 표준 채번(`B_AUTO_NUMBERING`: 2자 유형코드+날짜+일련, `TG`수기결의·`AG`AI전표·`BT`배치·`VT`부가세)의 관례를 따르되 **작성자 조각을 넣어** 사용자 간 충돌을 구조적으로 없애고 번호만으로 작성일·작성자·순번을 되짚게 했다. **구현 두 가지가 핵심** — ①채번 지점을 테이블 기본값에서 **BEFORE INSERT 트리거**로 옮겼다(기본값은 다른 컬럼값을 못 봐 작성자를 넣을 수 없다) ②순번을 시퀀스가 아니라 **카운터 테이블**로 셌다(시퀀스는 롤백해도 값이 안 돌아와 저장 실패가 곧 결번이 되는데, 회계 번호로는 부적절하다). 작성자 코드는 레지스트리에서 최초 작성 시 1회 부여하고 이후 바꾸지 않으며, 두 테이블 모두 RLS ON·정책 0 + service_role 단일 경로(C-6 계승). **기존 44건(`DRAFT-*`)은 재부여하지 않는다** — 일부가 ERP `REF_NO` 로 이미 들어가 있어 재부여하면 멱등 대사가 끊긴다. **검증**은 실제 INSERT 경로로 했다: 같은 사람 연속 `-001`·`-002`, 다른 사람 같은 날 `U002-001` 로 충돌 없음을 확인한 뒤 **전량 롤백**해 결번이 남지 않는 것까지 봤다(전표 44건·카운터 0행 그대로). **화면 용어**는 「초안」을 걷어내고 「이력·전표」로 통일했다 — 탭 `내 초안`→**`내 이력`**(순서도 맨 뒤로), 표 머리 `초안번호`→**`참조번호`**. 데이터·API 이름(`gl_draft`·`draft_no`·`status='draft'`)은 **바꾸지 않았고**, 화면 용어와 데이터 이름의 대응을 화면·함수 주석에 남겼다. 템플릿 상태의 `비공개(초안)`은 다른 개념이라 `비공개(작성중)`으로 갈랐다 | 최동혁(+Claude) |
| 2026-08-25 | ERP연계·회계 | `pages/자금_결의전표_입력_2026.html` · `supabase/functions/jeil-gl-draft/index.ts`(v5.8) · `supabase/_deploy_function.py`(신규) · `이관/sql/30_gl_apply_diag.sql`(신규) · `10_ERP_DB연계/etl/gl_apply_demo2.py`(v1.4) | **「전송 대기 중」이 왜 안 끝나는지 화면이 답하게 — 결함 4종 수정.** 관리자가 [🚀 ERP 전송]을 눌렀는데 「사내 중계가 곧 가져갑니다」만 뜬 채 아무 일도 일어나지 않는다는 지적. 로그 대사로 원인이 정확히 잡혔다 — **05:51:58 요청 → 05:52:03~05:53:21 폴링 17회 무응답 → 방치**. 중계가 돌지 않는데(스케줄러 미등록) 화면은 그 사실을 알 신호가 없어 **지킬 수 없는 약속을 계속 표시**했다. 게다가 **재전송이 사유를 지우고 있었다** — `apply_request` 가 `erp_apply_msg=null` 로 만들어 `DRAFT-20260824-0015` 의 08-24 실패 사유가 08-25 재전송 때 소실됐고 화면엔 상태만 '대기'로 남았다. **결함 4종**: ①중계 생존 신호 부재 — 적용 원장은 *일이 있을 때만* 쌓여 「조용한 것」과 「죽은 것」을 구분 못 한다 ②재전송이 실패 사유 삭제 ③실패 시 `erp_apply_at` 미갱신(언제 실패했는지 초안에 안 남음) ④실패·적체 목록 부재(건별로 눌러야만 사유 확인). **조치** — DB: 심박 테이블 `gl_relay_heartbeat` + `gl_relay_ping`, 진단 RPC `gl_apply_health`·`gl_apply_problems`, `gl_draft` 컬럼 4종(`erp_last_error` 는 **재전송에도 살아남는 보존 칸**, `erp_ready_at` 은 대기 진입 시각 — `erp_apply_at` 은 선점이 `now()` 로 덮어써 경과가 0으로 리셋된다), 가드 차단 사유를 콘솔 문구가 아닌 **줄·계정·사유** 그대로 기록. 릴레이 v1.4: 회차마다 심박 + **`--recheck`**(현재 규칙으로 사유 재판정 — 쓰기 직전에서 멈춰 **ERP 무투입**, `--dry-run` 과 달리 AG 채번을 쓰지 않는다). 화면: 중계 생존 배너(가동/정지/알 수 없음) + **「손봐야 할 건」 패널**(사유·경과·시도횟수 + 열기·폐기) + 대기 중에도 직전 사유 표시 + 폴링 만료 시 침묵 대신 원인 안내. **결과** — 전송 테스트 2건 성공(`AG202608240005` 22,508,030원 9라인·`AG202608250006`), **대기 큐 0건**. 낡은 사유 현행화: `DRAFT-20260820-0012` 가 「분개코드 미등재」로 남아 있었으나 재판정 실제 사유는 **미결 반제(G5)** — 규칙이 바뀌면(C-15) 옛 사유가 사람을 엉뚱한 곳으로 보낸다. 남은 실패 2건은 같은 오류(`21100902` 차변 사용)로 전표 자체가 잘못돼 재전송으로 풀리지 않는다. jsdom 회귀 35건 신규 + 기존 148건 통과. 부수로 `_deploy_function.py` 신설 — supabase CLI 부재로 함수 소스 75KB 를 손으로 옮기던 것을 Management API 업로드로 대체 | 최동혁(+Claude) |
| 2026-08-25 | ERP연계·회계 | `10_ERP_DB연계/etl/gl_apply_demo2.py` · `13_AX전표_서브원장_호출누락_개발요청.md`(신규) · `11`(v1.7) (커밋 `da0df6b`) | **AX 전표의 채무·부가세 서브원장 미생성 해소.** 릴레이가 만든 전표는 전표·라인·관리항목·정식장부 전기까지 정상인데 채무(`A_OPEN_AP`)·부가세(`A_VAT`) 원장이 생기지 않았다 — **전표와 시산표가 멀쩡해 겉으로 드러나지 않는 결손**이라 부가세 신고 매입세액 누락·미지급금 반제 누락으로 이어진다. 실측: AX 대상 26라인 연결번호 전건 공백(수기 5,483/5,483), 2025년 이후 미채움 8건이 전부 AX 전표. 원인은 표준 엔진이 전표만 만들고 서브원장은 만들지 않는데 `usp_a_create_temp_gl_subsys` 호출이 빠진 것. 엔진과 같은 트랜잭션에서 호출하도록 고치고, 실패 시 전표까지 롤백 + **연결번호가 비면 롤백**(호출 성공만 믿지 않음)을 더했다. DEMO2 실증 `AG202608240004` — 연결번호·채무·부가세 전건 정상, 금액 전표와 일치 | 최동혁(+Claude) |
| 2026-08-24 | ERP연계·회계 | `10_ERP_DB연계/12_AX001_계정매핑_등재요청.md`(신규) · `etl/deploy/`(6종) · `etl/_env.py` · `_routes.py` (커밋 `295c801`~`49874c0`) | **결의전표 전송 릴레이 ERP 서버 이전(EXE) + 전송 차단 계정 5종 실측.** 화면에서 전송을 눌러도 사람이 스크립트를 돌려야만 ERP에 들어가는 구조라 08-20 이후 4건이 적체됐는데, **실패가 아니라 미시도**임을 실측 확인(`erp_apply_msg` 공란·`erp_apply_at` null·적용원장 기록 0, 큐 RPC는 정상 반환). 기획서 `03 §D2-a`「상시 가동 장비 지정 필수」 미이행분이다. ERP 서버가 배치 가능함을 확인(아웃바운드 443·ODBC 17·DEMO2 동일 인스턴스·의존 `pyodbc` 하나)한 뒤, 벤더 운영 장비라 **PyInstaller 단일 EXE**로 배포해 파이썬을 설치하지 않았다. `_env.py`에 `env_root()` 신설(frozen이면 `sys.executable` 기준 — 번들은 임시폴더에 풀려 `__file__`로는 `.env`를 못 찾는다). 배포 키트 `etl/deploy/`와 **변경관리 런북**(서버·접속정보·키·대상DB·계정 변경 시 조치 + 재빌드 판단)을 신설했다. 구현 중 실측으로 결함 4건을 잡았다 — 한글 배치는 cmd.exe 파싱이 깨짐 / 괄호 블록 안 `%ERRORLEVEL%`는 항상 0이라 **스케줄러가 실패를 성공으로 보고**했을 것 / `Get-Date -Format` 공백으로 타임스탬프 공란 / BOM 없는 `.ps1`은 PowerShell 5.1이 cp949로 읽어 파싱 실패. **적체 4건 중 3건 전송 성공**(`AG202608180001`·`AG202608180002`·`AG202608240001`, 전건 라인 완전일치). 실행 루트를 `E:\ai.jeil\relay\`로 확정하고 서버에서 `--list`·`relay.cmd`까지 검증했으며, **작업 스케줄러 등록은 테스트 완료 후로 보류**(관리자 결정 — 현재는 수동 실행만). 함께 **전송 차단 계정 5종**을 실측해 회계팀 요청서로 문서화했다(12번 신규, 라우트 `/docs/erp/ax001-acct-mapping`) — 12종 중 7종 통과/5종 차단이고, **AX001의 `A_JNL_ACCT_ASSN` 매핑이 0건**이라 지금 통과하는 건 전부 자기참조·코드형 폴백 덕이며 계정코드형 분개코드가 `A_JNL_ITEM`에 77건만 등재(비용계정 473건)돼 **등재된 계정만 우연히 통과하는 상태**임이 드러났다. 등재는 회계팀·유니포인트 소관(미결 U-F) | 최동혁(+Claude) |
| 2026-08-24 | ERP연계·회계 | `10_ERP_DB연계/etl/deploy/`(신규 5종) · `etl/_env.py` · 전달폴더 `ai.jeil - ax/relay/` (커밋 `295c801`·`627f1bb`) | **결의전표 전송 릴레이 상시 실행 도입 — 실행 호스트를 워크스테이션(수동) → ERP 운영 서버(1분 주기)로 이전.** 화면 [🚀 ERP 전송]을 눌러도 사람이 `gl_apply_demo2.py`를 손으로 돌려야만 ERP에 들어갔다. 마지막 수동 실행이 08-20 오전이라 그 뒤 4건이 적체됐는데, **실패가 아니라 미시도**였다(실측: `erp_apply_msg` 전건 공란·`erp_apply_at` 전건 null·`gl_erp_apply_log` 08-20 이후 기록 0, 큐 RPC는 4건 정상 반환). 기획서 `03_중간DB_구축실행기획 §D2-a`의 「**상시 가동 장비 지정 필수**」 미이행분이다. **배치 근거 실측**(ERP 서버): Supabase 아웃바운드 443 `TcpTestSucceeded True` · `ODBC Driver 17 for SQL Server` 존재 · `JEILMNS_DEMO2`가 같은 인스턴스(`SERVER=localhost`) · 외부 의존은 `pyodbc` 하나뿐 · Python만 미설치. **벤더 운영 장비라 런타임 설치 흔적을 최소화**하기 위해 PyInstaller 단일 EXE(`gl_relay.exe` 7.6MB)로 묶어 파일 2개(+`.env`)만 두면 돌게 했다(파이썬 설치 경로는 대안으로 유지). `_env.py`에 `env_root()`를 신설 — 번들은 실행 시 임시폴더에 풀려 `__file__` 기준으로는 `.env`를 영영 못 찾으므로 frozen이면 `sys.executable` 기준으로 찾는다. **`relay.cmd`는 실측으로 결함 3건을 잡아 재작성**했다 — ①한글이 든 배치는 cmd.exe가 코드페이지 단위로 읽어 파싱이 깨진다(경로 포함 순수 ASCII화, Python 대체 경로도 `pysrc\etl`로 변경해 `.env` 위치를 EXE 방식과 통일) ②괄호 블록 안 `%ERRORLEVEL%`는 파싱 시점 값(항상 0)이라 **스케줄러가 실패를 성공으로 보고**했을 것 → 라벨 분기로 교체 ③`Get-Date -Format`에 공백이 있으면 인자 바인딩 실패로 타임스탬프가 빈다. `.ps1` 2종은 UTF-8 **BOM**으로 저장(Windows PowerShell 5.1은 BOM 없는 UTF-8을 cp949로 읽어 한글 주석에서 파싱 실패 — 서버 기본 셸이 5.1). **`변경관리.md` 런북 신설** — 서버·접속정보가 바뀔 때 어디를 고치는지(ERP 접속정보/서버 이전/Supabase 키 회전/DEMO2→운영/재빌드 판단/실행 계정/장애 진단) 문서화. **적체 4건 처리**: 리허설 후 3건 전송 성공(`AG202608180001`·`AG202608180002`·`AG202608240001`, 전건 라인 완전일치·자동 확정), 1건은 계정 `21100902`(개인경비) 분개코드 3단계 모두 실패로 fail-closed — 회계팀 매핑 등재 대상(U-F 계열). **남은 한계**: 멱등 방어는 *같은 초안번호* 기준이라 내용이 같은 별개 초안은 두 장 다 들어간다(같은 날 동일 적요·금액 2건 실사례) → 운영 전환 전 화면 중복 경고 필요 | 최동혁(+Claude) |
| 2026-08-21 | 포털 | `04_챗봇_포털_데모UI.html` · `JEIL_AX_포털데모_통합본.html` · `.backlog/3-done/REQ-0014` (커밋 `6a12ac0`) | **구 경로 즐겨찾기 자기치유 — 라우트가 바뀌어도 ★ 표시가 따라가게(REQ-0014).** 즐겨찾기 저장 키는 **카드의 `href` 그 자체**여서, 08-20 클린 라우트 개편으로 카드 경로가 바뀌자 그 전에 담아 둔 항목이 현재 어느 카드와도 일치하지 않게 됐다 — 칩은 남고 클릭도 되지만(구 `.html` → `cleanUrls` 리다이렉트) **카드 ★ 는 꺼진 채**라, 다시 누르면 같은 페이지가 새 경로로 하나 더 등록되는 중복이 생겼다. 조치는 **경로 대응표를 화면에 심지 않고 카드 라벨로 대조해 따라가는 자기치유** — `favMigratePaths()` 가 저장분 중 지금 유효하지 않은 경로만 골라 **라벨이 정확히 하나 일치하는** 카드가 있을 때만 키를 현재 경로로 옮겨 적고, 구·신 경로를 둘 다 갖고 있었으면 하나로 병합한다. **일치하는 카드가 없으면 건드리지 않는다**(임의 삭제 금지). 라벨 계산은 `favCardLabel()` 한 곳으로 모아 등록 경로와 대조 경로가 갈라질 수 없게 했고, 호출은 `applyFavStars()` 끝이라 로그인 후 서버 재렌더에도 다시 시도한다. `_routes.py` 표를 프런트에 복제하지 않았으므로 **앞으로 라우트가 또 바뀌어도 같은 방식으로 따라간다.** 검증: 로컬 5케이스(이관·무변경·중복병합·미매칭 보존·도구 무변경) + 멱등(반복 호출 시 추가 저장 0회) + 라이브 실측(3건→2건, 칩 2·★ 2 점등). REQ-0013 「잔여」의 후속 | 최동혁(+Claude) |
| 2026-08-21 | 포털 | `04_챗봇_포털_데모UI.html` · `JEIL_AX_포털데모_통합본.html` · `.backlog/3-done/REQ-0013` (커밋 `5be4b0a`) | **상단 즐겨찾기가 새로고침 후 사라지던 결함 수정(REQ-0013).** 사용자 증상은 "즐겨찾기 저장이 안 된다"였지만, 실측 결과 **저장은 정상**(`localStorage jeilax_fav_v1` 에 값 존재)이고 **다시 읽는 쪽이 비어 있었다**. 원인은 초기화 순서 — 초기화 IIFE `teamsBoot`(L1607~1653)가 즐겨찾기 블록보다 **위에서** `renderDashline()`·`applyFavStars()`를 부르는데, 저장 키가 `var FAV_KEY`(L1680)로 선언돼 그 시점엔 호이스팅된 `undefined` 였다 → `localStorage.getItem(undefined)` 로 키 `"undefined"` 를 조회해 **항상 빈 목록**을 읽었다. 함수 선언은 호이스팅되므로 오류 없이 "빈 즐겨찾기"만 그려져 증상이 저장 실패처럼 보였다. 조치는 저장 키를 **호이스팅되는 함수 선언 `favKey()`** 로 바꾼 최소 수정 — 키 이름·저장 포맷은 그대로라 이미 저장돼 있던 값이 그대로 되살아난다. 라이브 실측 검증: 등록 2건 → 새로고침 → 칩 2개·★ 2개 유지 → 해제 → 안내문 복귀. **잔여**: 즐겨찾기 키가 카드 `href` 라서 08-20 클린 라우트 개편 이전에 저장된 항목은 구 `.html` 경로를 갖는다(칩은 리다이렉트로 열리나 카드 ★ 표시는 안 켜짐) — 경로 정규화는 별도 요청 | 최동혁(+Claude) |
| 2026-08-21 | 회계 | `pages/자금_결의전표_입력_2026.html` · `.backlog/3-done/REQ-0012` (커밋 `95eb2de`) | **전표 서식에도 총액 1칸 배분 신설(REQ-0012, A안).** 총액 1칸으로 금액을 채우는 기능은 템플릿 적용 경로에만 있었다. 서식에는 총액 칸이 없어 줄마다 직접 넣어야 했고, 대체 수단 둘은 조건이 좁았다 — [선택 행에 차액 채우기]는 나머지가 이미 채워져 있어야 하고, 부가세 자동계산(C-14)은 **부가세 1줄+비용 1줄+대변 1줄**일 때만 작동한다(`vatCtx().one`). 즉 **차변이 두 줄 이상이면 서식에서 총액으로 채울 방법이 없었다.** 템플릿 엔진 `spreadAmounts()`는 줄마다 금액 규칙(입력/비율/고정/차액)이 정의돼 있어야 도는데 서식 행에는 그 메타가 없다 — 그래서 규칙을 **데이터가 아니라 화면 상태에서 읽는** 별도 함수 `spreadFormTotal()`을 신설했다(**사용자가 이미 넣은 금액 = 고정, 빈 칸 = 나눠 받을 자리**). 규칙: ①기입력 금액은 덮지 않고 방향별로 (총액−고정합계)만 나눈다 ②부가세 라인은 **세율로 먼저 가른다**(총액=공급가액+세액 — 이 처리를 빠뜨려 총액이 줄던 것이 REQ-0004 결함) 후 V1·V8 관리항목도 동기 ③남은 빈 줄은 균등 배분하고 **마지막 줄이 잔액을 흡수**해 합계 오차 0 ④나눈 값·원단위 조정분은 **'확인 필요'** 표시(사용자가 고치면 걷힘) ⑤계정 없는 줄·차대 미정 줄은 건드리지 않고 몇 칸을 제외했는지 알린다. **템플릿 경로는 무변경**(REQ-0002·0004 회귀 방지). 검증: jsdom **148건 전건 통과**(기존 125 + 신규 23). 라이브 `/work/voucher` 반영 확인 | 최동혁(+Claude) |
| 2026-08-20 | 전영역·거버넌스 | `_routes.py`·`_build_routes.py`(신규) · `vercel.json`(신규) · HTML/MD 74개 파일 내부 링크 627곳 · 빌더 4종(`lib/_html_builder.py`·`08/03`·`그리드`·`_build_bundle.py`) · `pages/_access-gate.js` · DB `public.portal_page.path` 10건 · `실제구축준비 자료/이관/sql/28_portal_page_clean_url.sql`(신규) | **공개 URL 클린 라우트 전면 개편 — 파일명과 주소를 분리.** 지금까지 라이브 주소가 파일명 그대로여서 한글이 `%EC%9E%90%EA%B8%88…`로 깨져 보이고, 번호 접두(`04_`)와 `.html` 확장자가 사용자에게 노출됐다. **파일은 하나도 옮기거나 바꾸지 않고**(git 이력·빌더 경로 보존) 주소 계층만 새로 얹었다 — `/main`(포털) · `/work/…`(부서 운영페이지 12) · `/admin/…`·`/vendor/…` · `/survey/…` · `/docs/…`(문서 60여) · `/demo/…` · `/tools/…`, **총 97개 라우트**. 예: `pages/자금_결의전표_입력_2026.html` → **`/work/voucher`**. 라우트 표의 **단일 출처는 저장소 루트 `_routes.py`** 이고 `python _build_routes.py --write`가 `vercel.json`을 재생성하면서 문서·화면의 내부 링크 **627곳을 일괄 치환**한다(미매핑 0건). **재생성 내구성**: MD→HTML 빌더 4종이 같은 표를 참조하도록 고쳐, 문서를 다시 빌드해도 링크가 옛 파일명으로 되돌아가지 않는다. **호환성**: 실제 파일이 제자리에 있고 Vercel `cleanUrls`가 구 `.html` 주소를 자동 리다이렉트하므로 기존 북마크·외부 공유 링크는 그대로 살아 있다. 함께 처리한 것 — 클린 URL은 경로 깊이가 달라져 상대경로 자산이 깨지므로 11개 화면의 `_access-gate.js`·`grid.js/css`·`config.js` 참조를 루트 절대경로로 전환했고, 로그인 후 카드 경로의 정본인 **DB `portal_page.path` 10건**도 클린 URL로 갱신했으며, 포털에 카드가 없어 URL 직접 입력으로만 열리던 **결의전표 입력 카드**를 추가했다. 명명 규약은 `02_명명규칙_폴더규약 §2.5`에 신설 | 최동혁(+Claude) |
| 2026-08-20 | 회계·거버넌스 | `pages/자금_결의전표_입력_2026.html` · `jeil-gl-draft`(v5.9) · `.backlog/3-done/REQ-0001~0004` (커밋 `6bb44ba`·`147810e`·`5d91233`·`063346e`·`1b23c10`) | **결의전표 입력 4건 개선 — 부가세 자동계산(결정 C-14, B2 개정)·총액 배분·화면 개편.** **①C-14(B2 개정)**: "부가세·원천징수 자동계산은 범위 제외"(2026-08-11 B2 부분승인)를 **부가세에 한해 개정**해 공급가액·세액·합계를 서로 채우는 양방향 자동계산을 도입했다(원천징수는 범위 밖 유지). 계산은 **화면에서만** 하고 서버는 받은 값을 그대로 검증한다. 자동값은 **제안**이며 사용자가 고친 값은 덮지 않는다. 세율은 운영 전표 실측 근거로 정했다 — A(1,422건 평균 9.99%)·B·L=10%, K(64건)·C(18건)=0%, **I 수입세금계산서·H 통관은 제외**(과세표준에 관세가 섞여 공급가액 대비 일치 0/4). A 유형 25건(2%)이 정확히 10%가 아니어서(원단위 조정) 강제하지 않는다. **②템플릿 총액 배분**: "총액 1칸 입력" 약속이 실제로는 깨져 있었다 — 같은 방향에 입력 라인이 혼자일 때만 총액을 넣어 차변 2줄이면 통째로 비었다. **직전 회차 비율**(관리자 결정, 원천 `tpl_apply_prev`)로 나누고 없으면 균등 폴백, 마지막 줄이 잔액을 흡수해 합계 오차 0. 나눠 넣은 금액은 **'확인 필요'** 로 표시(관리자 요청). **③총액 축소 결함 수정**: 부가세 라인이 균등 분할(176,000/176,000)돼 합계가 352,000→193,600으로 줄던 것. 배분 단계에서 세율로 먼저 가르도록 고쳤고, 같은 자리에서 영세율 균등분할·반올림 오판 2건을 회귀 테스트가 추가 검출. **④화면 개편**: 전표 시작 경로 4곳을 **시작 바**로 통합하고 **전표복사 탭을 모달로 흡수**(탭 5→4, 복사 시 탭 이동 2→0회). 월반복 카드·탭 4개는 유지(권고안). 서식은 현 구조(3행 1블록) 유지하고 가시성 6건 개선. **안내 문구 5곳**이 옛 흐름(회계 담당자가 ERP에 수기 등록)을 설명하고 있어 현재 자동 전송 흐름에 맞게 교체했고, 템플릿 목록에서 바로 삭제하는 기능도 더했다. **검증**: jsdom 테스트 누적 100건 이상 전건 통과, 구현 중 결함 4건을 테스트가 사전 검출. 요청 관리는 백로그 REQ-0001~0004(전건 성공 종료). | 최동혁(+Claude) |
| 2026-08-20 | 거버넌스 | `CLAUDE.md §14`(신설) · `.gitignore` · `.claude/skills/backlog/`(로컬) · `.backlog/`(로컬) | **요청 백로그(backlog Skill v0.3) 도입** — 작업 중 나온 새 요청·아이디어가 대화 스크롤에 묻혀 누락되던 문제를 해소. 수집(사람: `.backlog/0-inbox/`에 자유 형식 메모)과 실행(Claude)을 물리적으로 분리하고 그 사이에 기획 단계를 강제한다(inbox → 1-spec → 2-active(동시 1건, 락) → 3-done, 보류는 9-hold). **경계 규칙**: 항상 참인 규칙은 CLAUDE.md, 한 번 하면 끝나는 개별 작업 지시는 백로그. 프로젝트 설정 `.backlog/CONFIG.md`에 민감정보 별칭 규칙(§1 준용)·항상 검토 필요 항목(스키마·권한·인증·ERP 접근·민감 스키마·실거래)·완료 시 단일 출처 문서 갱신 연동을 명시. 스킬·백로그 데이터는 모두 git 제외(로컬 전용) | 최동혁(+Claude) |
| 2026-08-20 | ERP연계 | `app/erp-status.html`(관리자 체크박스) · `app/lib/api.js` · `10_ERP_DB연계/etl/etl_watch.py`·`etl/README.md` · 마이그레이션 `erp_sync_request_sensitive_v1` | **「데이터 업데이트」에 급여 집계 포함 옵션(전체관리자 전용)** — 급여는 민감 스키마(`erp_secure`)라 웹 버튼에서 기본 제외돼 왔다(그래서 다른 19종만 갱신됐다). 관리자 결정으로 전체관리자에 한해 포함을 허용하되, **판정은 서버 RPC가 하고 허용·거부 모두 `erp_secure.hr_access_log`에 감사 기록**한다. 일반 사내 사용자는 종전대로 19종만 | 최동혁(+Claude) |
| 2026-08-20 | ERP연계 | Supabase(마이그레이션 `gl_ctrl_ref_v1`·`v1b_dedupe`, `erp_ro.ctrl_ref_s` 신규) · `jeil-gl-draft`(v5.7) · `10_ERP_DB연계/etl/etl_run.py`(job `ctrl_ref`) · `실제구축준비 자료/이관/sql/27_gl_ctrl_ref.sql`(신규) · Supabase(마이그레이션 `erp_sync_overview_gl_mirror_ctrl_ref`) | **관리항목 참조 마스터 전면 연결(`ctrl_ref`) 신설·실적재 완료 + 연동 현황 뷰 16→20종 확장.** 관리항목 52종 중 31종이 가리키는 ERP 참조 마스터(`A_CTRL_ITEM.TBL_ID`)를, 종류마다 개별 테이블을 만드는 대신 `(관리항목코드,코드,명칭,보조)` 정규화 통합 미러 `erp_ro.ctrl_ref_s`로 연결했다 — **27종 9,279건 적재, 참조 보유 31종 전수 연결·미연결 0건**. 관리자 지시로 처음 제외했던 민감 6종(사번·계좌·법인/구매카드·어음·차입번호)도 전부 연결하되, 사번은 사번·성명·부서명 3컬럼만·재직자만 추출(주민번호·연락처 등 제외)하고 민감 항목 조회는 값 없이 건수만 감사기록했다. **같은 작업 중 결의전표 미러 3종(`gl_slip`계열, 08-18 실적재 완료)이 연동 현황 뷰 `v_erp_sync_overview`에 그동안 등재되지 않았던 사각지대(08-10 `bp_master` 누락과 동일 유형)를 발견**, `ctrl_ref`와 함께 반영해 **16종→20종**으로 확장했다. 단일 출처는 `10_ERP_DB연계/`(00 v0.34·02) | 최동혁(+Claude) |
| 2026-08-20 | ERP연계 | `app/erp-status.html`(버튼·진행률) · `app/lib/api.js` · `10_ERP_DB연계/etl/etl_watch.py`(신규)·`etl_run.py`·`etl/README.md` · `이관/sql/29_erp_sync_request.sql`(마이그레이션 `erp_sync_request_v1`) | **ERP 데이터 연동 현황 화면에 「데이터 업데이트」 버튼 신설** — 그동안 중간DB 적재는 관리자 PC에서 `etl_run.py`를 직접 돌리는 것이 유일한 경로였고 화면은 결과만 보여줬다. ERP가 사외 IDC라 웹에서 직접 ETL을 돌릴 수 없으므로 **요청 큐** 방식으로 연결했다: 버튼 → Supabase 큐(`etl_meta.sync_request`)에 요청 등록(사내 세션만) → ERP 접속 호스트의 새 러너 `etl_watch.py`가 집어가 ETL 실행 → 진행률 기록 → 화면이 폴링하다 완료 시 표를 자동 갱신. 급여 등 민감분은 요청 대상에서 자동 제외(관리자 직접 실행 유지), 중복 실행 방지·쿨다운 60초·러너 미가동 안내 포함. 검증은 ERP 미접속 상태의 큐 왕복 실측까지 — 실 집행 확인은 러너 기동 후. 상세는 `10_ERP_DB연계/00`(v0.34)·`02` | 최동혁(+Claude) |
| 2026-08-20 | 협력사포털 | `app/lib/supabaseClient.js`(세션 저장소 분리) · `app/vendor-login.html`(거래처 선택 패널) · `app/admin-vendors.html`(세션 검증·안내) | **세션 충돌 해소 + 관리자 로그인 화면에서 거래처 선택(관리자 지시).** **후속 픽스(같은 날)**: 새 비밀번호 설정 직후 포털에서 설정 화면이 다시 뜨고 모든 호출이 `401`로 실패하던 문제 해소 — 원인은 **GoTrue가 관리자 API로 비밀번호를 바꾸면 그 사용자의 세션을 전부 폐기**한다는 점(실측: 변경 직후 `auth.sessions` 0행)이라 남은 토큰이 즉시 무효가 되고 `refreshSession()`으로도 살아나지 않는다(JWT는 로컬 파싱만 되므로 `must_pw` 옛 값이 남아 설정 화면이 재표시됨). 조치: 변경 성공 직후 **새 비밀번호로 즉시 재로그인**해 유효 세션을 만들고(실패 시 '다시 로그인' 안내로 폴백), 협력사 로그인·포털 진입 시 `getUser()`로 **폐기된 세션을 감지해 정리**한다.  ①**증상**: 관리자 계정 삭제 시 `403 forbidden`, 관리자 계정 로그인 시 「거래처 권한 미연결」 안내만 뜨고 화면이 넘어가지 않음. **원인**: 사내 세션과 협력사 세션이 같은 도메인·같은 localStorage 키(`jeilax_sb_auth`)를 공유해, 관리자가 협력사 포털에 로그인하는 순간 **사내 세션이 협력사 세션으로 대체**됐다(사내 콘솔의 Edge Function 호출이 협력사 토큰으로 나가 403). **조치**: 협력사 대상 화면(협력사 로그인·모바일 포털)만 저장소 키를 `jeilax_sb_vendor`로 분리 → 두 세션이 서로 로그아웃시키지 않는다. 사내 콘솔은 Edge Function 호출 직전 세션 역할을 재확인해 원인을 그 자리에서 안내하고, 게이트에 [로그아웃 후 다시 로그인] 버튼을 추가했다. ②**로그인 화면 거래처 선택**: 관리자 계정으로 로그인하면 포털로 넘기지 않고 **로그인 카드 안에서 바로 거래처를 검색·선택**(포털 발주 보유 거래처 기본 목록 + ERP 거래처마스터 서버검색)하고, 선택 즉시 그 거래처로 포털이 열린다(`sessionStorage jeilax_vp_bp` 인계). 부수 변경: 세션이 분리되면서 「사내 SSO 세션으로 협력사 포털 열람」 경로는 성립하지 않으므로, 사내 담당자도 **관리자 계정을 발급받아** 사용하도록 안내 문구를 정정했다. 단일 출처는 `08_협력사발주포털/00_종합정리_요구사항이력`(⑱) | 최동혁(+Claude) |
| 2026-08-20 | 협력사포털 | `supabase/functions/vendor-set-password`(신규) · `vendor-provision` v6 · `vendor-reset-password` v7 · `vendor-admin-provision` v3 · Supabase(migration `vendor_force_password_change` — Auth Hook + `must_change_pw` 컬럼 2종) · `app/lib/api.js`(`changePassword` 경로 전환·`claimsOfToken`) · `app/vendor-login.html`(강제 설정 오버레이) · `pages/협력사_모바일_포털.html`(강제 설정 시트) · `app/admin-vendors.html`(문구·배지) · `실제구축준비 자료/이관/sql/28_vendor_password_policy.sql`(신규) | **협력사·포털 관리자 계정 비밀번호 정책 전환 — 무작위 임시비번 폐지, 고정 초기비번 + 최초 로그인 강제 변경(관리자 지시).** 발급·초기화 시 비밀번호를 **`jeilmns`(고정)** 로 설정하고 `app_metadata.must_change_password=true`를 건다. 이 플래그는 사용자가 지울 수 없고 **Edge Function `vendor-set-password`로 실제 비밀번호를 바꿔야만** 해제된다(초기비번 방치 차단). Auth Hook이 이를 JWT `app_metadata.must_pw`로 실어 보내, 로그인 화면은 **오버레이**, 포털은 **닫을 수 없는 시트**로 새 비밀번호 설정을 강제한다(설정 전에는 조회·등록 등 모든 조작 차단). 관리 화면은 '임시 비밀번호' 표기를 '초기 비밀번호'로 바꾸고 계정 목록·거래처 그리드에 **「초기비번」 배지**를 표시한다. 검증: Auth 최소 길이 6자 실측 확인(7자 `jeilmns` 통과 — 8자 이상으로 올리면 초기값 조정 필요), Hook이 기존 `role`·`vendor_bp` 클레임을 보존함을 실측 확인. 기존 계정 4건은 종전 비밀번호를 그대로 유지하며, 필요 시 관리 화면 [비번 초기화]로 새 정책에 편입된다. 단일 출처는 `08_협력사발주포털/00_종합정리_요구사항이력`(⑰) | 최동혁(+Claude) |
| 2026-08-20 | 협력사포털 | `app/admin-vendors.html`(관리자 계정 섹션) · `app/vendor-login.html` · `pages/협력사_모바일_포털.html`(거래처 선택·읽기전용) · `app/lib/api.js`(`vendorDirApi`·`getOrders({bp})`) · Supabase(migration `vendor_portal_admin_readonly`·`vendor_portal_admin_storage_read`, Edge Function `vendor-admin-provision` v1 신규·`vendor-reset-password` v6) · `실제구축준비 자료/이관/sql/27_vendor_portal_admin.sql`(신규) | **협력사 포털 「관리자 계정」 신설 — 거래처 선택 + 전체 거래처 조회(읽기 전용).** 포털이 「계정 1개 = 거래처 1개」 격리라 구매팀이 여러 협력사를 한 화면에서 볼 수 없던 제약을 해소했다. 거래처에 묶이지 않는 역할 **`vendor_admin`**(헬퍼 `is_vendor_viewer()`)을 신설하고 `sp_*` 6종·`vendor_master`·Storage(`vendor-photos`)에 **SELECT 정책만** 추가 — 쓰기 정책을 부여하지 않아 상태변경·사진등록·메시지·검수요청이 DB에서 차단된다(실측 UPDATE 0행·INSERT 42501). 협력사 담당자 개인정보(`vendor_account`)는 미개방(§1.7). 발급은 `app/admin-vendors.html` 신규 섹션(생성·📧메일발송·비번초기화·비활성/활성·삭제), 대장 `vendor_admin_account`, 감사는 기존 `vendor_account_log`(`create_admin`·`disable_admin`…) 재사용. 포털에는 **거래처 선택 화면**(포털 발주 보유 거래처 기본 + ERP 거래처마스터 3,702개사 서버검색)·관리자 바·읽기전용 가드·「계정 정보」 화면을 추가했다. **사내(@jeilm.co.kr) 이메일 발급은 차단**(같은 주소로 Entra SSO 계정이 생기면 role이 고정돼 사내 권한 상실) — 대신 **사내 SSO 세션은 포털 접속 시 동일 전체 조회 화면**으로 진입한다. **후속 픽스(같은 날)**: 관리자 화면에서 계정 생성이 「아무 반응 없음」으로 보이던 문제 해소 — 원인은 ①입력한 이메일이 이미 협력사 계정으로 등록돼 Auth가 `422 email_exists`로 거부(Edge Fn 400) ②그 오류가 페이지 최상단 배너(`#a_msg`)에 떠서 화면 아래 섹션에서는 보이지 않음. 조치: 결과·오류를 섹션 안(`#va_msg`)에 표시, `callFn`이 응답 본문·상태코드를 예외에 실어 전달, Edge Fn `vendor-admin-provision` v2 — 중복 이메일을 `409 code:email_exists`로 반환하며 **어떤 계정인지**(협력사 계정·거래처명) 알려주고, 관리자가 확인하면 `force:true`로 **기존 계정을 관리자 계정으로 전환**(거래처 소속 해제·`vendor_account` 행 제거·새 임시비번, 로그 `convert_admin`). 단일 출처는 `08_협력사발주포털/00_종합정리_요구사항이력`(⑯)·`실제구축준비 자료/이관/`(00 §5·§9, 03 작업로그) | 최동혁(+Claude) |
| 2026-08-20 | ERP연계 | `pages/자금_결의전표_입력_2026.html`(탭 개명·템플릿 기능) · Supabase(migration `gl_erp_apply_v3_autopost`·`gl_apply_ready_submitted_only`) · `실제구축준비 자료/이관/sql/26_gl_erp_apply.sql`(신규) · `10_ERP_DB연계/etl/gl_apply_demo2.py` · `10_ERP_DB연계/11_ERP직접등록_DEMO2_1차.md`(v1.4→v1.5) | **결의전표 ERP 직접등록 DEMO2 — AX001 신설(결정 C-12, 소급 등재)·「전송 성공=확정」 일원화(결정 C-13)·AX001 기준 2건째 성공.** (소급) 화면 [🚀 ERP 전송] 버튼 E2E 성공(`AG202608200001`) 및 미결 U-B(S0003 허용 계정 범위 부족)를 해소한 **AX 전용 거래유형 `AX001` 신설·채택**(ERP DEMO2에 등록, 분개코드 결정 3단계 확장, 대변 `21100907` 법인카드 하드코딩 거부) — 급식대 전표 재전송 성공(`AG202608190007`). (신규) 화면 「회계 담당자」 탭을 **「ERP 전송」 탭으로 개명**하고 ERP 전표번호 수기 입력칸·[확정 기록] 버튼을 **제거** — 전송 성공 시 RPC `gl_apply_record`가 초안을 자동으로 `submitted→posted`(확정)로 전이하고 ERP 채번번호를 회기입한다(수기 오기입·누락 경로 차단). 안전조치로 `gl_apply_ready`는 `status='submitted'`만 반환하도록 즉시 원복(한 번 넓혔을 때 수기 등록 건이 재노출돼 이중계상 위험 확인). **DEMO2 4번째 E2E 성공(AX001 기준 2건째)**: `DRAFT-20260820-0008`(275,000원 3라인) → **`AG202608200002`**, 라인 3/3 완전일치. 그동안 Supabase migration으로만 존재하던 적용 계층을 저장소 SQL(`26_gl_erp_apply.sql`)로 명문화. 신규 미결 **U-F**(AX001 계정 매핑 등재 권장, 회계팀). **같은 날 이어서 5번째 E2E 성공(AX001 기준 3건째)** — `DRAFT-20260820-0009`(330,000원 3라인, 계정 `53013501` 판)소모품비) → **`AG202608200003`**, 라인 3/3 완전일치, 처음 검증된 경비계정으로 커버리지 확장(U-F 근거 실사: 수기 TG 상위 25종 경비계정 중 17종 자기참조 코드 보유). 단일 출처는 `10_ERP_DB연계/`(00 v0.33·02·11 v1.6) | 최동혁(+Claude) |
| 2026-08-19 | ERP연계 | `10_ERP_DB연계/etl/gl_apply_demo2.py`(신규) · `10_ERP_DB연계/11_ERP직접등록_DEMO2_1차.md`(신규 v1.0) · `jeil-gl-draft`(v5.2) · `pages/자금_결의전표_입력_2026.html`(「ERP 적용」 열) (커밋 `2737dcb`) | **신규 트랙 "결의전표 ERP 직접등록 — 1차(DEMO2)" 개시(관리자 결정 C-11).** 결의전표 파일럿의 "초안은 포털, 확정은 ERP" 구조 중 회계 담당자의 ERP 재입력 단계를 데모 DB **`JEILMNS_DEMO2` 한정**으로 자동화 실증한다 — **운영(`JEILMNS`) 쓰기는 여전히 금지**(C-1 유지), 운영 전환은 별도 기회검토의 착수 게이트 G-1~G-8 통과·재심의가 선행돼야 한다. 전표번호는 AI 전용 **`AG` 대역**으로 분리 채번(ERP 표준 채번기 재사용, TG 대역과 완전 분리로 중복 원천 차단). 적용기 `gl_apply_demo2.py`(대상 DB 하드코딩+단일 트랜잭션+라인 완전대조, `--list/--dry-run/(확정)/--cleanup`), 포털 DB 마이그레이션 `gl_erp_apply_v1`·`v1b`·`v1c`(적용 추적 5컬럼 + 적용 원장 `gl_erp_apply_log` + RPC 3종), Edge Function `jeil-gl-draft` v5.2, 화면 「ERP 적용」 열. DEMO2 읽기전용 실사(DML 없음)로 AG 채번 가동·분개코드 구조(`A_JNL_ACCT_ASSN`→`A_JNL_FORM`)를 확정했다. ~~**구축 완료·적용 실행은 관리자 직접 실행 대기.**~~ → **같은 날 오후 관리자가 직접 실행해 1차 투입까지 성공했다.** 대상 초안 `DRAFT-20260819-0005`(총무팀, 275,000원, 쿠콘 7월분 스크래핑 수수료)를 `--dry-run`→확정 순으로 적용 — 분개코드 자동 해석 성공, AG 채번 **`AG202608190006`**(BATCH `BT202608190007`), 엔진 rc=1·오류 없음, 라인 대조 **3/3 완전일치**(대차 자동보정 미개입)로 COMMIT. 포털 회기입(`erp_apply_status='applied'`) 및 이중 적용 차단(멱등 3중 방어)이 실측으로 정상 작동함을 확인했다(원장 `gl_erp_apply_log` 2행: dry-run/rolled_back·commit/success). 특기(정상 특성): 채번 갱신도 트랜잭션에 포함돼 리허설 롤백 시 번호가 반환되고 확정에서 재사용됨(번호 낭비 없음). 단일 출처는 `10_ERP_DB연계/`(00 v0.32·02·09 v1.5·11 v1.1) | 최동혁(+Claude) |
| 2026-08-18 | ERP연계 | `pages/자금_결의전표_입력_2026.html` (커밋 `707ac08`) | **전표복사 바로 복사 버튼 + 관리항목 표시 정책 개편(관리자 지시).** ①전표복사 조회 목록의 각 행에 [📄 복사] 버튼 — 미리보기를 거치지 않고 바로 복사해 작성 탭으로 이동(기존 미리보기→복사 경로도 유지, 복사 로직 공용화). ②관리항목 표시 정책: **기본 숨김** → 전표 라인에서 줄 선택(☑) 시 해당 줄만 열림 · [전체 펼치기/접기] 버튼으로 전체 토글 · 그리드 배지 클릭은 그 줄 고정 펼침(✕로 접기). 접힌 줄에 필수 미입력이 있으면 안내줄로 경고해 놓침을 방지. ③펼친 카드 UI 개편: 차/대변 색 헤더 스트립+흰 입력 영역 분리, 항목 수·필수 수 표기, 입력칸 높이·포커스 링 개선(요약 칩 방식 제거 — 표시 정책으로 일원화). Vercel main 자동배포 | 최동혁(+Claude) |
| 2026-08-18 | ERP연계 | `pages/자금_결의전표_입력_2026.html` (커밋 `c38fbf1`·`f93d673`) | **결의전표 입력 — 옅은 음영 디자인 + 관리항목 입력 UX 개편(관리자 지시).** ①구획 인지용 옅은 음영: 섹션 제목 밴드·헤더 폼 패널·목록 표 줄무늬·관리항목 차/대변 흐릿한 그라데이션·활성 탭 배경(CSS만, 동작 무변경). ②관리항목 입력 편의: 전표 라인 그리드에 「관리항목」 상태 배지 컬럼(입력 n/전체·필수 미입력 ⚠, 클릭 시 해당 카드 펼침·스크롤·빈 칸 포커스), 카드 접기/펼치기(필수 미입력만 펼침·완료 카드는 요약 칩, 제목 클릭 토글), 「⤓ 전체」 일괄 채우기(계산서일 등 반복 항목을 다른 줄 빈 칸에만 복사), 필드 정렬 그리드화·Enter 다음 칸 이동·상단 필수 진행률(필수 m/n) 배지. 입력 중 그리드 배지는 부분 갱신(포커스 유지). **후속 픽스(커밋 `56327e5`)**: 접기/펼치기 상태를 그리드 얕은 복사본에 저장하던 탓에 전표복사 직후(전 카드 접힘) 제목을 눌러도 펼쳐지지 않아 관리항목 수정이 막히던 버그 해소 — 상태를 공유 객체(`__ctrlUi`)로 이전, 전표복사·전월 프리필 직후에는 전 카드 펼침, 날짜 관리항목 `YYYY/MM/DD`·`YYYY.MM.DD` 형식 정규화. Vercel main 자동배포 | 최동혁(+Claude) |
| 2026-08-18 | ERP연계 | `pages/자금_결의전표_입력_2026.html` · `jeil-gl-draft`(v5.1) · `10_ERP_DB연계/`(00·02·09) · `이관/sql/25_gl_slip_mirror.sql` (커밋 `a715f1f`) | **결의전표 파일럿 — char 패딩 결함 해소 + 계정·거래처 검색 팝업·관리항목 참조검색·디자인 개선** — 앞서 같은 날 "알려진 특성·조치 불필요"로 판단했던 ERP char 패딩(트레일링 스페이스)이 실제로는 계정별 관리항목 매칭 전부 miss·거래처명 조인 실패·입력경로 필터 오작동을 일으키는 결함이었음이 드러나 정정·해소했다. **조치 3중**: 마이그레이션 `gl_pad_trim_v1`(미러 5테이블 trim + RPC 3종 btrim 재정의) + ETL 5개 job `RTRIM` + 화면·Edge Function trim 방어 — 검증 거래처 조인 11,734/11,734·계정×관리항목 조인 42,466건 정상. 같은 배포에서 계정과목·거래처 검색 팝업(select 드롭다운 대체), 관리항목 참조검색(품목 신규 RPC `gl_item_search`), 관리항목 패널 카드형 디자인 개선도 배포(`jeil-gl-draft` v5→v5.1, Vercel main 자동배포). 단일 출처는 `10_ERP_DB연계/`(00 v0.30·02·09 v1.4) | 최동혁(+Claude) |
| 2026-08-18 | ERP연계 | `pages/자금_결의전표_입력_2026.html` · `jeil-gl-draft`(v5) · `10_ERP_DB연계/`(00·02·09) · `이관/sql/25_gl_slip_mirror.sql` | **결의전표 파일럿 「전표복사」 기능 신설·배포 + 결의전표 원장 미러링 결정(C-10)** — 본인이 ERP에 입력한 결의전표를 기간·검색으로 조회→미리보기→복사해 초안을 시작하는 탭을 신설. 이를 뒷받침해 기존 C-7("코드 마스터만 적재")을 확장하는 **관리자 결정 C-10**으로 결의전표 원장(헤더·라인·관리항목)을 중간DB에 미러링(`erp_ro.gl_slip_s`·`gl_slip_item_s`·`gl_slip_ctrl_s`, 마이그레이션 `gl_slip_mirror_v1`+`gl_slip_synced_at_v1`) — 통제 3중(RLS 전면차단+RPC 단일경로 / 조회 RPC 소유 필터 강제 / 민감 관리항목 EM·BA·D1·CP·NN ETL 단계 NULL 마스킹). ETL job 3종 신설, Edge Function `jeil-gl-draft` v4→**v5**(op `slip_list`·`slip_get`). ERP운영DB dry-run 3종 성공(3,224·16,865·42,371행) → **같은 날 오후 관리자 직접 실행으로 실적재까지 완료** — `gl_slip_s` 3,226행·`gl_slip_item_s` 16,879행·`gl_slip_ctrl_s` 42,422행(batch_run 3건 success), 민감 관리항목 마스킹 실측 확인(BA·EM·D1·NN `ctrl_val` 전량 NULL), 조회 RPC 소유 필터 실측 확인(등록자 78명 중 본인 20건만 반환). 잔여는 야간배치 스케줄 편입뿐. 단일 출처는 `10_ERP_DB연계/`(00 v0.29·02·09) | 최동혁(+Claude) |
| 2026-08-18 | 포털UI | `04_챗봇_포털_데모UI.html` · `JEIL_AX_포털데모_통합본.html`(재생성) | **상단 글로벌 바 신설(UX 개편)** — 왼쪽 사이드바와 별도로 전 뷰(챗봇·운영 페이지·관리자) 공통 상단 바를 추가. 구성: ①주요 이동 탭(챗봇/운영 페이지/관리자 — 현재 뷰 강조, 관리자는 권한 시에만) ②**통합 검색**(Ctrl+K 또는 `/`, 운영 페이지 카드·전 부서 업무 도구·주요 기능을 한 입력창에서 검색, ↑↓·Enter 키보드 탐색, 잠금 페이지는 검색 제외) ③즐겨찾기 칩(기존 챗봇 뷰 한정 대시보드 라인을 글로벌 바로 승격 — 어느 뷰에서든 표시·바로가기). 모바일은 1행 이동·즐겨찾기 + 2행 전체폭 검색으로 재배치, 햄버거 버튼 글로벌 바로 일원화. Playwright 검증(검색 3유형·키보드·뷰 전환·대화 유지·모바일) 후 main 푸시 → Vercel 자동 배포 | 최동혁(+Claude) |
| 2026-08-18 | 포털UI | `04_챗봇_포털_데모UI.html` · `JEIL_AX_포털데모_통합본.html`(재생성) | **메인 상단 즐겨찾기 대시보드 라인 신설.** 챗봇 메인 헤더 아래에 「★ 즐겨찾기」 바로가기 라인을 추가하고, 등록 위치 2곳 — ①사이드바 추천 업무 도구(☆ 토글, 클릭 시 챗봇 데모 실행) ②사내 운영 페이지 카드 우상단(☆ 토글, 클릭 시 페이지 새 탭 열기) — 에서 별표를 누르면 상단 라인에 칩으로 표시·바로가기되도록 했다. 잠금(권한 없음) 카드는 등록 제외, 서버 권한 재렌더(`loadPagePerms`) 후에도 ★ 자동 주입. 저장은 `localStorage 'jeilax_fav_v1'` — **데모 한정**(실구축 시 백엔드 사용자 설정으로 대체, 코드 주석 명시). Playwright 기능 테스트(등록·해제·칩 이동·데모 실행)로 검증 후 통합본 재생성 | 최동혁(+Claude) |
| 2026-08-12 | ERP연계·회계·코드 | 마이그레이션 `gl_template_v2`(`gl_draft_item_ctrl`·`gl_template_item_ctrl`·`gl_template_usage` 신설 + `gl_template` 월반복 속성 + RPC `gl_ctrl_master_get`) · `이관/sql/24_gl_template_v2.sql`(신규) · `jeil-gl-draft` **v4 배포** · `pages/자금_결의전표_입력_2026.html`(월반복 홈·관리항목 패널) · `10_ERP_DB연계/10`(v1.4) | **반복전표 자동화 P1·P3 구현 완료(관리자 지시).** 포털 DB에 관리항목(초안 라인·템플릿 규칙·계산서 그룹 `invoice_seq`)과 회차 사용 이력(4상태) 구조를 신설(C-6 규약 — RLS ON·정책 0·Edge Fn 단일 경로)하고, `jeil-gl-draft`를 v4로 배포했다 — 신규 op 3종(`tpl_recur_list` 월반복 카드 / `tpl_apply_prev` 본인 확정 초안 기반 전월 프리필 / `tpl_seed_bulk` 관리자 한정 시드 일괄 등재) + save 확장(필수 관리항목 검증은 신형 화면에만 하드, **세액 자동계산·검증 없음 — B2 결정 준수**), **구화면 하위호환 유지**. 화면에는 월반복 홈(미작성/작성중/제출됨/확정됨 배지·중복 작성 경고·이어서 작성), 관리항목 자동 입력 패널(계정×차대 필수 * 표시), 제출 전 요약(전월 대비 달라진 점), 회계 담당자 관리항목 표·TSV 복사를 신설했다. **3관점 코드 리뷰(정확성·보안·UX) 지적 16건 반영 후 재배포** — 치명 1건(수동 행 관리항목 유실), 민감 관리항목(사번·계좌) 부서/전사 템플릿 고정값 저장 차단, 타인 미제출(draft) 초안 열람 차단(최소권한), KST 회차 경계 보정, 월반복 회차 기록 위조 방지 포함. 무토큰 401 게이트 실측 확인. **잔여: 화면 라이브 반영은 git 커밋·gh-pages 배포 후(관리자), P4 시드 등재는 회계 검수(B2 잔여) 후** | 최동혁(+Claude) |
| 2026-08-12 | ERP연계·회계 | Supabase(`erp_ro.ctrl_item_s` 52행·`acct_ctrl_assn_s` 1,002행 실적재, `erp_load_scope` +2, 마이그레이션 `erp_sync_overview_ctrl_master`) · `이관/sql/23`(후속 기록) · `10_ERP_DB연계/10`(v1.3) | **B1 관리항목 마스터 실적재 완료(관리자 "실적재 진행" 지시).** `ctrl_item` 52/52 · `acct_ctrl_assn` 1,002/1,002(추출=적재, 유실 0, batch_run success 2건 — dry-run 1,001 대비 +1은 원천 증가분). 계정별 필수 플래그(DR/CR 'Y') 항목 377행 확보 — 결의전표 화면의 계정별 관리항목 자동 생성·필수 검증 데이터 선행 조건 충족. `erp_load_scope`에 accounting/ctrl_item·acct_ctrl_assn 'loaded' 등재, 연동 현황 뷰 14→16종 확장(fail-closed 규약에 따라 batch_run 기반 count, 실테이블 직접 count 안 함). 반복전표 자동화 P2 완료 — 다음은 P3(화면 구현)·P4(시드 검수 후 등재) | 최동혁(+Claude) |
| 2026-08-11 | ERP연계·데이터 | Supabase(`erp_ro.pur_order_s`·`pur_req_s`·`sales_orders_m`·`purchase_m`·`iv_dtl_s`·`inventory_d` 증분 갱신, `erp_secure.hr_payroll_m` 증분 갱신) · `10_ERP_DB연계/00`(v0.24)·`02` | **야간배치 실패 7개 job — 관리자 "직접작업진행" 지시로 실적재 완료.** 재동기화 1차 실행(2026-08-11 11:12 KST)에서 실패했던 7개 job(pur_order·pur_req·sales·purchase·iv_dtl·inventory·hr_payroll)을 관리자가 "직접작업진행"을 지시해 어시스턴트가 직접 실행했다(기존 `!` 직접 실행 게이트를 관리자가 이번 건에 한해 명시적으로 해제, 15:35 KST). 7개 job 전량 success, 추출=적재, 유실 0(pur_order 367·pur_req 1,267·sales 40·purchase 196·iv_dtl 660·inventory 2,276·hr_payroll 167 — 급여는 집계만, 개인 행·이름·주민번호·계좌 없음). date→datetime 승격 수정이 실적재에서도 정상 동작함이 확정됐다. 연동현황 14종 전체가 2026-08-11 동기화로 갱신, 18일 적재 공백(07-24→08-11) 해소. 미해소 유지: ODBC Driver 18 미설치(코드 우회만)·`A_ACCT.PROJECT_FG` 692행 전량 NULL·`timestamptz` 9시간 라벨 오차·`gl_slip` 의도적 미연계 | 최동혁(+Claude) |
| 2026-08-11 | ERP연계·회계·권한 | 마이그레이션 `gl_ctrl_master_v1`(erp_ro.`ctrl_item_s`·`acct_ctrl_assn_s` 신설 + `erp_master_upsert` 확장) · `이관/sql/23_erp_ctrl_master.sql`(신규) · `etl_run.py` job 2종(ctrl_item·acct_ctrl_assn) · `public.gl_period_lock` 33행 · `perm_module_catalog`(+accounting) · `perm_grant`/`perm_audit`(이훈희) · `10_ERP_DB연계/10`(v1.2) | **반복전표 자동화 승인 게이트 집행(관리자 결정 2026-08-11).** ①**B1 승인** — 관리항목 마스터 2종 미러 스키마·RPC 확장·ETL job 신설, dry-run 통과(52행·1,001행). 실적재는 관리자 직접 실행 대기(`etl_run.py --job ctrl_item` / `--job acct_ctrl_assn`). ②**B2 부분 승인** — 시드 등재는 진행하되 **부가세·원천징수 자동계산 규칙은 범위 제외**(세액은 실물 증빙 보고 수기 입력, 전월값 프리필만 제공. U-1 미해소·C-2 무개정). ③**B2-1 적용 완료** — ERP 월마감 원장 `C_CLOSE_STATUS` 실측(2026-06까지 마감 Y·07부터 미마감) 연동으로 `gl_period_lock` 2023-10~2026-06 33개월 등재. 마감월 통제 부재(09 §5.2)가 처음으로 해소됨. ⚠ ERP는 마감월에도 결의전표 소급 등록이 관찰돼 포털이 더 엄격해짐 — 예외는 회계 담당이 lock 해제. 새 달 마감 시 수동 추가 필요. ④**B3 집행** — 파일럿 이훈희·최동혁 확정. `accounting` 모듈을 권한 카탈로그에 신설(sensitive, A3 실행)하고 이훈희에 개인 예외 부여(감사 기록 동반). 실측: 이훈희 gl_draft 허용·비대상자 차단 유지·관리자 기허용 | 최동혁(+Claude) |
| 2026-08-11 | ERP연계·데이터 | `10_ERP_DB연계/etl/etl_run.py`(date→datetime 파라미터 승격) · `10_ERP_DB연계/etl/README.md`(ODBC 드라이버 절 신설) · Supabase(`erp_ro.acct_master_s` 등 7종 갱신) · `10_ERP_DB연계/00`(v0.23)·`02` | **야간배치 12개 job 재동기화 — 관리자 실제 실행(7 성공/7 실패) → 레거시 ODBC 드라이버 원인 확정·수정 → 실패 7개 재실행 대기(정정).** 08-10에 "명령 준비·미실행"으로 기록했던 재동기화를 관리자가 실제로 실행(`etl_run.py --job all`, 11:12 KST)한 결과 **7 성공**(usr_master·dept_master·usr_erp_module·item_master·acct_master **692**·cost_center·bp_master, 유실 0) **/ 7 실패**(pur_order·pur_req·sales·purchase·iv_dtl·inventory·hr_payroll, 전부 `HYC00`…`SQLBindParameter` 오류). **원인**: 이 PC의 레거시 ODBC 드라이버가 `SQL_TYPE_DATE` 파라미터 바인딩을 구현하지 않아 `datetime.date` 파라미터를 쓰는 job만 실패(절반이 성공해 로그상 함정 발생). `run_job()`에 date→datetime 승격 변환을 추가해 우회, 실패 7개 전부 `--dry-run` 재검증 정상. **실적재는 아직 미실행 — 관리자 `!` 대기.** 부수로 회계 마스터가 691→692로 늘었다 — 08-10 대사(차이 0)는 정확했으나 08-11 09:53 ERP에 신규 계정 1건(`55004101`)이 등록돼 반영됐다(관리자가 지칭한 "미적용 회계계정정보"는 결과적으로 실재·해소). 신규 이슈로 `timestamptz` 라벨 9시간 오차(ERP naive datetime이 UTC로 잘못 라벨링)를 발견·등재만 함(미조치, watermark 유실은 없음). | 최동혁(+Claude) |
| 2026-08-10~11 | ERP연계·거버넌스 | Supabase(마이그레이션 `erp_sync_overview_add_bp_master`) · `실제구축준비 자료/이관/sql/08_erp_pub_reporting_views.sql`(정본 최신화) · `10_ERP_DB연계/00`(v0.21→v0.22 병합)·`02` · 정책관리 미러(경로 설정 수정 포함) | **회계 마스터 대사 검증(차이 0) + 연동 현황 뷰 사각지대 수정(`bp_master` 13→14종) + 정본 SQL 드리프트 시정 + 적재 노후도 실측.** 관리자 지시("회계계정정보 동기화 진행")를 점검한 결과 **회계 마스터는 이미 최신이라 동기화할 차이가 없었다**(dry-run 691·133이 08-03 적재분과 신규·소멸·필드변경 전부 0, `project_fg` 전량 NULL은 재추출로도 재현돼 "가능성"에서 "ERP 원천 결함 확정"으로 승격). 대신 조사 중 두 가지가 드러났다 — ①연동 현황 뷰가 2026-07-24 실적재된 `bp_master`(4,650행)를 누락하고 있던 사각지대(13→14종으로 수정, 정본 SQL도 5종 옛 버전에서 최신 14종 정의로 교체), ②회계(08-03, 8일 경과)보다 **나머지 12개 job이 오히려 2026-07-24(18일 경과)에 정지**돼 더 노후한 상태(재동기화는 명령만 준비, 관리자 `!` 직접 실행 대기·미실행). 부수로 사내 OneDrive 정책관리 미러 경로 설정(`.claude/erp_db_mirror.path`)이 존재하지 않는 Windows 프로필을 가리키고 있어 동기화가 엉뚱한 위치에 쌓이던 오류를 발견·정정했다. | 최동혁(+Claude) |
| 2026-08-11 | ERP연계·회계 | `10_ERP_DB연계/10_반복전표_자동화_기획.md`(신규 v1.1) · `etl/analyze_repeat_gl.py`(신규) · `_build_html.py` · `.gitignore` · `00_문서대장` · `10_ERP_DB연계/00`(v0.22)·`02`·`09`(v1.2)·`index.html`(허브 카드 추가) — 관제 문서 3종에 전파 완료 | **결의전표 반복전표 자동화 — ERP 실측 분석·기획(템플릿 플랫폼 v2).** 관리자 요청("매달 같은 형태의 전표를 한 번 등록해 두고 변동값만 고쳐 재사용")에 따라 ERP운영DB 2026년 수기 결의전표 **2,287건 전수를 읽기전용 분석**(A2 승인 근거, NOLOCK·파라미터 바인딩, 민감 관리항목 값 미기록). **결과**: 동일 부서+계정조합+거래처가 3개월 이상 반복되는 패밀리 **156개 = 전표 964건(42.2%)**, 금액 완전고정 34개, 월1건형(1차 시드 후보) **36개**. 유형 6종 분류(세금계산서 고정/변동·개인경비·원천세보험·자금이체·무부가세). **관리항목 체계 실측** — ERP 화면 하단 그리드는 `A_TEMP_GL_DTL`, 계정별 필수 요건은 `A_ACCT_CTRL_ASSN`(997건)이 정의하며 현 gl_draft 파일럿에 없는 구조임을 확인. **기획(v2)**: 월반복 홈(상태 배지 4단계)·전월 프리필(본인 확정 초안 기반)·변동 필드 하이라이트·총액 자동 배분·관리항목 자동 생성(soft 검증 — 실물 계산서가 정답)·시드 일괄 등재·회계 담당자 대기열. 신규 ETL job 2종(ctrl_item·acct_ctrl_assn — 코드 마스터만, C-7 유지) 계획. **v1.0 작성 후 4관점 교차검증(수치·회계·UX·규칙) 지적 36건을 반영해 v1.1 개정**(부가세 하드검증 위험·배지 시점 중복작성 위험·분석도구 민감값 수집 등 치명 3건 포함). 승인 게이트 B1(마스터 적재)·B2(시드 검수+부가세/원천징수 규칙=U-1 해소+C-2 개정)·B2-1(마감 기준)·B3(파일럿 대상) 대기. 구현 미착수(기획 단계). | 최동혁(+Claude) |
| 2026-08-05 | ERP연계·회계 | `public.gl_template`·`gl_template_item`(신규) · 마이그레이션 `gl_template_v1` · `이관/sql/22_gl_template.sql`(신규) · `jeil-gl-draft` **v3** · `pages/자금_결의전표_입력_2026.html` · `10_ERP_DB연계/09`(v1.1) | **결의전표 템플릿 플랫폼 기본 구성 — 케이스별 분개를 등록해 두고 골라 쓰는 구조.** 계정과목 691개에서 매번 고르는 방식은 전 직원용으로 현실적이지 않아, "이 케이스는 어떤 계정을 차/대변에 쓰고 금액을 어떻게 나누는가"를 템플릿으로 저장하고 **사용자는 템플릿을 고르고 총액만 입력**하도록 했다. ① **DB 2종 신설** — 헤더(공개범위 org/dept/user·금액방식 total/manual·상태·`erp_case_cd`·사용횟수)와 라인(계정 + 배분규칙). 보안은 `gl_draft` 계승(C-6, RLS ON·정책 0 + Edge Function 단일 경로). **금액 실적치는 담지 않는다**(고정액·비율은 규칙값). ② **Edge Function v3** — op 6종 추가(`tpl_list`·`tpl_get`·`tpl_save`·`tpl_status`·`tpl_delete`·`tpl_use`). **공개 범위와 편집 권한을 분리** — 전사·부서 템플릿은 관리자·회계 담당자만 생성·수정(잘못된 분개가 전사로 퍼지는 것을 막는 장치), 개인 템플릿은 본인. 템플릿 저장 시에도 계정코드는 마스터 대조를 거치고, 차액(balance) 규칙은 방향당 1줄로 제한. ③ **화면** — 작성 탭 상단에 템플릿 카드·분류 칩·검색 + 총액 입력→[적용](헤더는 **빈 칸만** 채우고 라인 전체 교체), 「선택 행에 차액 채우기」, 「현재 전표를 템플릿으로」 역방향 등록, 4번째 탭 「템플릿 관리」(표준 그리드로 라인 편집·공개/비공개·삭제). ④ **템플릿 적용이 검증을 면제하지 않는다** — 적용 결과는 `save` 서버 재검증을 그대로 통과해야 한다. **등재된 템플릿은 0건**으로, 실제 케이스는 **A5 회계 규칙 확보 후 ERP_DB 기준으로 입력**한다(승인 없는 계정 조합을 임의 생성하지 않음). 부수: 계정 `bal_fg` 실측값이 `DR/CR`인데 화면이 `D/C`로 비교해 **차대 자동제안이 동작하지 않던 버그** 수정. | 최동혁(+Claude) |
| 2026-08-03 | ERP연계·문서 | `10_ERP_DB연계/09_결의전표_인수인계`(신규 v1.0) · `_build_html.py` · 연계 허브 카드 · `00_문서대장` | **결의전표 파일럿 종합 인수인계 문서 신설 — 재개 진입점.** 이틀에 걸쳐 06(추진계획)·07(연동 종합)·08(처리 프로세스)과 00·02 작업 이력에 흩어진 내용을, **시간이 지나거나 담당이 바뀐 뒤 여기부터 읽으면 바로 이어서 진행할 수 있게** 한 문서로 취합했다. 구성: ①30초 요약(다음 할 일 = S5 E2E 테스트 하나, 남은 승인 = A5 하나) ②**확정 결정 9건**(C-1~C-9 — 되돌리려면 재설계가 필요한 것들. C-8 전체관리자 한정·C-9 전표형태 상수 신규 추가) ③구축물 위치 맵(코드·DB·RPC·마이그레이션·문서 역할 구분) ④**실측으로 확정된 ERP 사실 12항목**(추정과 실측을 구분해 코드 변경 시 판단 근거로 삼도록) ⑤**반드시 알아야 할 것 4건**(프로젝트 검증 무력·마감월 0건·회기입 누락은 못 막음·테스트 데이터 잔존) ⑥**재개 절차 5단계**(STEP 1 상태 확인 SQL → STEP 2 S5 E2E 9단계 + 서버 검증 우회 시도 → STEP 3 A5 → STEP 4 `/security-review` → STEP 5 S6 회고) ⑦확대 갈림길 E3 ⑧미결정 5건 ⑨**작업 환경 메모**(ERP 접속 폴백 구조·ETL 명령·배포 방식·문서 빌드·git 미커밋 상태). 06~08은 세부 문서로 유지하고 09를 진입점으로 삼는 관계를 허브 카드(★ 강조)와 문서대장에 명시. | 최동혁(+Claude) |
| 2026-08-03 | ERP연계·회계 | `jeil-gl-draft` **v2**(형식 검증 신설) · `pages/자금_결의전표_입력_2026.html` · `10_ERP_DB연계/00`(v0.20)·`02`·`06`(v1.2)·`07`·`08` | **A2 승인·실행 — 결의전표 코드 체계 실측 확정 + 전표번호 형식 검증 신설.** 관리자 승인으로 `A_TEMP_GL` 샘플을 읽기 전용 1회성 조회(금액·적요는 최소수집으로 미조회)해 06 추진계획 **S2를 완료**했다. **① 등록자 형식 확정** — 2026년 결의전표 **2,947건 전량 이메일형 `INSRT_USER_ID`**(고유 76명, 비이메일형 0건)로, 기존 자동매핑(`lower(usr_id)=lower(upn)`)이 **추정에서 실측 확정으로 승격**되고 보정은 불필요했다(그룹웨어 전자결재가 전표등록자 기준이라는 관리자 요구사항의 근거 확보). **② `GL_TYPE`은 선택지가 아니라 상수** — 2026년 전량 `03` 단일값이라 화면 드롭다운을 `03 · 일반전표` 하나로 확정하고 서버 기본값 `GL_TYPE_DEFAULT='03'`을 두었다(실측 근거 없는 코드는 임의 추가하지 않는다는 원칙 적용). **③ `GL_INPUT_TYPE`** — `TG` 2,177(74%)·`UX` 426·`AP` 245·`AR` 82·`FN` 9·`DP` 6·`AC` 2로, **수기 결의 경로가 `TG`**이고 나머지는 타 모듈 자동생성분임을 확인. **④ `CONF_FG`** — 등록 직후 `U`(미승인)→회계 승인 시 `C`(C 2,719/U 228, 최근 10건 전부 `U`). **⑤ `TEMP_GL_NO` 형식 확정 → 검증 신설** — `TG`+YYYYMMDD+4자리 순번 **14자 고정**(2026년 전량 동일). 이 실측을 근거로 Edge Function에 **형식 검증 `erp_no_format`(400)** 을 신설하고 **v1→v2 ACTIVE 배포**했다 — 직전 E2E 테스트에서 전표번호에 `"1"` 같은 임의값을 넣어도 확정 처리되던 공백을 막는 조치다(기존 검사는 빈 값 여부·타 초안 중복뿐). 부수 확인: 부서코드 4자리, 사업장 `BA1` 단일. **남은 승인은 A5(회계 규칙) 하나**이며, 계정 691개를 실무용으로 좁히는 문제라 E2E 테스트 진행에는 지장이 없다. | 최동혁(+Claude) |
| 2026-08-03 | ERP연계·문서 | `10_ERP_DB연계/08_결의전표_처리프로세스.html`(신규) · 연계 허브 카드 · `00_문서대장` | **결의전표 처리 프로세스 문서 신설 — 등록부터 ERP 확정 기록까지 데이터 작업 명세.** "확정 기록 시 실제로 무엇이 처리되는가"를 설명할 자료가 없어, `jeil-gl-draft` v1 구현을 근거로 전 과정을 단계별 데이터 작업 단위로 문서화했다. 구성: ①**전체 흐름 스윔레인**(작성자·포털 서버·회계 담당자·ERP운영DB 4레인, 각 지점의 INSERT/UPDATE 표기) ②**7단계 카드** — 각 단계마다 *읽는 것·쓰는 것·검사 조건·실패 코드*를 분리 기술(1 bootstrap 초기적재 / 2 save 서버 재검증 7종 / 3 submit / 4 회계 검토 / 5 **ERP 등록은 사람이 직접** / 6 **확정 기록 post** / 7 ERP 내부 전기 — 포털 범위 밖) ③**상태 전이도** 4상태(draft·submitted·posted·void)와 이동 규칙(posted는 수정·폐기 모두 불가) ④**감사 기록 8종**(거부된 접근 `denied` 포함) ⑤**오류 코드 9종**의 의미·조치 ⑥주의점 5건 ⑦테스트 시나리오 10단계. **핵심 메시지**: 포털의 &lsquo;확정&rsquo;은 ERP에 쓰는 동작이 아니라 **사람이 등록한 결과(전표번호)를 받아 적는 기록 동작**이며, 이 회기입이 이중 입력을 막는 유일한 장치라 애플리케이션 검사(전표번호 필수·중복 거부)와 DB 제약(`gl_draft_posted_needs_no`)으로 이중 방어한다. 저장 시 **합계를 화면 값이 아니라 라인에서 재계산**하고 수정 시 **라인을 전량 삭제 후 재삽입**하는 이유(합계-라인 불일치 원천 차단)도 명시. 현재 한계 4건(프로젝트 검증 무력·마감월 0건·전표형태 잠정·관리자 한정)을 테스트 전 인지 항목으로 분리 표기. | 최동혁(+Claude) |
| 2026-08-03 | ERP연계·회계 | `erp_ro.acct_master_s`(691행)·`cost_center_s`(133행) 실적재 · 마이그레이션 `erp_sync_overview_accounting_master` · `erp_load_scope` 2행 · `10_ERP_DB연계/00`(v0.19)·`02`·`06`(v1.1)·`07` | **회계 코드 마스터 실적재 + 연동 현황 반영 + 권한 전체관리자 한정(A1 승인·A3/A4 보류 결정).** 관리자 지시로 결의전표 파일럿의 최대 차단 요인이 해소됐다. ① **실적재**: dry-run 재검증(691·133) 후 실행해 `acct_master` **691/691**·`cost_center` **133/133**(추출=적재, 유실 0, `batch_run` success 2건). 적재 범위는 **코드 목록뿐** — 금액·잔액·전표·분개 라인은 미적재 유지. ② **연동 현황 뷰 11→13종**: 회계 마스터 2종 등재. **⚠ 실테이블 `count(*)` 대신 `batch_run` 성공 건수로 표시** — 이 뷰는 `security_invoker`인데 회계 마스터는 설계상 `authenticated` GRANT 없는 fail-closed 구성이라 실 count 시 사내 사용자에게 permission denied가 나 **연동현황 화면 전체가 403**이 된다(2026-07-21 `iv_dtl` 사고와 동일 유형). 급여 집계 선례를 따라 회피했고 **권한을 열어 우회하지 않았다**. ③ **권한(A3·A4) 관리자 결정**: 테스트 단계이므로 `accounting` 모듈을 **어느 부서에도 부여하지 않고 전체관리자만** 사용 → `dept_erp_scope` 무변경(목표 상태가 이미 성립). 실측 재확인 — 비관리자 2명 `gl_draft` `allowed=false`, 관리자만 `true`. ④ `erp_load_scope`의 `accounting/acct_master`·`cost_center`를 `loaded`로 기록(챗봇 결측 배지·데이터 요청 완료 게이트 판정 근거), `gl_slip`은 `none` 유지. ⑤ **검증**: `gl_master_get()` 계정 691·코스트센터 133·부서 107 반환 → 마스터 미적재 배너·`master_empty` 503 해소. 계정 구성 BS 268·PL 235·MC 184·미분류 4, 차대 DR 507·CR 184로 차대 자동제안 동작. **발견 1건(등재만)**: `project_fg`가 **691행 전량 NULL** — 서버 재검증 7종 중 5번(프로젝트 필수 계정 강제)이 **현재 어떤 계정에도 걸리지 않는 무력 상태**. ERP 원천이 비어 있거나 다른 컬럼·체계로 관리될 가능성이 있어 A5·유니포인트 확인 대상으로 등재(임의 조치 없음). **잔여**: A2·A5 승인, S5 E2E 테스트 1건 + 서버 검증 우회 시도, 실사용 직전 `/security-review`. | 최동혁(+Claude) |
| 2026-08-03 | ERP연계·문서 | `10_ERP_DB연계/07_결의전표_연동_종합.html`(신규) · 연계 허브 index 카드 · `00_문서대장` | **결의전표 연동 종합 정리 뷰 신설(구조·현재상태·의사결정).** 06(남은 일)·00 §4(구축 내역)·02 §3(작업 이력)에 흩어진 전표 연동 내용을 한 문서로 합치고, 특히 **의사결정을 4갈래로 분리**해 무엇이 이미 확정됐고 무엇이 아직 열려 있는지 구분되게 했다: ①**확정 7건**(C-1 반영경로=초안+사람이 ERP 확정/대안 &lsquo;포털 직접등록&rsquo; 기각 · C-2 케이스=일반 경비 지출결의 · C-3 등록자=MS 계정 ID(그룹웨어 전자결재가 등록자 기준) · C-4 전표번호는 ERP 채번(포털은 `DRAFT-`) · C-5 권한 2단 분리(입력 `accounting`/확정 `finance`) · C-6 RLS 전면차단+Edge Fn 단일경로 · C-7 코드 마스터만 적재) ②**승인 대기 A1~A5**(승인 범위·없으면 생기는 일·주체·우선순위. **A1은 코드 목록이라 위험 낮고 A2는 실제 전표 열람이라 민감** — 묶지 말고 A1만 먼저 승인해도 진행 가능함을 강조) ③**미결정 5건**(부가세·프로젝트 필수계정·초안 보존기간·확정 권한 분리·E3 착수시점, 각각 &lsquo;언제 결정해야 하는지&rsquo; 명시) ④**확대 로드맵 E1~E5**(**E3 ERP 쓰기 API가 실질적 갈림길** — 현 구조는 회계팀 이중작업이라 확대 시 병목). 함께 정리: ERP 원천 6테이블 실측 매핑, 등록자 자동매핑 근거(99명 전원 이메일형), 서버 검증 7종, 실측 통과 항목과 **미실행 항목(업무규칙 우회 테스트 — 유효 토큰 필요)을 분리 명기**, 리스크 6건(대비책 ↔ 잔여위험 분리), 다음 액션 6단계. 파생 뷰 원칙 명시(원천이 정답). | 최동혁(+Claude) |
| 2026-08-03 | 거버넌스·문서 | `00_관리체계/04_현재상태_한장요약.html`(신규) · `00_문서대장` | **현재상태 한 장 요약(파생 뷰) 신설.** 프로젝트 현황이 총괄 대시보드·ERP 연계 관제·이관 관제·CHANGELOG 4곳에 나뉘어 있어 "지금 어디까지 왔고 무엇이 막혀 있나"를 한 화면에서 볼 수 없었다. 원천을 대사해 A4 한 장 분량으로 압축한 요약 뷰를 만들었다(자체완결 HTML, 인쇄 대응). 구성: ①헤드라인(만드는 단계는 거의 끝났고 **막힌 것은 대부분 사람의 결정**) ②KPI 6종(영역 10·평균 진행률 71%·의사결정 23/34·라이브 화면 12·ERP 연동 소스 12·**Entra Secret D-128**) ③영역별 현황 10종(상태·진행률 미터·막고 있는 것) ④최근 완료/진행 중/막힌 것 3분할 ⑤다음 할 일 6단계(1순위 **A1 회계 마스터 조회 승인**) ⑥대기 의사결정 11건 ⑦리스크 5건 ⑧원천 바로가기. **파생 뷰 원칙 명시** — 수치는 원천에서 계산했고 어긋나면 원천이 정답, 수기 기입 금지(CLAUDE.md §10). | 최동혁(+Claude) |
| 2026-07-31 | ERP연계·문서 | `10_ERP_DB연계/06_결의전표_추진계획`(신규 v1.0) · 연계 허브 index · `00_문서대장` | **결의전표 파일럿 추진계획 문서 신설.** 구축은 끝났는데 "왜 아직 못 쓰나·다음에 뭘 해야 하나"가 관제 문서 여러 곳에 흩어져 있어, **남은 일만** 모은 실행 문서를 만들었다. 구성: ①한 눈에 보기(현재 막힌 지점 = 마스터 미적재 + 권한 미부여, 둘 다 사람 승인 필요 — **가장 먼저 할 일은 A1 하나**) ②승인 게이트 A1~A5(무엇을 승인하나·없으면 생기는 일·결정 주체. A1은 코드 목록이라 위험 낮고 A2는 실제 전표 열람이라 민감 — **A1만 먼저 승인해도 진행 가능**함을 명시) ③실행 단계 S1~S6(실행 명령·완료 기준 포함, S5는 전표 1건 E2E 7단계 + 서버 검증 우회 시도) ④확대 로드맵 E1~E5(**E3 ERP 쓰기 API가 핵심 갈림길** — 파일럿은 회계 담당자 이중작업이라 실무 확대의 병목) ⑤리스크 6건(1순위: 이중 입력, 2순위: 회계 담당자 부하) ⑥미결정 5건. 연계 허브에 카드 등재 + 현재 위치 문구 갱신, 정책관리 미러 동기화. | 최동혁(+Claude) |
| 2026-07-31 | ERP연계·회계 | `10_ERP_DB연계/00·02`(v0.18) · `이관/sql/21_gl_draft.sql`(신규) · `jeil-gl-draft` **v1** · `pages/자금_결의전표_입력_2026.html`(신규) | **결의전표 초안 입력 파일럿 — 회계(A 모듈) 첫 개방, 기반 구축 완료(실적재·권한부여는 승인 대기).** 전 직원이 ERP 화면에서 각자 입력하던 결의전표를, 케이스 1개(일반 경비 지출결의)에 한해 사내 포털에서 **ERP와 동일한 형태로** 입력받게 하는 파일럿이다. **아키텍처는 "초안은 포털, 확정은 ERP"** — 포털은 ERP에 쓰지 않는다(CLAUDE.md §1.2·§1.6). 포털이 검증된 초안을 만들고 회계 담당자가 ERP 화면에서 최종 저장한 뒤 전표번호를 되돌려 기입해야 초안이 닫히며, 이 회기입이 **이중 입력을 막는 유일한 장치**라 DB 제약으로 강제했다(같은 전표번호 중복 기입도 거부). ① **ERP 구조 실측**: `A_TEMP_GL`(결의전표 헤더 28,219행)·`A_TEMP_GL_ITEM`(라인 294,043행) → 승인 시 `A_GL`로 전기. ② **등록자 매핑**(핵심 요구사항 — 그룹웨어 전자결재가 전표등록자 기준): `usr_master_s` 99명 전원이 이메일형 ERP 계정이라 MS 계정(UPN)과 `lower()` 비교로 자동 매핑되며, 등록자는 **JWT에서만** 취득하고 매핑 실패 시 저장을 거부한다(ERP 계정 없는 사람 명의의 전표 차단). ③ **ETL** job 2종(`acct_master`·`cost_center`) — **금액·잔액·전표가 아닌 코드 마스터만** 적재(dry-run 691행·133행 검증 완료). 기존 13KB `erp_etl_upsert` 재작성 위험을 피해 마스터 전용 RPC `erp_master_upsert` 분리 신설. ④ **포털 DB** 6종 전부 RLS ON·정책 0개·anon/authenticated 회수 — 금액이 담기는 첫 포털 쓰기 기능이라 뷰 직접 노출 없이 Edge Function 단일 경로. ⑤ **서버 재검증 7종**(차대일치·계정유효·금액>0 정수·라인 2줄↑·프로젝트 필수계정·마감월·등록자) — 합계는 화면 값을 믿지 않고 라인에서 재계산. ⑥ **화면**은 표준 그리드(§13)로 차/대변 라인 + 실시간 차대 합계바, 회계 담당자 탭에 ERP 입력용 요약·전표번호 회기입. **실측 검증**: 권한격리(관리자만 허용·비관리자 3명 전원 차단), 인증게이트(무토큰·위조토큰 401). **잔여는 전부 사람 결정** — 회계 마스터 조회 승인(A1)→실적재, `A_TEMP_GL` 샘플 조회 승인(A2: 등록자 ID 형식·`GL_TYPE` 실코드), `accounting` 모듈 신설·부여(A3), 파일럿 대상자(A4), 회계 규칙(A5: 경비 계정·상대계정·부가세·마감 기준). | 최동혁(+Claude) |
| 2026-07-24 | ERP DB 연계 | `10_ERP_DB연계/00·02`(v0.17) | **야간배치 수동 실행 — 12개 ETL job 전량 성공(실패 0)** + **`bp_master` 적재버그 RCA·수정·재성공**. 최종배치(07-21/07-22) 이후 누적 3~4일치 미실행분을 일괄 반영(pur_order·pur_req·item_master·iv_dtl·inventory·hr_payroll·purchase 등 건수 증가). `bp_master`는 최초 HTTP 400(`erp_etl_upsert`의 `bp_master_s` 분기 인라인 주석이 컬럼값 3개를 가려 20 vs 17 불일치)으로 실패했으나 원인분석 후 함수 수정(`실제구축준비 자료/이관/sql/20_erp_etl_upsert_bp_master_fix.sql`)·재실행으로 3/3 해소, "ODBC 미실행" 구기록도 소급 정정. 후속 조사 필요 이슈 2건(연동 현황 뷰 `rows_upserted` 최신 배치 미반영 정황, `erp_etl_upsert` 함수 전체의 번호 마이그레이션 미승격) 등재. 정책관리 미러 동기화(v0.17·20260724) | 최동혁(+Claude) |
| 2026-07-23 | 보안·품질 | `11_제품기획/08 §9`(전체 점검 결과) | **전체 코드리뷰·검증 점검 실시.** Supabase 보안 어드바이저 24건·성능 어드바이저 **147건 전량** 확인 + RLS/SECURITY DEFINER 실측 + 요청접수 신규 코드 리뷰 + 저장소↔배포 정합성 + 시크릿 스캔. **오탐 정리**: 경보 대부분이 설계 의도로 확인됐다 — `rls_enabled_no_policy` 15건은 fail-closed 패턴, `multiple_permissive_policies` **125건**은 정책이 `{public}` 롤 대상이라 anon까지 열거될 뿐 실제 게이트는 `is_internal()`·`vendor_bp()`(둘 다 **서버 통제 `app_metadata` 기반**이라 사용자 조작 불가), SECURITY DEFINER 공개실행 4건은 전부 내부 인증 가드 보유(`vendor_master_sync`=portal_admin 검사, `update_my_vendor_contact`=`auth.uid()` 의존, `erp_finance_overview`=`perm_effective` finance 검사). 저장소 시크릿 노출 0건. **발견 13건**: 높음 3(저장소↔배포 드리프트 — `jeil-chat` 저장소는 P2b~d 포함인데 운영은 v28 / **관리자 1명 SPOF** — 완료 게이트 도입으로 의존이 커졌는데 백업 없음 / **`erp_load_scope` 감사 컬럼 부재** — 완료 게이트를 여는 통제인데 `updated_by`가 없어 변경 주체 미추적), 중간 4(강제완료 흔적 미보존·**개인정보 보존기간 미이행**(`chat_retention_days=180` 설정만 있고 파기 배치 없음, pg_cron 미설치 — 최고령 로그 2026-07-07이라 2027-01부터 정책과 실제 괴리)·`erp_ro` 모듈차등 RLS 부재(현재 internal JWT 보유자 0명이라 실제 노출은 없으나 사내 세션 도입 시 앱 게이트 우회 가능)·`jeil-chat` 116KB 단일파일), 낮음 4(상수 중복·search_path·anon EXECUTE·유출비밀번호 차단 비활성), 정보 2(동조자에게 원 요청자 사유 노출·`auth_rls_initplan` 16건). 상세·근거는 `08 §9-2`. | 최동혁(+Claude) |
| 2026-07-23 | 챗봇 | 커밋 `791d949` · `11_제품기획/11 §17` | **P2b~P2d 일괄 구현 — 코드 완료·`jeil-chat` 배포 대기(⏳).** 남은 조회불가 지점을 모두 카드로 전환하고 결측 배지의 출처를 코드 상수에서 `erp_load_scope`로 옮겼다. **A5** 재고 배지 SSOT 전환(적재가 끝나 `state=loaded`가 되면 배지·요청 버튼이 자동 소멸, 조회 실패는 배지만 생략하는 fail-soft) · **A1** 품목 이력 0건·인사 집계 기간밖을 평문 → `notice` 카드 + 데이터 적용요청(조건 오입력 대비 `confirm_first` 신설 — 재확인 액션을 앞세우고 요청 버튼을 뒤로) · **A2** 범위 밖 문서 2지점(driveId·폴더경로) → `notice/deny` + 권한요청(원장은 `doc`) · **A3** 문서 검색 0건에 요청 경로 추가("범위 밖"인지 "검색어 문제"인지 구분 못 하던 지점) · **A4** `get_my_requests` 도구 신설(본인 접수분+동조 참여분, 타인 요청 조회 불가 — 완료 통보 부재로 재요청하던 §15-4 공백 해소). **배포 제약 발견**: `jeil-chat/index.ts`가 106KB→**116KB**로 커져 MCP 인라인 배포(파일 전체를 응답에 실어 보내는 방식)의 한 응답 한도를 초과 — 잘린 호출은 실행되지 않아 배포 사고는 없었으나, 이후 이 함수는 **CLI 경로**(`npx supabase functions deploy jeil-chat`)를 기본으로 한다. 구조적으로는 도구정의·도구구현·게이트웨이 3분할 권고(별도 과제). `deno check` 통과(잔여 경고 2건은 900번대 기존 항목). | 최동혁(+Claude) |
| 2026-07-23 | 챗봇·ERP연계 | 커밋 `86eedde` · 마이그레이션 `erp_load_scope_v1` · `이관/sql/19_erp_load_scope.sql`(신규) · `jeil-portal-request` **v4** · `04` 요청함 · `11_제품기획/11 §16` | **적재범위 레지스트리 `erp_load_scope` 신설 + 요청함 가시성 개선.** ① **레지스트리**: 적재범위 지식이 `jeil-chat` 코드 5곳에 문자열로 흩어져 있어 문구 한 줄 고치는 데도 106KB 재배포가 필요했고 같은 사실이 프롬프트·도구설명·카드각주에 제각각 복제돼 있었다(실제로 "수금액 미매핑"은 원인이 서로 달랐다). 표 하나를 단일 출처로 세워 **챗봇 결측 배지·데이터 요청 완료 게이트·요청함 현황**이 모두 이 표를 보게 했다. 시드 11행은 **전부 실측**(추정값 없음): 재고 입고량=칸 수정(분류규칙 미확정, 1,847행 중 입고 1행)·재고량=새 테이블(원천이 이동이력, 0행)·출고량=적재됨, 매출 수금액=칸 수정(추출 SQL엔 있으나 원천 컬럼 공백)·수주액=새 테이블(원천 S_SO 미확정)·매출액=적재됨, 5개 모듈 적재기간=2026년 이후. RLS 전면차단(정책 0·anon/authenticated 회수) 검증. ② **완료 게이트 확장**: `data` 유형은 판정 불가라 사람 확인에 맡겼던 것을 레지스트리 기반 기계 판정으로 전환 — 모듈×작업유형 항목이 전부 `loaded`여야 완료 가능. 라이브 검증: `REQ-2026-0003` 완료 시도 → *"'재고'의 '입고량·재고량'가 적재범위 레지스트리에서 아직 미적재 상태입니다"* 409 차단. 레지스트리에 해당 모듈 행이 없으면 판정 불가로 통과(없는 근거로 막지 않음). ③ **요청함 가시성**: 대상 칸에 미연계 항목(⚠)·작업유형 뱃지, 「작업유형별 묶음」 카드(이 작업 한 번으로 몇 건이 해결되는지), 「미연계 항목 현황」 표(상태·사유·**항목별 미해결 요청 수와 동조 인원**·기록 버튼) 신설. ④ **`scope_set`은 적재를 실행하지 않고 결과만 기록**한다(CLAUDE.md §1.6) — 실제 ETL은 `10_ERP_DB연계` 절차로 수행하고 끝난 뒤 기록해야 게이트가 열린다. 버튼 확인 문구에 명시. ⑤ 긴급도에서 처리 기한 문구 제거(보통/급함만). **잔여**: `jeil-chat`의 재고 배지는 아직 코드 상수라 레지스트리와 **이중 출처** 상태(값은 동일) — P2b 배포 때 조회로 교체 예정. | 최동혁(+Claude) |
| 2026-07-23 | 챗봇·권한 | 커밋 `d9e9fc4`·`dd8b89b` · `jeil-chat` **v28** · `jeil-portal-request` **v3** · DB `ai_gateway_config.system_prompt` · `11_제품기획/11`(§14-9·§15) · `09_ADR`(ADR-011 확장) | **조회불가 2분류 전환(P2a) + 요청 처리↔권한 반영 연결.** ① **2분류**: 사용자에게 보이는 조회불가를 「권한이 없음」·「데이터가 없음」 2가지로 단순화(관리자 결정). 화면은 버튼 2종이되 원장 `kind`는 5종 유지 — 서버가 `request.ui`(perm\|data)와 `kind`를 함께 내리고 프론트는 반송만 한다. 이로써 **프론트가 요청 유형을 `payroll` 여부로 하드코딩해 모든 요청이 「권한」으로 접수되던 결함**이 제거됐다(라이브 실측: 재고 요청이 `kind=data`로 정상 접수). 문서 승인범위는 "문서는 있는데 못 본다"이므로 화면상 권한요청·원장은 `doc`으로 분리. ② **결측 배지**: 재고 도구 반환에 `gap`/`gap_why`/`fix_type` 신설 — `입고합계 0 ⚠미적재`(분류규칙 미확정)·`재고합계 - ⚠미연계`(원천이 이동이력 테이블이라 잔고 없음). 값이 0으로 나와 실적으로 오해되던 가장 위험한 케이스를 값이 아닌 배지로 드러낸다(실측: 1,847행 중 입고 1행·재고 0행). ③ **묶음 축 = 담당자 작업 유형**(관리자 결정): `period`(기간 확대)·`column`(칸 수정)·`table`(새 테이블). ERP 모듈로 묶으면 반나절이면 되는 기간 확대가 유니포인트 협의 대상과 한 묶음이 되어 전체가 대기하므로, 실제로 고치는 단위로 묶는다. 중복병합 키에도 `fix_type` 포함(같은 모듈·다른 작업은 별건). ④ **완료 게이트 신설**: 요청함에서 done으로 닫아도 실제 부여를 잊으면 요청자는 완료 통보를 받고도 못 본다 — 실측 결과 `REQ-2026-0001`이 정확히 그 상태였다(done·처리메모 있음, 요청자 `erp_modules=[]`). 자동 부여는 CLAUDE.md §1.6·§5.4 위반이므로 **부여는 사람이·확인은 서버가** 방식 채택: `perm`/`perm_sensitive`는 `perm_effective`(SSOT), `doc`은 `ai_document_scope`로 기계 판정해 미반영이면 409 차단, 판정 불가한 `data`·`feature`는 안내만, 예외는 `force=true` 명시 우회(사유는 `handled_note`). 목록에도 `fulfilled`를 실어 **「반영됨」·「미반영」** 뱃지 표시. ⑤ **처리 기한 미약속**(관리자 결정): 긴급도 옵션에서 "1영업일 내 확인"·"당일 처리" 문구 제거 — 유니포인트 협의 의존분에 지킬 수 없는 기한을 화면에서 약속하지 않는다. ⑥ **프롬프트 함정 해소**: "입고량·재고량은 미적재입니다"라는 단정이 모델로 하여금 **도구 호출 자체를 건너뛰게** 만들어 「재고 현황 보여줘」가 수치 없이 프롬프트 재진술로 끝나던 문제 확인 → DB `system_prompt`를 "반드시 조회하고 미적재 표기는 카드 배지에 맡긴다"로 교체(**Edge Function 재배포 불필요**). 교훈: 미적재 지식을 프롬프트가 아니라 도구 반환값에 둔다. **라이브 검증**: 회귀 4경로(매출·발주·품목이력·권한) 정상, 계좌번호 사유 `***-****-****` 마스킹, 동일요청 병합·다른 작업유형 별건 접수, 완료 게이트 409 차단 확인. **잔여**: P2b(무데이터·기간밖)·P2c(범위 밖 문서)·P2d(`get_my_requests`), 문서검색 0건에 요청 경로 없음, 요청함 작업유형 묶음 화면, `TEAMS_WEBHOOK_URL` 미등록. | 최동혁(+Claude) |
| 2026-07-23 | 챗봇·제품기획 | `11_제품기획/11`(§14 신설, 상태줄 갱신) | **요청접수 P1 기능점검 + 미연계 데이터(C·D·E) 확장 기획검토.** ① 점검: "열람 권한 요청하기가 안 보인다"는 제보를 실측한 결과 **기능 결함이 아니라 계정 조건**이었다 — `perm_effective('dh.choi@…')`가 `is_admin=true` + ERP 모듈 8종 전부이므로 서버 게이트(`jeil-chat:447`)가 거부 카드를 아예 만들지 않는다(붙을 카드가 없어 버튼도 없음). 라이브에서 거부 카드를 직접 주입해 버튼 렌더 확인, 백엔드는 `stats/list/mine` 정상·관리자 접수 시 `already_granted` 409 차단·사유 15자 하한·위조 `kind` 거부·무인증 401까지 전 경로 실측 정상. 배포 이후 챗봇을 쓴 비관리자 계정 0명이라 실사용 트리거 기회도 아직 없었음(재현 경로: 비관리자 `hh.lee2@…`(총무팀, payroll·finance 무권한)로 급여 질의). ② 기획검토(§14): 미연계 응답은 **7곳**에서 나오며 그중 `request`를 싣는 곳이 0곳 → P1 버튼이 절대 나타나지 않음을 전수 확인. C가 권한(A·B)과 다른 3가지(원인 판정 불가 4갈래 ⓐ미적재ⓑ기간밖ⓒERP에도 없음ⓓ조건오입력 / 리드타임 자릿수 차이 — 유니포인트 협의 의존 / **가장 위험한 건 "안 나옴"이 아니라 "0으로 나옴"**인 부분적재)를 정리하고, `erp_ro.table_dict` **2,658행**을 요청 자동분류 근거로 활용하는 설계를 제시(적재범위 레지스트리 `erp_load_scope` 권고, `request.kind`·`gap` 확장, 유형 추가 0·뷰 추가 0, `needs_vendor_review` 재활용으로 `blocked` 상태 신설 회피). **착수 전 선수정 결함 2건 발견**: `04:1957` 요청 유형이 `payroll` 여부로 하드코딩돼 C·D·E도 전부 `perm`으로 접수됨(라우팅·통계 오염), `jeil-chat:1051` notice에 `request` 누락. P2를 P2a(결측 표기 정확성)~P2d(`get_my_requests`)로 재분할하고 106KB 재배포 비용 때문에 **묶음 배포** 권고. **추가 미결정 4건**(`erp_load_scope` 신설 여부·`data` 승인의 의미·E 유형 노출 여부·C 유형 SLA 분리) 관리자 판단 대기. | 최동혁(+Claude) |
| 2026-07-22 | 챗봇 | 커밋 `da48325`(main push) · `04_챗봇_포털_데모UI.html` 라이브 | **요청접수 P1 실배포 완료 확인 — git push origin main 성공, 라이브 검증**. 같은 날 기재된 요청접수 P1 구현 항목(바로 아래) 작성 시점엔 `04_챗봇_포털_데모UI.html`(요청버튼·모달·관리자 「요청함」 탭)이 git 미커밋 상태였다. 이후 커밋 `da48325`(30 files, +2018/-31)로 DB 마이그레이션·Edge Fn·04 프론트를 함께 커밋해 `git push origin main` 성공 → Vercel 자동배포 완료, 작업트리 clean 확인. 라이브 `ai.jeilm.co.kr/04_챗봇_포털_데모UI.html`에서 『열람 권한 요청하기』 버튼·`jeil-portal-request` 호출 문자열 노출 확인. 총괄 대시보드(`00_관리체계/index.html`)의 『04 프론트 미커밋·미배포』 단서를 정정. | 최동혁(+Claude) |
| 2026-07-22 | ERP·협력사포털 | `public.vendor_master`(재적재 109개사) · `erp_ro.bp_master_s`(신규) · `erp_etl_upsert`·`etl_run.py`(bp_master 잡 추가) | **협력사 거래처 정보 실데이터 반영 — 발주거래처 누락 해소(17→109개사)**. 제보: 협력사 정보 화면에 발주거래처가 안 보임. 원인: `vendor_master`가 데모 시드 17개사에 머물러 ERP 실제 발주거래처 103개사 중 87개사 미등록 상태였고, 기존 17건 사업자번호는 전부 『거래처코드 뒤3자리-81-임의5자리』 패턴의 가짜값(실제 사업자등록번호 아님)이었음. 조치: 발주·매입·매입상세 합집합 기준 109개사로 재적재(거래처명 5건은 ERP 최신 실적 기준으로 교정, 기 발급 계정 4건은 무영향), 가짜 사업자번호 17건 제거(NULL — ERP 거래처마스터 연계 전까지 공란 유지), `erp_ro.bp_master_s` 신설(B_BIZ_PARTNER 복제용, RLS internal+authenticated SELECT, **대표자주민등록번호·은행계좌번호는 의도적으로 적재 대상에서 제외**), ETL 화이트리스트·`etl_run.py`에 `bp_master` 잡 추가(기존 분기 보존 확인). | 최동혁(+Claude) |
| 2026-07-22 | 챗봇·권한 | DB 마이그레이션 `portal_request_v1` · Edge Function **`jeil-portal-request` v1**(신규) · `04_챗봇_포털_데모UI.html`(요청 버튼·접수 모달·관리자 「요청함」 탭) · `이관/sql/18_portal_request.sql` · `11_제품기획/11`(Accepted) · `09_ADR`(ADR-011 확정) | **요청접수 P1 구현·배포 — 챗봇 "조회 불가"가 그 자리에서 티켓이 된다.** 기획검토(같은 날 앞 항목)에서 확정한 설계를 권한 유형(A 부서 ERP 모듈 없음 / B 급여·인사 민감제한)에 한해 구현했다. **`jeil-chat`은 손대지 않았다** — 서버가 이미 `notice` 카드에 `request:{module,dept}`를 내려주고 프론트가 칩으로만 표시하고 있었기에(죽어 있던 정보), 프론트에 버튼·모달만 붙여 P1이 성립했다. 신설: 원장 `public.portal_request`(6유형·상태 7종·동조자 배열·RLS 전면차단 — 정책 0, anon/authenticated 권한 revoke 검증), Edge Fn `jeil-portal-request`(create·mine·cancel·list·transition·stats). 보안 핵심은 **접수 시 `perm_effective` 서버 재판정** — 프론트가 임의 모듈을 보내도 "이미 권한 있음"이면 접수를 거부하므로 위조·스팸이 서명키 없이 차단된다. 남용 방지는 1일 5건 상한 + 사유 15자 하한 + 동일 (유형·대상·부서) **자동 병합**(새 행 대신 동조 카운트 누적 → 개인 예외 남발 대신 부서축 일괄 부여 판단 근거). 사유·처리메모는 저장 전 주민번호·계좌·카드 패턴 마스킹(성명은 제외, §1.7). 관리자 콘솔 「요청함」 탭은 요약 카드·상태 전이·부서×대상 수요 히트맵·미처리 배지를 제공하되 **권한 부여 버튼은 의도적으로 두지 않았다**(부여는 「권한 상세」에서 사람이 클릭 — CLAUDE.md §1.6·§5.4). **알림 채널 = Teams 확정**(관리자 결정): Graph 앱 권한 추가 없이 채널 워크플로 웹훅 방식 — 코드는 배포 완료이고 `TEAMS_WEBHOOK_URL` 시크릿 등록만 남았다(미등록이면 알림만 건너뛰고 접수는 정상). **잔여**: P2(중간DB 미적재·문서범위·기능 없음 응답은 아직 평문이라 요청 버튼이 붙을 자리가 없다 → `jeil-chat` 반환부에 `notice` 카드 신설 + `get_my_requests` 도구 후 재배포), 미결정 5건(부서장 2단 승인·상태이력 분리·`data` 유형 유니포인트 협의 게이트·SLA 수치·보존기간). | 최동혁(+Claude) |
| 2026-07-22 | 제품기획·챗봇 | `11_제품기획/11_챗봇_데이터요청접수_설계.md`(신규) · `09_ADR`(ADR-011 제안) · `00_문서대장` | **챗봇 "조회 불가 → 요청접수" 기능 기획검토(구현 전).** 챗봇이 데이터를 못 주는 지점을 실측한 결과 **한 종류가 아니라 5유형**(A 부서 ERP 모듈 권한 없음 / B 급여·인사 민감 제한 / C 중간DB 적재범위 밖 / D 문서 연동범위 밖 / E 도구 미구현)이며 **처리 주체가 전부 다르다**는 점을 확인했다. 현재는 A·B만 `notice` 카드로 안내되고 "요청 문구 초안 작성"(`ask` 액션)까지만 제공 → 사용자가 복사해 Teams·구두로 보내므로 **요청이 시스템 밖에서 유실**되고, `chat_log`에 거부·무데이터 플래그가 없어 **"조회 불가가 하루 몇 건인가"를 지금은 답할 수 없다**. 설계안: 신규 뷰 없이(ADR-008 5종 고정 유지) `notice.actions`에 `kind:"request"` 1종 추가 + 요청원장 `public.portal_request` 1테이블(RLS 전면차단, `chat_*` 패턴 계승) + Edge Fn `jeil-portal-request` 1종 + 관리자 콘솔 「요청함」 탭(표준 그리드). 접수 시 **`perm_effective` 서버 재판정**으로 위조·스팸 원천 차단(서명키 불필요), 동일 요청 **자동 병합(동조 카운트)** 으로 개인 예외 남발 대신 부서축 일괄 부여 판단 근거 확보. 권한 부여·ETL 확대 **실행은 관리자 클릭으로 분리**(CLAUDE.md §1.6·§5.4 — 접수는 실거래 액션 아님). 단계: P1 권한유형(2~3일) → P2 C·D·E 확장(현재 평문 응답에 카드 신설, 실질 가치 절반) → P3 알림·SLA·수요 히트맵. **미결정 6건**(알림 채널 Teams 여부·부서장 2단 승인 여부·상태이력 분리·`data` 유형 유니포인트 협의 게이트·SLA 수치·보존기간) 관리자 판단 대기. | 최동혁(+Claude) |
| 2026-07-22 | 권한·보안 | DB 마이그레이션 4건 · Edge Function 4종 · `04` 관리자 콘솔 · `이관/sql/17_perm_core.sql` · `이관/00 §5.1` | **권한 코어 v1 — 부서축 CORE + 개인 예외 + 판정 단일화**. 현행 권한 구조를 실측한 결과 세 가지 문제가 확인됐다: ① 판정 로직이 4곳(`jeil-me`·`jeil-chat`·`jeil-hr`·`erp_finance_overview`)에 복제돼 있었고 `jeil-hr`는 부서명 하드코딩이라 콘솔 설정과 어긋났다, ② 개인 단위 예외 수단이 없어 한 사람에게 모듈 하나를 더 주려면 급여 포함 전권(`portal_admin`)뿐이었다, ③ 대행·프로젝트성 권한에 기간 개념이 없어 회수 누락 위험이 있었다. 해결: 판정 SSOT `public.perm_effective(upn)` 신설(게이트 4종 전부 이 결과만 강제), 개인 예외 `perm_grant`(role·dept·erp_module·page·**onedrive**, `deny>allow>부서`, 만료 자동 소멸, 사유 필수), 권한 변경 감사 `perm_audit`, 모듈 카탈로그 SSOT를 코드→DB(`perm_module_catalog`). 관리자 콘솔에 개인 예외 부여/회수·**유효 권한 시뮬레이터**(서버 판정 그대로 조회)·감사 로그 추가. 향후 팀별 OneDrive 폴더 권한도 같은 축으로 확장. **후속 조치**(같은 날): 고아 페이지였던 `sales_2026`(수주현황 대시보드)의 소유 부서를 `영업팀`→**`사업관리팀`**으로 이관 — ERP 사용자 매핑에 영업팀 소속이 0명이라 관리자 외 전원 차단 상태였다(관리자 결정, 사업관리팀 10명 접근·타 부서 차단 유지, `perm_audit` 기록). **잔여**: `erp_ro.*` RLS가 아직 "사내 전원 SELECT"라 모듈 차등은 앱 게이트 한정(REST 직접조회 차단은 다음 단계), 부서 권한 실사(23부서 대부분 5개 모듈 일괄 보유), 부서 관리자 0명·백업 전체 관리자 미지정. | 최동혁(+Claude) |
| 2026-07-22 | ERP 연계·데이터품질 | `10_ERP_DB연계/00`(v0.15)·`02`(SQL 검증 결과) | **중간DB(erp_ro) 정합성 SQL 검증 — 실측 완료(이상 없음)**. S3041-00065 챗봇 오답 이슈의 후속으로, 사용자 우려("Supabase 중간DB가 실제 ERP와 안 맞게 연결된 것 아닌가")에 SQL 실측으로 답했다. item_code NULL/공백·품목마스터 고아(발주·구매요청·매입)·번호형식 위반·PK 중복을 발주 3,088·구매요청 4,064·매입상세 1,981·품목 58,533행 전건 점검한 결과 **전부 0건**(구조적 오류·누락 미발견), `etl_meta.batch_run` 최근 12건 전부 success·rows_read=rows_upserted. 발견된 2건(발주→구매요청 고아 49건=전부 2025년 PR 참조, (po_no+item_code) 중복 31건=동일 PO 내 po_seq 분리)은 조사 결과 데이터 오류가 아니라 각각 "2026년만 적재하는 범위 한계"·"ERP 원본 구조 정확 보존"으로 설명됨. **잔여 과제**: ERP 원본(M_PUR_ORD/M_PUR_REQ) 직접 대사는 ODBC 접속 설정 읽기 권한 거부로 미실행 — 관리자 `etl_run.py --dry-run` 직접 실행 필요. | 최동혁(+Claude) |
| 2026-07-22 | 챗봇 UI·대화공유 | `supabase/functions/jeil-chat-history/index.ts`(v3→**v4**), `04_챗봇_포털_데모UI.html`, 통합본 | **공유 작업 폴더 실시간 반영 — 다른 사용자가 작업한 내용이 새로고침 없이 보이도록 주기 동기화 적용**. 사용자 제보: 챗봇에서 팀원이 추가한 대화·메시지가 실시간으로 안 보임. 원인: 워크스페이스를 **로그인 시 `bootstrap` 1회만** 불러오고 이후 갱신 경로가 수동(↻ 버튼·새로고침)뿐이었음 — 공유 폴더(`chat_work_member`)의 팀원 대화는 서버엔 쌓이지만 화면은 최초 스냅샷에 고정. 조치: ① 서버 **`sync` op 신설**(폴더·세션 목록 스냅샷 + 열어둔 대화의 새 메시지 **증분**; `session_messages`에 `after_seq` 추가, purge 미수행 경량 경로, 접근 상실 세션은 `gone:true`). ② 프론트 **주기 폴링 15초** — 탭이 숨겨지면 정지하고 복귀·포커스 시 즉시 1회, 스트리밍 중 정지, 연속 실패는 지수 백오프(최대 2분). 목록은 **지문 비교로 변경 시에만** 다시 그려 사이드바 깜빡임·조작 방해 없음. 팀원 발화는 기존 공유 말풍선(회색+발화자 라벨)으로 붙고 다음 질문의 맥락(`chatHistory`)에도 포함. ③ 중복 방지: 내 계정 메시지는 이 탭이 이미 그린 것으로 보고 렌더 없이 `seq`만 전진(내 응답 저장 완료를 감안해 전송 1.5초 뒤 1회 추가 동기화). Realtime(WebSocket)은 `chat_*` 테이블이 RLS 전면차단이라 미사용 — 서버 접근판정을 거치는 Edge Function 폴링으로 구현(동기화 지연 최대 15초 허용). | 최동혁(+Claude) |
| 2026-07-22 | 챗봇·ERP | `supabase/functions/jeil-chat/index.ts`(v25→**v26**), `10_ERP_DB연계/`(RCA) | **챗봇 ERP 조회 정합성 개선 — 품목코드→발주/구매요청 이력 조회 신설(원인분석 문서화)**. 사용자 제보: 품목코드 `S3041-00065`의 발주·구매요청이 실제 ERP·중간DB에 있는데도 챗봇이 "없다"고 답하거나 품목코드를 발주번호로 혼동. **원인분석(SQL·코드 실측): 데이터·매핑 문제 아님·모델 이슈 아님 → 챗봇 도구 설계 공백**. ① 데이터 정상 확인(erp_ro에 PO202607210001·PR202607020013이 품목코드·연결관계까지 정확 적재). ② **주원인**: 품목코드로 발주/구매요청을 역조회하는 도구 부재(`v_erp_po_pr_link`에 item_code 있으나 도구가 필터로 미노출, `get_erp_item`은 마스터만 반환). ③ 부원인: 프롬프트가 PO/PR '번호'로만 조회 안내. ④ 혼동유발: `get_erp_po_pr`가 품목코드를 po_no로 받아도 0건 무응답으로 조용히 반환. **조치**: 신규 도구 **`get_erp_item_orders(item)`**(품목코드/명→구매요청·발주·매입 이력, 요청→발주→입고→매입 추적, pur_order 모듈 권한 강제, list 뷰), SYSTEM_PROMPT에 번호 형식 구분(PO…/PR…/품목코드)+도구 지정 보강, `get_erp_po_pr` 입력 가드(PO/PR 형식 아니면 재안내·재시도도구=get_erp_item_orders). SQL 실측: S3041-00065·AACZ15-00002(구매요청12·발주11) 등 정상 반환. 범위 한계 명시(erp_ro는 2026년만 — 이전 연도 미적재). 상세·단일 출처 `10_ERP_DB연계/02`. | 최동혁(+Claude) |
| 2026-07-22 | ERP·권한 | Supabase 마이그레이션 `erp_finance_module_and_overview_rpc`, `pages/자금현황_대시보드.html`(신규), `app/lib/api.js`, `supabase/functions/jeil-me`(CATALOG)·`jeil-chat-admin`(CATALOG), portal_page 등록 | **자금·경영 현황 대시보드 신설(연/월 선택·ERP 실데이터) + 자금 데이터 부서 권한 게이트**. ① **연/월 선택 대시보드**(`pages/자금현황_대시보드.html`) — ERP 중간DB 월집계(매출·매입·발주·수지) 실데이터, 연도·월 드롭다운(가용 기간 자동), KPI 카드(매출·매입·수지차 흑/적자·발주·수주)·월별 추이 막대차트(순수 CSS)·매출/매입 상위 거래처 Top10. Azure SSO 자체 인증(erp-status와 동일 콜드스타트-안전 패턴). ② **권한 게이트(사용자 결정: 자금/회계/재무/관리자만)** — `finance` ERP 모듈 신설(CATALOG 2함수 추가 → 관리자 콘솔 「권한 상세」 탭에서 부서별 가감 가능), `dept_erp_scope`에 자금팀·내부회계관리팀 초기 부여, portal_page 등록(전사공개+erp_module=finance → jeil-me가 **자금/회계/관리자에게만 카드 노출**). ③ **서버 강제** — 데이터는 `erp_finance_overview(연,월)` SECURITY DEFINER RPC가 세션 이메일(auth.jwt)→부서→finance 모듈/관리자 여부로 검증 후에만 반환(비권한=`forbidden` 예외→화면 잠금). SQL·브라우저 실측: 내부회계관리팀·관리자 허용(7개월·거래처Top), 생산팀 forbidden 차단 확인. 방침: 사내 운영 페이지는 기본적으로 권한 있는 부서만 열람(default-gated), 추가 조정은 관리자 콘솔. | 최동혁(+Claude) |
| 2026-07-22 | 챗봇 UI·안정성 | `04_챗봇_포털_데모UI.html`, 통합본 | **로그인 상태인데 "소속 부서 로그인 후 표시"로 굳던 버그 수정 — jeil-me 콜드스타트 재시도**. 사용자 제보(로그인됐는데 소속부서 미표시+404). 라이브 실토큰으로 진단: jeil-me는 200 정상(dept=총무팀·is_admin), 토큰도 유효 — **로그인이 끊긴 게 아니라** 페이지 로드 시 첫 `jeil-me` 호출이 `'Unexpected token'`(비-JSON 응답 파싱) 오류로 실패한 것이 원인. 코드가 `res.json()`을 `res.ok` 확인 전에 호출해 오류를 못 잡고 포기 → `MY_PERM` null → `applyMyDept` 미실행으로 dept가 placeholder에 고정(수동 재호출 시 즉시 정상). 근본원인=**Supabase Edge Function 콜드스타트**(첫 요청이 HTML/빈 오류 응답). 수정: `fetchMe()` 헬퍼 신설 — 응답을 text로 받아 **안전 파싱** + **최대 3회 재시도(500·1000ms 백오프, 401/403은 재시도 안 함)**, loadPagePerms가 이를 사용하고 실패 시 dept를 '로그아웃'처럼 보이지 않게(🏢 부서 확인 중… → 자동 재시도 3회 → 실패 시 새로고침 안내). 부수: favicon 404 제거(`<link rel="icon" href="data:,">`), MS Graph 프로필사진 404는 사진 미설정 시 정상(무해·핸들링됨). 라이브 실토큰으로 로직 검증(정상 1회 성공·오류 3회후 무한루프 없이 실패). | 최동혁(+Claude) |
| 2026-07-21 | 챗봇 UI·관리자콘솔 | `04_챗봇_포털_데모UI.html`, 통합본 | **관리자 콘솔 실운영 기준 재정비 + 데모 표기 제거**. 조사 결과 콘솔의 "기획 예시" 섹션 대부분이 **실제 아키텍처와 불일치하는 허구**(Anthropic Workspace 지출한도·가짜 sk-ant 키·크레딧 등급제·미배선 보안 토글·가짜 인명 사용현황)임을 확인 → 정리 방침 "허구 제거 / 서버 실데이터 바인딩 / 데모버튼 삭제 / 정직한 정책은 읽기전용 참고". 변경: ① **데모 표기 제거** — 상단 데모 배너는 로그인(실토큰) 시 숨김(미로그인 방문자만 노출), 진입 버튼 "(데모)" 삭제 + **전체 관리자(portal_admin)에게만 노출**(`is_admin` 게이트), liveNote·프로필의 "기획 예시/스탠다드 등급" 문구 정리. ② **API 탭** — 가짜 API키 관리표·Anthropic Workspace 지출한도·미배선 보안 토글 제거 → 실제 「API 키·시크릿(OPENAI_API_KEY=Supabase 시크릿)」+「적용 중인 게이트웨이 보안 정책(읽기전용, 실제 강제 7종)」으로 대체. ③ **사용량 탭** — 목업 카드($232.35 등)·크레딧 등급 정책·가짜 직원표·승인대기 제거 → 서버가 이미 주던 **대화 저장 현황(세션·메시지 수)** 실데이터 카드 신설(chat_sessions/chat_messages 바인딩). ④ **권한 탭** — 목업 카드를 실데이터(관리자 수·매핑 부서 수·사용자 수) 바인딩, 역할 매트릭스·제한영역표의 가짜 저장/수정 버튼 제거하고 읽기전용 참고표로 정리. 실측 검증(Chrome): sk-ant·데모alert·지출한도·가짜인명·"기획 예시" 전부 0건, 신규 실데이터 5종(luSessions/luMessages/lpAdminCnt/lpDeptCnt/lpUserCnt) 바인딩 확인. jeil-chat-admin 서버는 필드 이미 제공 — 서버 변경 없음. | 최동혁(+Claude) |
| 2026-07-21 | 챗봇 UI | `04_챗봇_포털_데모UI.html`, 통합본 | **사내 운영 페이지·사이드바 간결화 + 부서선택 → 소속부서 자동 표시**. 사용자 요청(텍스트 과다·직관성): ① **부서 선택 드롭다운 제거** — 사이드바의 5개 부서 수동 선택 `<select>`를 없애고 **로그인 계정의 실제 소속 부서**(`jeil-me`의 `MY_PERM.dept_nm`)를 배지로 표시(`applyMyDept()`), 22개 실부서→업무 템플릿 5종 매핑(`deptKeyFromName`, 미매칭 시 기본 세트)으로 "추천 업무 도구" 자동 렌더. 사내 운영 페이지 상단 제목도 하드코딩("IT 자산관리 · 내 권한: 부서 관리자")에서 "📁 사내 운영 페이지 · <내 부서>(권한)"로 동적화, 데모용 부서 필터 select·"+페이지 등록" 버튼 제거. ② **텍스트 간결화** — 섹션별 장황한 안내문(admin-note) 3개 삭제, 카드 8종의 설명을 제목+한 줄+배지 2개로 축약(내부 테이블명·구현 세부 노출 정리). ③ **"ERP 권한정보(Role 현황)·변경내역 모니터링" 카드 제거**(요청). Chrome 실측: 소속부서 표시(공정설계팀·미매칭 부서도 정상)·드롭다운/필터/등록버튼/권한카드 제거·카드 8종 렌더 확인. | 최동혁(+Claude) |
| 2026-07-21 | ERP 연계·보안 | Supabase 마이그레이션 `grant_iv_dtl_s_authenticated_select` | **매입 상세 GRANT 누락 수정 — 사내 로그인해도 연동 현황 전체가 403 차단되던 결함**. 콘솔 오류 `permission denied for table iv_dtl_s`로 규명: `iv_dtl_s` 신설 시 `service_role`에만 SELECT를 부여하고 **`authenticated`(사내 로그인 사용자) GRANT를 누락**. `v_erp_sync_overview`·`v_erp_iv_dtl` 등이 `security_invoker=true`(조회자 권한 실행)라, 사내 계정이어도 `iv_dtl_s` GRANT가 없어 뷰 조회 전체가 403 → 연동 현황 화면이 "사내 로그인 필요"로 오인 표시됨(직전 커밋의 SSO 게이트를 통과한 뒤 이 2차 오류에 걸림). 수정: 기존 erp_ro 테이블과 동일하게 `grant select on erp_ro.iv_dtl_s to authenticated`. **격리 검증**: 접근통제는 GRANT가 아니라 RLS(`internal_select_iv_dtl = is_internal()`)가 담당 — JWT 시뮬레이션으로 협력사(vendor) 0행·사내(internal) 정상 조회·연동현황 11행 전체 확인, 보안 어드바이저 신규 경고 0(iv_dtl_s 관련 없음). erp_ro 전 테이블 GRANT 전수 점검 결과 나머지는 정상(table_dict만 미부여이나 노출 뷰 미참조 — service_role 전용 사전이라 의도된 상태). | 최동혁(+Claude) |
| 2026-07-21 | ERP 연계·인증 | `app/erp-status.html` | **ERP 연동 현황 화면 로그인 게이트 결함 수정 — 로그인 상태인데도 "사내 로그인 필요"만 뜨던 문제**. 사용자 제보로 확인: 이 화면은 `erpApi`만 import하고 **Supabase 세션 확인·SSO 로직이 아예 없어**, 다른 사내 화면(`pages/외주발주_검사진행현황_2026.html`)에서 미리 로그인해 둔 경우가 아니면 조회가 RLS로 차단돼 항상 게이트가 표시됐음. 챗봇 포털의 **Entra 토큰(`jeilax_auth`)과 이 화면의 Supabase Auth 세션은 별개 경로**라 포털 로그인만으로는 열리지 않았던 것(권한·뷰 GRANT는 정상 — authenticated SELECT 확인). 수정: 외주발주 화면과 동일 패턴으로 `supabase.auth.getSession()` + `claimsOf()`(app_metadata.role) 검사 + **Azure SSO 자동 로그인**(`?sso=1` 무한 리다이렉트 가드) 적용, 상태별 안내 분리 — 세션 없음=자동 SSO 후 실패 시 「사내 계정으로 로그인」 버튼, 협력사·무권한 계정=「사내 계정으로 다시 로그인」(signOut 후 재로그인) 버튼. 콘솔 오류 없이 종료되도록 `gateCheck()` 함수로 분리. | 최동혁(+Claude) |
| 2026-07-21 | ERP 연계 | `10_ERP_DB연계/etl/etl_run.py`, `erp_ro.iv_dtl_s`·`pur_req_s`·`purchase_m`·`pur_order_s`·`sales_orders_m`·`item_master_s`·`inventory_d` | **매입 상세(`iv_dtl`) 신규 연동 + 구매요청 상세 백필 — 실적재 완료(관리자 직접 실행, `batch_run` 7건 성공·0건 실패)**. 같은 날 오전 준비해 둔(아래 줄 참조) 스키마·ETL·뷰를 실행해 신규 `iv_dtl`(`M_IV_DTL`) 2026-01-05~07-20 **1,981행** 적재(송장 213건·발주 432건·매입금액 35.6억원·발주 연결 100%, 미연결 36건은 데이터 품질 참고 항목) + `pur_req` 상세 14컬럼 백필 `--full` **4,063행** 재적재(조달구분 채움 99.98%). 매입 상세를 신규 적재한 김에 기존 월집계(`purchase_m`)와 대사하니 6월 +19.2억·7월 +1.5억 차이가 드러났는데, **원인은 데이터 오류가 아니라 적재 시점 차이**(7/8 이후 ERP 신규 등록분 미반영)로 확인 — `purchase --full`·`pur_order`(증분)·`sales`·`item_master`(증분)·`inventory` 5종을 재적재해 매입 대사 차이를 전월 0으로 해소(매핑·로직 정상 검증). `hr_payroll`(민감)은 별도 승인 사항이라 이번 재적재에서 제외. 관제 문서 `10_ERP_DB연계/00`(v0.13)·`01`(§4.10)·`02` 갱신, 정책관리 미러 동기화(v0.13). | 최동혁(+Claude) |
| 2026-07-21 | ERP 연계 | Supabase 마이그레이션 3건(`erp_pur_req_detail_and_iv_dtl`·`erp_etl_upsert_pur_req_detail_iv_dtl`·`erp_pur_req_detail_view_and_iv_dtl_views`, +`erp_sync_overview_add_iv_dtl`), `10_ERP_DB연계/etl/etl_run.py` | **구매요청 상세 확충 + 매입 상세(라인) 연동 신설 — 스키마·ETL·뷰 준비 완료(적재는 관리자 실행 대기)**. ① **원천 구조 실측**(`erp_ro.table_dict`): `M_PUR_REQ`는 HDR/DTL 분리가 없는 **라인 단위 단일 테이블**(49컬럼·63,423행)이라 "DTL 반영"은 **미적재 상세 컬럼 확충**으로 해석 — `pur_req_s`에 조달구분(`procure_type`)·확정수량·릴리즈수량·구매그룹/조직·저장위치·수주라인·도면번호·추적번호·변경차수·MRP오더·생성/수정자 **14컬럼 추가**(원천에 단가·금액 컬럼 없음 확인 — 구매요청 금액은 연결 발주 기준이라는 기존 원칙 유지). ② **매입 상세 신설**: `M_IV_DTL`(52,478행·66컬럼)이 실재하는데 그동안 `M_IV_HDR` 월집계만 적재해 "무엇을·얼마에" 매입했는지 확인 불가였음 → `erp_ro.iv_dtl_s`(PK `iv_no+iv_seq_no`, 품목·수량·단가·금액·부가세 + **발주 연결키 `po_no`/`po_seq_no`·입고 연결키 `mvmt_no`**) 신설, RLS ON+사내 SELECT 정책. ③ 적재 RPC `erp_etl_upsert`를 화이트리스트 방식 유지한 채 두 대상으로 확장(기존 분기 무변경). ④ 노출 뷰: `v_erp_pur_req` 상세 컬럼 추가(기존 컬럼·순서 보존), **`v_erp_iv_dtl`·`v_erp_iv_dtl_monthly`(월×품목 집계) 신설**, 연동 현황에 `iv_dtl` 등재(적재 전 "미연동" 표시). ⑤ ETL `iv_dtl` 잡 추가(증분 `UPDT_DT` 지원)·`pur_req` SQL 확충, 문법 검증 통과(잡 11종). **잔여: ERP 운영DB 접속이 필요한 실적재는 관리자 직접 실행**(`python etl_run.py --job iv_dtl`, `--job pur_req`). | 최동혁(+Claude) |
| 2026-07-21 | ERP 연계·관리체계 | Supabase 마이그레이션 `erp_sync_overview_all_sources`, `supabase/functions/jeil-chat-admin/index.ts`(v9→**v10**), `04_챗봇_포털_데모UI.html`, `app/erp-status.html`, 통합본 | **ERP DB 연동 현황 전체 노출 + 관리자 콘솔 반영**. 점검 결과 연동 현황 화면(`app/erp-status.html`)은 이미 있었으나 **원천 뷰 `v_erp_sync_overview`가 5종(발주·품목·매출·매입·재고)만 노출** — 실제로는 10종이 적재 중이어서(구매요청 3,784·부서마스터 351·사용자마스터 99·ERP권한 397·급여 143) **연동 자산의 절반이 화면에서 보이지 않던 상태**. ① 뷰를 10종 전부로 확장(+`sensitive`·`sort` 컬럼, security_invoker=true 유지 — 사내만 조회). 급여는 `erp_secure` 민감 스키마라 **실테이블을 세지 않고 배치 적재건수만** 표시(금액·명세 미노출). 원천 표기도 실제 조인 기준으로 정정(발주=`M_PUR_ORD_HDR + M_PUR_ORD_DTL`). ② **관리자 콘솔(API 탭)에 「ERP DB 연동 현황」 실데이터 표 신설** — 기존엔 '사내 운영 페이지' 카드로만 접근 가능했음(admin v10이 뷰를 service_role로 조회해 `erp_sync`로 반환). ③ 상세 화면에 **연결 정보 표** 추가: 원천 UNIERP MS SQL(사외 IDC·DB `JEILMNS`), ODBC 읽기전용(`SELECT … WITH (NOLOCK)`·화이트리스트 SQL·DDL/DML 없음), ETL→RPC(`erp_etl_upsert`/`erp_secure_upsert`)→`erp_ro`/`erp_secure`, **수동 실행**(자동 스케줄러 미도입), 접속정보는 `.env`에만 보관(소스·문서 미기재), RLS 격리. 라벨 하드코딩 제거(뷰가 단일 출처)·XSS 이스케이프 보강. | 최동혁(+Claude) |
| 2026-07-21 | 협력사포털·보안 | `pages/외주발주_검사진행현황_2026.html`, `app/lib/api.js` | **외주 발주·검사 메시지 발신자 표기를 챗봇 규약으로 통일 + 저장형 XSS 차단**. ① 기존: 사내 발신자가 말풍선에 `'구매팀'`으로 **하드코딩**되고 전송 시 `sender_name`에 리터럴 `'제일엠앤에스 구매팀'`을 저장 — DB의 `sender_name`은 표시에 쓰이지도 않아 "제일엠앤에스 구매팀/최동혁/이메일 원문"이 혼재(실데이터 확인). ② 변경: `erpApi.userLabels()`·`myLabel()` 공통 함수 신설(원천 `v_erp_user_dept` — 챗봇 라벨과 동일, 표시는 화면에 맞춰 **"부서 · 이름"**), 전송 시 로그인 본인 라벨을 `sender_name`에 저장하고, 렌더는 `sender_id`(이메일)→부서 매핑을 우선해 **과거 메시지까지 소급 보정**(미매핑·구 데모 데이터는 기존 이름→'구매팀' 순 폴백, 6개 시나리오 유닛검증 통과). 목록 카드의 최근 메시지 미리보기 접두('구매: ')도 동일 라벨로 통일. 협력사 화면(`협력사_모바일_포털.html`)은 이미 `sender_name`을 표시하므로 자동 반영. ③ **보안 부수 수정**: 같은 렌더 함수에서 메시지 본문·시각이 이스케이프 없이 innerHTML로 삽입되던 **저장형 XSS**(협력사 입력이 사내 화면에서 실행 가능)를 `esc()` 도입으로 차단(본문·발신자명·미리보기 전부). | 최동혁(+Claude) |
| 2026-07-21 | 챗봇 UI·공유 | `supabase/functions/jeil-chat-history/index.ts`(v2→**v3**), `04_챗봇_포털_데모UI.html`, 통합본 | **팀원 초대 UX 고도화 — Outlook '받는 사람' 방식 인원 검색·칩 선택 + 초대 링크**. ① 서버 `people_search` op 신설: 사내 인원을 **이름·부서·아이디** 부분일치로 검색(원본=ERP 사용자·부서 매핑 `v_erp_user_dept` 89명·22부서 — Graph 조직 검색은 앱 위임권한이 `User.Read`뿐이라 미사용, 부서 정보도 ERP가 정확). 규모가 작아 **전량 조회 후 서버 메모리 필터**(PostgREST 필터 문자열에 사용자 입력 미주입 — 주입 차단), 정렬은 이름 시작일치>부서>아이디, 이미 소유자·팀원인 사람은 `taken` 플래그로 표시. `member_add`는 **복수 초대(emails[])** 지원(단수 email 하위호환·상한 20명 합산 검증). ② 프론트: Outlook 메일 받는사람과 동일한 **칩(chip) 입력기** — 검색어 입력 시 드롭다운(아바타·이름·부서·이메일), ↑↓ 이동(이미 팀원은 자동 건너뜀)·Enter/쉼표/세미콜론/Tab 확정·Backspace 마지막 칩 삭제·✕ 개별 제거, 검색 결과 없으면 사내 이메일 직접 입력 허용(아이디만 쓰면 `@jeilm.co.kr` 자동 보완, 서버 재검증), 늦게 도착한 이전 검색 응답 폐기(경합 방지). ③ **초대 링크**: 초대 완료 시 `#work=<uuid>` 딥링크 노출 — 복사 버튼 + **Outlook 메일 초안(mailto) 열기**(자동 발송 금지, 사람이 직접 전송 — CLAUDE.md §1.6). 링크 수신자가 열면 로그인 후 해당 폴더·최근 대화로 자동 이동(OAuth 리다이렉트로 fragment가 유실되므로 sessionStorage 보관 후 복원), **링크는 권한을 부여하지 않고 화면 연결만** — 미초대자는 "접근할 수 없습니다" 안내(접근 판정은 서버 멤버십 유지). Chrome 실측: 부서명 검색·이미 팀원 회색 처리·키보드 건너뜀·클릭 선택·도메인 보완·Backspace·초대 후 칩 비움·링크 생성 전 항목 통과. | 최동혁(+Claude) |
| 2026-07-21 | 챗봇·DB·보안 | Supabase 마이그레이션 `chat_work_team_sharing`(chat_work_member + 접근 RPC 2종 + chat_append_message 확장), `supabase/functions/jeil-chat/index.ts`(v24→**v25**), `jeil-chat-history/index.ts`(v1→**v2**), `04_챗봇_포털_데모UI.html`, 통합본, `11_제품기획/09_ADR`(ADR-009 개정) | **작업 폴더 팀 공유 — 팀원 초대·공유 대화·공동 지시(ADR-009 개정: "본인만"→"본인 또는 폴더 팀원")**. ① DB: `chat_work_member`(폴더당 최대 20명, RLS ON+정책 0) 신설, 접근 판정을 DB RPC(`chat_work_access`=소유자·팀원 / `chat_session_access`=생성자·공유 폴더 팀원)로 **일원화** — Edge 3함수가 동일 판정 사용, `chat_append_message`도 접근 검증으로 확장하되 **발화자 upn은 실작성자 기록**(감사 정확). SQL 실측: 소유자·팀원 허용/비팀원 차단/팀원 append 성공/외부인 append 예외. ② jeil-chat-history v2: `member_list/add/remove`(초대=소유자만·사내 도메인 한정·본인 나가기 허용), bootstrap이 공유 폴더(소유자 표기 라벨)+팀원 세션까지 반환, `session_messages`에 발화자 라벨('부서_이름_아이디' 규약) 동봉, 세션 삭제는 생성자·폴더 소유자만. ③ jeil-chat v25: 세션 이어쓰기·폴더 새 대화·work 메모 컨텍스트 주입을 공유 접근으로 확장(팀원도 같은 대화방에서 AI에게 지시 가능). ④ 프론트: 폴더 편집 모달에 **팀 공유 섹션**(멤버 목록·초대·제거, 팀원 모드는 열람+나가기), 공유 폴더 👥 아이콘·소유자 툴팁, 타인 발화는 회색 말풍선+발화자 라벨+이니셜 아바타, 타인 시작 대화 👤 표시, 공유 대화 **↻ 동기화** 버튼(팀원 새 메시지 재조회 — 실시간 아님·수동 동기화), 폴더 접근 상실 시 내 대화는 '일반 대화'로 회수 표시. 공유 고지문("초대한 팀원은 폴더의 모든 대화를 열람·참여") 명시. ⑤ 보안 셀프점검(전용 에이전트, 인가 매트릭스 11개 op 전수 검증): High 0건·**Medium 1건(F1) 발견 즉시 수정** — 제거된 팀원이 "자기가 만든" 폴더 세션에 계속 접근 가능하던 회수(revocation) 공백을 "폴더 소속 대화 접근=현재 구성원 자격 기준"으로 교정(마이그레이션 `chat_session_access_require_work_membership` + bootstrap 필터·세션삭제 접근 검증 보강), SQL 실측으로 제거 후 차단·소유자 유지·개인 대화 유지 확인. XSS 싱크 전수(textContent-only)·PostgREST 필터 주입·권한 상승 경로 없음 확인. | 최동혁(+Claude) |
| 2026-07-21 | 챗봇 UI | `04_챗봇_포털_데모UI.html`, 통합본 | **사용자 아바타 빈 원 문제 해소 — img 요소 + 이니셜 자동 폴백 구조로 교체**. 사용자 제보(내 메시지 아바타가 빈 파란 원): 기존 CSS background-image 방식은 사진 로드가 조용히 실패하면 빈 원이 남는 구조 → `makeUserWho()`(img 요소, onerror 시 실명 이니셜 폴백) + 소급 적용도 onload 성공 시에만 교체(실패 시 이니셜 유지)로 재구성. 실제 blob 사진·깨진 URL 두 경로 Chrome 실측 — 사진 원형 채움·폴백 '최' 확인, 빈 원 원천 차단. | 최동혁(+Claude) |
| 2026-07-21 | 챗봇 UI | `04_챗봇_포털_데모UI.html`, 통합본 | **AI 아바타 선명화 + MS 프로필 사진 소급 적용**. 사용자 제보(로고가 제대로 안 보임): 심벌이 34px 원 안 19px로 작아 흐릿 → 96px 고해상 원본으로 재생성(3KB)하고 좌측 상단 브랜드 마크와 동일한 Ji 심벌을 **앱아이콘형(둥근 사각·흰 배경) 22px** 표시로 교체. 사용자 아바타는 MS 프로필 사진 우선 — 사진 로드 시점 이전에 그려진 말풍선(이니셜)에도 **소급 적용** 루프 추가. Chrome 실측(로고 자연폭 96/표시 22·둥근사각·소급 전후 bg) 통과. | 최동혁(+Claude) |
| 2026-07-21 | 챗봇 UI | `04_챗봇_포털_데모UI.html`, 통합본 | **로그인 실명 표시 + AI 아바타 JEIL 로고 교체**. 사용자 제보(최동혁인데 홍길동 표시): 환영 인사·사용자 말풍선 아바타('홍')가 데모 하드코딩이라 로그인 후에도 남던 것 — `USER_NAME/USER_INITIAL/USER_PHOTO` 전역 도입, setUser가 환영 인사(.hello-nm)·새 대화 템플릿(WELCOME_HTML)·이후 말풍선 아바타(프로필 사진 로드 시 사진, 아니면 실명 이니셜)까지 동기화. 미로그인 데모는 기존 홍길동 유지. AI 응답 아바타는 "AI" 텍스트 → **JEIL 심벌마크(Ji)** 로 교체(doc/회사로고/01_심벌마크.png를 64px·2KB로 경량화해 base64 인라인, 환영 카드·스트리밍·복원·데모 4경로 공통). Chrome 실측: 환영 인사·템플릿·아바타 6항목 검증 통과. | 최동혁(+Claude) |
| 2026-07-21 | 챗봇 UI | `04_챗봇_포털_데모UI.html`, 통합본 | **work 사이드바 상태 3분리 — 로그인 상태에서 예시 화면으로 되돌아가던 문제 해소**. 사용자 화면 점검 제보(예시 질문 ↔ 폴더 목록이 번갈아 표시)로 확인: 기존 로직이 "실시간 유효 토큰+로드 완료"가 아니면 무조건 예시로 폴백 → 페이지 로드 직후·토큰 만료(무음 갱신 전) 순간에 로그인 사용자에게 예시가 노출됐고, 그 상태에서 예시 버튼을 누르면 대화가 폴더가 아닌 미분류로 저장됨(서버 흔적 확인). 수정: `hasStoredAuth()`(저장 인증 기준 표시 판정) 도입 — 미로그인=예시 / 로그인+미로드="불러오는 중" / 로드됨=목록 유지(만료 순간에도), bootstrap 실패 시 "↻ 다시 불러오기" 버튼. 검증 중 신규 회귀도 발견·수정: initWorkspace의 동기 renderWorkBox 호출이 스크립트 하단 `var WS` 할당보다 먼저 실행돼 저장 토큰 보유 첫 로드에서 스크립트 전체가 죽는 문제(푸시 전 로컬에서만 존재) — WS 미할당 가드 추가. Chrome에서 3상태+크래시 시나리오 실측 통과. 서버 저장 내용 SQL 점검: 폴더 2(작업2·2)·세션 5·메시지 정상. | 최동혁(+Claude) |
| 2026-07-21 | 챗봇 UI | `04_챗봇_포털_데모UI.html`, 통합본 | **챗봇 화면 전면 폴리시 — 사이드바 확장(264→304px) + 대화 UI 현대화**. 말풍선 라운드(비대칭 꼬리)·그림자·등장 애니메이션(prefers-reduced-motion 존중), 추천 칩 필형 정리, 입력창 포커스 링 + **여러 줄 자동 높이 확장**(최대 132px, 전송 시 원복), 사이드바·채팅 스크롤바 슬림화, 버튼 트랜지션 일관화. 기능 변경 없음(스타일·입력 UX만). Chrome 실화면(데모 대화+로그인 모의 work 목록) 검증 후 통합본 재생성. | 최동혁(+Claude) |
| 2026-07-21 | 챗봇 UI | `04_챗봇_포털_데모UI.html`, 통합본 | **work 사이드바 UI 정돈 + 예시 질문 실업무 교체**. ① 렌더 깨짐 수정 — "작업 폴더 · 대화" 헤더가 세로로 무너지던 원인은 기존 `.tpl button{width:100%}` 규칙이 특이도로 ＋버튼(`.wk-add`)을 이겨 버튼이 가로 100%로 팽창(제목 6px 압착)한 것 — `.tpl .wk-add` 재정의로 해소(브라우저 실측 검증). ② 아코디언 정돈 — 폴더 행(아이콘·이름 말줄임·대화 건수 배지·선택 하이라이트), 대화 행 들여쓰기 가이드선, ✎/✕는 호버 시 표시, "＋ 새 대화" 점선 버튼, 일반 대화는 💬 아이콘. ③ 예시자료 교체 — 사이드바 미로그인 예시 3버튼·환영 메시지 추천 칩을 가짜 프로젝트명("2025-095-SUL-EC 수지 분석" 등)에서 **실업무 질문**(🧾 구매요청·발주 정보 조회 / 📦 품목정보 확인 / 👥 2026년 인원현황)으로 변경, 클릭 시 해당 질문 자동 전송(`askExample` — 로그인 시 실데이터 도구 응답). 로컬 서버+Chrome으로 데모·로그인(모의) 두 상태 화면 검증 후 통합본 재생성. | 최동혁(+Claude) |
| 2026-07-21 | 챗봇·DB·보안 | Supabase 마이그레이션 3건(`create_chat_work_session_message`·`ai_gateway_config_work_context`·`chat_append_message_trim_title`), `supabase/functions/jeil-chat/index.ts`(v23→**v24**), `supabase/functions/jeil-chat-history/index.ts`(**신설 v1**), `supabase/functions/jeil-chat-admin/index.ts`(v8→**v9**), `04_챗봇_포털_데모UI.html`, 통합본, `11_제품기획/09_ADR(ADR-009 신설)`·`04_데이터베이스_설계` | **챗봇 대화 관리 3기능 — 응답 중지 + 사용자별 대화내역 서버 저장(본인 확인·삭제) + work(작업 폴더)별 맥락 관리(ADR-009)**. ① **응답 중지**: 프론트 `sendLive`에 AbortController 도입, 스트리밍 중 보내기 버튼이 "중지"로 전환(Enter 연타 차단·스트리밍 중 화면 전환은 confirm 가드). 게이트웨이 v24는 **이중 감지**(req.signal 방어적 + writer.write 실패=신뢰 신호)로 OpenAI 업스트림 fetch를 abort — 중지 후 토큰 소비 즉시 차단, 부분 응답은 `stopped=true`로 저장하고 usage 미수신 시 보수적 추정(문자수/3)으로 chat_log 기록. ② **대화내역 서버 저장(정책 변경)**: 기존 "대화 원문 미저장" 원칙을 관리자 결정으로 **"본인만 열람·삭제 가능한 서버 저장"** 으로 변경(ADR-009 등재). 신규 `chat_work`/`chat_session`/`chat_message`(원문+`views` jsonb — 복원 시 구조화 뷰 카드 재현) — 전부 RLS ON+정책 0(클라이언트 전면 차단), 접근은 Edge Fn(service_role) **upn 필터 경유뿐**(RPC `chat_append_message`가 원자 seq 채번·제목 자동생성·소유 검증, 타인 upn 시도는 예외 — SQL 실측 확인). 열람·삭제는 본인 전용 **`jeil-chat-history`**(bootstrap/work CRUD/세션 목록·이름변경·삭제·메시지 복원, soft delete 30일 후+보존기간 초과분 기회적 hard purge). **관리자용 원문 조회 API는 만들지 않음**(admin v9 usage에 세션·메시지 건수만). ③ **work 2단 구조+컨텍스트**: 사이드바를 work 아코디언(폴더 생성·이름/메모 모달 편집·삭제, 대화 이름변경·삭제·복원, "일반 대화" 미분류 그룹)으로 재구성, 새 대화 버튼 신설. work 메모는 게이트웨이가 시스템 컨텍스트로 주입하며 **적용범위는 관리자 콘솔 › 게이트웨이 설정에서 제어**(`ai_gateway_config` 6컬럼: work_context_mode/max_chars/history_turns/chat_save_enabled 킬스위치/retention_days/session_max_messages). 하위호환: 저장 opt-in(구버전 body는 v23 동작 동일)·세션 메타는 별도 SSE 키 `jeilax_meta`(구버전 무시). 배포: 3함수 모두 배포본-로컬 diff 완전 일치, 무토큰 401 정상 기동, verify_jwt=false 유지. 프론트 스트리밍 중 대화 전환 경합(부분 응답이 새 대화 맥락에 오염) 방어 포함, 통합본 재생성. 라이브 E2E(로그인 후 중지·저장·복원·컨텍스트 반영)는 관리자 확인 대기. | 최동혁(+Claude) |
| 2026-07-20 | 챗봇 | `supabase/functions/jeil-chat/index.ts`(v21→v23), `supabase/functions/jeil-chat-admin/index.ts`(v7→v8), `04_챗봇_포털_데모UI.html`, 통합본 | **사용자 아이디 표기 규약 도입 — 『부서_이름_아이디』통일**(예: 총무팀_최동혁_dh.choi@jeilm.co.kr). 매핑 원천은 ERP 사용자정보 `v_erp_user_dept`(이메일→부서·이름) 단일 — 미매핑(퇴사자·시스템 계정)·비이메일 값은 원본 유지 폴백. 적용: 챗봇 구매요청 목록·발주↔구매요청 연결·구매요청 상세의 요청자(데이터+뷰 컬럼, v23에서 list 뷰 컬럼 누락 보완·요청부서 컬럼은 중복 제거), `get_my_access` 계정 표기, SYSTEM_PROMPT 표기 준수 지시, 관리자 콘솔 사용자별 사용량·최근 호출(`by_user.label`·`recent.upn_label`, 이미 조회 중인 `v_erp_user_dept` 재사용 — 추가 쿼리 0). 원칙: 표시 계층 변환만 — 감사 로그(`chat_log`·`hr_access_log`) 원본 upn 불변. 라이브 E2E 확인 — 계정 `총무팀_최동혁_dh.choi@jeilm.co.kr`, 요청자 `생산팀_이민호_mh.lee1@jeilm.co.kr` 등. git 커밋 `cc2e5f9`(v22 대비)·`4995179`(v23 대비) main 푸시 완료 → Vercel 자동배포. | 최동혁(+Claude) |
| 2026-07-20 | 챗봇·제품기획 | `04_챗봇_포털_데모UI.html`, `supabase/functions/jeil-chat/index.ts`(v20→v21), `11_제품기획/10_챗봇_응답렌더링_설계.md`(상태 "P1+P2+P3 구현 완료"로 갱신) | **챗봇 뷰 액션(P3) 구현·배포 완료 — jeil-chat v20→v21 + 프론트 액션 바**. ADR-008 범위 내 후속(§3-4 보류 해제)이며 신규 ADR 아님. 뷰 페이로드에 `actions`(카드당 최대 4) 규약 추가: `ask`(클릭 시 해당 질문을 챗봇 입력창에 넣어 자동 전송 — 일반 질문과 동일 경로) · `link`(새 탭, 프론트가 화이트리스트 검증 — `https://` 또는 `pages/` 상대경로만, `:` 포함 상대경로 차단) · (클라이언트) series/ranking/list 카드 "표 복사" 버튼(TSV 클립보드, 그리드 표준 §13.5 규약과 동일). 적용 사례: 접근제한 notice 카드 "권한 요청 초안 작성"(CLAUDE.md §1.6 준수 — 모델이 초안만 작성, 복사·전송은 사람) · 급여 접근제한 카드 "전사 인원현황만 보기"(권한 내 대안 유도) · 내 포털 권한(record) 카드 접근 가능 운영페이지 바로가기 상위 3(실제 열람 차단은 각 페이지 게이트 `jeil-me`가 재판정) · 인원현황 카드 "급여총액 추이" 후속 칩은 **권한 보유자(인사팀·관리자)에게만** 노출(비권한자에게 차단당할 질문 유도 금지) · 발주 Top 카드 "1위 상세 보기" 후속 칩. 배포: verify_jwt=false 유지, 배포본-로컬 소스 diff 0, 부트 31ms 정상. 라이브 E2E 확인 — 발주 랭킹 카드 액션 칩 2종 표시→클릭→자동 질문 전송→PO record 카드+진행단계 steps(요청✓→확정✓→발주완료(현재)→입고→매입) 체이닝, 내 권한 카드 링크 3종(`rel=noopener`·`target=_blank`·화이트리스트 경로 통과) 정상. 설계문서 `11_제품기획/10` 상태를 "P1+P2+P3 구현 완료"로 갱신(§3-4 신설·§5 단계표·§7 결정표 반영, ADR-008 본문은 P1+P2 범위 결정 그대로 — P3는 그 결정의 구현 후속이라 별도 ADR 미등재). git 커밋 `6c3dd5f` main 푸시 완료 → Vercel 자동배포. | 최동혁(+Claude) |
| 2026-07-20 | 챗봇·제품기획 | `04_챗봇_포털_데모UI.html`, `supabase/functions/jeil-chat/index.ts`(v19→v20), `11_제품기획/10_챗봇_응답렌더링_설계.md`(신설)·`09_ADR_의사결정기록.md`(ADR-008) | **챗봇 응답 렌더링 P1+P2 구현·배포 완료 — 마크다운 렌더러 + 서버 주도 구조화 뷰 5종(ADR-008)**. 배경: 라이브 챗봇 렌더러가 mdLite 3줄(굵게+줄바꿈)뿐이라 표·목록·헤딩이 원문 텍스트로 노출됐고(로그인 실데이터 화면이 데모보다 못생겨 보이는 역전 현상), 모델이 도구의 구조화 수치를 재서술하는 과정에서 수치 오류 위험이 있었음. 검토한 방식 B(모델이 템플릿 블록 직접 방출)는 기각 — fail-ugly(스키마 위반 시 JSON 원문 노출)·스트리밍 불가·프롬프트 비대·모델 교체 취약. **채택한 방식 C**: 마크다운 렌더 + 서버 주도 구조화 뷰 병행. 프론트(04): `renderMd` GFM 마크다운 렌더러(표·목록·헤딩·펜스·링크는 http/https만, `escHtml` 선행으로 XSS 차단) + 스트림 60ms 스로틀·스크롤 가드 + `addUser` XSS 결함(`&` 미이스케이프) 수정 + `renderView` 구조화 뷰 5종(series/ranking/record/list/notice, textContent-only 렌더로 2차 XSS 방지). node 단위테스트 15/15 통과, 통합본 재빌드. 게이트웨이 `jeil-chat` v19→v20: 도구 17종에 `__view` 배선, `run()` 루프에서 SSE `jeilax` 이벤트로 구조화 뷰 송출(모델 미경유 — 도구 수치가 DB 직결로 카드 표시, 수치 환각 구조적 차단·토큰 증가 0). verify_jwt=false 유지, 배포본-로컬 diff 0·부트 정상. 기획 신규: `11_제품기획/10_챗봇_응답렌더링_설계.md`(상태 Accepted) + ADR-008 등재. git 커밋 `94a651e`(코드 3파일 +690행) main 푸시 완료 → Vercel 자동배포. | 최동혁(+Claude) |
| 2026-07-20 | 챗봇·ERP연계·보안 | `supabase/functions/jeil-chat/index.ts`(v18→v19), `10_ERP_DB연계/`(P4) | **챗봇 권한체계 실연동(v19) — "권한 확인"·"인원현황" 오답 원인 해소 + 소급 권한구멍 차단**. 조사 결과 권한체계(jeil-me/jeil-hr/dept_erp_scope/portal_page)와 인원 데이터(erp_secure.hr_payroll_m, 2026.01~06 113·119·101·100·94·98명)는 이미 정상 적재돼 있었고, **챗봇만 그 체계에 연결돼 있지 않았던 것**이 원인이었음. 신규 도구 3종: `get_my_access`(본인 역할·소속·열람가능 모듈·페이지 조회, 호출자 UPN 고정, 전 직원용) · `get_hr_headcount`(전 직원=전사 총원만, 부서별 분포는 payroll 권한자만) · `get_hr_payroll`(급여 집계, 인사팀·관리자 전용, service_role RPC 경유). **소급 발견한 권한 구멍 차단**: 포털 도구 3종(`get_order_summary`·`get_order_detail`·`get_inspection_pending`)이 그동안 `ERP_TOOL_MODULE` 매핑 누락으로 권한 강제 없이 전 직원에게 협력사 발주번호·금액·납기를 노출하고 있었음 → `pur_order` 모듈 권한으로 강제. 인사 도구는 허용·거부 모두 `hr_access_log` 감사기록. verify_jwt=false 유지, 배포본 로컬 소스 문자단위 일치 검증. 소스는 수정·배포 완료·git 미커밋 상태. **관리자 결정**: ①인원현황 공개등급(전사 총원=전 직원, 부서별 분포·급여액=인사팀·관리자 전용) ②협력사 발주·검사 조회권한=신규 모듈 대신 기존 `pur_order` 재사용. **미결(경영 판단 보류)**: `dept_erp_scope`가 23개 부서 전부 동일 모듈 보유로 부서별 차등이 사실상 없음(payroll만 예외) — 구조는 지원하나 정책 결정 대기, 이번엔 미변경. | 최동혁(+Claude) |
| 2026-07-16 | 그리드·거버넌스 | `그리드/index.html`·`_build_html.py`·`표준그리드_가이드.md(+html)` | **그리드 데모 자기완결화** — ESM import가 file://에서 차단돼 더블클릭 시 그리드가 안 뜨던 문제를 빌드 주입(GRID_*_INLINE 마커, 원본 app/lib/grid.js·grid.css 단일 출처 유지)으로 해소, 멱등 빌드·드리프트 감지(export 패턴 불일치 시 중단) 포함. 총괄 대시보드 그리드 카드 링크도 가이드 정본으로 교정(같은 날 5bbff9e). | 최동혁(+Claude) |
| 2026-07-16 | 거버넌스·관리체계 | `00_관리체계/index.html`(2탭 개편), `.claude/agents/jeilax-master.md`(신설·로컬), `README`·`00_문서대장`·`01_기준정보(§9)`·`CLAUDE.md §10·§12` | **총괄 대시보드 신설 + 총괄관리자(jeilax-master) 등록** — 관리 허브를 2탭으로 개편(기본 탭=총괄 대시보드): 영역 10종 현황(상태·진행률·의사결정 게이트) · KPI 자동계산(상태별 영역 수·최근 14일 업데이트·결정 대기·최근접 기한 D-day) · 진행 타임라인 48건(영역 필터·접기) · 의사결정 로그 22건(결정됨 13/대기·예정 9) · AI 에이전트 현황 4종. 데이터는 index.html 내 `AX_DASH` 인라인 블록이 단일 데이터(원천 4곳 — 이관 00·03 / 10_ERP 00·02 / 본 CHANGELOG / 11_제품기획 09_ADR — 의 파생 뷰)이며, 총괄 서브에이전트 **jeilax-master(통칭 총괄관리자)** 가 지속 갱신한다. 주요 작업 마무리 시 호출 규칙을 CLAUDE.md §10에 통합, `01_기준정보 §9 AI 에이전트 레지스트리` 신설(4종 — erp-db-connector는 총괄 위임 제외 명기) | 최동혁(+Claude) |
| 2026-07-13 | ERP 연계 | ETL(재고 입출고), `10_ERP_DB연계/`(v0.10), 정책관리 미러 | **재고 입출고 ETL 롤링 31일 → 2026년 전체 적재 전환** — inventory_d 1,810행, 연간 재고 흐름 조회 가능. 관제 문서·미러 v0.10 갱신 *(소급 기재 2026-07-16 — 총괄 대시보드 첫 대사에서 누락 발견)* | 최동혁(+Claude) |
| 2026-07-13 | 협력사포털·ERP 연계 | `pages/외주발주_검사진행현황_2026.html` | **외주 발주현황 ERP 발주원장 실연동(8→570건 전체 노출) + 협력사 오버레이** *(소급 기재 2026-07-16)* | 최동혁(+Claude) |
| 2026-07-13 | 협력사포털 | 발주 진행현황 화면 | 상단·상세 간결화, 발주일 날짜 필터, 체크박스 의미 명시 *(소급 기재 2026-07-16)* | 최동혁(+Claude) |
| 2026-07-13 | 협력사포털 | 외주 발주현황 | 프로젝트코드 필터 연결 + 대량조회 성능·안정성 개선 *(소급 기재 2026-07-16)* | 최동혁(+Claude) |
| 2026-07-09 | 챗봇·문서연계·보안 | Supabase(마이그레이션 `ai_document_scope_whitelist` — `ai_document_scope` 테이블 신설·파일럿 시드), Edge Function `jeil-chat`(v17), `02_MS연동_OneDrive_데이터연계.html`(§8-4·8-6 갱신) | **AI 문서 범위 화이트리스트 실적용·배포(v17) — 파일럿 `ai.jeil` 사이트** — §8 기획을 실제 코드로 구현. ① `ai_document_scope`(SSOT, RLS enable·정책없음=service_role 전용) 신설 + 파일럿 Team `ai.jeil`(sites/ai_jeil, driveId `b!QKCqjrc…`) 승인 시드. ② `jeil-chat` v17: 문서도구(`search_my_documents`·`read_document`)에 **이중 게이트** 적용 — 승인 컨테이너(drive_id) ∩ 사용자 위임 권한. 검색은 KQL `path:` 스코프 + 승인 driveId 하드필터, 판독은 driveId 승인검증, **범위 미설정 시 fail-closed**. system_prompt·도구설명에 화이트리스트 명시. ACTIVE·401 정상기동(전 직원 챗봇 무중단·안전폴백). **실측 근거**: 스코프 없던 상태에서 "ai.jeil" 검색이 dh.choi 개인 OneDrive 재무 CSV(원장·지급이체 은행계좌 포함) **6,317건** 노출 확인 → 화이트리스트 필요성 실증, v17로 파일럿 사이트만 격리. 잔여: ai.jeil 멤버십 기반 자동 동기화 배치(현재 수동 등록)·파일럿 테스트문서 등재·부서원 격리검증 | 최동혁(+Claude) |
| 2026-07-09 | 거버넌스 | `02_MS연동…`(§8-6 결정 기록) | **ERP_DML(DML 감사로그) AI 적용 — 테스트 단계 승인**(관리자 최동혁). 현재 ai.jeil 팀 멤버 한정이라 안전. 전사/운영 확대 시 민감도 재심사 후 결정(급여·자금은 여전히 등재 금지). | 최동혁(+Claude) |
| 2026-07-09 | 챗봇·문서연계·보안 | Supabase(마이그레이션 `ai_document_scope_folder_designation` — scope_type·item_id·path_prefix 추가), Edge Function `jeil-chat`(v18), `02_MS연동…`(§8-7·8-8 신설) | **폴더 지정 모델 — 3단계 스코프(site/library/folder) + AI DML 폴더 적용** — 자동 driveId 추측이 엉뚱한 라이브러리를 잡던 문제(SiteAssets 오지정)를 근절: 관리자/사용자가 대상 폴더를 명시 지정하는 모델. `ai_document_scope`에 scope_type(site/library/folder)·path_prefix(정규화 경로 접두)·item_id 추가. `jeil-chat` v18: 검색은 KQL `path:` 한정 + (승인 driveId 일치 AND 경로가 지정 접두로 시작) 하드필터, 판독은 문서 webUrl이 지정 폴더 하위인지 2차 검증 → **폴더 레벨 정밀 강제**(그 위에 사용자 위임 트리밍). **AI DML(ERP_DML)을 folder 스코프로 적용** — 챗봇이 ai.jeil의 ERP_DML 폴더만 검색·판독(상위 Shared Documents 다른 폴더 자동 제외). 지정 방법 3종(A 링크붙여넣기 권장·B ai.jeil공유+폴더확정·C 직접등록), 원칙="후보는 자동, 최종 폴더 지정은 명시적". 전 프로젝트 확대 무결성=프로젝트당 행1개·코드변경0·fail-closed. v18 ACTIVE·401 정상. 잔여: 방법A 링크해석 콘솔UI·방법B 후보열거 배치. (배포본은 토큰절약 위해 주석 축약본, 디스크 index.ts가 풀 레퍼런스·로직 동일) | 최동혁(+Claude) |
| 2026-07-09 | 챗봇·문서연계 | Supabase `ai_document_scope`(id=1 driveId 교정), `02_MS연동…`(§8-6 갱신) | **파일럿 화이트리스트 driveId 교정 + end-to-end 검증** — 사용자 스크린샷으로 발견: `ai.jeil` 사이트에 문서 라이브러리가 2개(SiteAssets/전자필기장 vs Shared Documents/문서)인데 최초 시드가 빈 SiteAssets(OneNote) driveId(`…ZcwLkg…`)였음 → 실제 데이터(`ERP_DML`: DOC 기획문서 20여개·data/dml_data.js·SQL·scripts·BAK)가 있는 Shared Documents drive(`…ovvBu8GzGR…`)로 교정. 위임 토큰으로 README.txt 본문 판독 성공 → v17 검색·판독 범위 정상 포함 확인. 교훈: 사이트 승인 시 대상 라이브러리 driveId 정확 지정 필요(자동 동기화 배치가 자동화). ERP_DML은 DML 감사로그 — 파일럿 멤버 한정이나 전사 확대 전 민감도 재확인 대상 | 최동혁(+Claude) |
| 2026-07-09 | 챗봇·문서연계 | `실제구축준비 자료/02_MS연동_OneDrive_데이터연계.html`(§8 신설) | **AI 작업 범위 거버넌스 — ai_admin 화이트리스트 모델 기획** — 방식 A(위임·라이브 검색)가 사용자 ACL 전체를 범위로 삼아 "OneDrive 폴더가 너무 많아 관리 불가"한 문제를 해소하는 설계. **2중 게이트**: (AI 승인 컨테이너) ∩ (사용자 본인 ACL). 거버넌스 기본키=전용 서비스계정 `ai.jeil@jeilm.co.kr`(2026-07-09 확정, "사이트/라이브러리에 이 계정 구성원 추가 = AI 사용 허가", SharePoint 권한 UI 자기서비스), 범위 단위=SharePoint 사이트/라이브러리. 야간 배치가 ai_admin 접근 컨테이너를 열거→`ai_document_scope`(Supabase SSOT) 갱신, jeil-chat v17(계획)이 `search_my_documents`/`read_document`에 스코프 필터 적용(호출은 여전히 사용자 위임 토큰=권한 트리밍 유지, 서비스계정 토큰 대리읽기 금지). 민감 사이트(급여·인사·계약) 등재금지. 열거 API 정확값은 W7 구현 시 핀 고정. 코드·설정 변경 없음(기획만) | 최동혁(+Claude) |
| 2026-07-09 | 챗봇·문서연계 | `supabase/functions/jeil-chat`(v16)·`ai_gateway_config`(DB 프롬프트)·`02_MS연동_OneDrive_데이터연계.html`(§7-6) | **실 챗봇 OneDrive 문서 연계 착수·배포(방식 A 라이브 위임)** — 설정확인: Entra 앱에 위임 `Files.ReadWrite.All`이 이미 관리자 동의(2026-06-12), 포털 로그인이 해당 scope 요청 → 사용자 토큰에 파일권한 존재·`jeil-chat`이 수신. **Entra·프론트 변경 없이** `jeil-chat` v16에 문서 도구 2종(`search_my_documents`·`read_document`) 추가 — 호출자 위임 토큰으로 Graph 검색·읽기(보안 트리밍, 본인 권한 범위만), Excel(Workbook)·텍스트 본문 판독. DB 시스템프롬프트에 도구 안내 반영. 부팅 정상(401). dh.choi 실테스트 가능(포털 로그인→"내 OneDrive 회의록 찾아줘"). 잔여: 부서원 계정 격리검증·Sites.Read.All(사이트 일부)·docx/pdf 본문추출 | 최동혁(+Claude) |
| 2026-07-09 | 챗봇·문서연계 | `실제구축준비 자료/02_MS연동_OneDrive_데이터연계.html`(§7-5 추가) | **OneDrive 문서 연계 — 실계정 검증(dh.choi) 로그 기록** — Graph Explorer(프로덕션 앱 미변경)에 dh.choi@jeilm.co.kr 위임 로그인해 실제 권한 기준 검증: `/me`(신원)·`/me/drive/root/children`(실제 OneDrive 폴더)·`/sites?search=*`(부서 팀사이트 실존: 구매팀·사업관리팀·공정설계팀 등)·`/search/query`(접근가능 31,980건 보안 트리밍)·Workbook `usedRange`(회의록 양식.xlsx 셀 값 판독) 전부 200. 위임 권한만으로 조회·검색·본문판독 실증 → 방식 A·부서폴더 격리 모델 검증. 부서 격리 최종검증(일반 부서원 계정)·실 챗봇 통합(앱 위임권한+동의, search_docs 도구)은 잔여. 코드·설정 변경 없음(검증만) | 최동혁(+Claude) |
| 2026-07-09 | 챗봇·문서연계 | `실제구축준비 자료/02_MS연동_OneDrive_데이터연계.html`(§7 추가) | **OneDrive 문서 연계 적용가능성 검토(현행 실측 기준) 추가** — 문의 검토. 챗봇 `jeil-chat` 실가동 구조 기준으로 재검증: "OneDrive 권한 개별 적용"은 방식 A(위임·라이브 Graph 검색, 자동 보안 트리밍)가 정합 — 별도 권한테이블 불필요·OneDrive ACL이 통제선, 현 도구호출 구조에 `search_documents` 도구로 확장. 선행조건은 Entra 위임권한(`Files.Read`·`Sites.Read.All`)+관리자 동의. RAG(방식 B)는 2차(권한 트리밍 선결). 대안 M365 Copilot/Azure AI Search 병기. 코드·설정 변경 없음(검토만) | 최동혁(+Claude) |
| 2026-07-09 | 챗봇·관리콘솔 | Supabase(마이그레이션 `ai_model_settings_tables`·`ai_model_settings_seed` — `ai_model`·`ai_gateway_config`·`ai_routing_rule` 신설+시드), Edge Function `jeil-chat-admin`(v7)·`jeil-chat`(v15), `04`(모델 설정 탭·통합본 재생성), `실제구축준비 자료/06_챗봇_사용모델설정_SSOT.md`(신설) | **관리자 콘솔 '모델 설정' 실연동 — 데모 목업 → 실제 디테일 설정 가능** — 그간 `alert('데모 미동작')`이던 모델 설정 탭을 실제 Supabase 저장·게이트웨이 소비로 전환. ① 설정 SSOT 3테이블 신설(RLS enable·정책없음=service_role 전용, `chat_log` 패턴): 모델 카탈로그(`ai_model` 단가·활성화·callable) / 게이트웨이 설정(`ai_gateway_config` 싱글턴 — 기본모델·max_tokens·temperature·상한·시스템프롬프트) / 라우팅 규칙(`ai_routing_rule`). 시드값은 현행 하드코딩값과 동일(폴백 무결). ② `jeil-chat-admin` v7: `model_settings` 조회 + `save_ai_models`·`save_ai_config`·`save_ai_routing` 저장 액션(관리자만·검증: callable은 벤더로 강제, 기본모델은 active+callable만, enforced는 규칙유형으로 강제). ③ `jeil-chat` v15: 요청마다 DB 설정 로드해 **실제 소비**(시스템프롬프트·상한·max_tokens·temperature·기본모델·keyword_length 라우팅·모델별 단가) — 조회 실패 시 기존값 안전 폴백(전 직원 챗봇 무중단). ④ `04` 모델 패널을 실데이터 렌더·저장으로 배선. 현 게이트웨이는 OpenAI 프록시라 OpenAI만 callable, Anthropic은 카탈로그·단가 표시용. 프론트(`04`·통합본) 라이브 반영은 git push 필요(§8 절차) | 최동혁(+Claude) |
| 2026-07-08 | ERP 연계·챗봇 | Supabase 마이그레이션(`erp_process_link_enrich`), `이관/sql/15`(신설), `supabase/functions/jeil-chat/index.ts`(v14), `10_ERP_DB연계/`(`00`·`02` 갱신, v0.8), 정책관리 미러 | **챗봇 ERP 프로세스(발주·구매요청·매입·입고) 응답 품질 개선 — 실적용·배포** — 중간DB 노출 뷰 3종(`v_erp_po_pr_link`·`v_erp_pur_req`·신규 `v_erp_pur_top_po`) 보강: 입고/매입 진행수량·외주구분·납기경과 플래그·연결수주·요청부서(이메일 해석) 컬럼 추가, 발주 총액 집계 오류 해소. `jeil-chat`은 **[치명적 버그 수정]** 도구 호출을 1라운드→상한 멀티라운드(`MAX_ROUNDS=4`)로 교체(재조회 불가로 안내문만 반복되던 사용자 루프 해소), 상태코드 한글 해석 주입, 신규 도구 2종 추가 — version 14 ACTIVE 배포. 실측으로 재고 입고·재고량 미적재/매출 수주·수금 미매핑/발주 외주구분 전건 'N' 등 ETL 데이터 갭 확인(유니포인트·ETL 점검 대상으로 등재) | 최동혁(+Claude) |
| 2026-07-08 | ERP 연계·보안 | Supabase 마이그레이션·`jeil-hr` Edge Function(신규 v1), `erp_secure.hr_payroll_m`(143행), `10_ERP_DB연계/`(`00`·`01`·`02` 갱신, v0.9), 정책관리 미러 | **인사 급여 집계 실적재 완료 + 게이트 API `jeil-hr` 배포**(관리자 직접 실행) — 승인-준비 상태였던 `erp_secure.hr_payroll_m`에 실제 데이터를 적재했다: ETL job `hr_payroll`로 143행(급여 월×부서 137+퇴직 월×전사 6, 2026.01~06) 적재, 개인별·이름·주민번호·계좌는 여전히 미포함(집계만). 게이트 API `jeil-hr` v1 배포(Entra 검증→`dept='인사팀'`/`portal_admin`만→감사→집계 반환). 자금(F 모듈) 경영 승인·실적재는 별개로 미착수, `AI-Portal-HR` Entra 그룹·유니포인트 재직상태 정의는 잔여 | 최동혁(+Claude) |
| 2026-07-08 | ERP 연계·보안 | Supabase(마이그레이션 `payroll_module_hr_gate`·`erp_secure_hr_schema`), `이관/sql/13`·`14`(신설), `pages/인사_인원급여추이_2026.html`, 통합본, `10_ERP_DB연계/`(`00`·`01`·`02` 갱신, v0.7), 정책관리 미러 | **급여(민감) 권한 강화 + `erp_secure` 착수(승인-준비)** — 급여를 민감 모듈 `payroll`로 신설해 `dept_erp_scope`를 인사팀 전용으로 한정하고 인사 페이지를 서버 게이트(비인사팀 직접 접근 차단), 관리자 콘솔 급여 ⚠ 민감표시(일괄/제안 제외). 인사 데모 페이지 개인급여를 샘플값+마스킹으로 안전화. 민감 분리 스키마 `erp_secure`(`hr_payroll_m` 집계·`hr_access_log` 감사)를 승인-준비 빈 구조로 신설했으나 **실 데이터는 미적재** — 실 적재는 경영 승인+유니포인트 View 협의+`AI-Portal-HR` Entra 그룹이 선행돼야 함 | 최동혁(+Claude) |
| 2026-07-08 | ERP 연계 | Supabase(마이그레이션 `erp_dept_module_suggest`·`erp_dept_erp_suggest_public_view`), `이관/sql/12`(신설), `jeil-chat-admin` v5, `10_ERP_DB연계/`(`00`·`01`·`02`·`05` 갱신, v0.6), 정책관리 미러 | **부서별 ERP 모듈 제안값(참고) 신설·실적재** — 신규 ETL job `usr_erp_module`로 ERP 역할·메뉴 권한(사용자↔역할↔메뉴↔ModuleInitial)을 근거로 `erp_ro.usr_erp_module_s` 397건 적재, 부서 과반 접근 모듈을 관리자 콘솔 '권한 상세' ERP 매트릭스에 ◆ 참고표시로 반영(자동 덮어쓰기 안 함 — 관리자 결정, 실질 통제선은 편집형 `dept_erp_scope`). 실측: ERP 읽기권한이 광범위(평균 17역할)해 ERP-역할 신호는 부서 변별력이 약함을 확인 — 참고 제안 결정의 타당성 입증 | 최동혁(+Claude) |
| 2026-07-08 | 권한·포털 | Supabase(`portal_page`·`dept_erp_scope`·`dept_permission`), Edge Function `jeil-me`(신규)·`jeil-chat-admin`(v4)·`jeil-chat`(소스), `04`(권한 상세 탭·카드 게이팅), `pages/_access-gate.js`+운영 8종, `이관/sql/10·11` | **권한 설정 실제 적용(Enforcement) — 서버 판정 기반 페이지·ERP 접근 강제.** ① 신원·역할(전체관리자/부서관리자/일반)·부서 판정 엔드포인트 `jeil-me`(Entra Graph 재검증→ERP 매핑 부서→권한). ② 부서 운영 페이지: 포털 카드 잠금 + 8개 페이지 URL 직접 접근 서버 차단(권한 없으면 🔒). ③ 페이지 공개범위·부서×ERP 모듈 권한을 콘솔 '권한 상세'에서 편집·영속(portal_admin만). ④ erp_scope 페이지 레벨 강제 라이브, 챗봇 Tool 게이팅은 소스 반영(재배포 대기). 부서 기준은 ERP 사용자-부서 매핑, ERP 모듈 기본값 시드(편집 가능) | 최동혁(+Claude) |
| 2026-07-08 | ERP 연계 | Supabase(마이그레이션 `erp_user_dept_exclude_unmatched`), `이관/sql/09 §3-2`, `10_ERP_DB연계/`(`00`·`02`·`05` 갱신, v0.5), 정책관리 미러 | **사용자-부서-사원 매핑·대사 실적재 완료 + 자동제외 정책 확정** — 관리자 직접 실행으로 `erp_ro.dept_master_s` 351건·`usr_master_s` 99건 실적재 완료. 대사 결과 정상 매핑 89건·불일치 10건(부서불일치 6·상태이상 3·테스트 1) 확정, 불일치는 정상 매핑에서 자동 제외하는 정책 확정. 잔여이슈: 상태이상 3건(김해품질팀·영업팀·자재물류파트) 현업 확인 권고, `usr_master` upsert-only prune 후속 | 최동혁(+Claude) |
| 2026-07-08 | ERP 연계 | Supabase(마이그레이션 `erp_user_dept_mapping`), `이관/sql/09`(신설), `10_ERP_DB연계/`(`05` 신설 + `00`·`01`·`02` 갱신), 정책관리 미러 | **사용자-부서-사원 매핑·대사 계층 신설** — `Z_USR_MAST_REC`(사용자 마스터, `usr_id`=MS 이메일 SSOT)와 `B_ACCT_DEPT`(부서 마스터)를 대사해 이메일 기준 사용자↔부서↔사원 관계를 조회하는 스키마·뷰(erp_ro 5종+public 노출 3종)·ETL job 2종 구축. 대상 99건 중 부서명 25/29 일치, 불일치 4건(해외법인·외부회계·재무 세분화·admin) 처리방침 결정 대기. 실적재는 미실행(0행, 관리자 `!` 대기). 전용 관리 문서 `10_ERP_DB연계/05_사용자부서_매핑대사.md` 신설 | 최동혁(+Claude) |
| 2026-07-08 | ERP 연계 | Supabase(마이그레이션 2건·`jeil-chat` v7), `app/lib/api.js`, `pages/구매_거래처별매입집계_2026.html`, `이관/sql/08`(신설), `10_ERP_DB연계/`, `11_제품기획/06·09` | **P4 착수 — ERP 중간DB→앱/챗봇 실데이터 연결** — 사내 전용 노출 뷰 `public.v_erp_*` 6종 신설(erp_ro REST 비노출 유지·`security_invoker`+`internal_select_*` RLS로 사내만, 협력사/anon 0행 격리 실테스트 통과). `api.js`에 `erpApi` 추가 + 구매 부서페이지 파일럿 실연동(비로그인 목업 폴백). 챗봇에 ERP Tool 4종(매출·매입·재고·품목) 추가 배포. 실적재는 관리자 `!` 직접 실행. 재현 SQL `이관/sql/08`. 매핑은 유니포인트 확정 전 '가설'·전용계정 `jeilax_ro` 미전환 | 최동혁(+Claude) |
| 2026-07-08 | 제품기획·거버넌스 | `11_제품기획/`(신설, 문서 10종+README+index+빌드), `00_문서대장`, 본 CHANGELOG | **전사 제품기획·아키텍처 SSOT 신설** — 여러 곳에 분산된 현재 시스템 구조(3계층 데이터·인증 3경로·챗봇·이관)를 CTO 관점 단일 세트로 통합·격상. PRD·SRS·시스템아키텍처·DB설계·FE/BE설계·ERP연계(P4)·Azure이관·보안·ADR 10종. **Supabase 라이브 실측**(get_advisors·list_tables·list_migrations)으로 전 테이블 RLS ON·보안 advisor(WARN 2·INFO 2)·성능 136건을 08 보안에 근거 기록. 발견 불일치(챗봇 Claude/OpenAI 표기 등)·미결정(스택·호스팅·인증 D1·BFF)은 09 ADR에 이슈/결정대기로 등재. 코드 무변경(순수 문서화). 정본 중복 없이 링크 참조 | 최동혁(+Claude) |
| 2026-07-07 | ERP 연계 | `.claude/agents/erp-db-connector.md`(신설), `10_ERP_DB연계/agent_kit/`(신설), `02_진행상태.md`, `00_현재상태_스냅샷.md` | **ERP_DB 연결 전담 에이전트(`erp-db-connector`) 신설 — 문서·DB 작업 역할 분리** — 기존 `erp-db-link-manager`(문서·미러 관제)와 별개로 ERP운영DB 연결테스트·`table_dict`·ETL dry-run/실적재·`batch_run` 점검을 담당하는 신규 에이전트 도입. 배포킷 `10_ERP_DB연계/agent_kit/`(비밀값 없음)로 ERP_DB 작업환경에도 동일 설치 가능하게 함, 정책관리 미러에 `agent_kit/` 동봉 | 최동혁(+Claude) |
| 2026-07-07 | 그리드·거버넌스 | `그리드/`(가이드·데모·샘플), `app/lib/grid.js`·`grid.css`, `app/admin-vendors.html` | **편집 그리드 표준 컴포넌트 신설 + 첫 적용** — 바닐라 ESM 표준그리드(의존성 0)+가이드+데모, 협력사 계정발급 화면(#vmGrid) 적용. 이후 CLAUDE.md §13 표준으로 승격 *(소급 기재 2026-07-16 — 총괄 대시보드 첫 대사에서 누락 발견)* | 최동혁(+Claude) |
| 2026-07-07 | 협력사 포털 | `app/admin-vendors.html`, DB(Realtime) | **계정관리에 담당자 정보 실반영** — 그리드 담당자명을 vendor_account 실데이터로 표시(기존 항상 빈값) + 연락처 컬럼 신설. vendor_account Realtime 발행 켜서 협력사가 포털에서 수정하면 관리 화면에 즉시 반영(일괄 편집 중엔 입력 보호) | 최동혁(+Claude) |
| 2026-07-07 | 협력사 포털 | `pages/협력사_모바일_포털.html`, `app/lib/api.js`, DB(RPC) | **모바일 메시지 체감결함 수정 + 내 정보 수정 신설** — 한글 IME Enter 오발송 가드, 낙관적 말풍선(전송 무반응 제거)·연타 중복 방지, 스레드 뒤로가기 출처 복귀(상세→상세). 내정보에서 담당자 이름·연락처 본인 수정(user_metadata + `update_my_vendor_contact` RPC로 vendor_account 컬럼 제한 동기 — 사내 관리 화면에도 반영) | 최동혁(+Claude) |
| 2026-07-07 | 협력사 포털 | `03_실구축기획/08`(신설), `app/lib/api.js`, `pages/*2026.html·모바일_포털.html`, DB | **전체 프로세스 상태머신 재설계** — 검수 불합격 시 흐름이 영구 교착되던 구조 결함(판정이 상태를 전이시키지 않음) 해소. SAP QM(UD)·ISO 9001 8.7 벤치마킹으로 5상태 머신 정의(new→prod→insp→합격 done/불합격 rework→재검수). 판정 시 자동 전이, 재검수요청 시 판정 리셋+차수 발번, 합격 없이 완료 불가·완료 후 되돌리기 불가 가드, 기존 데이터 정합화 | 최동혁(+Claude) |
| 2026-07-07 | ERP 연계 | Supabase(`erp_ro`·`etl_meta`), `10_ERP_DB연계/etl/`(신설), `.env.example` | **중간DB 구축 착수(D1-a 실행)** — 03 기획 §4 DDL 적용(1차 4종+배치메타+RLS 사내한정), ERP 테이블 사전 `table_dict` 신설(사내 ERP_DB 메타 2,658테이블 적재용), service_role 전용 적재 RPC, ETL 스크립트 3종(사전적재·4종 배치 초안·README). 실적재는 .env 키 입력 대기 | 최동혁(+Claude) |
| 2026-07-07 | 협력사 포털 | `pages/외주발주_검사진행현황_2026.html`, `pages/협력사_모바일_포털.html`, `app/lib/api.js`, DB | **실가동 전면 개선** — 협력사 등록 사진 실표시(서명URL 일괄+캐시+폴백, 기존엔 항상 합성 SVG), 사진 검토 신설(사내 확인/반려+사유 → 협력사 재촬영 배너, RLS로 협력사 위조 차단), 검토 전 사진 삭제, 검수요청 취소 DB 영속화(+IR 재발번), 발주별 담당 구매자 실데이터(buyer 컬럼), read_at 기반 미읽음, 판정 이력 전체 표시, 죽은코드 정리 | 최동혁(+Claude) |
| 2026-07-07 | 기준정보 | 포털DB, `이관/sql/05·07`, `08_ERP_중간DB_샘플데이터`, `04_데모UI` | **샘플데이터 업체명 실명화** — 마스킹 업체명 17개를 실제 업체명으로 통일(DB+시드+문서), 데모UI 자사명 ○○사→제일엠앤에스. 개인정보(인사·급여) 마스킹은 유지 | 최동혁(+Claude) |
| 2026-07-07 | AI 챗봇 | `supabase/functions/jeil-chat*/`, `04_챗봇_포털_데모UI.html`, DB, `실제구축준비 자료/06_*` | **U-1 토큰·비용 기록 + 1단계 포털DB 조회 도구 가동** — chat_log에 토큰·추정비용·사용도구 기록(스트림 usage 캡처), Tool Use로 발주 현황 요약·발주 상세·검수 판정대기 3종 도구(읽기전용) 연결. 관리자 콘솔 사용량 탭에 토큰·비용 표시 | 최동혁(+Claude) |
| 2026-07-07 | AI 챗봇 | `supabase/functions/jeil-chat-admin/`, `04_챗봇_포털_데모UI.html`, `실제구축준비 자료/06_*`(신설) | **챗봇 연결 완료 + 관리자 콘솔 실데이터 연동** — 사용량·권한·게이트웨이 상태 실측 API(`jeil-chat-admin`, portal_admin 전용)와 관리자 탭 "● 실데이터" 섹션. 데이터 접근권한(Entra 그룹 RBAC)·데이터 적용방식(Tool Use 3단계)·사용량 관리(토큰·한도·알림) 고도화 기획 06 문서 신설 | 최동혁(+Claude) |
| 2026-07-07 | 협력사 포털 | `app/admin-vendors.html`, `app/lib/api.js`, `pages/협력사_모바일_포털.html`, DB | **협력사관리 메시지 고도화** — 관리 화면에 협력사 메시지 섹션 신설(발주별 스레드·미읽음 배지·실시간 수신·열람 시 읽음처리·새 스레드 시작). `sp_message`에 `sender_name` 컬럼 추가로 메시지 저장 실패 버그 수정 + 조회 인덱스. api.js에 `adminMsgApi`·`markRead` 신설, 협력사 포털 열람 시 사내 발신분 읽음 연동. 메시지 발신자명은 로그인 사용자 정보로 표기(사내=Entra 표시명, 협력사=담당자명(회사명)) | 최동혁(+Claude) |
| 2026-07-07 | AI 챗봇 | `supabase/functions/jeil-chat/`, `04_챗봇_포털_데모UI.html`, `.env.example` | **챗봇 실 API 연결 착수** — OpenAI 프록시 Edge Function `jeil-chat` 신설(키는 서버 시크릿만, Entra 토큰 Graph 검증으로 사내 한정, SSE 스트리밍, `chat_log` 감사 기록). 데모 UI 자유입력을 실 응답으로 전환(미로그인=데모 폴백). OPENAI_API_KEY 시크릿 등록 대기 | 최동혁(+Claude) |
| 2026-07-03 | ERP 연계 | `10_ERP_DB연계/03_*` | **중간DB 구축 실행기획 v0.1 신설** — 연결 방안 D1(중간DB 위치)~D3(추출 원천)·스키마 DDL·ETL 처리방법·준비사항·6주 일정. 정책관리 미러 동기화 | 최동혁(+Claude) |
| 2026-07-03 | ERP 연계 | `10_ERP_DB연계/`(신설), `.claude/agents/` | **ERP DB 연계 관제 체계 신설** — 현재상태·기획·진행상태 3종(SSOT) + HTML 빌드 + 사내 OneDrive `정책관리/ERP_DB연계/` 미러 동기화 스크립트. 전담 에이전트 `erp-db-link-manager` 등록(작업 시 문서·미러 자동 갱신) | 최동혁 |
| 2026-06-30 | 거버넌스 | `00_관리체계/`(신설), `CLAUDE.md`, 루트 | **관리체계 허브 신설** — 기준정보 레지스트리·문서대장·명명규칙·변경관리 + 공통 빌드 모듈. 빈 `그리드/` 제거, 비용·호스팅 4종 `09_비용_호스팅/`로, 메일발송 메모 `08/`로 이동 | 최동혁 |
| 2026-06-29 | 협력사 포털 | `app/`, `supabase/functions/`, `이관/sql/` | 협력사 계정 일괄 발급(엑셀형), Graph 토큰 부재 시 자동 재인증 메일, 비밀번호 변경, Supabase 연동·RLS 10테이블 | 최동혁 |
| 2026-06-29 | 인프라 | `이관/00_현황스냅샷` | 라이브 ai·ax 모두 **Vercel**로 일원화(GitHub Pages→Vercel 전환) | 최동혁 |
| 2026-06-15 | 데모 | `04_챗봇_포털_데모UI.html`, 통합본, `05_니즈조사/` | 사이드바 크레딧 박스 제거→설문 참여 항목, 설문폼 작성자 자동반영·기한 비노출(원본: `doc/02_변경이력`) | 최동혁 |

> 신규 변경은 **맨 위 행에 추가**한다(최신 우선).
