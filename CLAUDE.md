# CLAUDE.md — JEIL AX 프로젝트 작업 규칙

> 이 파일은 Claude Code가 이 저장소에서 작업할 때 **반드시 따라야 하는 기준·규칙**이다.
> 작업 시작 전 항상 이 파일을 먼저 읽고, 충돌이 있으면 이 문서의 규칙이 우선한다.
> 최종 갱신: 2026-06-26 · 관리자: 최동혁(dh.choi@jeilm.co.kr)

---

## 0. 한 줄 요약

제일엠앤에스(JEIL M&S) 사내 **AI 포털**(ERP 연계 챗봇 + 부서별 운영 관리페이지 + 협력사 발주·검사 포털).
현재 **정적 HTML 데모 + 기획 단계** → **실제 구축(ERP 실연계·로그인 운영)** 전환 중.
운영 도메인 **ai.jeilm.co.kr**, 로그인은 **Microsoft Entra ID(=Teams/MS365 계정) SSO + MFA**.

---

## 1. 절대 규칙 (NEVER — 위반 금지)

1. **비밀값을 코드/커밋에 절대 넣지 않는다.** Entra Client Secret, MS SQL 접속 계정·비밀번호, 연결 문자열, API 키, 토큰, 내부 서버 IP를 소스에 하드코딩하거나 커밋하지 않는다. 반드시 환경변수(`.env`)·시크릿 매니저로 분리한다.
2. **ERP(UNIERP) 운영 MS SQL에 직접 쓰기/직접 조회를 하지 않는다.** 읽기 전용 + 중간 DB 경유만 허용(§4). 가상드라이브 우회, 운영 DB 직접 SELECT, DDL/DML은 금지.
3. **`MS_connect/`, `doc/`, `.claude/` 폴더 내용을 커밋하거나 공개 배포본에 포함하지 않는다.** (Entra 식별자·Secret·내부 작업현황 포함. `.gitignore`로 제외됨 — 해제 금지.)
4. **새로 만드는 문서·코드에 Entra Tenant/Client ID, Secret 값을 적지 않는다.** 기술 원본 값은 `MS_connect/`에만 둔다.
5. **배포(특히 OneDrive 동기화 폴더)에서 작업 폴더를 직접 `git push` 빌드하지 않는다.** OneDrive(E:) ↔ 빌드환경 동기화가 수십 분 지연될 수 있음 → §8 배포 절차를 따른다.
6. **금전·발주 확정·전자결재 등 실거래 액션을 사용자 대신 실행하지 않는다.** 데이터 조회·집계·초안 작성까지만. 최종 승인 클릭은 사람이 한다.
7. **개인정보(급여·주민번호·인사평가 등)는 권한 그룹(§5) 밖으로 노출되는 화면/로그/캐시에 남기지 않는다.** 단, **직원 이름(성명)은 개인정보 마스킹 대상에서 제외**한다(2026-07-07 관리자 결정) — 이름은 실명 표기 가능하되, 이름과 결합된 급여·평가·주민번호 등 민감 값은 여전히 보호한다.

---

## 2. 프로젝트 현재 상태 (사실 기준, 추측 금지)

- **저장소**: `cdhnayajeil-code/jeil_ax` (GitHub). 브랜치 `main`(소스) + `gh-pages`(배포). 운영 도메인 `ai.jeilm.co.kr`(CNAME, HTTPS 적용 완료).
- **현재 산출물은 대부분 단일 파일 정적 HTML**(바닐라 JS, 외부 빌드 없음). 데모/목업 + 기획 문서(HTML/MD/DOCX).
- **로그인**: Entra ID 앱 `JEIL-AX-Portal`(단일 테넌트 jeilm.co.kr), **SPA + PKCE(S256)** 방식. 토큰은 브라우저 `localStorage 'jeilax_auth'`에 저장(offline_access 포함). 조건부 액세스 "Require MFA for internal users"로 MFA 자동 적용. → 운영 전환 시 이 방식의 보안 한계는 §6에서 재검토.
- **ERP**: UNIERP, **사외 IDC의 MS SQL**. 현재는 화면이 추출 샘플 데이터로 동작(실연계 전).
- **빌드**: `python _build_bundle.py` → 데모/운영페이지/설문폼을 base64 인라인한 단일 공유 파일 `JEIL_AX_포털데모_통합본.html` 재생성.

### 폴더 지도

```
JEIL_AX/
├─ CLAUDE.md                         ← 본 파일 (작업 규칙)
├─ 00_관리체계/                       ← ★기준정보·문서대장·명명규칙·변경관리 허브(단일 출처): index.html + 00~03
├─ index.html / CNAME                ← 진입·도메인
├─ 01~03_*.html                      ← 기획서 (도입/니즈조사/챗봇포털)
├─ 04_챗봇_포털_데모UI.html           ← 포털 메인 데모(로그인·부서선택·운영페이지 링크)
├─ 05_니즈조사/                       ← 설문폼 + assets + 응답(OneDrive 자동집계)
├─ 06_실행기획_고도화/
├─ 07_부서운영페이지_확장기획.html
├─ 08_협력사발주포털/                  ← 협력사 발주·검사 시스템 (기획+데모)  ★신규 핵심
├─ 09_비용_호스팅/                    ← 비용계산기·비용/호스팅 기획(브랜드 도구 묶음)
├─ 10_ERP_DB연계/                     ← ★ERP DB 연계 관제(현재상태·기획·진행상태 단일 출처, 정책관리 미러 동기)
├─ pages/                            ← 부서별 운영 관리페이지(영업/구매/인사/자금/자재/외주 등)
├─ app/                              ← 실구축 프론트(협력사 로그인·관리, Supabase 연동 api.js)
├─ supabase/functions/              ← Edge Function(vendor-provision·approve·reset)
├─ 실제구축준비 자료/00~05,07_*.html   ← 실구축 종합기획 7부 + 16주 로드맵
│   └─ 이관/                          ← ★이관 관제 센터(단일 출처): 현 시스템 상태·Vercel+Supabase→Azure 이관 (index.html 허브 + 00~03)
├─ _build_bundle.py                  ← 통합본 빌드 스크립트
├─ MS_connect/   (git 제외)          ← Entra/MFA 연동 기술문서·식별자·Secret·작업현황
├─ doc/          (git 제외)          ← 로고 등 내부 자산
└─ .claude/      (git 제외)          ← 로컬 권한 설정
```

작업 전 해당 영역의 README / `00_*` 종합문서를 먼저 읽는다 (예: 협력사 포털은 `08_협력사발주포털/00_종합정리_요구사항이력.html`).

---

## 3. 아키텍처 방향 (스택 미정 — 결정 기준과 권고)

> 스택은 아직 확정 전이다. 아래 권고를 **기본값**으로 삼되, 확정되면 본 절을 갱신한다.

### 3.1 권고 구조 (3계층 분리)

```
[브라우저/모바일]  ── Entra(Teams) 로그인 ──►  [백엔드 API]  ── 읽기전용 ──►  [중간 DB(복제본)]  ◄─ 야간배치 ─  [ERP 운영 MSSQL(사외 IDC)]
   프론트엔드                                  인증·권한·집계                  포털 전용 데이터(쓰기 가능)
```

핵심은 **프론트가 ERP/MSSQL에 직접 붙지 않는다**는 것. 반드시 **백엔드 API 계층**을 둔다. 현재 데모는 프론트만 있고 이 계층이 없음 → 실구축의 1순위 신설 대상.

### 3.2 스택 후보 비교 (결정 시 이 표 기준)

| 항목 | 후보 A: .NET/C# + MSSQL | 후보 B: Node/Next.js + Postgres 중간DB |
|---|---|---|
| ERP MSSQL 친화 | ◎ (동일 생태계, EF/ADO 직결) | ○ (드라이버 경유) |
| Entra/Teams 연동 | ◎ (MSAL.NET 성숙) | ○ (MSAL.js/next-auth) |
| 사내 IIS/Windows 호스팅 | ◎ | △ |
| 프론트 개발 속도·UI | ○ | ◎ (현 HTML 데모 계승 쉬움) |
| 클라우드(Azure) 배포 | ◎ | ◎ |
| 권장 적용 | ERP 연동 백엔드 API | 포털 프론트 + 챗봇 오케스트레이션 |

**현실적 권고**: 프론트/챗봇은 후보 B 계열로 빠르게, **ERP 연동 백엔드는 후보 A(.NET)** 또는 별도 읽기 API로 분리하는 **하이브리드**. 단일 스택 강제하지 말 것. 결정 전까지 새 코드는 "API 호출 인터페이스"를 추상화해 스택 교체 비용을 낮춘다.

### 3.3 스택 확정 전 코딩 원칙

- 비즈니스 로직을 HTML 인라인 `<script>`에 묻지 말고, 데이터 접근을 **API 클라이언트 모듈 1곳**으로 모은다(현재는 mock, 이후 실 API로 교체).
- 화면은 "데이터 출처 = API"라고 가정하고 작성한다. localStorage 의존(`jeilax_auth`, `jeilax_link_v1`)은 데모 한정임을 주석으로 명시한다.

---

## 4. ERP(MS SQL) 연계 규칙

1. **운영 MSSQL은 읽기 전용.** 포털은 **중간 DB(복제/스테이징)** 만 바라본다. 운영 → 중간 DB는 **야간 배치**(또는 CDC) 동기화. 실시간 단건 조회가 꼭 필요하면 읽기 전용 뷰/저장프로시저로 화이트리스트 한정.
2. **네트워크**: 파일럿은 IP 허용, 운영은 VPN. 접속 정보·연결 문자열은 환경변수로만.
3. **포털 전용 쓰기 데이터**(사진, 검수 메모, 진행상태, 메시지 등)는 **별도 포털 DB/테이블**에 저장하고 ERP 키로 매핑한다. ERP 테이블에 직접 쓰지 않는다.
4. **알려진 ERP 매핑(협력사 발주·검사)**: `M_PUR_REQ`(발주요청) · `M_PUR_ORD`(발주) · `M_CONFIG_PROCESS`(SUBCONTRA_FLG=외주구분) · `M_PUR_GOODS_MVMT`(입출고) · `Q_INSPECTION`(검사) · `M_IV`(품목). 신규 화면은 이 매핑을 재사용·확장한다.
5. **쿼리는 파라미터 바인딩만.** 문자열 연결 SQL 금지(인젝션 방지). 대량/집계 쿼리는 중간 DB에서, 운영 DB 부하를 만들지 않는다.
6. 유니포인트(ERP 벤더) 협의가 필요한 사항(스키마 접근·뷰 생성)은 임의 진행하지 말고 **사용자에게 확인 요청**.

---

## 5. 인증·권한 (Entra / Teams 연동 + RBAC)

1. **로그인은 Microsoft Entra ID 단일 경로**(=Teams/MS365 계정). 사내 사용자는 별도 비밀번호 없이 SSO + MFA. 자체 회원가입·자체 비밀번호 저장 금지.
2. **redirect URI**는 SPA 플랫폼에 정확히 등록된 값만 동작(현재 `https://ai.jeilm.co.kr/`, `…github.io/jeil_ax/`). 새 호스트/경로 추가 시 Entra 앱 등록 변경이 선행되어야 함 — 코드만 바꾸면 AADSTS50011로 실패. **Entra 앱 설정 변경은 사용자(관리자)가 직접** 하고, Claude는 변경 목록만 제시한다.
3. **권한 모델(RBAC)은 Entra 보안그룹 기준**으로 설계: `AI-Portal-Admins`, `AI-Portal-Finance`, `AI-Portal-HR`(필요 시 `AI-Portal-Subcontractor`, `AI-Portal-Sales` 등 확장). 화면·API·데이터를 그룹 클레임으로 인가한다.
4. **권한 판정은 백엔드에서.** 프론트의 메뉴 숨김은 UX일 뿐, 실제 데이터 차단은 API/DB 레벨에서 한다. 프론트 토큰만으로 민감 데이터를 내려주지 않는다.
5. **협력사(사외) 사용자**는 사내 직원과 동일 Entra 테넌트에 넣지 않는다. 별도 인증 경계(B2B 게스트 또는 협력사 전용 인증)로 분리하고, 협력사는 **자기 발주 건만** 접근(행 수준 보안). §9 참조.
6. **토큰 저장**: 데모는 localStorage지만, 운영은 XSS 노출 위험 → HttpOnly 쿠키 + 백엔드 세션, 또는 BFF 패턴을 §6에서 검토.

---

## 6. 보안 기준

- **전송 구간 HTTPS 강제.** 포털 코드는 `isHttps()` 참일 때만 로그인 허용(현 동작 유지).
- **시크릿 회전**: Entra Client Secret 만료 **2026-12-09** → 11월 중 갱신(미갱신 시 로그인 전면 중단). 만료 추적은 일정에 등록한다.
- **운영 전환 시 토큰 보관 재설계**: localStorage 토큰 → BFF/HttpOnly 쿠키 검토(XSS 내성).
- **감사 로그**: 누가·언제·어떤 ERP 데이터/화면을 봤는지 백엔드에서 로깅(개인정보·금액 화면 우선).
- **입력 검증·출력 인코딩**으로 XSS/인젝션 방지. 외부 입력(협력사 업로드 사진·메모)은 파일형식·용량·확장자 검증 + 별도 스토리지.
- **민감 데이터 최소화**: 화면·API 응답에 필요한 컬럼만. 급여·인사·자금 데이터는 그룹 인가 필수.
- 보안 점검이 필요한 변경은 `/security-review` 스킬로 셀프 점검 후 사용자에게 결과 보고.

---

## 7. 코딩·문서 컨벤션

- **언어**: 코드 주석·문서·커밋 메시지·UI 텍스트는 **한국어**. 부서명 표기 통일(예: "인사총무팀" 아님 → **"인사팀"**). 챗봇 명칭은 **jeil-chat**(구 UNI-Chat 표기 금지).
- **HTML 데모**: 단일 파일 자기완결 원칙(외부 CSS/JS 분리 최소화, 공유 편의). 단 신규 실구축 코드는 모듈 분리.
- **데이터 접근은 한 모듈로.** mock↔실API 전환을 한 곳에서. 화면 곳곳에 fetch 흩지 않는다.
- **파일/폴더 명명**은 기존 규칙 계승: `NN_주제_설명.html`, 날짜는 `YYYY-MM-DD` 또는 `YYYYMMDD`.
- 새 기능은 해당 영역 README / `00_종합` 문서에 요구사항 이력을 갱신한다(특히 08 협력사 포털).

---

## 8. Git·배포 규칙

1. **커밋 메시지는 한국어, 무엇을·왜를 명확히**(기존 스타일 계승). 예: `운영페이지 외주 협력사 발주·검사 포털 섹션 신설 — 사내 대시보드+협력사 모바일 묶음`.
2. **`MS_connect/ doc/ .claude/` 는 절대 add 하지 않는다.** 커밋 전 `git status`로 민감 폴더·`.env`·키 파일이 스테이징되지 않았는지 확인.
3. **배포 절차(중요)**: OneDrive(E:) 작업폴더에서 바로 push 빌드하지 말 것 — 동기화 지연으로 누락/충돌 발생. 권장: **`/tmp`에서 `git clone` → 패치·빌드 → 커밋/푸시(또는 GitHub 웹 업로드)**. `gh-pages` 반영 후 `ai.jeilm.co.kr`에서 실제 확인.
4. **main과 gh-pages 동기화**에 유의(과거 main 미갱신 사례 있음). 라이브에 올린 변경은 `main`에도 반영한다.
5. `JEIL_AX_포털데모_통합본.html`은 손으로 고치지 말고 **항상 `python _build_bundle.py`로 재생성**한다.

---

## 9. 협력사 발주·검사 포털 (신규 핵심)

- 구조: **협력사 모바일 포털 ↔ 사내 외주 발주·검사 대시보드**, ERP 연계 + 실시간 양방향 동기화. 기획·데모는 `08_협력사발주포털/`.
- 데모 연동키 `jeilax_link_v1`(localStorage)은 **데모 한정** — 실구축에선 백엔드 API + 포털 DB로 대체.
- 기능: 협력사(발주 조회·검사 증빙 사진 등록·진행상태·검수요청·메시지), 사내(10단계 프로세스 보드·사진 검수·합/부 판정+판정자 기록·양방향 메시지).
- ERP 매핑은 §4.4. 신규 작업은 `08_…/00_종합정리_요구사항이력.html`에 이력 추가.
- **보안 경계**: 협력사는 사외 사용자 → 인증 분리(§5.5), **행 수준 보안으로 자기 발주 건만**. 업로드 파일 검증 필수.

---

## 10. 작업 진행 방식 (Claude Code 행동 규칙)

- 변경 전 관련 파일·문서를 읽고, **추측 대신 사실 확인**(브랜치/도메인/매핑/gitignore).
- 비밀값·Entra 설정·ERP 스키마 접근·실거래처럼 **사람이 결정/실행해야 하는 일은 임의 진행하지 말고 명확히 요청 목록으로 제시**한다.
- 큰 변경은 단계로 쪼개고, 완료 후 `git diff`로 자가 검토. 보안 민감 변경은 `/security-review`.
- 출력물은 한국어, 간결하게. 사내 비개발자도 이해할 수준의 설명을 곁들인다.
- **이관/시스템 상태가 바뀌면 `실제구축준비 자료/이관/`(관제 센터, 단일 출처)를 갱신한다**: 배포·데이터·인증·계정 변경 → `00_현재시스템_상태스냅샷` + 변경이력, 작업 수행 → `03_이관진행상태` 체크리스트·작업로그. `.md` 수정 후 `python _build_html.py`로 HTML 재생성(`index.html`은 자체완결, 직접 관리).
- **ERP DB 연계 관련 작업·상태 변화가 생기면 `10_ERP_DB연계/`(단일 출처)를 갱신한다**: 전담 서브에이전트 **`erp-db-link-manager`**(로컬 `.claude/agents/`)에 위임 — 문서(00 현재상태·01 기획·02 진행상태) 갱신 → `python _build_html.py` → `python _sync_mirror.py`로 사내 OneDrive 정책관리 미러 동기화(경로는 `.claude/erp_db_mirror.path`, 저장소에 기재 금지).
- **기준정보·문서가 바뀌면 `00_관리체계/`(관리 허브, 단일 출처)를 갱신한다**: 조직·권한·ERP매핑·프로세스 변경 → `01_기준정보_레지스트리`, 문서 추가·이동·상태변경 → `00_문서대장`, 모든 의미 있는 변경 → `03_변경관리_CHANGELOG` 한 줄. 명명·배치는 `02_명명규칙_폴더규약`을 따른다. (운영현황은 위 이관 관제가 출처 — 중복 기록 금지.) `.md` 수정 후 `python _build_html.py`로 HTML 재생성.
- **주요 작업을 마무리하면 총괄 서브에이전트 `jeilax-master`(통칭 "총괄관리자", 로컬 `.claude/agents/`)를 호출한다**: `00_관리체계/index.html`의 **총괄 대시보드**(`AX_DASH` 데이터 블록 — 영역 현황·진행 타임라인·의사결정 로그·에이전트 현황)를 갱신하고 원천 4곳(이관 00·03 / 10_ERP 00·02 / 03_CHANGELOG / 11_제품기획 09_ADR)과 대사한다. 진행상황 점검·의사결정 등재/전이 때도 호출. ERP 연계 세부는 erp-db-link-manager에, 그리드 표준화는 grid-standardizer에 위임 가능하되 **erp-db-connector는 위임 금지**(실행성 — 관리자 직접 기동). KPI는 자동 계산이므로 수치 수기 기입 금지, 대시보드는 파생 뷰(원천이 정답).

---

## 11. 향후 설정해야 할 항목 (아직 미설정 — 실구축 준비 체크리스트)

> "지금 인지하지 못한 설정"을 모아둔 목록. 착수 순서대로. 항목 완료 시 이 절을 갱신.

**A. 저장소·코드 거버넌스 (단기, 코드성)**
- [ ] `.gitignore` 보강: `.env`, `*.key`, `*.pem`, `appsettings*.json`, `secrets/`, `node_modules/`, 빌드 산출물 추가.
- [ ] `.env.example`(키 이름만, 값 없음) 추가로 필요한 환경변수 목록 문서화: ERP_DB_CONN, ENTRA_TENANT_ID, ENTRA_CLIENT_ID, ENTRA_CLIENT_SECRET 등.
- [ ] `.claude/settings.json`에 팀 공용 권한·금지 명령 정의(현재 로컬 `Bash(cd *)`만). 위험 명령(push/force, rm, DB write) 차단 룰.
- [~] 디렉터리 분리 계획: `demo/`(현 정적 데모) ↔ `app/`(실구축 프론트) ↔ `api/`(백엔드) ↔ `docs/`(기획). 데모와 운영 코드 혼재 방지. (2026-06-30: `00_관리체계/`(거버넌스 허브)·`09_비용_호스팅/` 신설, `app/`·`supabase/` 가동으로 부분 진행.)
- [x] **문서 거버넌스·기준정보 체계** 수립 — `00_관리체계/`(문서대장·기준정보 레지스트리·명명규칙·변경관리) 2026-06-30.
- [ ] 시크릿 스캐닝(gitleaks 등) + PR 점검 도입.

**B. 인프라·호스팅 (의사결정 필요)**
- [ ] 호스팅 확정: Azure(권고) vs 사내 IIS vs 기타 — `실제구축준비 자료/01_도메인_DNS_인프라구성.html` 기준 경영진 결정.
- [ ] 스테이징 도메인 `ai-dev.jeilm.co.kr` 구성(운영과 분리).
- [ ] 인증서 전략: GitHub Pages는 자체 인증서 업로드 불가(Let's Encrypt만). 회사 인증서 쓰려면 IIS/Azure 직접 호스팅.
- [ ] 중간 DB 인스턴스 + ERP→중간DB 야간배치(또는 CDC) 파이프라인 구축, 유니포인트 협의.
- [ ] 운영 DB 접근 네트워크: 파일럿 IP허용 → 운영 VPN 전환.

**C. 인증·권한 운영 (관리자 작업)**
- [ ] Entra 보안그룹 신설/배정: `AI-Portal-Admins/Finance/HR`(+협력사/영업 확장).
- [ ] Entra Client Secret **2026-12-09 만료 갱신**(11월 중) — 일정 등록.
- [ ] 운영 redirect URI 정리, 임시 테스트 페이지/저장소 삭제.
- [ ] 협력사(사외) 인증 경계 설계: B2B 게스트 vs 전용 인증, 행 수준 보안.
- [ ] 운영 토큰 보관 재설계(localStorage → BFF/HttpOnly 쿠키).

**D. 챗봇·데이터 (실구축 핵심)**
- [ ] 챗봇 백엔드 오케스트레이션 + ERP/문서 데이터 소스 연결 설계(`04_챗봇전환_데이터관리_운영.html`).
- [ ] 문서 연계: OneDrive/SharePoint + AI 지식 라이브러리 인덱싱(02 문서). Cloudium 직접 연계 제외.
- [ ] 감사 로그·접근 로깅 설계(민감 화면 우선).

**E. 운영·품질**
- [ ] 백업·장애복구(중간DB·포털DB), 모니터링/알림.
- [ ] 테스트 전략(권한 인가 테스트 필수), CI(빌드·시크릿 스캔).
- [ ] 개인정보 처리 점검(급여·인사·자금 데이터 마스킹·보존기간 — 직원 이름은 마스킹 제외, §1.7).

---

## 12. 참고 문서

- 실구축 종합기획 7부 + 16주 로드맵: `실제구축준비 자료/00~05,07_*.html`
- 호스팅/비용 기획: `09_비용_호스팅/`(`JEIL_AX_호스팅_기획서.*`, `JEIL_AX_비용기획_보고서.*`, `JEIL_AX_비용계산기.html`)
- 관리체계 허브(기준정보·문서대장·명명규칙·변경관리): `00_관리체계/`(README + index.html[**총괄 대시보드**+관리 허브 2탭, 총괄관리자 지속 갱신] + 00~03)
- Entra/MFA 기술 원본(비공개): `MS_connect/`(README + 01~05 + TASK/)
- 협력사 포털 요구사항 이력: `08_협력사발주포털/00_종합정리_요구사항이력.html`
- ERP DB 연계 관제(현재상태·기획·진행상태): `10_ERP_DB연계/`(index.html 허브 + 00~02, 미러: 사내 OneDrive `ERP_DB/정책관리/ERP_DB연계/`)

---

## 13. 그리드 표준 (편집 그리드 단일 컴포넌트)

> 웹에서 보이는 **수정 가능한 표(편집 그리드)** 는 직접 `<table>`+`innerHTML`로 만들지 말고 **표준 컴포넌트**를 쓴다. 순수 읽기 전용 표시표·기획 문서의 표는 예외(그대로 둬도 됨).

1. **표준 컴포넌트**: `app/lib/grid.js`(바닐라 ESM, 의존성 0) + `app/lib/grid.css`. `import { createGrid } from "./lib/grid.js"` → `createGrid(container, options)`.
2. **단일 출처(문서·데모)**: 가이드 `그리드/표준그리드_가이드.md`(+`.html` 빌드), 데모/레퍼런스 `그리드/index.html`, 붙여넣기 샘플 `그리드/샘플양식.csv`. 변경 시 가이드·샘플을 함께 갱신하고 `python 그리드/_build_html.py`로 HTML 재생성.
3. **역할 분리**: `grid.js`는 **UI만** 담당한다. 데이터 조회/저장/검증로직(DB·Edge Function 호출)은 콜백(`onSave`·`onBulkAction`·`onRowAction`·`onPaste`)에서 호출측이 처리하고, 데이터 접근은 `app/lib/api.js`·Supabase 클라이언트로 모은다. **grid.js에 DB/비즈니스 로직을 심지 않는다.**
4. **스타일 격리**: CSS는 `.grid__` 스코프만 사용한다. **전역 `table/th/td` 셀렉터를 재정의하지 않는다**(같은 화면의 다른 표 오염 금지). 호스트 `:root` 팔레트(navy/accent/green/red/line…)를 재사용한다.
5. **규약**: CSV 내보내기는 **UTF-8 BOM** 접두(엑셀 한글 깨짐 방지), 클립보드 복사는 TSV. 붙여넣기 컬럼 분류는 `pasteKey`(email/name/code/none). **외부 라이브러리/CDN 금지**(자기완결 바닐라).
6. **적용 사례**: `app/admin-vendors.html`(협력사 계정발급 — `#vmGrid`). 새 편집 화면은 이 표준으로 통일한다(서브에이전트 `grid-standardizer`가 마이그레이션·관리, 로컬 전용).
