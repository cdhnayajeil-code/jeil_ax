# 11 · 챗봇 데이터 요청접수 설계 — "조회 불가 → 요청 버튼 → 접수·처리"

> 관리자: 최동혁(dh.choi@jeilm.co.kr) · 기준일: 2026-07-23 · 상태: **Accepted — P1(권한)·P2a(결측 표기·2분류)·완료 게이트 구현·배포 완료 / P2b~P2d 잔여 (ADR-011)**
> 관련: [10 응답렌더링](10_챗봇_응답렌더링_설계.md) · [05 FE·BE](05_프론트엔드_백엔드_설계.md) · [06 ERP연계](06_ERP연계_챗봇활용_설계.md) · [04 DB설계](04_데이터베이스_설계.md) · [08 보안](08_보안_데이터안정성.md) · [09 ADR](09_ADR_의사결정기록.md) · `CLAUDE.md §1.6·§5.4·§7·§13`

챗봇이 **"그 데이터는 못 보여드립니다"** 로 끝나는 지점에서, 사용자가 그 자리에서 **요청 버튼**을 눌러 접수하고, 관리자가 **요청함**에서 처리·회신하는 흐름을 설계한다.
결론부터: **새 뷰·새 화면을 만들지 않고**, 이미 있는 `notice` 뷰의 `request` 정보를 실제 접수 액션으로 승격(`actions.kind:"request"` 1종 추가)하고 **요청 원장 테이블 1개 + Edge Function 1개 + 관리자 콘솔 탭 1개**만 신설한다. 근거는 §1~§3, 설계는 §4~§8, 단계·미결정은 §9~§11.

---

## 1. 현재 상태 (실측, 2026-07-22)

### 1-1. "조회 불가"는 한 종류가 아니다 — 5유형, 처리 주체가 다 다르다

`supabase/functions/jeil-chat/index.ts` 실측 기준.

| # | 유형 | 발생 지점 | 현재 응답 | 카드(`__view`) | 실제 해결 주체 |
|---|---|---|---|---|---|
| A | **부서 ERP 모듈 권한 없음** | `index.ts:448~459` (게이트 1곳, 전 ERP 도구 공통) | "관리자에게 …권한을 요청해 주세요" 안내문 | ✅ `notice/deny` + `request` 칩 + `ask` 액션 | 포털 관리자(권한 부여) |
| B | **급여·인사 민감 데이터 제한** | `index.ts:987~995` | "인사팀(또는 관리자)만 열람" | ✅ `notice/deny` | 포털 관리자 + 인사팀 합의 |
| C | **중간DB 적재범위 밖(데이터 없음)** | `index.ts:764`, `1005` 등 | "2025년 이전 건은 미적재이니 원본 ERP를 확인하세요" | ❌ **없음(평문)** | ERP 연계 담당(ETL 범위 확대·유니포인트 협의) |
| D | **문서 연동 승인범위 밖** | `index.ts:1051`, `1093`, `1099` | "AI 연동 승인 범위에 없어 열람 불가" | 부분(`1051`만 `notice/info`) | AI 문서연동 담당(`ai_document_scope` 등재) |
| E | **도구 자체가 없음(미구현 영역)** | 시스템 프롬프트 `index.ts:57` | "추측하지 말고 원본 확인을 권하세요" | ❌ 없음 | 제품기획(백로그 → 신규 Tool) |

즉 사용자 눈에는 똑같이 "안 나온다"지만, **A·B는 권한 문제, C는 데이터 파이프라인 문제, D는 스코프 등록 문제, E는 기능 부재**다. 요청 기능의 핵심 가치는 **버튼을 만드는 것이 아니라 이 5유형을 자동 분류해 올바른 담당에게 꽂는 것**이다.

### 1-2. 프론트에는 이미 "요청 대상" 표시가 있다 — 버튼만 없다

`04_챗봇_포털_데모UI.html:1788~1799`

```js
} else if(v.view === 'notice'){
  …
  if(v.request){
    var rq = _cvEl('div', 'cv-req');
    rq.appendChild(_cvEl('span','cv-chip','요청 대상: ' + v.request.moduleKo + '(' + v.request.module + ')'));
    if(v.request.dept) rq.appendChild(_cvEl('span','cv-chip','소속: ' + v.request.dept));
    card.appendChild(rq);
  }
}
```

서버는 이미 `request:{module, moduleKo, dept}` 를 내려주고 있고 프론트는 그걸 **칩으로 보여주기만** 한다. **접수 경로가 없어서 정보가 화면에서 죽는다.**

### 1-3. 지금 사용자가 하는 행동 = 채널 이탈

현재 제공되는 최선의 액션은 `kind:"ask"` — **"권한 요청 초안 작성"**(`index.ts:457`, `994`)이다. 즉 챗봇이 메신저용 문구를 써주면 사용자가 **복사해서 Teams/구두로 직접** 보낸다. 결과:

- 요청이 **시스템 밖**에 존재 → 유실·중복·누락, 처리 이력 없음.
- 관리자는 **수요를 알 수 없음** → 어느 부서가 어떤 모듈을 반복 요구하는지 데이터가 없다.
- 요청자는 진행상태를 알 수 없어 **재요청**(관리자 피로).

### 1-4. 수요 계측이 아예 없다

`chat_log` 컬럼은 `upn·model·messages_count·prompt_chars·prompt_tokens·completion_tokens·est_cost_usd·tools_used·session_id·stopped` 뿐 — **거부(deny)·무데이터(no_data) 플래그가 없다.** 그래서 "조회 불가가 하루 몇 건 나는가"를 **지금은 답할 수 없다.** 요청 원장이 생기면 이 수치가 자동으로 쌓인다(§8-2).

### 1-5. 받아줄 뒷단은 이미 있다

| 자산 | 상태 | 요청 처리에서의 역할 |
|---|---|---|
| `perm_effective(upn)` RPC | ✅ 판정 SSOT(ADR-010) | 접수 시 **서버 재판정**으로 요청 정당성 검증 |
| `perm_grant` / `dept_erp_scope` | ✅ 운영 중 | 승인 시 권한 반영 대상 |
| `perm_audit` | ✅ 운영 중 | 권한 변경 감사 |
| 관리자 콘솔(`04` 탭 구조) | ✅ 운영 중 | 「요청함」 탭 추가 지점 |
| 표준 그리드 `app/lib/grid.js` | ✅ 표준(CLAUDE.md §13) | 요청함 목록 UI |
| `chat_*` 테이블 패턴(RLS 전면차단 + Edge Fn 전용) | ✅ 확립 | 요청 원장의 보안 모델 그대로 계승 |

**신설이 필요한 것은 테이블 1개·Edge Function 1개·관리자 탭 1개뿐이다.**

---

## 2. 문제 정의 · 목표

**문제**: 챗봇이 조회 불가를 알리는 순간 사용자 여정이 끊긴다. 해결 요청은 시스템 밖(구두·메신저)으로 나가 유실되고, 관리자는 수요를 계측하지 못하며, 권한·적재범위 정책이 데이터 없이 결정된다.

**목표(성공 정의)**

| 지표 | 현재 | 목표(도입 3개월) |
|---|---|---|
| 조회 불가 → 시스템 내 접수 전환율 | 0%(측정 불가) | 60% 이상 |
| 요청 평균 처리 리드타임 | 측정 불가 | 권한 유형 **1영업일** 내 1차 응답(내부 관리지표 — 화면에는 기한을 표시하지 않는다, §11-5) |
| 동일 요청 중복 접수 | 통제 없음 | 자동 병합으로 중복 건 10% 미만 |
| 권한 정책 개선 근거 | 없음 | 부서×모듈 요청 히트맵 월 1회 리뷰 |

**비목표(이번 범위 아님)**

- 전자결재 연동(그룹웨어 상신) — 요청은 **포털 내부 티켓**이며 결재문서가 아니다.
- 자동 권한 부여 — 접수·라우팅까지만. **부여 클릭은 사람**(CLAUDE.md §1.6, §5.4).
- 요청자 임의 데이터 업로드 — 데이터 요청은 "적재 요청"이지 사용자 데이터 투입이 아니다.

---

## 3. 설계 원칙 (이 문서의 판단 기준)

1. **뷰 5종 고정 유지(ADR-008).** `series/ranking/record/list/notice` 외 신규 뷰 신설 금지 → 요청 버튼은 **`actions`의 새 kind 1종**으로 흡수한다.
2. **요청 컨텍스트는 서버가 만든다.** 프론트가 "모듈=payroll" 같은 값을 조립해 보내지 않는다. 프론트는 서버가 준 `request` 객체를 **그대로 반송**하고, 서버는 접수 시 **`perm_effective`로 재판정**해 정당성을 확인한다(위조 요청 차단, 서명키 불필요).
3. **접수는 실거래 액션이 아니다.** 티켓 생성까지만 → CLAUDE.md §1.6 저촉 없음. 권한 부여·ETL 확대 실행은 관리자 화면의 별도 클릭.
4. **개인정보 최소화.** 사유 입력란은 자유 텍스트지만 **급여액·주민번호·평가 등 민감값 기재 금지** 안내 + 저장 전 패턴 마스킹. 성명은 마스킹 제외(§1.7).
5. **원장은 하나.** 5유형 모두 같은 테이블에 담고 `kind`로 구분한다. 유형별 테이블 분리 금지(관리 화면 중복 방지).
6. **데이터 접근 단일 진입점(ADR-004).** 프론트는 `app/lib/api.js` 경유, DB 접근은 Edge Function(service_role)만.

---

## 4. 기능 설계

### 4-1. 전체 흐름

```
[사용자] 질문
   └─ jeil-chat 도구 실행 → 조회 불가 판정(A~E)
        └─ notice 카드 + actions:[{kind:"request", …}]        ← 서버가 컨텍스트 동봉
[사용자] "요청하기" 클릭 → 사유·긴급도 입력 모달 → 전송
        └─ jeil-portal-request (Edge Fn)
             ├─ 본인 확인(JWT) + perm_effective 재판정(정당성)
             ├─ 중복 open 건 탐지 → 있으면 '동조(+1)' 처리
             └─ portal_request INSERT (open)
[관리자] 콘솔 › 요청함 탭 (배지 n건)
        ├─ 상세 확인 → 상태 전이(ack/doing/done/rejected/duplicate)
        ├─ [권한 부여 화면으로] 프리필 이동  ← 실제 부여는 관리자 클릭
        └─ 처리메모 작성 → 요청자에게 회신
[요청자] 챗봇에 "내 요청 진행상황" → 조회(get_my_requests) 또는 포털 내 배지
```

### 4-2. 유형별 라우팅·처리 규칙

| kind | 트리거(§1-1) | 담당 | 처리 액션 | 비고 |
|---|---|---|---|---|
| `perm` | A | 포털 관리자 | `dept_erp_scope`(부서축) 또는 `perm_grant`(개인 예외) 편집 | 만료일 지정 권장(대행·프로젝트성) |
| `perm_sensitive` | B(급여·인사) | 포털 관리자 **+ 인사팀 확인** | 동일하되 **2인 확인** 필수 | 자동 승인 UI 제공 금지 |
| `data` | C | ERP 연계 담당(`10_ERP_DB연계`) | ETL 대상·기간 확대 검토 → 필요 시 **유니포인트 협의**(CLAUDE.md §4.6) | 협의 필요 플래그 별도 |
| `doc` | D | AI 문서연동 담당 | `ai_document_scope` 등재 검토 | 민감 컨테이너(급여·자금) 등재 금지 |
| `feature` | E | 제품기획 | 백로그 등록 → 신규 Tool/화면 | `02_SRS` FR-ID로 승격 |
| `quality` | (사용자 신고) | 데이터 소유 부서 | 값 오류·정합성 확인 | "숫자가 이상해요" 경로, §9 P3 |

### 4-3. 상태머신

```
        ┌──────────── cancel(요청자, open/ack 한정) ────────────┐
        ▼                                                       │
   [open] ──ack──► [ack] ──start──► [doing] ──complete──► [done]
        │              │                │
        └──reject──────┴────────────────┴──► [rejected]   (사유 필수)
        └──dup─────────────────────────────► [duplicate]  (원본 id 링크)
```

- `open`: 접수 직후. 요청자에게 접수번호 즉시 표시.
- `ack`: 관리자 확인(1차 응답 SLA 지점).
- `done`/`rejected`: 처리메모 필수 → 요청자 회신.
- `duplicate`: 자동 병합 시에도 사용(§4-5).

### 4-4. UI — 챗봇 카드 액션 확장(신규 뷰 없음)

`10_챗봇_응답렌더링_설계.md` P3 액션 규약에 **`kind:"request"` 1종 추가**:

```ts
// 서버(jeil-chat) — 기존 A유형 게이트(index.ts:452~459) 확장안
return {
  접근제한: true, 요청안내: true, 모듈: erpMod, 부서: scope.dept, 안내,
  __view: {
    view: "notice", title: "데이터 접근 권한 안내", kind: "deny", text: 안내,
    request: { module: erpMod, moduleKo: modKo, dept },
    actions: [
      { kind: "request", label: "권한 요청하기",          // ★신규
        req: { kind: "perm", module: erpMod, dept,
               tool: name, args_digest: "…",             // 어떤 질문에서 막혔는지(값 아님, 인자 요약)
               reason_hint: `${modKo} 데이터 열람 필요` } },
      { kind: "ask", label: "요청 문구 초안 작성", prompt: "…" }   // 기존 유지
    ]
  }
}
```

프론트(`04` 렌더러)는 `kind:"request"` 를 만나면 버튼을 그리고, 클릭 시 **사유(필수, 200자)·긴급도(보통/급함)** 모달을 띄운 뒤 `req` 객체를 **그대로** `jeil-portal-request` 로 POST한다. 접수 성공 시 버튼은 `✅ 접수됨 #REQ-0042` 로 치환(중복 클릭 차단).

**C·E 유형은 `__view` 자체가 없다**(§1-1) → 이번 작업에서 `notice/info` + `request` 액션을 신설해야 한다. 이게 프론트보다 **서버 작업량이 큰 이유**다.

### 4-5. 중복 병합

동일 `(upn, kind, module, dept)` 로 **open/ack 상태 건이 이미 있으면** 새 행을 만들지 않고 기존 건에 `+1`(동조 카운트)·최근 사유 append. 다른 사람의 동일 요청이면 `supporters` 배열에 추가 → 관리자는 **"7명이 요청 중"** 을 보고 부서축 일괄 부여를 판단할 수 있다. 이게 개인 예외(`perm_grant`) 남발을 막는 실질 장치다.

### 4-6. 남용 방지

- 사용자당 **1일 5건** 상한(초과 시 안내 반환).
- 사유 **15자 미만**이면 접수 거부(품질 하한). 프론트·서버 양쪽에서 검증.
- 이미 권한이 있는데 요청하면(재판정 결과 allow) → 접수 대신 **"이미 권한이 있습니다. 새로고침 후 재시도"** 안내.

---

## 5. 데이터 모델 (신설 1테이블)

```sql
-- public.portal_request — 포털 요청 원장(챗봇 조회불가 → 사용자 요청 접수)
-- RLS 전면차단 + Edge Function(service_role) 전용 — chat_* 패턴 계승
create table public.portal_request (
  id            bigserial primary key,
  req_no        text unique not null,               -- 표시용 'REQ-2026-0042'
  kind          text not null check (kind in ('perm','perm_sensitive','data','doc','feature','quality')),
  status        text not null default 'open'
                check (status in ('open','ack','doing','done','rejected','duplicate','cancelled')),
  requester_upn text not null,
  requester_dept text,
  target_module text,                               -- perm: erp_module / data: 테이블·기간 / doc: drive 경로
  target_detail jsonb not null default '{}',        -- tool·args_digest·요청 상세(개인정보 미포함)
  reason        text not null,                      -- 사용자 사유(마스킹 후 저장)
  urgency       text not null default 'normal' check (urgency in ('normal','high')),
  supporters    text[] not null default '{}',       -- 동조자 upn (중복 병합)
  dup_of        bigint references public.portal_request(id),
  assignee_upn  text,                               -- 처리 담당(라우팅 결과)
  handled_note  text,                               -- 처리·반려 사유(요청자 회신 문구)
  needs_vendor_review boolean not null default false, -- data 유형: 유니포인트 협의 필요
  created_at    timestamptz not null default now(),
  acked_at      timestamptz,
  closed_at     timestamptz
);
create index on public.portal_request (status, kind, created_at desc);
create index on public.portal_request (requester_upn, created_at desc);
alter table public.portal_request enable row level security;   -- 정책 미부여 = 전면 차단
comment on table public.portal_request is
  '포털 요청 원장 — 챗봇 조회불가(권한·데이터·문서·기능)에서 접수. RLS 전면차단, Edge Fn(service_role) 전용.';
```

**설계 근거**

- `target_detail`은 **jsonb** — 유형마다 필요한 필드가 달라 컬럼화하면 5유형×N 컬럼으로 번진다. 조회 화면은 유형별 렌더만 분기.
- `args_digest`는 **원본 인자값이 아니라 요약**(예: `ym=2024-05`) — 질문 원문·민감값 저장 금지.
- 상태 전이 이력은 별도 테이블을 만들지 않고 **`perm_audit` 패턴 재사용 여부를 P2에서 판단**(§11 미결정 3).

---

## 6. 백엔드 (Edge Function 1종 신설)

`supabase/functions/jeil-portal-request/index.ts`

| 액션 | 메서드 | 권한 | 동작 |
|---|---|---|---|
| `create` | POST | 로그인 사용자 | 본인 JWT 확인 → **`perm_effective` 재판정** → 중복 병합 검사 → INSERT/동조 |
| `mine` | GET | 로그인 사용자 | 본인이 낸 요청 목록·상태 |
| `cancel` | POST | 요청자 본인 | `open/ack` 한정 취소 |
| `list` | GET | **관리자** | 요청함(필터: 상태·유형·부서) |
| `transition` | POST | **관리자** | 상태 전이 + `handled_note` 기록 |

**정당성 재판정이 핵심이다.** 프론트가 `{kind:'perm', module:'payroll'}` 를 임의로 보내도, 서버는 `perm_effective(upn)` 로 **정말 그 모듈이 없는지** 확인하고 없을 때만 접수한다. 이미 있으면 접수하지 않는다 → 위조 요청·스팸이 원천 차단되고 별도 서명키가 필요 없다.

챗봇 조회용 도구 **`get_my_requests`** 를 `jeil-chat` 에 1개 추가하면 "내 요청 어떻게 됐어?"를 대화로 답할 수 있다(뷰: `list` 재사용).

---

## 7. 관리자 콘솔 — 「요청함」 탭

`04_챗봇_포털_데모UI.html` 관리자 콘솔에 탭 1개 추가(기존 탭 구조 `:610~` 계승).

- **목록**: 표준 그리드(`app/lib/grid.js`, CLAUDE.md §13). 컬럼 = 접수번호·유형·요청자(부서_이름_아이디 규약)·대상·동조수·긴급도·경과일·상태.
- **상단 배지**: `open` 건수 · 1영업일 초과 건수(빨강).
- **상세 패널**: 사유·요청 컨텍스트(막힌 도구·조건)·동조자 목록·처리메모 입력.
- **원클릭 전환**: `perm` 유형 → **[권한 부여 화면 열기]**(부서·모듈 프리필). 부여 자체는 기존 권한 화면에서 관리자가 실행 → `perm_audit` 자동 기록. **요청함에서 직접 부여하는 버튼은 만들지 않는다**(§3-3, 오클릭 방지).
- **수요 통계**: 부서×모듈 요청 히트맵(월간) — 개인 예외 남발 대신 부서축 정비 판단 근거.

---

## 8. 보안·감사·계측

### 8-1. 위협과 대응

| 위협 | 대응 |
|---|---|
| 프론트에서 요청 페이로드 위조(타 부서·타인 명의) | 요청자는 **JWT에서만** 취득(본문 무시), 대상은 **서버 재판정**(§6) |
| 사유란에 개인정보·급여액 기입 | 입력 안내 + 저장 전 패턴 마스킹(주민번호·계좌·금액 형식). **성명은 마스킹 제외**(CLAUDE.md §1.7) |
| 스팸·항의성 대량 접수 | 1일 5건 상한 + 사유 품질 하한 + 중복 자동 병합 |
| 요청 원장 직접 조회(REST) | RLS 전면 차단, Edge Fn service_role 전용 |
| 반려 사유에 민감 정보 노출 | `handled_note`는 요청자에게 그대로 노출됨 → 관리자 입력 가이드 명시 |
| 요청 = 권한 자동 부여로 오인 | UI 문구를 "요청 접수"로 고정, 부여는 별도 화면 |

### 8-2. 계측(부수 효과가 아니라 목적)

요청 원장이 쌓이면 **지금 답할 수 없는 질문**(§1-4)에 답할 수 있다.

- 부서별 어떤 ERP 모듈이 반복 요청되는가 → **부서축 권한 기본값 재설계** 근거.
- `data` 유형 요청의 기간 분포 → **ETL 적재범위 확대 우선순위**(`10_ERP_DB연계/04` 증분동기화 기획과 직결).
- `feature` 요청 빈도 → **신규 챗봇 Tool 백로그 우선순위**(`02_SRS`).

추가로 `chat_log`에 `denied` boolean 1컬럼을 두면 **"막혔지만 요청하지 않은"** 이탈률까지 잡힌다(전환율 지표의 분모). 권장.

---

## 9. 단계별 구현 계획

| 단계 | 범위 | 산출물 | 상태 |
|---|---|---|---|
| **P1** | 유형 A·B(권한). 원장 + `create`/`mine`/`cancel`/`list`/`transition`/`stats` + 카드 요청 버튼·접수 모달 + 요청함 탭 + 수요 히트맵 | 마이그레이션 `portal_request_v1`, `jeil-portal-request` v1, `04` 렌더러·모달·콘솔 탭 | ✅ **완료(2026-07-22)** |
| **P2** | 유형 C·D·E 확장 — **미적재·문서범위·미구현 응답에 `notice` 카드 신설**(현재 평문이라 요청 버튼이 붙을 자리가 없다), 라우팅·담당 배정, `get_my_requests` 도구 | `jeil-chat` 반환부 5~8곳 수정 + 재배포 | ⬜ 잔여 |
| **P3** | Teams 알림 실가동(웹훅 시크릿 등록), SLA 경과 배지, `quality`(값 오류 신고) 유형, `chat_log.denied` 계측 | 코드는 P1에 포함(웹훅 미설정이면 무동작) | 🔶 시크릿 등록 대기 |

P1만으로도 §1-3의 채널 이탈 문제는 해소된다. **P2가 실질 가치의 절반**이다 — 조회 불가의 상당수가 권한이 아니라 **적재범위(C)** 이기 때문이다.

### 9-1. P1 구현 실적 (2026-07-22)

| 계층 | 산출물 | 비고 |
|---|---|---|
| DB | `public.portal_request` + 접수번호 시퀀스 + `updated_at` 트리거 | RLS 전면차단 검증 완료(정책 0 · anon/authenticated SELECT 권한 없음) |
| API | `jeil-portal-request` v1 (`verify_jwt=false`, 내부 Entra 검증) | 무인증 호출 401 확인 |
| 프론트(챗봇) | `notice` 카드에 요청 버튼 + 사유·긴급도 모달, 접수 후 버튼이 `✅ 접수됨 REQ-…`로 치환 | **`jeil-chat` 수정 불필요** — 서버가 이미 `v.request`를 내려주고 있어(§1-2) 프론트만으로 P1 성립 |
| 프론트(관리자) | 콘솔 「요청함」 탭 — 요약 카드·목록·상태 전이·수요 히트맵, 탭 배지(미처리 건수) | 권한 부여 버튼은 **의도적으로 두지 않음**(§3-3) |
| 정본 SQL | `실제구축준비 자료/이관/sql/18_portal_request.sql` | ADR-105(마이그레이션 파일화) 방향 준수 |

---

## 10. 대안 검토 (왜 이 안인가)

| 대안 | 장점 | 단점 | 판정 |
|---|---|---|---|
| **현행 유지**(ask 액션으로 문구 초안만) | 개발 0 | 채널 이탈·유실·계측 불가 지속 | ✗ |
| **그룹웨어 전자결재 상신 연동** | 결재 이력 공식화 | 권한 요청에 결재는 과중, 그룹웨어 연동 개발·계정 이슈, 조회불가 즉시성 상실 | ✗(단, `perm_sensitive`는 향후 검토) |
| **Teams 메시지 자동 발송** | 관리자 즉시 인지 | 원장이 남지 않아 계측·중복병합 불가. **알림은 원장의 보조**여야 함 | △(P3 보조 채널) |
| **요청 전용 신규 뷰 타입 신설** | 표현 자유도 | ADR-008 뷰 5종 고정 위반, 도구별 템플릿 증식 | ✗ |
| **본 안**(원장 + notice 액션 확장 + 요청함) | 최소 신설, 계측·병합·라우팅 확보, 기존 자산 재사용 | 유형 C~E 서버 수정 필요 | ✅ |

---

## 11. 미결정 사항 (관리자 결정 필요)

1. ~~**알림 채널**~~ → **결정: Teams**(2026-07-22, 관리자). 단 **Graph 앱 권한(ChannelMessage.Send) 방식은 쓰지 않는다** — Entra 앱 등록 변경이 선행돼야 하고 권한 범위가 넓다. 대신 **Teams 채널 워크플로 웹훅**(Power Automate) URL을 Supabase 시크릿 `TEAMS_WEBHOOK_URL`로 등록하는 방식을 채택했다. 코드는 배포 완료 상태이며, 시크릿이 없으면 알림만 조용히 건너뛴다(접수는 정상). 설정 절차는 §13.
2. **승인 라인** — 권한 요청에 **부서장 확인**을 넣을지(2단 승인) 아니면 포털 관리자 단독 판단으로 갈지. 현재 `perm_effective`에 `dept_admin_of` 축이 이미 있어 2단 확장은 가능.
3. **상태 이력 테이블 분리 여부** — `portal_request` 컬럼 갱신만으로 갈지, 전이 이력을 별도 로그로 남길지(감사 요구 수준에 의존).
4. **`data` 유형의 유니포인트 협의 게이트** — ETL 확대 요청이 벤더 협의를 요하면(§4-2) 요청 상태를 `doing`에 묶어둘지 별도 `blocked` 상태를 둘지.
5. ~~**SLA 수치 확정**~~ → **결정: 처리 기한을 화면에서 약속하지 않는다**(2026-07-23, 관리자). 긴급도는 보통/급함 **우선순위 구분 용도로만** 쓰고 옵션에서 "1영업일 내 확인"·"당일 처리" 문구를 제거했다. `data` 유형은 "검토 착수 + 정기 업데이트 시 적용 검토"로 회신한다(§14-8 10번과 통합). 내부 관리지표로서의 리드타임 집계는 유지(§2 목표표).
6. **요청 보존기간** — `done/rejected` 건의 보존(제안: 2년) 및 개인정보 관점 파기 정책.

---

## 12. 다음 액션

- [x] **ADR-011 확정** — 포털 내부 원장 방식 채택(2026-07-22)
- [x] **P1 구현·배포** — 원장·API·요청 버튼·요청함
- [x] `05 FE·BE` Edge Function 명세 · `04 DB설계` 테이블·RLS 반영
- [ ] **Teams 웹훅 URL 발급 → Supabase 시크릿 `TEAMS_WEBHOOK_URL` 등록**(관리자 작업, §13)
- [x] **P2 기획검토** — 미연계 데이터(C·D·E) 확장 설계·결함 2건·단계 재분할(§14, 2026-07-23)
- [ ] **P2 착수 결정** — §14-6 P2a~P2d 묶음 구현 + `jeil-chat` 재배포 / §14-8 미결정 4건 판단
- [ ] §11 잔여 미결정 5건(승인 라인·상태이력·벤더 게이트·SLA·보존기간) 관리자 판단
- [ ] `02_SRS`에 FR-ID 등재

---

## 13. Teams 알림 설정 절차 (관리자 작업)

Entra 앱 등록은 **건드리지 않는다**. 채널 워크플로 웹훅 URL 하나를 서버 시크릿에 넣는 것으로 끝난다.

**① Teams에서 웹훅 URL 발급**

1. Teams → 알림을 받을 **채널** 선택 → 채널 헤더 우측 **⋯ → 워크플로**
2. 템플릿 검색창에 `웹후크`(또는 `webhook`) → **「웹후크 요청이 수신되면 채널에 게시」** 선택
3. 연결 계정 확인 → **다음** → 팀·채널 확인 → **워크플로 추가**
4. 생성 완료 화면에 나오는 **HTTP POST URL을 복사**(`https://…logic.azure.com/…` 형태)

> 이 URL은 **비밀값**이다 — 아는 사람은 누구나 해당 채널에 글을 쓸 수 있다. 문서·소스·메신저에 남기지 말고 시크릿에만 보관한다(CLAUDE.md §1.1).

**② Supabase 시크릿 등록**

Supabase 대시보드 → 프로젝트 → **Edge Functions → Secrets** → 추가:

| 키 | 값 |
|---|---|
| `TEAMS_WEBHOOK_URL` | ①에서 복사한 URL |

저장 즉시 반영된다(함수 재배포 불필요). 미등록 상태에서는 알림만 건너뛰고 접수는 정상 동작한다.

**③ 확인**

챗봇에서 권한 없는 데이터를 조회 → 카드의 **「열람 권한 요청하기」** → 사유 입력 후 접수 → 지정 채널에 `🔔 새 포털 요청 REQ-…` 카드가 도착하면 정상.

> 구 O365 커넥터(`…webhook.office.com`) URL을 쓰는 경우에도 동작한다 — 서버가 URL 형식을 보고 MessageCard/Adaptive Card 페이로드를 자동 분기한다. 다만 커넥터는 Microsoft가 단계적으로 폐지 중이라 **워크플로 방식을 권장**한다.

---

## 14. 확장 기획검토 — "연결되지 않은 데이터"(유형 C·D·E) 요청

> 검토일 2026-07-23 · 대상: P1에서 제외한 **미연계 데이터** 3유형. 결론은 §14-6(단계 재분할)·§14-8(추가 미결정).

### 14-1. 실측 — 지금 무엇이 어떻게 끝나는가

`jeil-chat/index.ts` 전수 확인 결과, 미연계 응답은 **7곳**에서 나오고 그중 요청 버튼이 붙을 수 있는 곳은 **0곳**이다.

| 구분 | 지점 | 현재 응답 | 카드 | `request` |
|---|---|---|---|---|
| **C-1** 무데이터 | `:764` | "이 품목은 중간DB(2026년)에 등록된 구매요청·발주·매입이 없습니다. 2025년 이전 건은 미적재…" | ❌ 평문 | ❌ |
| **C-2** 기간 밖 | `:1005` | "해당 기간 인사 집계 데이터가 없습니다. 중간DB 적재 범위를 확인하세요(2026년 이후)" | ❌ 평문 | ❌ |
| **C-3** **부분적재(결측 컬럼)** | `:626·632·634`, 프롬프트 `:52`·도구설명 `:114` | 재고 **입고량·재고량 미적재**, 매출 **수금액·수주액 미매핑(0)** | ✅ `record`(주석만) | ❌ |
| **D-1** 문서범위 미설정 | `:1051` | "AI 문서 연동 범위가 설정되지 않아 검색을 제공하지 않습니다" | ✅ `notice/info` | ❌ |
| **D-2·3** 범위 밖 문서 | `:1093`, `:1099` | "AI 연동 승인 범위에 없어 열람할 수 없습니다" | ❌ 평문 | ❌ |
| **E** 도구 없음 | 시스템 프롬프트 `:57` | "추측하지 말고 원본 확인 권유" | ❌ | ❌ |

즉 **P1의 요청 버튼은 이 7곳 어디에도 나타나지 않는다.** 프론트 렌더러는 `v.request.module`이 있을 때만 버튼을 그리는데(`04:1884`), 이 경로들은 `request`를 싣지 않는다.

### 14-2. C는 A·B(권한)와 근본적으로 다르다 — 같은 방식으로 처리하면 안 되는 3가지

**① 서버가 원인을 판정할 수 없다.**
권한은 `perm_effective`로 즉시 참·거짓이 나온다. 반면 "데이터가 없다"는 최소 4가지 서로 다른 원인의 결과다 — ⓐ ETL 미적재 ⓑ 적재 기간 밖 ⓒ **ERP 원본에도 없음** ⓓ 사용자가 조건(품목코드·기간)을 잘못 입력. 지금 코드는 넷을 구분하지 않고 한 문장으로 답한다. 여기에 그대로 요청 버튼을 달면 **ⓒ·ⓓ가 ERP 연계 담당에게 "적재해달라"는 요청으로 쌓인다.** 권한 유형의 `already_granted` 재판정(§4)에 해당하는 방어 장치가 C에는 없다.

**② 해결 리드타임이 자릿수 단위로 다르다.**
권한 승인은 관리자 클릭 1회(분 단위). 적재 확대는 추출 SQL 작성 → dry-run 검증 → 실적재 → 대사이며, **유니포인트 협의가 끼면 주~월 단위**다(`10_ERP_DB연계` 기준 P1 협의는 현재도 대기 중). §11-5의 단일 SLA(1영업일/5영업일)를 C에 그대로 적용하면 **지킬 수 없는 약속**이 된다.

**③ 가장 위험한 건 "안 나오는 것"이 아니라 "0으로 나오는 것"이다.**
C-3(부분적재)은 조회가 **성공**하고 값이 **0**으로 나온다. 사용자는 이를 실적으로 오해한다. 현재 방어선은 시스템 프롬프트 한 줄과 카드 각주뿐 — **모델이 지켜주기를 기대하는 구조**다. 이건 요청 기능 이전에 **응답 정확성 문제**이고, 미연계 요청 기능에서 가장 먼저 손대야 할 지점이다.

### 14-3. 이미 갖춘 자산 — `erp_ro.table_dict` 2,658행

| 자산 | 실측 | 역할 |
|---|---|---|
| `erp_ro.table_dict` | **2,658행**(ERP 전체 테이블 사전) | "이 정보가 **ERP에 존재하는가**"를 기계 판정 → §14-2① 의 ⓒ 분리 |
| 적재 실체 7종 | `item_master_s` 58,533 · `bp_master_s` 4,649 · `pur_req_s` 4,064 · `pur_order_s` 3,088 · `inventory_d` 1,847 · `purchase_m` 151 · `sales_orders_m` 37 | "적재됐는가·어디까지인가" 판정 |
| `portal_request.needs_vendor_review` | ✅ 컬럼 존재(P1에서 신설, 미사용) | 벤더 협의 필요 건 표시 |

**`table_dict`가 이 기획의 차별점이다** — 요청을 사용자 문장이 아니라 사전 대조로 자동 분류할 수 있다. 단 테이블명 2,658개는 ERP 내부 구조 정보이므로 **사용자 카드에 노출하지 않고 관리자 요청함에서만** 근거로 표시한다.

### 14-4. 설계안

**(a) 적재범위 레지스트리 `erp_load_scope` 신설(권고)**
현재 `index.ts:52·114·626·764·1005`에 **문자열로 흩어진** 적재범위 지식("2026년 이후", "입고량 미적재", "수금액 미매핑")을 데이터로 승격한다.

```
erp_load_scope(module, table_key, period_from, period_to,
               missing_cols text[], state('loaded'|'partial'|'none'),
               erp_source, expandable bool, note)
```

챗봇 응답·요청함·`10_ERP_DB연계` 문서가 **같은 출처**를 본다. 지금은 코드를 고쳐야 안내 문구가 바뀌고, 그때마다 `jeil-chat` 106KB를 재배포해야 한다.

**(b) `notice` 카드의 `request` 확장** — 뷰 타입은 늘리지 않는다(ADR-008 준수).

```jsonc
request: {
  kind: "data" | "doc" | "feature",       // ← 신설. 프론트가 그대로 반송
  module: "inventory", moduleKo: "재고", dept: "자재물류팀",
  gap: { type: "field"|"period"|"table"|"scope", detail: "입고량·재고량" }
}
```

**(c) 요청 유형은 늘리지 않는다.** 기존 `data`/`doc`/`feature` 3종을 그대로 쓰고 세분은 `target_detail.gap`에 담는다. 테이블·상태머신 변경 0.

**(d) 벤더 협의 상태(§11-4 미결정에 대한 권고)** — `blocked` 상태를 새로 만들지 말고 **`doing` + `needs_vendor_review=true`** 로 표기한다. 상태머신을 건드리지 않고 요청함에서 「유니포인트 협의 대기」로 필터할 수 있다.

**(e) 유형별 라우팅**

| 유형 | 담당 | 후속 |
|---|---|---|
| `data` | ERP 연계 담당 | `10_ERP_DB연계/02_진행상태` 항목화(에이전트 `erp-db-link-manager`) |
| `doc` | 문서연동 담당 | `ai_document_scope` 등재 |
| `feature` | 제품기획 | `01_PRD` 백로그 |

### 14-5. 착수 전 선수정이 필요한 결함 2건 (실측 확인)

1. **`04:1957` — 요청 유형이 하드코딩되어 있다.**
   ```js
   var kind = (_reqCtx.module === 'payroll') ? 'perm_sensitive' : 'perm';
   ```
   C·D·E 카드에 버튼이 붙어도 **전부 `perm`(권한 요청)으로 접수**되어 라우팅·통계·히트맵이 오염된다. 서버가 `request.kind`를 내리고 프론트는 그대로 반송하도록 바꿔야 한다(§14-4b). **P2의 첫 작업이어야 한다.**
2. **`:1051`의 `notice/info` 카드에 `request`가 없다.** 카드가 이미 있는데도 버튼이 붙지 않는 유일한 지점 — 필드 3개 추가로 해결된다.

### 14-6. 단계 재분할 (기존 P2를 4개로 쪼갬)

| 단계 | 범위 | 수정 지점 | 근거 |
|---|---|---|---|
| **P2a** | **결측 표기 정확성** — 부분적재 응답에 `notice` + `request{kind:'data',gap:'field'}` | `index.ts:626·632·634` + `04:1957` 결함 수정 | §14-2③ 가장 위험하면서 가장 저렴 |
| **P2b** | 무데이터·기간 밖 — `erp_load_scope` 조회 후 ⓐⓑ/ⓒⓓ 분기, ⓓ는 "조건 다시 확인"을 요청 버튼보다 **앞에** 배치 | `:764`, `:1005` | §14-2① 오분류 방어 |
| **P2c** | 문서범위(D) + 미구현(E) | `:1051`(필드 3개) · `:1093` · `:1099` · 프롬프트 `:57` | 저비용 |
| **P2d** | `get_my_requests` 도구 — "내 요청 어떻게 됐어?" | 도구 1개 | 재요청 억제 |

**배포는 P2a~P2d를 묶어 1회로 한다** — `jeil-chat`은 106KB 단일 파일이라 MCP 인라인 재배포 비용이 크다(§14-7).

### 14-7. 비용·리스크

| 항목 | 평가 |
|---|---|
| 개발량 | 서버 반환부 7곳 + 프론트 1줄 + (선택)레지스트리 1테이블. **신규 화면·뷰 타입 0** |
| 배포 | `jeil-chat` 재배포 필수(P1은 불필요했음). 106KB 인라인 — **묶음 배포 권장** |
| 요청 남발 | `data`는 서버 재판정이 **원리적으로 불가** → 사유 필수·자동 병합·일 5건 제한이 유일한 방어. **부서×모듈 병합이 권한 유형보다 훨씬 중요**(같은 적재범위 요청은 1건으로 수렴) |
| 오분류 | 조건 오입력(ⓓ)까지 요청으로 유입 → P2b의 액션 순서 배치로 완화, 완전 차단은 불가 |
| 기대치 관리 | E(기능없음)에 요청 버튼을 달면 "요청하면 만들어준다"는 신호가 된다 → 회신 문구에 **검토 대상임**을 명시 |

### 14-9. P2a 구현·실측 실적 (2026-07-23)

§14-6의 P2a를 **2분류 구조로 확장해** 구현·배포했다. 라이브(`ai.jeilm.co.kr`) 실측 결과를 함께 남긴다.

| 계층 | 산출물 | 실측 |
|---|---|---|
| 서버 | `jeil-chat` **v28** — `request.ui`(perm\|data)·`kind` 부여, 재고 도구에 `gap`/`gap_why`/`fix_type` 신설, 문서범위 카드에 `request` 추가 | 「재고 입출고 현황」 카드에 `입고합계 0 ⚠미적재`(fix_type `column`) · `재고합계 - ⚠미연계`(fix_type `table`) 표시 |
| API | `jeil-portal-request` **v3** — `gap` 저장, 중복병합 키에 `fix_type` 포함, `by_fix_type` 집계, **완료 게이트**(§15) | 동일 요청 재접수 → 병합 / 같은 모듈·다른 `fix_type` → 별건 접수 확인 |
| 프론트 | 결측 배지(`.cv-gap`+툴팁), 버튼 2분류, 모달 제목·안내 유형별 분기, 요청 유형 하드코딩 제거, 긴급도 기한 문구 제거 | `REQ-2026-0003` 접수 → 원장 `kind=data`(이전엔 `perm`으로 오분류), 사유의 계좌번호 `***-****-****` 마스킹 |
| 프롬프트 | DB `ai_gateway_config.system_prompt` 수정(재배포 불필요) | 「재고 현황 보여줘」가 도구를 건너뛰던 문제 해소 — 조회 후 카드 표시 |

**프롬프트에서 발견한 함정**: 기존 문장이 *"입고량·재고량은 미적재입니다"* 로 단정해 두니 모델이 **"조회할 필요 없다"** 고 판단해 도구를 부르지 않고 그 문장만 재진술했다. 결측 배지를 도입한 뒤에는 이 문장이 오히려 기능을 막는다 → *"반드시 조회하고, 미적재 표기는 카드 배지가 처리한다"* 로 교체했다. **미적재 지식을 프롬프트에 넣지 말고 도구 반환값에 두라**는 것이 이번 작업의 교훈이다.

### 14-8. 추가 미결정 사항 (관리자 결정 필요)

7. ~~**`erp_load_scope` 신설 여부**~~ → **결정: 신설**(2026-07-23). 마이그레이션 `erp_load_scope_v1`, 정본 `이관/sql/19_erp_load_scope.sql`, 시드 11행(전부 실측). 챗봇 결측 배지·완료 게이트·요청함 현황이 이 표 하나를 본다. **잔여**: `jeil-chat`의 재고 배지는 아직 코드 상수 — P2b 배포 때 레지스트리 조회로 교체(그때까지 값은 동일하게 유지·이중 출처 상태).
8. **`data` 요청 승인의 의미** — 승인 = ETL 착수 확정인가, 검토 대기열 등재인가. 유니포인트 협의 의존분을 어떻게 약속할지와 직결(§14-2②).
9. **E(기능 없음) 유형 노출 여부** — 사용자에게 "그 기능은 없습니다 + 요청하기"를 보일지, 조용히 백로그만 쌓을지.
10. **C 유형 SLA 분리** — §11-5의 단일 SLA 대신 `data`는 **1차 응답만 SLA(1영업일), 완료 SLA 없음**을 기본안으로 제안.

---

## 15. 요청 처리 ↔ 권한·데이터 반영의 연결 (2026-07-23)

> 관리자 지시: *"요청함에서 처리되면 권한과 내용이 유기적으로 적용되는지 분석"*. 결론부터 — **연결돼 있지 않았고, 실제 사고가 이미 발생해 있었다.**

### 15-1. 실측 — 완료 처리와 실제 반영이 어긋나 있었다

| 접수번호 | 유형 | 상태 | 처리메모 | 요청자의 실제 권한 |
|---|---|---|---|---|
| `REQ-2026-0001` | 민감권한(payroll) | **done**(닫힘) | "TEST 처리" | `erp_modules = []` · `perm_grant` 0건 |

**완료로 닫혔는데 요청자는 여전히 아무것도 못 본다.** 요청자 입장에서는 "완료됐다"는 회신을 받고 다시 조회했다가 또 막히고, 다시 요청하게 된다(§1-3의 재요청 문제가 형태만 바꿔 재발).

이는 P1 설계의 의도적 분리(§3-3 "권한 부여 버튼은 두지 않는다 — 사람이 권한 화면에서 클릭") 때문에 생긴 **필연적 부작용**이다. 부여를 사람에게 맡긴 것 자체는 옳지만(CLAUDE.md §1.6·§5.4), **"부여했는지 확인하는 장치"가 없었다.**

### 15-2. 왜 자동 부여로 해결하면 안 되는가

「완료」 클릭이 곧바로 권한을 부여하게 만들면 문제가 사라지긴 한다. 그러나 그건 **요청함이 권한 부여 화면이 되는 것**이고, 다음을 위반한다.

- CLAUDE.md §1.6 — 최종 승인 클릭은 사람이 한다.
- §5.4 — 권한 판정·부여는 권한 화면(단일 경로)에서. 부여 경로가 둘이 되면 감사(`perm_audit`)와 정책이 갈라진다.
- 민감 모듈(`payroll`)은 2인 확인 대상(§4-2)인데, 요청 완료 클릭 하나로 부여되면 그 통제가 무력해진다.

### 15-3. 채택안 — 부여는 사람이, 확인은 서버가

**완료(done) 전이에 반영 검증 게이트를 둔다.** 부여를 대신하지 않고, **부여되지 않았으면 닫지 못하게** 한다.

| 유형 | 판정 방법 | 미반영 시 |
|---|---|---|
| `perm` · `perm_sensitive` | `perm_effective(요청자)` → `is_admin` 또는 `erp_modules ∋ target_module` | **409 차단** + 부여 화면 안내 |
| `doc` | `ai_document_scope` 활성 행 존재 | **409 차단** + 범위 등록 안내 |
| `data` · `feature` · `quality` | **자동 판정 불가** | 차단하지 않음 + *"실제 적재·구현을 확인한 뒤 닫으라"* 안내 |

- **예외 경로**: 권한 외 방식으로 해결했거나 반려에 준하는 종결이 필요하면 `force: true`로 관리자가 명시 우회한다. 프론트는 확인 대화상자를 띄우고 **기본값은 중단**이며, 우회 사유는 `handled_note`에 남는다.
- **사전 표시**: 요청함 목록이 권한 유형에 `fulfilled`를 함께 내려 **「반영됨」·「미반영」** 뱃지를 보여준다. 완료를 누르기 전에 누락이 눈에 보인다.
- **판정 출처는 하나**: 게이트도 `perm_effective`(ADR-010)만 본다. 요청함이 별도 권한 로직을 갖지 않는다.

### 15-4. 남는 연결 공백

| 공백 | 현재 | 비고 |
|---|---|---|
| `data` 유형 반영 확인 | 자동 판정 불가 — 사람이 확인 | `erp_load_scope`(§14-4a)를 신설하면 "해당 칸이 적재됐는가"를 기계 판정할 수 있다. 미결정 7번과 직결. |
| 요청자 알림 | 완료 시 요청자에게 능동 통지 없음 | 챗봇 `get_my_requests` 도구(P2d) 또는 포털 배지 필요 |
| 이미 잘못 닫힌 건 | `REQ-2026-0001`은 `done` + `fulfilled=false`로 남아 있음 | 화면에 「미반영」으로 드러난다. 실제 부여 후 재처리하거나 사유를 남겨 정리 필요 |


---

## 16. 적재범위 레지스트리 `erp_load_scope` (2026-07-23 신설)

### 16-1. 무엇을 해결하는가

적재범위 지식이 **코드 5곳에 문자열로** 흩어져 있었다(`jeil-chat:52·114·626·764·1005`). 문구 한 줄을 고치려면 106KB Edge Function을 재배포해야 했고, 같은 사실이 시스템 프롬프트·도구 설명·카드 각주에 **제각각 복제**돼 서로 어긋날 여지가 있었다(실제로 "수금액 미매핑"은 원인이 두 가지로 달랐다 — §14-2③).

이제 **표 하나가 단일 출처**다.

```
erp_load_scope(module, field_key, label_ko, state, gap_label, gap_why,
               fix_type, erp_source, period_from/to, expandable, note)
```

| 소비처 | 쓰임 |
|---|---|
| 챗봇 카드 | 결측 배지 문구·툴팁 (**P2b 배포 시 연결** — 현재는 코드 상수) |
| 데이터 요청 **완료 게이트** | 모듈×작업유형 항목이 전부 `loaded`여야 완료 가능 ✅ 연결됨 |
| 요청함 「미연계 항목 현황」 | 상태·사유·**항목별 미해결 요청 수**·기록 버튼 ✅ 연결됨 |

### 16-2. 시드 (전부 실측 — 추정값 없음)

| 모듈 | 항목 | 상태 | 작업유형 | 근거 |
|---|---|---|---|---|
| inventory | 출고량 | 적재됨 | — | 1,847행 중 1,846행 값 있음 |
| inventory | **입고량** | 미적재 | 칸 수정·추가 | 분류규칙(IO_TYPE_CD) 미확정 — 입고 1행 |
| inventory | **재고량** | 미적재 | 새 테이블 연결 | 원천이 이동이력 테이블 — 0행 |
| sales | 매출액 | 적재됨 | — | 37행 중 36행 값 있음 |
| sales | **수금액** | 미적재 | 칸 수정·추가 | 추출 SQL엔 있으나 원천 컬럼이 공백 — 협의 대상 |
| sales | **수주액** | 미적재 | 새 테이블 연결 | 원천(S_SO) 미확정 — 협의 대상 |
| inventory·sales·purchase·pur_order·payroll | 적재기간 | 부분적재 | 기간 확대 | 2026년 이후만 |

### 16-3. 상태 기록의 원칙

`scope_set`은 **적재를 실행하지 않는다 — 결과를 기록만 한다**(CLAUDE.md §1.6). 실제 ETL은 `10_ERP_DB연계`의 담당 절차로 수행하고, 끝난 뒤 이 화면에서 `적재완료`로 기록해야 해당 데이터 요청의 완료 게이트가 열린다. 버튼 확인 문구에도 이 점을 명시했다.

**부작용 하나**: 적재 없이 기록만 바꾸면 게이트가 거짓으로 열린다. 이건 기술로 막을 수 없고 운영 규율의 영역이다 — 그래서 기록 버튼을 관리자 전용으로 두고 확인 대화상자에 경고를 넣었다.

---

## 17. 구현 현황 (2026-07-23 기준)

> **배포 상태를 코드 상태와 구분해 적는다.** 커밋됐다고 동작하는 것이 아니다.

| 단계 | 범위 | 코드 | 배포 |
|---|---|---|---|
| P1 | 권한 유형(A·B) 요청 버튼·원장·요청함 | ✅ | ✅ `jeil-chat` v28 · `jeil-portal-request` v4 |
| P2a | 2분류 전환·결측 배지·작업유형 묶음 | ✅ | ✅ |
| — | 완료 게이트(요청 처리 ↔ 권한 반영) | ✅ | ✅ v3 |
| — | `erp_load_scope` 레지스트리 | ✅ | ✅ v4 (`erp_load_scope_v1`) |
| **P2b** | 무데이터·기간밖 카드화(품목 이력 0건·인사 기간밖) | ✅ `791d949` | ⏳ **`jeil-chat` 재배포 대기** |
| **P2c** | 범위 밖 문서 2지점 + 검색 0건 요청 경로 | ✅ `791d949` | ⏳ 〃 |
| **P2d** | `get_my_requests` 도구 | ✅ `791d949` | ⏳ 〃 |
| **A5** | 결측 배지를 `erp_load_scope` 조회로 전환 | ✅ `791d949` | ⏳ 〃 |

### 17-1. 배포 제약 — MCP 인라인 한계

`jeil-chat/index.ts`는 **116KB 단일 파일**이다. MCP 배포는 파일 전체를 응답에 실어 보내는 방식이라, 106KB(v28)에서 이미 한계에 가까웠고 116KB에서는 **한 응답 한도를 초과해 실패**한다(잘린 호출은 실행되지 않으므로 배포 사고는 발생하지 않는다).

→ 이후 `jeil-chat` 배포는 **CLI 경로**를 기본으로 한다.

```
npx supabase functions deploy jeil-chat --project-ref dvzohdqtjzocgcclgwro
```

**구조적 대응(권고)**: 이 파일은 도구 정의(TOOLS)·도구 구현(runTool)·게이트웨이(Deno.serve)가 한 파일에 있다. 모듈 3분할하면 배포 단위가 작아지고 리뷰도 쉬워진다 — 다만 분할 자체가 대규모 변경이라 별도 과제로 둔다.

### 17-2. P2b~P2d 설계 메모

- **`confirm_first`**: 무데이터 카드에서 요청 버튼을 **뒤로** 보내는 신호. §14-2①의 오분류(ⓓ 조건 오입력)를 줄이기 위해, "품목 정보 다시 확인"·"2026년 인원현황 보기" 같은 재확인 액션을 앞세운다. 프론트 렌더러가 `insertBefore` 대신 `appendChild` 한다.
- **배지 SSOT 전환의 부수 효과**: 결측이 하나도 없으면(`state=loaded`) 배지도 요청 버튼도 **자동으로 사라진다**. 적재 완료 후 코드를 고칠 필요가 없다.
- **`get_my_requests`는 게이트 대상이 아니다**: 본인 요청 조회는 전 직원 가능해야 하므로 `ERP_TOOL_MODULE`에 매핑하지 않았다. 요청자는 호출자 UPN으로 고정되어 타인 요청은 조회 불가.
