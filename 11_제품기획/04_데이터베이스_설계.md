# 04 · 데이터베이스 설계서

> 관리자: 최동혁(dh.choi@jeilm.co.kr) · 기준일: 2026-07-08 · 상태: Draft
> 관련: [03 아키텍처](03_시스템아키텍처_설계.md) · [06 ERP연계](06_ERP연계_챗봇활용_설계.md) · [08 보안](08_보안_데이터안정성.md)
> 실제 DDL/RLS SQL 정본(값 복제 금지): [`이관/sql/01~07`](../실제구축준비%20자료/이관/sql/)

이 문서는 데이터 모델의 **구조·격리·명명·매핑**을 정의한다. 표의 행수는 2026-07-08 Supabase 라이브 조회(`list_tables`) 실측이다.

---

## 1. 스키마 3분할

| 스키마 | 용도 | 접근 | REST 노출 | 상태 |
|---|---|---|---|---|
| `public` | 포털 자체 생성(발주·검사·계정·챗봇로그) | 클라이언트 RLS + Edge Fn | O(RLS 보호) | 실가동 |
| `erp_ro` | ERP 읽기 사본(집계·스냅샷) | 사내 SELECT(설계)·적재는 RPC | **X(비노출)** | 파일럿 |
| `etl_meta` | 배치 이력·메타 | service_role 전용 | **X(비노출)** | 파일럿 |
| `erp_secure` | 민감(자금·인사) | — | X | **P5 예약·미생성** |

- 접미사 규약: `_m`(월집계) · `_d`(일집계) · `_s`(스냅샷). 공통 컬럼: `synced_at`·`src_updated`·`batch_id`(UUID).
- `erp_ro`/`etl_meta`를 REST에 노출하지 않고, 적재는 `security definer` RPC(`erp_etl_upsert`/`erp_etl_batch`)로만. → 클라이언트가 ERP 사본을 직접 못 읽음(P4 노출은 별도 뷰/RPC 설계 필요, [06](06_ERP연계_챗봇활용_설계.md)).

## 2. 테이블 목록 (라이브 실측 · RLS 전부 ON)

### 2.1 public — 발주 포털

| 테이블 | 행수 | 설명 |
|---|---|---|
| `sp_order_header` | 8 | 발주 헤더(발주번호·거래처·품목·수량·납기·구매자) |
| `sp_order_state` | 8 | 상태(status new/prod/insp/rework/done, step 1~10) |
| `sp_photo` | 2 | 생산·검사 사진(경로, 사내 리뷰 상태) |
| `sp_message` | 7 | 발주 건별 메시지(supplier/internal, 발신자명·인덱스) |
| `sp_insp_request` | 10 | 검수요청 |
| `sp_inspection` | 2 | 검수 판정(합/부, 사내 전용 쓰기) |
| `sp_inspection_log` | 2 | 검수 이력(판정자·차수, 비가역) |

### 2.2 public — 계정/거버넌스

| 테이블 | 행수 | 설명 |
|---|---|---|
| `vendor_master` | 17 | 거래처 마스터(`bp_cd` PK, ERP 17개사 시드). 운영 시 `erp_ro`의 거래처와 대응 |
| `vendor_account` | 3 | 협력사 계정(발급분) |
| `vendor_account_log` | 18 | 계정 변경 감사로그 |
| `vendor_application` | 0 | 가입신청(pending/approved/rejected) |
| `portal_admin` | 1 | 협력사관리 관리자(부트스트랩 `dh.choi@jeilm.co.kr`) |

### 2.3 public — 챗봇

| 테이블 | 행수 | 설명 |
|---|---|---|
| `chat_log` | 9 | 챗봇 사용 감사(UPN·모델·토큰·추정비용·사용도구·`session_id`·`stopped`). 원문은 저장하지 않음(원문은 아래 chat_message). Edge Fn(service_role)만 기록 |
| `chat_work` | — | **작업 폴더(work)** — 사용자 생성(이름·메모). 메모는 해당 work 대화에 시스템 컨텍스트로 주입(적용범위=`ai_gateway_config`). soft delete(2026-07-21 신설, ADR-009) |
| `chat_session` | — | **대화 세션** — work 소속(선택, null=일반). 제목(첫 질문 60자 자동)·message_count·last_message_at. soft delete 30일 후 purge |
| `chat_message` | — | **대화 원문**(user/assistant) + `views` jsonb(jeilax 뷰 복원용) + `stopped`(중지 부분응답). 세션 FK cascade. **본인만 열람·삭제**(jeil-chat-history 경유, ADR-009) |
| `ai_gateway_config` 外 | 1·5·5·1 | 모델 설정 SSOT(`ai_model`·`ai_routing_rule`·`ai_document_scope`) + work 컨텍스트·저장 정책 6컬럼(2026-07-21 확장) |
| `portal_request` | — | **포털 요청 원장** — 챗봇이 "조회 불가"를 알린 지점에서 사용자가 접수(권한·데이터 적재범위·문서범위·기능·품질 6유형). 동조자 배열로 중복 자동 병합, 상태 7종. RLS 전면차단, `jeil-portal-request` 전용(2026-07-22 신설, ADR-011) |
| `erp_load_scope` | — | **중간DB 적재범위 레지스트리** — 무엇이 어디까지 적재됐나(module×field_key, state/gap/fix_type/erp_source). 챗봇 결측 배지·데이터 요청 완료 게이트·요청함 현황의 단일 출처. RLS 전면차단, `jeil-portal-request` 전용(2026-07-23 신설, 설계 11 §16) |

### 2.4 erp_ro — ERP 읽기 사본

| 테이블 | 행수 | 접미사 | 내용 |
|---|---|---|---|
| `item_master_s` | 58,246 | 스냅샷 | 품목 마스터(`B_ITEM`) |
| `pur_order_s` | 2,699 | 스냅샷 | 발주(2026년, `M_PUR_ORD_*`) — 협력사 포털 우선연동 |
| `inventory_d` | 656 | 일집계 | 입출고(`M_PUR_GOODS_MVMT`) |
| `purchase_m` | 63 | 월집계 | 거래처별 매입(`M_IV_HDR`) |
| `sales_orders_m` | 10 | 월집계 | 매출(`S_BILL_HDR`) |
| `table_dict` | 2,658 | 사전 | ERP 테이블 사전(컬럼 문서화 452, 11모듈) |

### 2.5 etl_meta

| 테이블 | 행수 | 내용 |
|---|---|---|
| `batch_run` | 5 | 배치 이력(job·시각·상태·건수). 뷰 `v_last_success`=화면 「데이터 기준시각」 |

## 3. ERD (핵심 관계)

```
                        vendor_master(bp_cd) ──1:N── vendor_account
                              │                          │
                              │ bp_cd                    │ (auth.users ← app_metadata.vendor_bp)
                              ▼                          ▼
   sp_order_header(po_no,bp_cd) ──1:1── sp_order_state(po_no, status, step)
        │po_no                              
        ├──1:N── sp_photo(po_no)            
        ├──1:N── sp_message(po_no, sender)  
        ├──1:N── sp_insp_request(po_no)     
        └──1:N── sp_inspection(po_no) ──1:N── sp_inspection_log(po_no, 차수, 판정자)

   [ERP 사본 · 조인축 없음(현재 단절)]
   erp_ro.pur_order_s(po_no?, bp_cd) …… 향후 sp_order_header.po_no 와 매핑(P4)
   erp_ro.item_master_s / sales_orders_m / purchase_m / inventory_d
   etl_meta.batch_run ──(job별 최신)──▶ v_last_success
```

- **포털 격리축**: `bp_cd`(거래처). **조인축**: `po_no`(발주번호, `PO+YYYYMMDD+일련`).
- ERP 사본(`erp_ro`)과 포털(`public`)은 현재 FK로 연결돼 있지 않다. 매핑은 [06](06_ERP연계_챗봇활용_설계.md)에서 설계.

## 4. RLS 정책 매트릭스

전 테이블 RLS ON. 두 헬퍼가 정책 전제:

- `public.is_internal()` = JWT `app_metadata.role == 'internal'`
- `public.vendor_bp()` = JWT `app_metadata.vendor_bp`(JSON 배열) → `text[]`
- **Custom Access Token Hook**(`public.custom_access_token_hook`)이 로그인 JWT에 `role`·`vendor_bp` 주입. `@jeilm.co.kr`은 미지정 시 `internal` 자동. `supabase_auth_admin`만 실행, `authenticated/anon` revoke.

| 테이블군 | 사내(internal) | 협력사(vendor) | anon |
|---|---|---|---|
| `sp_order_*`, `sp_photo`, `sp_message`, `sp_insp_request` | 전체(`for all`) | 자기 `bp_cd` 행만(`bp_cd = any(vendor_bp())`) | 차단 |
| `sp_inspection`, `sp_inspection_log` | 읽기+쓰기 | **읽기만** | 차단 |
| `vendor_master`/`account`/`application` | 관리(`is_vendor_admin`) | 제한적 자기 | 차단 |
| `chat_log` | (정책 없음 — Edge Fn service_role만) | 차단 | 차단 |
| `chat_work`·`chat_session`·`chat_message` | (정책 없음 — Edge Fn의 **upn 필터**가 유일한 경로. 본인 것만, ADR-009) | 차단 | 차단 |
| `portal_request` | (정책 없음 — `jeil-portal-request`가 유일한 경로. 요청자는 JWT에서만 취득, 관리자 op는 `perm_effective.is_admin`, ADR-011) | 차단 | 차단 |
| `erp_load_scope` | (정책 없음 — `jeil-portal-request` 전용. 조회·기록 모두 관리자 op. `erp_source`에 ERP 내부 테이블명이 담겨 사용자 화면에는 노출하지 않는다) | 차단 | 차단 |
| `etl_meta.batch_run` | (정책 없음 — service_role만) | 차단 | 차단 |
| Storage `vendor-photos` | 경로 첫 폴더 `{bp_cd}` ∈ `vendor_bp()`일 때 협력사 접근 | | 비공개 |

- `chat_log`·`batch_run`의 "RLS ON + 정책 없음"은 **의도된 설계**(클라이언트 전면 차단, Edge Fn/RPC만 접근). 보안 advisor가 INFO로 표기하나 취약점이 아님 → [08 보안 §실측](08_보안_데이터안정성.md).
- `is_vendor_admin()` = `is_internal()` AND `portal_admin` 등록. `set search_path=''`로 하드닝.

## 5. RPC / 뷰

| 객체 | 종류 | 접근 | 용도 |
|---|---|---|---|
| `erp_etl_upsert(table, rows)` | RPC(security definer) | service_role | ETL 적재(청크 500) |
| `erp_etl_batch(...)` | RPC(security definer) | service_role | 배치 start/finish 기록 |
| `update_my_vendor_contact(p_name,p_phone)` | RPC(security definer) | authenticated | 본인 담당자명·연락처 수정 |
| `custom_access_token_hook` | Auth Hook | supabase_auth_admin | JWT claim 주입 |
| `chat_append_message(...)` | RPC(security definer) | service_role 전용(public/anon/authenticated revoke) | 챗봇 메시지 원자 append(seq 채번·제목 자동·upn 검증) |
| `etl_meta.v_last_success` | 뷰 | (내부) | job별 최신 성공시각 |

> **점검 포인트**: `update_my_vendor_contact`가 anon/authenticated에 REST로 노출됨(보안 advisor WARN). 세션 없는 호출은 무효여야 하며, 자기 행만 수정하도록 내부 가드를 재확인할 것 → [08 §실측 결과](08_보안_데이터안정성.md), [09 ADR](09_ADR_의사결정기록.md).

## 6. 마이그레이션 이력 (라이브 `list_migrations`, 16건)

| 버전 | 이름 | 의미 |
|---|---|---|
| 20260629003814 | harden_function_search_path | 함수 search_path 하드닝 |
| 20260629003845 | revoke_execute_rls_auto_enable | 실행권한 회수·RLS 자동활성 |
| 20260629004306 | add_order_header_demo | 발주 헤더 시드 |
| 20260629005626 | vendor_onboarding_schema | 협력사 온보딩 스키마 |
| 20260629005851 | vendor_signup_trigger | 가입 트리거 |
| 20260707050738 | create_chat_log | 챗봇 감사로그 |
| 20260707052520 | sp_message_sender_name_and_indexes | 메시지 발신자명·인덱스 |
| 20260707054035 | chat_log_tokens_cost | 토큰·비용 |
| 20260707060216 | sp_photo_review_and_vendor_policy_split | 사진 리뷰·정책 분리 |
| 20260707060231 | sp_order_header_buyer | 구매자 |
| 20260707062639 | erp_ro_etl_meta_schemas | **중간DB 스키마** |
| 20260707062751 | erp_etl_rpc | **적재 RPC** |
| 20260707063949 | order_state_machine_v2 | **상태머신 v2(5상태)** |
| 20260707070309 | vendor_self_contact_update_rpc | 본인 연락처 RPC |
| 20260707074523 | vendor_account_realtime | 계정 실시간 |
| 20260707075440 | erp_ro_pur_order_s | **발주 스냅샷** |

> DDL·RLS의 재현 SQL은 `이관/sql/01~07`에 있으나, `erp_ro`/`etl_meta` DDL은 Supabase MCP `apply_migration`으로 직접 적용되어 `.sql` 파일이 리포에 없다(근거=마이그레이션 이력·ETL RPC 호출). **개선 권고**: 향후 마이그레이션은 `supabase/migrations/`에 파일로 남겨 이관·감사 가능성을 높인다 → [09 ADR](09_ADR_의사결정기록.md).
>
> **추가 이력(2026-07-21, 챗봇 대화내역 — ADR-009)**: `create_chat_work_session_message`(chat_work/chat_session/chat_message + RLS + chat_log 확장 + `chat_append_message` RPC) · `ai_gateway_config_work_context`(work 컨텍스트·저장 정책 6컬럼). 그 사이 2026-07-08~13의 ERP 뷰·권한·모델설정 마이그레이션 다수는 라이브 `list_migrations`가 정본.
>
> **추가 이력(2026-07-22, 포털 요청 원장 — ADR-011)**: `portal_request_v1`(`portal_request` + 접수번호 시퀀스 + updated_at 트리거 + RLS 전면차단). 정본 SQL은 `실제구축준비 자료/이관/sql/18_portal_request.sql`.
> **추가 이력(2026-07-23, 적재범위 레지스트리)**: `erp_load_scope_v1`(`erp_load_scope` + module/state 인덱스 + RLS 전면차단 + 실측 시드 11행). 정본 SQL은 `실제구축준비 자료/이관/sql/19_erp_load_scope.sql`. 설계 [11 §16](11_챗봇_데이터요청접수_설계.md).

## 7. 확장(vector/pg_cron) 현황

- 라이브 `list_extensions` 기준 활성: `pgcrypto`, `supabase_vault`, `pg_stat_statements`, `uuid-ossp`, `plpgsql`.
- **미설치**(향후 고도화 시 필요): `vector`(챗봇 문서 RAG 3단계 임베딩), `pg_cron`(중간DB 스케줄 배치·리프레시). → [06](06_ERP연계_챗봇활용_설계.md)·[09 ADR](09_ADR_의사결정기록.md).
