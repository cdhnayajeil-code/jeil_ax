# 결의전표 릴레이 — 변경관리 런북

> **무엇이 바뀌면 어디를 고치는가.** 서버·계정·접속정보·대상DB는 언젠가 반드시 바뀐다.
> 그때 이 문서만 보고 처리할 수 있게 남긴다.
> 최종 갱신: 2026-09-23 · 관리: 최동혁 · 통합 러너 **r1.7**(전달 폴더 반영 완료 · 서버 교체 대기) · 릴레이 v1.7(소스) · 서버 실행본 r1.3(2026-09-18 빌드 — ETL 확장·기안서 대장 미포함)

---

## 0. 지금 배포 상태 (사실 기준)

| 항목 | 값 |
|---|---|
| 실행 방식 | **단일 EXE**(`jeil_runner.exe`) — 서버에 파이썬 미설치. 릴레이·ETL·퇴사 CLI 가 이 파일 하나에 통합됨(2026-09-18, r1.3). 구 `gl_relay.exe` 는 하위호환으로만 남는다 |
| 실행 호스트 | ERP 운영 서버(사외 IDC · 상시 가동) |
| 실행 계정 | `ADMIN` |
| 배포 루트 | `E:\ai.jeil\relay\` |
| 대상 DB | `JEILMNS_DEMO2` (코드 하드코딩 · CLI 변경 불가) |
| 거래유형 | `AX001` (기본값, `--trans-type` 으로 변경 가능) |
| 주기 | **작업 스케줄러 미등록 — 수동 실행만**(2026-08-24 관리자 결정). 2026-09-11 부터는 **트레이 러너 `jeil_runner.exe`(README C안)** 가 60초 주기로 `--queue --max 5` 를 대신 돌린다(서버 적용은 관리자 실행 대기) |
| 접속정보 | `E:\ai.jeil\relay\.env` (EXE와 같은 폴더) — **소스·문서에 값 미기재** |
| 전달 폴더 | `…\jeil_ax\relay\` — 경로 정본은 `.claude/erp_relay.path`. **`python deploy/_sync_relay.py` 가 채운다**(손으로 복사하지 않는다). 파일 전달용, **실행 위치가 아니다** |

```
E:\ai.jeil\relay\
├─ jeil_runner.exe       ← **통합 러너**(2026-09-18 r1.3) — 트레이 스케줄러 + 릴레이·ETL·퇴사 CLI
├─ .env                  ← 접속정보 3키. ACL 잠금
├─ relay.cmd             ← 작업 스케줄러 진입점(내부에서 `jeil_runner.exe relay` 호출)
├─ gl_relay.exe          ← (옛 배포본) 통합 전 릴레이 — 새로 올릴 때는 두지 않는다
├─ runner_config.json    ← 러너 연동 기준(창에서 저장)
├─ runner_history.jsonl  ← 러너 실행 내역
├─ logs\relay_YYYYMM.log
└─ logs\runner\         ← 러너 로그(runner_YYYYMM.log · runs\<작업>\<시각>.log)
```

`.env` 키 3개 — **값은 여기 적지 않는다**(`CLAUDE.md §1.1`).

```
SUPABASE_URL
SUPABASE_SERVICE_ROLE_KEY
ERP_DB_CONN
TEAMS_WEBHOOK_URL      ← 선택. 없으면 알림만 건너뛰고 전송은 정상 동작
```

> **`TEAMS_WEBHOOK_URL`** — 전송 실패·적체를 Teams 채널로 알린다(2026-08-25 신설).
> Teams 채널 › 커넥터 › Incoming Webhook 에서 발급한 URL 한 줄. **URL 자체가 비밀값**이므로
> `.env` 에만 두고 로그·문서에 남기지 않는다. 미설정이면 알림만 조용히 건너뛴다.
> 스케줄러를 걸기 전에 설정하는 것을 권한다 — 알림 없이 무인 운영하면 실패가 아무도 보지 않는 곳에 쌓인다.

---

## 1. ERP 접속정보가 바뀌면 (계정·비밀번호·포트)

**고칠 곳: `E:\ai.jeil\relay\.env` 의 `ERP_DB_CONN` 한 줄. 그것뿐이다.**

```
ERP_DB_CONN = DRIVER={ODBC Driver 17 for SQL Server};SERVER=localhost;DATABASE=JEILMNS;UID=...;PWD=...;TrustServerCertificate=yes;
```

- **EXE 재빌드 불필요.** 접속정보는 번들에 들어 있지 않다.
- 바꾼 뒤 확인: `gl_relay.exe --list` → 대기 건이 나오면 정상.
- `DATABASE=` 는 무엇으로 적어도 된다. 릴레이가 접속 시 `JEILMNS_DEMO2` 로 강제 치환하고,
  붙은 뒤 실제 DB명을 재확인해 다르면 **아무것도 실행하지 않고 중단**한다(가드 2).

> 워크스테이션에서 쓰던 `%USERPROFILE%\.erp\` DPAPI 저장소는 **계정·장비에 묶여 있어
> 서버에서 복호화되지 않는다.** 서버는 `ERP_DB_CONN` 경로만 동작한다.

---

## 2. 실행 서버가 바뀌면 (다른 장비로 이전)

새 장비가 갖춰야 할 조건 5가지 — **전부 만족해야 한다.**

| 조건 | 확인 방법 |
|---|---|
| 항상 켜져 있음 | 상시 가동 장비인지(기획서 D2-a 필수 조건) |
| ERP MSSQL 접근 | `sqlcmd -S <서버> -Q "select db_name()"` |
| Supabase 아웃바운드 443 | `Test-NetConnection <프로젝트>.supabase.co -Port 443` → `TcpTestSucceeded : True` |
| ODBC 드라이버 17 또는 18 | `Get-OdbcDriver -Name "*SQL Server*"` |
| x64 Windows | EXE 는 x64 빌드 |

옮기는 절차:

1. `gl_relay.exe` · `relay.cmd` 복사 → 새 배포 루트
2. `.env` 를 **새 서버에서 직접 작성**(OneDrive·메일 경유 금지) + ACL 잠금
3. **`relay.cmd` 의 `set "GL_RELAY_ROOT=..."` 한 줄**을 새 경로로 수정
   (경로는 **ASCII 로만** — 한글이 들어가면 cmd.exe 파싱이 깨진다)
4. `gl_relay.exe --list` 로 연결 확인
5. `register_task.ps1 -Account <계정> -Root <새경로>` 로 등록
6. **구 서버의 작업을 먼저 해제**한다 — `register_task.ps1 -Remove`
   (두 곳에서 동시에 돌아도 선점(`gl_apply_claim`)이 중복 투입을 막지만,
   로그가 갈라져 추적이 어려워진다)
7. 이 문서 §0 표와 `실제구축준비 자료/이관/00_현재시스템_상태스냅샷` 갱신

---

## 3. Supabase 프로젝트·키가 바뀌면

**고칠 곳: `.env` 의 `SUPABASE_URL` · `SUPABASE_SERVICE_ROLE_KEY`.**

- **EXE 재빌드 불필요.**
- 서비스 롤 키 회전 시에는 **워크스테이션 `.env` 와 서버 `.env` 를 함께** 바꿔야 한다.
  한쪽만 바꾸면 다른 쪽이 조용히 401 로 실패하고, 큐는 계속 쌓인다.
- 키 회전 후 확인: 양쪽에서 `--list` 가 대기 건을 보여주는지.

---

## 4. 대상 DB를 바꿔야 하면 (DEMO2 → 운영)

**`.env` 로는 바뀌지 않는다. 코드 변경 + 승인이 필요하다.**

`gl_apply_demo2.py` 의 `TARGET_DB = "JEILMNS_DEMO2"` 가 하드코딩이고 CLI 파라미터가 없다.
운영(`JEILMNS`) 쓰기는 **결정 C-1 로 금지**되어 있다. 바꾸려면:

1. 경영/관리자 승인 + 유니포인트 협의 (C-1 개정)
2. 미결 **U-F** 해소 — `AX001` 계정매핑이 `A_JNL_ACCT_ASSN` 에 미등재(회계팀 등재 필요)
3. **되돌리기 절차 확보** — DEMO2 는 `--cleanup` 으로 지울 수 있지만 운영은 불가.
   전표 취소 절차가 먼저 정의돼야 한다.
4. 화면 단계 **중복 경고** 도입 — 멱등 방어는 *같은 초안번호* 기준이라
   내용이 같은 별개 초안은 두 장 다 들어간다(2026-08-24 실사례: 동일 적요·금액 2건).
5. 코드 수정 → **EXE 재빌드** → 재배포

---

## 5. 릴레이 코드가 바뀌면 (EXE 재빌드가 필요한 경우)

**재빌드 필요**: `gl_apply_demo2.py` · `_env.py` · `_erp_conn.py` 수정 시

```powershell
python 10_ERP_DB연계\etl\deploy\build_exe.py
#   산출물: 10_ERP_DB연계\etl\deploy\dist\gl_relay.exe
```

교체 절차(운영 중 무중단):

```powershell
Disable-ScheduledTask JEIL_AX_GL_Relay        # 새 회차 진입 차단
#   진행 중인 건이 끝날 때까지 로그로 확인
Copy-Item .\gl_relay.exe E:\ai.jeil\relay\ -Force
E:\ai.jeil\relay\gl_relay.exe --list                # 새 EXE 동작 확인
Enable-ScheduledTask JEIL_AX_GL_Relay
```

**재빌드 불필요**: `.env` 변경, 주기 변경, `--max` 변경, 작업 계정 변경

> 실행 중 EXE 는 파일 잠금이 걸린다. 교체가 거부되면 작업을 먼저 Disable 하고
> 진행 중인 회차가 끝나기를 기다린다.

---

## 6. 실행 계정이 바뀌면

```powershell
.\register_task.ps1 -Remove
.\register_task.ps1 -Account <새계정>
```

`.env` ACL 에 새 계정 읽기 권한을 반드시 추가한다. 빠뜨리면 작업은 도는데
`SUPABASE_URL 가 없습니다` 로 매번 실패한다.

```powershell
icacls E:\ai.jeil\relay\.env /grant:r "<새계정>:(R)"
```

---

## 7. 장애 진단 순서

| 증상 | 먼저 볼 곳 |
|---|---|
| 전송대기가 안 줄어든다 | `Get-ScheduledTask JEIL_AX_GL_Relay \| Get-ScheduledTaskInfo` → `LastTaskResult` |
| 〃 (작업은 정상) | `logs\relay_YYYYMM.log` 마지막 회차 |
| `SUPABASE_URL 가 없습니다` | `.env` 위치(EXE와 같은 폴더인지) · ACL 에 실행 계정 있는지 |
| ODBC 오류 | `Get-OdbcDriver` · `ERP_DB_CONN` 의 DRIVER 이름 철자 |
| 특정 전표만 계속 실패 | 화면 [ERP 전송] 탭 「손봐야 할 건」의 사유 — 대개 계정·차대 구성 오류 |
| `sending` 에 갇혔다 | 30분 뒤 `gl_apply_reclaim` 이 자동 반납. 급하면 다음 회차를 기다린다 |
| 트레이 러너 작업이 계속 「실패」 | 창 [실행 내용] 의 마지막 줄(= 내역 「요약」). `SUPABASE_URL 가 없습니다` 면 `.env`, ODBC 권한 오류면 `ERP_DB_CONN` 계정의 JEILMNS SELECT 권한 |
| 데이터 업데이트가 「일부 실패 — 생략 2종」 | 서버 러너에 계정 수집 접속정보(`ENTRA_*`·`.env.local`)가 없다 — ERP 데이터는 갱신됨. 계정까지 갱신하려면 관리자 PC 러너를 켜거나(SQL 49 적용 시 자동 우선) 서버에 접속정보를 둔다 |
| 트레이 러너 작업이 「실패 — 시간 초과(N분)」 | 자식이 상한 안에 끝나지 않아 트리째 종료됐다. 실행 내용 마지막 줄로 멈춘 지점을 보고, 정상적으로 오래 걸리는 작업이면 [연동 기준] 「시간 상한(분)」을 늘린다 |
| 내역에 「러너 비정상 종료 — 결과 미확인」 | 러너가 로그오프·재부팅·강제 종료로 사라졌을 때 돌던 실행. 릴레이는 멱등 확인으로 중복 투입되지 않고, 데이터 업데이트는 다음 요청에서 다시 적재된다 |
| 퇴사 처리 화면이 「곧 집어갑니다」인데 안 줄어든다 | 서버 러너는 퇴사 큐를 보지 않는다 — 관리자 PC `etl_watch.py` 를 켠다. 안내를 바로잡으려면 SQL 49 적용(승인 대기) |
| 러너가 안 뜨고 「이미 실행 중」 | 다른 세션·상승 권한으로 이미 떠 있다 — 트레이 아이콘 확인, 없으면 작업 관리자에서 `jeil_runner.exe` 종료 후 다시 |
| 트레이 아이콘이 안 보인다 | 러너가 로그오프로 함께 종료됐다 — 다시 로그인해 `jeil_runner.exe` 실행(또는 `register_runner_task.ps1` 등록) |
| 실패 사유가 낡았다(규칙이 바뀐 뒤) | `gl_relay.exe --recheck` — 현재 규칙으로 다시 판정해 사유를 현행화한다. **ERP 무투입**(읽기 검증만) |

포털 쪽 원장으로도 볼 수 있다 — `public.gl_erp_apply_log`(mode·status·detail),
`public.gl_draft.erp_last_error`(재전송해도 남는 보존 칸).

**화면이 먼저 답한다(2026-08-25~).** [ERP 전송] 탭 상단에 중계 생존 배너가 뜬다.
릴레이가 회차마다 심박(`gl_relay_heartbeat`)을 남기고, 화면이 그것을 읽는다.

| 배너 | 뜻 | 할 일 |
|---|---|---|
| 🟢 사내 중계 가동 중 | 10분 이내 심박 있음 | 없음 |
| 🔴 응답하지 않습니다 | 심박이 10분 넘게 없음 | 서버에서 `relay.cmd` 실행 또는 스케줄러 확인 |
| 🟡 알 수 없습니다 | 심박 기록이 아예 없음 | **서버 EXE 가 구버전**일 수 있다(심박은 v1.4부터) — §5 로 재배포 |

---

## 8. 변경 이력

| 일자 | 변경 | 비고 |
|---|---|---|
| 2026-09-23 | **통합 러너 r1.7 — 국세청(e세로) 전자세금계산서·부가세 계산서 원장 적재 신설(REQ-0082)** | ETL job 두 개를 붙였다 — `etax_master`(`A_TS_ETAX_MASTER` → `erp_ro.etax_master_s`, 2026년 1,748건) · `vat_ledger`(`A_VAT` → `erp_ro.vat_s`, 1,760건). 정본 SQL `이관/sql/71`. **⚠ `vat_ledger` 의 모집단은 `A_VAT` 전량이 아니다** — ERP 뷰 `AV_A_VAT_ETAX` 가 `B_TAX_BIZ_AREA`·`B_CONFIGURATION` 에 INNER JOIN 하고 `ISSUE_DT_FG='Y'` 로 거른다(2026년 원본 1,783 → 뷰 1,760). 추출 SQL 이 같은 조건을 복제하며, 이걸 빠뜨리면 ERP자료대사(CM903M2) 화면과 23건 어긋난다. 적재 RPC 는 공유 `erp_master_upsert` 대신 전용 `erp_vat_upsert` 를 쓴다(기존 20분기 함수를 건드리지 않으려고). ⚠ **ETL 추출 SQL 이 바뀌었으므로 EXE 재빌드가 필수**다(§17.2) — 빌드 후 `jeil_runner.exe etl --help` 의 `--job` 목록에 `etax_master`·`vat_ledger` 가 보이는 것을 실물로 확인했다. **서버 교체 후 별도 `--full` 백필은 불필요**하다(신규 표라 최초 실행이 곧 전량). 회귀 104건 통과 | 보안: 대표자명·이메일·주소(e세로)와 신용카드번호·현금영수증번호(A_VAT)는 추출 SQL 에서 아예 뽑지 않는다 |
| 2026-09-23 | **r1.6 보강 — 설정 창에서 기안서 대장 작업을 켤 수 있게** | 작업 종류만 붙여 놓고 **설정 창(「연동 기준」 탭)에 입력 패널을 안 만들어** 둔 것을 고쳤다. 그 상태로는 `runner_config.json` 을 손으로 고쳐도 창에서 [저장(적용)] 을 누르는 순간 `collect()` 가 그 종류를 몰라 `params` 를 `{}` 로 덮어써 **대장 경로가 조용히 사라진다**(noop 이 기존 params 를 보존하는 이유와 같은 함정). 패널에 「대장 파일(비우면 .env)」·「스캔 대사 CSV」·「리허설」·「전량 교체하지 않음」 을 만들고 저장 분기를 넣었다. 곁들여 `param_warnings` 에 두 줄 추가 — 대장 파일이 이 호스트에서 안 보이면 창과 로그 머리에 「.env 의 PROPOSAL_LEDGER_XLSX 를 확인하세요」, Supabase 접속정보가 없으면 실패 예고. 회귀 4건 추가(88건 통과) |
| 2026-09-23 | **통합 러너 r1.6 — 구매 기안서 대장 적재 신설(REQ-0077)** | 서브커맨드 `proposal`(모듈 `proposal_ledger.py`) + 작업 종류 `proposal_ledger` 를 붙였다. 원천이 ERP MSSQL 이 아니라 **구매팀 Teams 엑셀 대장**이라 `etl_run.py` 와 분리했고, 적재 대상도 `erp_ro` 가 아니라 포털 전용 `public.pur_proposal`(정본 SQL `이관/sql/68`)이다. 엑셀은 **행 그대로**(1,102행) 넣고 건(830건) 집계는 뷰가 한다 — 한 기안이 업체·품목별로 여러 행이라 합치면 전표 대사를 못 한다. **매번 전량 교체**한다(증분만 하면 지워진 행이 미러에 남는다). 외부 라이브러리 없이 zipfile+xml 로 xlsx 를 읽어 EXE 용량은 그대로다. ⚠ 대장 경로는 호스트마다 다르므로 `.env` 의 `PROPOSAL_LEDGER_XLSX` 로 준다 — **파일이 없는 호스트는 작업을 건너뛰고 사유를 남긴다**(§17.6). 기본 스케줄은 **꺼진 채**(daily 07:30) 들어간다. 회귀 `test_proposal_ledger.py` 14건 신설 + 기존 86건 통과, EXE 실물로 `proposal --dry-run` 1,102행/830건 확인 |
| 2026-09-22 | **통합 러너 r1.4 — ETL 추출 확장 반영(REQ-0070)** · 전달 폴더 취합 자동화 | 발주(`pur_order`) 추출이 17→28컬럼으로 늘어(단가·적요·비고·납품장소·창고코드·담당자 3후보·발주유형·참조번호·추적번호) **EXE 재빌드가 필요**했다 — `etl_run.py` 는 EXE 안에 통째로 박히므로 소스만 고치면 서버는 옛 코드를 계속 돈다. 빌드 후 EXE 내부 `etl_run` 을 꺼내 신규 상수(`PO_PRC`·`DELIVERY_PLCE`·`PO_TYPE_CD`·`APPLICANT`·`erp_etl_upsert_pur_order`)가 들어갔는지 확인했다. 적재 RPC 는 공유 `erp_etl_upsert` 대신 job 전용 `erp_etl_upsert_pur_order` 를 쓴다. ⚠ **서버 교체 후 최초 1회 `jeil_runner.exe etl --job pur_order --full`**(증분은 기존 행을 안 채운다). 곁들여 `deploy/_sync_relay.py` 를 만들어 **저장소 → 전달 폴더** 취합을 명령 하나로 묶었다(EXE·배포문서·조사SQL·안내문). |
| 2026-09-18 | **러너를 단일 통합 프로그램으로 합침(r1.3 · 관리자 직접 지시)** — 서버에 두는 EXE 를 `jeil_runner.exe` **하나**로 줄였다. 릴레이(`gl_relay.exe`)를 없애고 `jeil_runner.exe relay …` 서브커맨드로 흡수했고, 러너에 딸린 CLI 를 같은 방식으로 한 표(`CLI_TOOLS`)에 모았다 — `relay`(결의전표 전송) · `sync`(화면 요청 처리) · `etl`(중간DB 배치) · `offboard`(퇴사 3축) · `mailbox`(사서함 공유 전환). **앞으로 러너 관련 기능은 이 표에 한 줄 추가로 붙인다**(CLAUDE.md §17). EXE 를 `--console` 로 빌드해 서버에서 점검 출력을 바로 보고, 트레이 앱으로 뜰 때는 `hide_console()` 이 그 창을 감춘다(자식 작업은 종전대로 `CREATE_NO_WINDOW`). `offboard_axes.py`·`exo_admin.py` 에 `main()` 추출(서브커맨드 진입점), `relay.cmd` 가 통합 EXE 를 먼저 찾고 옛 `gl_relay.exe` 는 폴백으로만 쓰도록 수정. 회귀 `test_runner_cli.py` 7건 신설 + 기존 59·16건 통과, EXE 실물로 서브커맨드 4종 확인 | 파일이 둘이면 한쪽만 낡는다 — 2026-09-11 에 실제로 서버 릴레이가 v1.6 인데 전달본만 v1.7 이었다(REQ-0017). 배포·갱신 지점을 하나로 만들어 그 어긋남을 구조적으로 없앴다 |
| 2026-09-11 | **트레이 스케줄 러너 `jeil_runner.exe` 신설(r1.2 · REQ-0046)** — 창+트레이에서 릴레이 큐·데이터 업데이트 요청 처리·야간 배치를 보고·실행·중지·설정. 적대적 리뷰 2회(r1.0 20건 · r1.1 재검증 19건 2/2 확정) 반영: [중지]·종료·**시간 상한**이 프로세스 트리째(작업 개체) · 퇴사 처리는 능력이 상한(서버는 큐 미선점) · `need()` SystemExit 격리 · 토큰 **설정 누락만** 축 차단(일시 오류는 축별 재시도) · 예약 알림 스로틀 파일 영속 · 계정·급여 **생략은 일부 실패**로 사유 표시 · 접속 오류 원문 가림 · `.env` 비주입 · 설정 손상·작업 0건 시 꺼진 기본값+일시정지 · idle 로그 1개만(삭제 대상 경로 검증) · **한 PC 한 러너**(루트 잠금+전역 뮤텍스) · 비정상 종료 실행 기록 · 폴더·상위 폴더 ACL 허용 목록 점검. `etl_watch.py`(plan_targets·반환 상태·심박 메모 `+nosens`·`--once` 실패 시 exit 1)·`_env.py`(`JEIL_AX_ENV_ROOT`) 수정. **서버 적용 = 관리자 직접**(C-2~C-4) · **DB 보강 SQL 49 승인 대기**(요청 종류별 선점·재사용·가동 판정) | 작업 스케줄러는 「보이지 않아서」 보류됐고(08-24) ETL 러너는 워크스테이션에서 사람이 띄울 때만 돌아 화면 요청이 방치됐다(09-11 실측 2건). r1.0 은 onefile 자식에 `terminate()` 가 부트로더만 죽여 [중지] 후에도 작업이 계속 돌았다(리뷰 실측) |
| 2026-09-09 | 릴레이 **v1.7 재빌드**(G7 관리항목 실존 가드 + 오류문구 순화, 소스 08-28/커밋 08-31) → 전달 폴더 반영, 구본 `gl_relay.v16-20260826.exe` 백업 | **서버 실행 루트 교체 미완**(심박 v1.6, 09-11 확인) — REQ-0017. 이 표에 기록이 빠져 있어 09-11 소급 기재 |
| 2026-08-25 | **심박·재판정 추가(릴레이 v1.4)** — 회차마다 `gl_relay_ping` 으로 생존 신호를 남기고, `--recheck` 로 실패·적체 건의 사유를 현재 규칙으로 다시 판정한다. **EXE 재빌드·재배포 필요** | 종전엔 화면이 중계 생존을 알 방법이 없어, 중계가 멈춰도 「전송 대기 중 — 곧 가져갑니다」만 계속 띄웠다(2026-08-25 실측: 요청 후 폴링 17회 무응답, 사유 표시 0). 적용 원장은 *일이 있을 때만* 쌓이므로 생존 판정에 쓸 수 없다 — 그래서 별도 심박을 둔다 |
| 2026-08-24 | **릴레이 상시 실행 도입** — 실행 호스트를 워크스테이션(수동) → **ERP 운영 서버(1분 주기)** 로 이전. 배포 방식은 **단일 EXE**(서버에 파이썬 미설치) | 기획서 `03_중간DB_구축실행기획 §D2-a` 「상시 가동 장비 지정 필수」 이행 |
| 2026-08-24 | `relay.cmd` 를 **순수 ASCII**로 재작성 + 라벨 분기 + 타임스탬프 포맷 수정 | ①한글이 들어간 배치는 cmd.exe 파싱이 깨진다 ②괄호 블록 안 `%ERRORLEVEL%` 는 파싱 시점 값(항상 0)이라 실패가 성공으로 보고된다 ③`Get-Date -Format` 에 공백이 있으면 인자 바인딩 실패로 타임스탬프가 빈다 — 셋 다 실측 확인 후 수정 |
| 2026-08-24 | **전달 폴더 경로 변경** — `ai.jeil - ax\relay\` → **`jeil_ax\relay\`**. 실행 루트(`E:\ai.jeil\relay\`)는 불변 | 전달 폴더가 바뀌어도 고칠 곳은 `README §0·§1` 의 `$src` 뿐이다. 배포본 9개 SHA256 원본 대조 일치 확인 |
| 2026-08-25 | **실행 주체 기록 + 실패·적체 알림 추가** — `applied_by` 에 `호스트/계정/실행방식` 기록(종전엔 전부 `gl_apply_demo2.py` 로 같아 워크스테이션·서버 구분 불가), `TEAMS_WEBHOOK_URL` 설정 시 실패·적체 알림. **EXE 재빌드 필요** | 운영에서 「누가 언제 어디서 넣었나」에 답하려면 최소한 호스트·계정은 남아야 한다. 알림은 무인 운영의 선행 조건 — 없으면 실패가 안 보이는 곳에 쌓인다 |
| 2026-08-25 | **서브원장 생성 SP 호출 추가** — 엔진 뒤에 `usp_a_create_temp_gl_subsys` 를 같은 트랜잭션에서 호출하고, 연결번호·채무·부가세를 검증한다. **EXE 재빌드·재배포 필요**(코드 변경) | 엔진은 전표만 만들고 서브원장은 만들지 않는다. 이 호출이 없던 동안 「전표·시산표는 멀쩡한데 채무·부가세 원장이 없는」 상태가 만들어졌다(2026-08-24 실측: AX 대상 26라인 `SUBSYS_NO` 전건 공백 vs 수기 5,483/5,483) |
| 2026-08-24 | **실행 루트 확정** — `E:\ai.jeil\` → **`E:\ai.jeil\relay\`**(관리자 실제 배치에 맞춤). `relay.cmd` 의 `GL_RELAY_ROOT` 기본값과 문서 20곳을 함께 갱신 | 서버에서 한 단계 깊은 곳에 풀어 두고 `relay.cmd` 를 돌리면 EXE 를 못 찾아 **exit 9** 로 조용히 끝난다(2026-08-24 실측 — 적용 원장에 흔적 0). 실행 루트를 바꿀 때 `relay.cmd` 를 같이 고치지 않으면 매번 이 증상이 난다 |
| 2026-08-24 | `_env.py` 에 `env_root()` 신설 — frozen(EXE)이면 `sys.executable` 기준으로 `.env` 탐색 | 번들은 임시폴더에 풀려 `__file__` 기준으로는 `.env` 를 못 찾는다 |
