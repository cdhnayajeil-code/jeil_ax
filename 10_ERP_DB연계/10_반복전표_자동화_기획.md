# 10. 결의전표 반복전표 자동화 — ERP 실측 분석·기획

> **작성일: 2026-08-10 · 최종 갱신: 2026-08-20**(v1.5 — B2 개정: 결정 C-14로 부가세 자동계산 도입, 원천징수는 계속 범위 제외. 이전 v1.4 · 2026-08-12 P1·P3 구현완료 / v1.3 · 2026-08-12 B1 실적재 / v1.2 · 2026-08-11 승인 게이트 결정 반영) · 관리: 최동혁 · 전담 에이전트: `erp-db-link-manager`
> 매달 같은 형태로 반복 작성되는 결의전표를 **한 번 등록해 두고 변동값만 고쳐 재사용**하는 자동화 툴의 분석·기획 문서.
> ERP운영DB 2026년 수기 결의전표 **2,287건 전수**를 읽기전용으로 분석해 실측 근거로 기획했다(추측 없음). 본문 수치는 분석 도구(`etl/analyze_repeat_gl.py`) 출력으로 전부 재현 가능하다.
> 기존 결의전표 파일럿(06·07·08·09)과 템플릿 플랫폼 v1(09 §3.4) 위에 얹는 **v2 확장**이다.
> 비밀값(접속 IP/ID/비밀번호) 기록 금지 — 서버 지칭은 별칭 **"ERP운영DB"** 만 사용.

---

## 1. 30초 요약

**무엇을 확인했나** — 2026년 1~8월 수기 결의전표(TG) 2,287건 중 **42.2%(964건)가 "동일 부서 + 동일 계정 조합 + 동일 거래처"로 3개월 이상 반복**되는 156개 패밀리에 속한다. 월평균 약 **134건**이 매달 거의 같은 내용을 다시 입력하는 전표다. 이 중 34개 패밀리는 **금액까지 완전 고정**(예: (주)쿠콘 275,000원, KT 968,000원, 거산엔지니어링 임대료 27,500,000원)이라 날짜만 바꾸면 그대로 재사용 가능하다.

**무엇을 만들려 하나** — 템플릿 플랫폼 v1(구조만 완성, 등재 0건)을 확장해:
① ERP 기작성 전표에서 **시드 템플릿을 자동 생성**(분석으로 후보 확보 완료),
② 사용자는 **"이번 달 작성" 클릭 → 전월 값이 채워진 폼에서 변동값만 수정 → 제출**,
③ 계정별 **관리항목**(공급가액·계산서일·부가세액 등 — 이번 분석으로 체계 실측 완료)을 자동 생성·자동 제안.

**핵심 발견(보너스)** — 승인 대기 중이던 **A5(회계 규칙) 중 "계정 후보 좁히기" 항목의 실측 근거가 확보**됐다. 반복 패밀리가 실제 사용하는 계정은 691개 중 **88개**(3개 패밀리 이상 반복 등장하는 핵심 계정은 **44개**)로 실측됐고, 계정별 필수 관리항목 규칙도 ERP 설정 테이블(`A_ACCT_CTRL_ASSN` 997건)에서 그대로 가져올 수 있다. 단 A5의 나머지 항목(기본 상대계정·부가세 처리·마감 기준)은 여전히 회계 담당 결정이 필요하다 — §8.

---

## 2. 무엇을, 어떻게 분석했나

| 항목 | 내용 |
|---|---|
| 분석 대상 | `A_TEMP_GL`(헤더) · `A_TEMP_GL_ITEM`(라인) · `A_TEMP_GL_DTL`(관리항목) — 2026-01-01~08-10, `GL_INPUT_TYPE='TG'`(수기 결의) |
| 조회 방식 | **읽기전용 SELECT만**(`WITH (NOLOCK)` + 파라미터 바인딩, DDL/DML 없음) — CLAUDE.md §4 준수 |
| 규모(도구 stats로 재현) | 헤더 2,287건 · 라인 10,858건 · 관리항목 27,634건 + 마스터(관리항목 52·계정별요건 997·계정 691·거래처 4,660) |
| 민감값 처리 | 은행계좌·사원번호·카드·어음번호는 **컬럼 미조회 또는 값 미기록**(관리항목 EM/BA/D1/CP/NN은 고유값 '개수'만 저장 — 고정/변동 판별에 충분). 분석 출력(JSON)은 OneDrive 밖 `%LOCALAPPDATA%\jeilax\erp_analysis\` 로컬 전용, 커밋·중간DB 적재 금지 |
| 반복 판정 | 전표별 시그니처 = 정렬된 (계정코드, 차/대변) 목록 + 거래처 집합. 같은 부서에서 같은 시그니처가 **3개월 이상** 등장하면 반복 패밀리. 제목의 연/월 표기를 제거한 "제목 줄기" 기반 보조 탐지 병행 |
| 승인 근거 | A2(전표 샘플 조회) 승인(2026-08-03) + 관리자 지시(2026-08-10, 반복전표 자동화 분석) |
| 분석 도구 | `10_ERP_DB연계/etl/analyze_repeat_gl.py`(재실행 가능 — §6.6 시드 갱신에 재사용. 유형·월1건형·월오프셋 판정을 출력에 저장해 본문 수치 전부 재현 가능) |

---

## 3. 분석 결과 ① — 반복 전표가 얼마나 되나

### 3.1 전체 볼륨

| 구분 | 수치 |
|---|---|
| 2026년 수기(TG) 결의전표 | **2,287건** (1월 311 · 2월 244 · 3월 352 · 4월 389 · 5월 284 · 6월 326 · 7월 341 · 8월 40) |
| 작성자 / 부서 | 78명 / 26개 부서 |
| **반복 패밀리(3개월+)** | **156개** — 소속 전표 964건, **전체의 42.2%** |
| 반복 월수 분포 | 8개월 연속 4개 · 7개월 27개 · 6개월 19개 · 5개월 23개 · 4개월 29개 · 3개월 54개 |
| 금액까지 완전 고정 | **34개 패밀리** (변동 122개) |
| 고신뢰(5개월+) | 73개 패밀리 · 567건 |
| 순수 월반복(6개월+ 이면서 전표수=월수) | **36개 패밀리 · 242건** ← 1차 시드 등재 후보 |
| 제목줄기 기반 추가 후보 | 30개 (전도금 정산·운반비·급여 등 계정 조합이 매달 조금씩 다른 형태) |

### 3.2 부서별 분포 (상위)

| 부서 | 패밀리 | 전표 수 | 비고 |
|---|---|---|---|
| **총무팀** | **65** | **421** | 최다 — 시설관리·임대료·통신·급식 등 월정 거래처 지급이 집중 |
| 사업 1파트 | 21 | 146 | 출장·개인경비 정산 반복 |
| 자재물류팀 | 16 | 80 | 운반비·포장비 |
| 인사팀 | 12 | 73 | 4대보험·원천세·프리랜서 — **구조 완전 고정, 금액만 변동** |
| 자금팀 | 12 | 71 | 계좌대체·금융수수료 |
| 김해생산팀 | 6 | 50 | 김해 사업장 시설·운반 |
| 운영지원팀 | 9 | 44 | 생수·비품 등 |

### 3.3 유형 분류 (전표 구조 기준)

판정 규칙(분석 도구가 패밀리별 `type`으로 저장): 부가세대급금(11103301) 포함 → T1(총액 완전고정)/T2(변동) · 예수금/미지급세금/미지급급여 포함 → T5 · '개인' 계정 포함 → T3 · 보통예금 포함 + 자산/비용만 → T6 · 그 외 → T7. (T4 결번 — 급여형은 T5에 통합)

| 유형 | 패밀리 / 전표 | 구조 | 자동화 방식 |
|---|---|---|---|
| **T1 세금계산서 매입·고정형** | 25 / 153 | 차) 부가세대급금 + 비용계정(1~n) / 대) 미지급금(거래처). 금액 완전 고정 | **날짜만 갱신해 복사** + 제출 전 요약 확인 — 최상급 자동화 |
| **T2 세금계산서 매입·변동형** | 44 / 283 | T1과 동일 구조, 금액만 매월 변동 | **공급가액 1칸 입력** → 부가세·미지급금·판/제/연 배분 자동 제안 |
| **T3 개인경비 정산형** | 27 / 159 | 차) 비용 / 대) 미지급금(개인). 관리항목 사번(EM)·현금지급요청일(XC1) | 계정·사번 프리필, 금액·적요만 입력 |
| **T5 원천세·보험·급여형** | 11 / 67 | 다계정 판/제/연 + 예수금 차/대 혼합(4대보험 4종·원천세·급여) | 라인 구조 완전 고정 — **금액 열만 채우는 월간 폼** |
| **T6 자금이체형** | 7 / 44 | 차/대 보통예금(+수수료). 관리항목 은행(BK)·계좌(BA) | 계좌 짝 프리필 |
| **T7 무부가세 거래처지급 등 기타** | 42 / 258 | 차) 비용 / 대) 미지급금(거래처), 계산서 없음(해외법인·아파트관리비 등) | T2와 동일하되 부가세 라인 없음 |

> **계산서유형(V4) 실측 분포**: A(일반과세) 68패밀리 · K(면세계산서) 4 · B(불공제 추정) 2 · C(수입) 1 · L 1 · Q(매출측) 1. **1차 구현 범위는 A·K 한정** — B·C·L·Q는 부가세 분개 구조가 달라(§4.2) 시드 후보에서 자동 제외하고 일반 작성으로 폴백한다.
> 전 유형이 기존 템플릿 v1의 배분 규칙(`input`/`pct`/`fixed`/`balance`)으로 표현 가능함을 확인했다 — **새 배분 개념은 불필요**하고, 부족한 것은 §5의 4가지다.

### 3.4 대표 사례 (실측 — 시드 등재 1순위)

| 패밀리 | 반복 | 구조(계정) | 변동값 |
|---|---|---|---|
| (주)쿠콘 국세청 스크래핑 (총무팀) | 7개월, 총액 고정 275,000 | 차) 부가세대급금 25,000 + 판)지급수수료(기타) 250,000 / 대) 미지급금(거래처) 275,000 | 결의일자·계산서일(V2)·만기일(C1)·적요 월표기뿐 |
| KT 서울사무소 기가오피스 (총무팀) | 6개월, 총액 고정 968,000 | 차) 부가세대급금 88,000 + 제)통신비 269,984 + 판)통신비 610,016 / 대) 미지급금 968,000 | 〃 (판/제 배분도 고정) |
| 거산엔지니어링 공장 임대료 (총무팀) | 7개월, 고정 27,500,000 | 차) 부가세 2,500,000 + 제)지급임차료 25,000,000 / 대) 미지급금 | 〃 |
| 한성세종푸드 급식대 (총무팀) | 8개월 31건(주차 단위) | 차) 부가세 + 판/연/제 복리후생비(식대) 3계정 배분 / 대) 미지급금 | 공급가액·판/제/연 배분액 |
| 4대보험 4종 (인사팀) | 각 7개월 | 판/연/제 세금과공과·보험료 + 예수금 차/대 (전표당 5~8라인) | 금액 전부(구조·거래처 고정) |
| 프리랜서 용역비 (인사팀) | 7개월 | 차) 수수료 / 대) 예수금(소득세)+예수금(주민세)+미지급급여(잔액) | 총액 1개 → 원천징수 파생 계산 가능(§4.2) |
| 개인 출장경비 정산 (사업 1파트) | 8개월 23건 | 차) 제)여비교통비 / 대) 미지급금(개인) | 금액·사번·프로젝트 |

---

## 4. 분석 결과 ② — 관리항목 체계 실측 (이번 기획의 열쇠)

ERP 결의전표 화면 하단의 "관리항목" 그리드(공급가액·계산서일·거래처코드…)는 **`A_TEMP_GL_DTL`**(전표번호+라인순번+관리항목코드 `CTRL_CD`+값 `CTRL_VAL`)에 저장되며, **어느 계정에 어떤 관리항목을 입력해야 하는지는 ERP 설정 테이블 `A_ACCT_CTRL_ASSN`(997건)이 계정×차/대변 단위로 정의**하고 있다. 관리항목 종류는 `A_CTRL_ITEM` 52종.

**현 포털 파일럿(gl_draft)에는 관리항목 개념이 아예 없다** — 세금계산서 매입형(T1·T2, 반복 전표의 45%)을 지원하려면 이 구조가 반드시 필요하다.

### 4.1 주요 계정별 관리항목 (실측 — 반복 패밀리 964건 기준 사용 빈도순)

"필수"는 `A_ACCT_CTRL_ASSN`의 차/대변 플래그(DR_FG/CR_FG)가 'Y'인 항목, "선택"은 설정상 연결돼 있으나 플래그가 없는데 실무에서 쓰이는 항목이다. **서버 필수검증은 플래그 'Y' 항목만 대상**으로 한다(플래그의 정확한 의미는 유니포인트 확인 항목 — §8).

| 계정 [패밀리/전표] | 필수(플래그 Y) | 선택(실측 사용) |
|---|---|---|
| 21100901 미지급금(거래처) [82/552] | BP(거래처) · C1(만기일) | XC1(현금지급요청일) |
| 11103301 부가세대급금 [69/436] | **V1 공급가액 · V2 계산서일 · V4 계산서유형 · V5 신고사업장 · V6 거래처코드 · V8 부가세액** | V11(전자발행여부) |
| 21100902 미지급금(개인) [27/159] | EM(사번) | XC1(현금지급요청일) |
| 43001101 제)여비교통비(국내) [17/100] | PC(프로젝트코드) | BP(거래처) |
| 43003305 제)지급수수료(기타) [10/71] | PC(프로젝트코드) | BP(거래처) |
| 53014307 판)지급수수료(기타) [11/67] | (없음) | PC · BP |
| 43001103 제)여비교통비(해외) [9/59] | PC(프로젝트코드) | BP(거래처) |
| 21100101 외상매입금 [11/59] | BP(거래처) | C1(만기일) |
| 11100105 보통예금 [9/52] | BK(은행) · BA(계좌번호) | — |

### 4.2 파생 계산 규칙 (실측 검증 결과 — **전부 '제안값'이며 실물 증빙이 정답**)

| 규칙 | 실측 근거와 한계 |
|---|---|
| V8(부가세액) ≈ V1(공급가액) × 10% — 과세(V4=A). 면세(K)는 V8=0 | 다수 패밀리에서 성립하나 **정확히 10%가 아닌 실전표가 실존** — 렌탈료 전표는 품목별 부가세 절사 후 합산이라 최대 10원 수준 차이, 통신료 전표는 청구액 절사 단수차를 잡이익 라인(9원)으로 처리. **따라서 V8은 자동 계산 '제안값' + 수기 수정 가능으로 설계하고, 서버 검증은 경고(soft) 또는 계산서당 ±10원 허용으로 한다.** 수입(C)·불공제(B)는 10% 관계 자체가 불성립 — 1차 범위 제외 |
| 미지급금(대변) = Σ(V1+V8) — **계산서 그룹 단위** | 한 전표에 계산서 여러 건(예: 코로케이션 2건, 렌탈 5건)이 실존 — 라인·전표 단위가 아니라 **계산서 그룹별** 대응이며, 단수차 잔액(잡이익/잡손실 라인) 허용이 필요. 데이터 모델에 계산서 그룹 키(§6.3 `invoice_seq`) 필수 |
| V6(거래처코드) = 미지급금 라인 BP | 전 세금계산서형 패밀리에서 일치 |
| V5(신고사업장) = TX1(이천)/TX2(김해) — **부서로 유추 불가** | 김해 관련 패밀리 18개 중 TX2 단독 9 · TX1 단독 6 · 혼재 3으로 전표별로 갈린다. **규칙 강제 금지 — 템플릿별 전월 실측값 프리필로만 처리** |
| 프리랜서 원천징수: 소득세=총액×3% → **주민세=⌊소득세×10%⌋(10원 미만 절사)** → 미지급급여=잔액 | 헝가리법인 케이스에서 '총액×0.3%' 단순식은 6원 과대(실측 10,740 vs 계산 10,746) — 절사 규칙 포함식이 실전표와 일치. 소액부징수(1천원 미만) 경계는 검증 규칙에 주석으로 유지 |
| V2(계산서일)·C1(만기일)은 매월 **유사** 일자로 +1개월 이동 | 동일 일자가 아니다(휴일·월말 보정 실존). +1개월 제안 시 **월말은 말일로 클램프**(1/31→2/28), **항상 '변동' 취급으로 잠금 제외**, 제안값 배지 표시 + 제출 전 확인(§6.1) |
| 적요 월 표기: **당월형 84% · 전월형 11%**(월표기 전표 623건 실측) | 전역 기본값 없음 — **패밀리별 실측 오프셋(`month_offset` 0/-1)을 분석 도구가 자동 판정해 템플릿에 저장** |

---

## 5. 현 템플릿 플랫폼(v1)과의 갭 — 왜 v2가 필요한가

v1(2026-08-05, 09 §3.4)은 "케이스별 분개 형태 등록 → 골라서 총액 입력" 구조를 만들었지만 **등재 0건**으로 미가동이다. 이번 분석 기준으로 부족한 것:

| # | 갭 | 영향 |
|---|---|---|
| G1 | **관리항목(CTRL) 구조 부재** — 초안·템플릿 모두 저장 불가 | 반복 전표의 45%(세금계산서형)를 표현 못함. 회계 담당자가 ERP 하단 그리드를 결국 수기로 다시 입력 |
| G2 | **부가세 케이스 미지원** — 파일럿은 부가세 미사용 케이스 한정(확정 결정 C-2) | T1·T2 유형 지원 불가. **C-2 범위 개정은 승인 사항**(§8 B2) |
| G3 | **기작성 전표 기반 생성 부재** — 템플릿을 백지에서 수작업 등록해야 함 | 등재 0건이 증명하는 병목. 실측 후보 156개가 있는데 활용 경로가 없음 |
| G4 | **월반복 워크플로 부재** — "지난달 것 이번 달 것으로" 개념 없음 | 사용자가 매달 템플릿을 찾아 총액부터 다시 입력. "이번 달 미작성" 파악 불가 |

---

## 6. 기획 — 반복전표 자동화 (템플릿 플랫폼 v2)

### 6.0 설계 원칙

1. **"초안은 포털, 확정은 ERP"(C-1) 불변** — 이 기획은 입력 단계 자동화이며 ERP 쓰기(E3)와 무관하게 성립한다.
2. **전표 이력은 중간DB에 적재하지 않는다(C-7 유지)** — 중간DB에는 코드 마스터 2종만 추가한다(§6.5). 단 **시드 템플릿의 고정 금액(T1형 fixed 값) 저장은 v1 "실적 금액 미저장" 원칙의 명시적 변경**이며 §8 B2 승인 범위에 포함한다. 변동 금액의 프리필은 ERP 값이 아니라 **포털이 스스로 만든 데이터(본인 최근 확정 초안)** 를 참조한다(§6.2 F2).
3. **템플릿을 적용해도 서버 재검증은 전부 통과해야 한다**(v1 원칙 유지) + 관리항목 검증 추가. 단 부가세 관계식은 hard가 아닌 **soft(경고)** — 실물 계산서가 정답이다(§4.2).
4. **자동 제출은 없다** — 어떤 자동화 수준에서도 사람이 확인·제출한다(§1.6 실거래 액션 금지).

### 6.1 목표 사용자 경험 (핵심 플로우)

```
[월반복 홈]                        [작성 폼(프리필)]                [제출 전 확인]         [제출]
내 반복전표 카드 목록       ──►    전월 값 채워짐             ──►   요약 1화면:      ──►  기존 gl_draft
· 쿠콘 스크래핑   ✅8월 확정        · 결의일자 = 오늘                 총액·계산서일·        파이프라인
· KT 기가오피스   📤8월 제출됨      · 적요 월표기 자동 갱신            만기일·귀속월 +       (검증→제출→
· 급식대         ✏️작성중(dh.lee)   · 계산서일·만기일 '제안값'         전월과 달라진 점       회계담당자→
· 전기안전관리    ⚠8월 미작성       · 변동 필드만 하이라이트           n건 리스트            ERP 입력→
  [이번 달 작성]/[이어서 작성]      · 고정 필드 잠금(해제 아이콘)                            회기입 확정)
```

- **T1 고정형**: 프리필 확인 → 요약에서 날짜 2개(계산서일·만기일) 확인 → 제출. **금액 입력 0회**, 확인 1회.
- **T2 변동형**: 공급가액 1칸 입력 → 부가세·미지급금·판/제/연 배분·관리항목 자동 제안. 입력 1회.
- **T5 보험형**: 라인 고정 폼에서 금액 열만 채움(고지서 보고 옮겨 적기).
- 현행 ERP 직접 입력 대비: 전표 1건당 헤더 6필드 + 라인×(계정 검색 포함 7~10필드) + 관리항목 5~7필드 → **0~3필드 + 확인 1회**로 축소.
- **1차 지원 범위**: 작성·수정 폼은 데스크톱 기준. **카드 홈(F1)과 T1 고정형의 확인·제출 경로는 모바일 대응 목표**(자리 비운 담당자가 배지 보고 즉시 처리하는 시나리오가 모바일 가치 최대).

### 6.2 기능 명세

| # | 기능 | 내용 |
|---|---|---|
| **F1 월반복 홈** | 「전표 작성」 탭 상단(또는 별도 「월반복」 탭)에 내/내부서 반복 템플릿 카드. **배지 4상태: ⚠미작성 / ✏️작성중(작성자 표시) / 📤제출됨 / ✅확정됨** — 판정은 `gl_template_usage`(초안 생성 시점 기록, 상태 동기 갱신 §6.4). 미확정 초안이 있으면 [이어서 작성] 버튼 + [이번 달 작성] 클릭 시 "이미 ○○○님이 8/1 제출한 건이 있습니다" 중복 경고. 정렬 기본 = 미작성 우선 → 최근 사용순, 거래처·유형 검색, 카드 20개 초과 시 그룹 접기. 주차·수시 반복 케이스는 월1회 배지 대신 **"이번 달 3건 · 최근 8/4(7일 경과)" 카운터** 표시 |
| **F2 전월 프리필** | **프리필 정본 이원화**: 라인 구성·고정값·파생 규칙은 템플릿 정의에서, 변동 필드 기본값은 **요청자 본인의 최근 확정(posted) 초안**에서 가져온다(부서 공유 템플릿이어도 타인 초안은 참조하지 않음 — 본인 이력이 없으면 규칙값만 프리필). EM·BA 등 개인 식별 관리항목은 부서 템플릿의 프리필·카드 표시 대상 제외. 결의일자=오늘, **적요 월 표기는 템플릿 토큰(`{YY}년{M}월`)+실측 오프셋(`month_offset`)으로 자동 갱신**, 날짜형 관리항목(V2·C1)은 직전 회차 값+1개월(월말 클램프)을 '제안값' 배지로. 일회성 수정이 다음 달로 전이되지 않도록 스냅샷 갱신은 자동이 아니라 **"이번 값을 다음 달 기본으로 저장" 명시 선택** |
| **F3 변동 필드 하이라이트** | 템플릿 라인·관리항목마다 고정/변동 플래그(시드는 실측 `amt_distinct`로 자동 판정, 날짜형은 항상 변동). 변동 필드만 입력 활성·강조, 고정 필드는 잠금 + **눈에 보이는 해제 아이콘** + 최근 3개월 값 툴팁(요금 인상 감지 근거). 고정액을 해제·수정해 제출하면 **"템플릿의 고정값도 갱신할까요"** 선택 제공(다음 달 재발 방지). 오제출 방어는 반복 모달 대신 **제출 전 요약의 "전월과 달라진 점 n건 / 같은 점" 차이 리스트**로(습관적 통과 방지) |
| **F4 총액 자동 배분** | v1 배분 규칙(`input`/`pct`/`fixed`/`balance`) 재사용 — 비용 판/제/연 비율 배분·차액(`balance`) 채움만 수행. **[B2 결정 2026-08-11] 부가세·원천징수 자동계산은 범위 제외** — V8(부가세액)·원천징수액은 자동 계산하지 않고 사용자가 실물 증빙 값을 직접 입력(전월값 프리필만). §4.2의 파생식은 향후 승인 시를 위한 실측 기록으로만 유지 |
| **F5 관리항목 자동화** | 계정 선택 시 `acct_ctrl_assn_s` 기준 관리항목 필드를 차/대변 구분 반영해 자동 생성(**필수검증은 플래그 'Y' 항목만**). 파생값(V1·V6·V8)은 자동 계산 제안 + 수정 가능. **「ERP 입력용 요약」에 관리항목 표 추가 — `acct_ctrl_assn` seq 순서(ERP 입력 순서)로 정렬, 항목별·전체 TSV 복사 버튼**(그리드 표준 §13 규약 재사용) — 회계 담당자가 ERP 하단 그리드까지 그대로 옮겨 붙이게 |
| **F6 라인 복사·증감** | 표준 그리드에 행 복사 버튼(관리항목 세트 동반 복제, 에스원형 다라인 대응). **행 삭제 시 관리항목 동반 삭제 + `balance` 라인 자동 재계산, `pct` 라인 증감 시 비율 재정규화(또는 수동 모드 전환 안내), 신규 행 기본 플래그=변동.** 구조가 템플릿과 달라지면 초안·카드에 "템플릿과 다른 구성" 표시 |
| **F7 전표→템플릿 승격** | v1 「현재 전표를 템플릿으로」 확장: 확정(`posted`)된 내 초안에서 원클릭 승격 + 월반복 여부·제목 토큰·고정/변동 플래그 지정 UI. **일반 사용자 승격분은 공개범위 `user` 고정 — `dept`/`org` 전환은 관리자·회계 담당자 승인 시에만**(v1 거버넌스 유지) |
| **F8 시드 일괄 등재** | 분석 도구가 만든 후보(1차: 월1건형 **36개** → 검수 후 5개월+ 73개로 확대)를 회계 담당 검수(§8 B2) 후 `erp_case_cd`=패밀리 ID로 일괄 등재. **V4가 B·C·L·Q인 패밀리는 자동 제외**(§3.3). `month_offset`·고정/변동 플래그는 실측 자동 판정. 공개범위 기본 `dept`, 작성자가 특정인인 케이스는 `user` — **`user` 범위 템플릿은 담당자 교체 시 관리자·회계 담당자가 범위를 `dept`로 변경하는 이전 절차**를 함께 규정. 찾는 템플릿이 없으면 "일반 작성 + 부서 공유 요청" 안내 경로 제공 |
| **F9 처리 현황 보드** | 회계 담당자 탭에 **미처리(제출됨) 초안 대기열 + 건별 완료 체크오프** 뷰를 추가하고, 월별 반복전표 처리율은 그 파생으로 계산. 미작성 알림은 후순위(L3) |

### 6.3 데이터 모델 확장 (DDL 스케치 — 정본은 구현 시 `23_gl_template_v2.sql`)

```
-- 초안 라인 관리항목 (ERP A_TEMP_GL_DTL 대응)
gl_draft_item_ctrl   (draft_id, item_seq, ctrl_cd, ctrl_val,
                      invoice_seq)   -- 계산서 그룹 식별(다계산서 전표 검증 단위 §4.2)

-- 템플릿 라인 관리항목 + 값 규칙
gl_template_item_ctrl(template_id, item_seq, ctrl_cd,
                      val_rule,      -- fixed | input | derived(파생계산) | date_shift(+1개월·월말클램프)
                      fixed_val)     -- fixed 규칙일 때만(B2 승인 범위)

-- 템플릿 헤더 확장
gl_template + recur_cycle('monthly'|null), title_pattern(월 토큰),
              month_offset(0|-1 — 적요 월표기 실측 오프셋),
              src_family_key(분석 패밀리 식별자),
              vat_mode('taxable'|'exempt'|null — V4 A/K 대응, B·C·L·Q는 1차 미지원)

-- 템플릿 라인 확장
gl_template_item + amt_locked(고정/변동)   -- 변동 프리필은 본인 최근 확정 초안 참조(컬럼 저장 안 함)

-- 회차별 사용 이력 (F1 배지·F9 처리율의 근거)
gl_template_usage    (template_id, use_ym, draft_id, status, used_by, used_at)
                      -- use_ym = 결의일자의 연월 기본. 소급 작성 대비 폼에 '처리 회차' 선택 필드,
                      --          배지·처리율 판정은 전부 이 값 기준
                      -- 기록 시점 = 초안 생성 시(§6.4), status는 초안 상태와 동기

-- 중간DB 코드 마스터 2종 (RLS는 기존 erp_ro 규약 동일)
erp_ro.ctrl_item_s      (ctrl_cd, ctrl_nm, ...)                              -- 52행
erp_ro.acct_ctrl_assn_s (acct_cd, ctrl_cd, seq, dr_fg, cr_fg, default_val)   -- 997행
```

전부 RLS ON·정책 0(Edge Function 단일 경로) — 기존 gl_* 테이블 규약(C-6)과 동일.

### 6.4 Edge Function(`jeil-gl-draft`) op 확장

| op | 역할 |
|---|---|
| `tpl_recur_list` | 월반복 템플릿 + 이번 회차 상태(F1 카드 데이터 — 미작성/작성중/제출됨/확정됨 + 작성자) |
| `tpl_apply_prev` | 본인 최근 확정 초안 + 날짜/적요 갱신 제안값 반환(F2) |
| `save` 확장 | 라인 관리항목 배열 수용 + **초안 생성 시 `gl_template_usage` 기록**(템플릿 유래 초안). **서버 재검증 — hard**: 필수 관리항목 누락(플래그 'Y'만) · V6=BP 일치 · 차대일치(기존) / **soft(경고)**: V8≈V1×10%(계산서 그룹당 ±10원) · 계산서 그룹별 미지급금=ΣV1+V8(단수차 잡이익/잡손실 허용) · V2가 결의일자보다 미래 등 이상치 |
| `submit`·`post`·`void` 확장 | `gl_template_usage.status` 동기 갱신(F1 배지 소스) |
| `tpl_save` 확장 | 관리항목 규칙·월반복 속성·`month_offset`·고정/변동 플래그 저장 |
| `tpl_seed_bulk` **신설** | 시드 일괄 등재 — **관리자 한정, Edge Function 단일 경로 유지(C-6), 등재 행위를 감사 로그(`gl_draft_log` 상당)에 기록** |

### 6.5 중간DB에 새로 담는 것 / 담지 않는 것

| 구분 | 내용 |
|---|---|
| **담는다(신규 ETL job 2종)** | `ctrl_item`(52행)·`acct_ctrl_assn`(997행) — **코드/설정 마스터**로 A1 승인의 연장선. `erp_master_upsert` 경로 재사용, `--dry-run` 검증 후 관리자 직접 실행(기존 관례). 참고: `A_ACCT_CTRL_ASSN.DEFAULT_VALUE`는 실측 997행 전량 공란 — 기본값 프리필 용도로는 쓸 수 없고 프리필은 F2로 일원화 |
| **담지 않는다** | 전표·분개 라인·금액 이력(C-7 유지). 시드 생성용 반복 분석은 `analyze_repeat_gl.py`가 로컬(OneDrive 밖)에서 수행하고 결과 JSON은 커밋·적재하지 않음. 시드의 고정 금액만 B2 승인 하에 포털 템플릿에 저장(§6.0-2) |

### 6.6 시드 템플릿 등재 절차 (F8 상세)

1. 분석 도구 실행(완료) → 후보 156개 중 **1차 = 6개월+ 월1건형 36개** 추출(V4 B·C·L·Q 유형 제외 적용)
2. 후보별 시드 정의 자동 생성: 계정 라인(고정/변동 플래그·배분 비율) + 관리항목 규칙(V계열 파생·날짜 시프트) + 제목 토큰·`month_offset`
3. **회계 담당 검수**(§8 B2): 계정 구성 적정성 + **부가세·원천징수 파생 규칙 승인 + 고정 금액 저장 승인** — A5 중 '계정 후보' 항목의 실측 대체
4. 검수 통과분 `tpl_seed_bulk`로 일괄 등재(감사 기록) → 파일럿 부서부터 노출
5. 분기 1회 재분석으로 신규 패밀리 추가·불용 템플릿 정리(`use_cnt`·`last_used_at` 근거)

### 6.7 자동화 수준 로드맵

| 레벨 | 내용 | 상태 |
|---|---|---|
| L0 | 빈 폼 수기 입력 (현 파일럿) | 가동(관리자 한정) |
| **L1** | 템플릿 선택 → 총액 입력 (v1) | 구조 완성, 등재 0건 |
| **L2** | **월반복 프리필 + 변동값만 수정 + 관리항목 자동** (본 기획) | ← 이번 목표 |
| L3 | 월초 자동 초안 생성 + 미작성 알림(제출은 사람) | 파일럿 후 판단(§8 B4). **전제**: 현행 "등록자는 JWT에서만 취득" 규약과 충돌하므로, 자동 생성분은 등록자 미확정 상태로 두고 사용자가 열어 제출하는 시점에 JWT로 등록자를 확정하는 방식이어야 함 |
| L4 | ERP 자동 등록 (쓰기 API) | = 기존 E3. 유니포인트 협의 + 경영 승인 필요, 본 기획 범위 밖 |

### 6.8 구현 순서 (제안)

| 단계 | 내용 | 선행 | 상태(2026-08-12) |
|---|---|---|---|
| ~~**P1 구조**~~ ✅ | DDL(§6.3) + Edge Function 확장(§6.4) + ETL job 2종 | — | **완료(2026-08-12)** — 마이그레이션 `gl_template_v2`(관리항목·usage·월반복 속성·RPC `gl_ctrl_master_get`, 정본 24번) + **`jeil-gl-draft` v4 배포**(신규 op 3종: `tpl_recur_list`·`tpl_apply_prev`·`tpl_seed_bulk` + save/get/submit/void/post 확장, 구화면 하위호환 유지) |
| ~~**P2 마스터 적재**~~ ✅ | `ctrl_item`·`acct_ctrl_assn` 실적재 | ~~승인 B1~~ ✅ | **완료(2026-08-12)** — 52행·1,002행, 연동 현황 16종 등재 |
| ~~**P3 화면**~~ ✅ | F1~F6 화면 구현(월반복 홈·프리필·하이라이트·관리항목 필드) | P1 | **완료(2026-08-12)** — 월반복 홈(배지 4상태·중복 경고·이어서 작성), 전월 프리필(본인 확정 초안 기반 + 날짜 +1개월 제안값 배지), 관리항목 자동 패널(필수* 표시), 제출 전 요약(총액·날짜·전월 差), 회계 담당자 관리항목 표+TSV 복사, 템플릿 편집기 월반복 속성. **3관점 코드 리뷰(정확성·보안·UX) 지적 16건 반영** — 관리항목 유실(치명)·구화면 하위호환·KST 회차·민감 관리항목(사번·계좌) 부서 템플릿 고정값 차단·타인 미제출 초안 열람 제한 등. 라이브 반영은 커밋·배포(gh-pages) 후 |
| **P4 시드 등재** | 36건 검수→등재(F8, `tpl_seed_bulk`) — **B2 결정에 따라 세액 자동계산 없는 형태로 등재** | 승인 B2(계정 구성 검수 잔여) | 검수 대기 — 검수 완료 시 분석 도구 출력에서 시드 JSON 생성 후 일괄 등재 |
| **P5 파일럿** | **이훈희·최동혁**(B3 확정) 실사용 → 기존 S5 E2E 테스트와 통합 진행 | ~~B3 + 마감 등재~~ ✅ | 권한·마감·코드 준비 완료 — **화면 커밋·배포 후 개시 가능** |
| P6 회고·확대 | 기존 S6과 통합 — 처리율(F9)로 정량 평가 | P5 | — |

---

## 7. 사용자 편의성 검토 (요청 명시 대응)

| 요청 사항 | 반영 |
|---|---|
| "매달 같은 방식의 전표를 한 번 등록해 놓고 이후에도 처리" | F7 승격 + F8 시드 일괄 등재 — 사용자가 등록할 필요조차 없는 후보 36개(→73개)는 시드로 선등재 |
| "기작성된 전표를 기준으로" | F2 프리필(백지 시작 없음) — 구조는 템플릿, 변동 기본값은 본인 최근 확정 초안 |
| "변동되는 비용이나 항목들만 수정" | F3 고정/변동 플래그(실측 자동 판정) + F4 총액 1칸 입력 → 나머지 자동 제안 |
| "항목들을 복사해서 적용" | F6 라인 복사(관리항목 포함) + F2 전표 단위 복사 |
| 놓치기 쉬운 것 보완 | F1 상태 배지(4상태) — 반복 전표는 '입력이 어려운 것'보다 '잊는 것'이 실무 리스크. 주차 단위 케이스도 카운터로 커버 |

**검토 소견(설계에 반영된 판단)**
- 입력 최소화보다 **오입력 방지**가 우선 — 프리필은 편하지만 "전월 값 그대로 제출" 사고를 만든다. 방어선은 ①변동 필드 강조 ②제출 전 "전월과 달라진 점" 차이 리스트 ③날짜형은 항상 변동 취급 ④파생 관계식 soft 검증의 4겹이다. 반복 모달은 수개월 내 습관적 통과로 무력화되므로 쓰지 않는다.
- 적요 월 표기는 실측상 **당월형이 84%**로 다수고 'N월분' 표기만 전월형이 우세 — 전역 기본값 대신 패밀리별 실측 오프셋을 저장한다(§4.2).
- 주차 단위(급식대·월 31건)는 반복 빈도가 가장 높아 가장 잊기 쉬운 케이스다 — 월 1건 배지 대신 "이번 달 n건 + 경과일" 카운터로 커버하고, `recur_cycle='weekly'`(월 기대 건수 기반 "8월 3/4건" 배지)는 2차 로드맵에 둔다.
- 회계 담당자 쪽은 요약 '보여주기'가 아니라 **옮겨 붙이기(TSV 복사)와 대기열 체크오프**까지 가야 이중 작업 부담이 실제로 준다(F5·F9).

---

## 8. 승인 게이트 — **2026-08-11 관리자 결정 반영**

| # | 항목 | 결정(2026-08-11) | 실행 상태 |
|---|---|---|---|
| ~~**B1**~~ ✅ | 관리항목 마스터 2종 적재 | **승인 → 실적재 완료(2026-08-12, 관리자 지시)** | 미러 2종(`ctrl_item_s`·`acct_ctrl_assn_s`) + `erp_master_upsert` 확장(마이그레이션 `gl_ctrl_master_v1`, 정본 23번), ETL job 2종 신설 후 **실적재 완료 — 52행 · 1,002행(필수 플래그 Y 377행), batch_run success·유실 0**. `erp_load_scope` 2건 `loaded` 등재, 연동 현황 뷰 14→16종 확장(`erp_sync_overview_ctrl_master`, batch_run 기반). DR_FG/CR_FG 플래그 의미 확인은 유니포인트 문의 항목 유지 |
| **B2** ◔ | 시드 템플릿 검수·등재 | **부분 승인(2026-08-11) → 부가세 부분 개정(결정 C-14, 2026-08-20)** | 2026-08-11 원결정: **파생 자동계산 규칙(§4.2)을 범위에서 제외** — 세금계산서형·원천징수형 템플릿도 등재는 진행하되, 부가세액(V8)·원천징수액은 자동 계산하지 않고 **사용자가 실물 증빙 값을 직접 입력**(전월값 프리필만 제공, 세액 관계식 검증·경고 비활성). F4는 차대 균형 배분(`balance`)·판/제/연 비율 배분만 수행. **→ 2026-08-20 결정 C-14로 개정**: 결의전표 입력 화면(`gl_draft`)에 **부가세 자동계산을 도입**한다(계산은 화면에서만, 서버는 검증만, 사용자 수정값 미override) — 세율 판정은 운영 전표 실측 근거(A·B·L 유형 10% 자동, K·C 0%, I·H는 관세 혼입으로 실측 불일치해 자동계산 제외). **원천징수는 이 개정에서도 계속 제외**(U-1 부가세 부분 해소·원천징수 부분 미결). 반복전표 자동화(v2) 쪽 시드 템플릿의 세액 관계식 검증 활성화 여부는 **별도 판단 필요**(이번 C-14는 파일럿 입력화면 대상, v2 템플릿 F4 확장은 아직 결정 없음) — 시드 36건의 계정 구성 검수는 여전히 회계 담당 확인 후 등재(P4) |
| ~~**B2-1**~~ ✅ | 회계기간 마감 기준 | **ERP 회계마감 데이터 연동 적용** | ERP 월마감 원장 **`C_CLOSE_STATUS`**(작업년월·마감여부·마감일자) 실측 — **2026-06까지 전월 마감(Y), 2026-07부터 미마감**. `gl_period_lock`에 2023-10~2026-06 **33개월 등재 완료**(note에 근거 기록). ⚠ ERP 실측상 마감월에도 결의전표 소급 등록이 관찰됨(예: 1월 귀속 전표가 8월 등록) — 포털이 ERP보다 엄격해지며 예외는 회계 담당이 해당 월 lock 해제로 처리. ⚠ 새 달 마감 시 수동 추가 필요(자동 동기 job은 후속 과제) |
| ~~**B3**~~ ✅ | 파일럿 대상 | **이훈희·최동혁** | `accounting` 모듈을 권한 카탈로그에 신설(sensitive, A3 실행)하고 이훈희(`hh.lee2@jeilm.co.kr`, 총무팀)에 개인 예외(`perm_grant`) 부여 — 감사(`perm_audit`) 기록 동반. **실측 검증**: 이훈희 `gl_draft` 허용, 비대상자 차단 유지("ERP 모듈 권한 없음"), 최동혁은 전체관리자로 기허용. 부서 부여는 계속 없음(C-8 유지) |
| B4 | (후속) L3 자동 초안 생성 | 미결 | 파일럿 회고 후. 등록자 JWT 규약과의 충돌 해소 방안(§6.7) 포함 |

---

## 9. 리스크·한계

| 리스크 | 대비 |
|---|---|
| 프리필 값 무확인 제출(오입력) | 4겹 방어(§7 소견) — 변동 강조 + 차이 리스트 + 날짜 상시 변동 + soft 관계식 검증. 잔여: 사용자 습관화는 완전 차단 불가 |
| 시드의 계정 구성이 회계 방침과 다를 가능성 | B2 검수 없이는 등재하지 않음(v1 원칙 유지) |
| 부가세 예외(불공제 B·수입 C·수정세금계산서 음수·면세) | 1차 범위 A·K 한정 + 시드 자동 제외(§3.3). 음수 금액 허용 여부는 B2에서 결정 |
| 배지 오판(제출~확정 공백·소급 작성) | usage 기록을 초안 생성 시점으로 + 4상태 배지 + `use_ym` 기준 명시·회차 선택 필드(§6.3) |
| 이중 입력·회계 담당자 부하(기존 리스크) | 변화 없음 — 근본 해소는 E3(L4). 단 관리항목 TSV 복사·대기열(F5·F9)로 재입력 부담은 추가 경감 |
| 분석 기준이 2026년 한정 | 분기 1회 재분석(§6.6-5). 12월 결산기 특수 전표는 반복 판정에서 자연 제외됨 |
| `A_ACCT_CTRL_ASSN`의 실무 예외(설정에 없지만 관례로 입력하는 항목) | 시드는 **실측 사용 관리항목**(A_TEMP_GL_DTL 실데이터)을 우선하고 설정 테이블은 검증용으로 병행 |
| **본 문서의 공개 배포 노출** — 거래처 실명·계약 단가(§3.4)가 HTML 빌드·커밋 시 공개 정적 호스팅(gh-pages)에 노출됨 | 기존 관제 문서 관례와 동일한 수준이나, 계약 단가는 영업 정보 성격이 있어 **배포 포함 여부는 관리자 결정 필요** — 결정 전까지 본 문서는 커밋하되 gh-pages 배포 제외(또는 §3.4 익명화) 옵션을 열어 둔다 |

---

## 10. 변경 이력

| 일자 | 변경 내용 | 작성 |
|---|---|---|
| 2026-08-20 | v1.5 — **B2 개정(결정 C-14) — 부가세 자동계산 도입, 원천징수는 계속 제외**. 결의전표 입력 화면(`gl_draft`)에 부가세 자동계산이 배포되면서(REQ-0001~0004, `09_결의전표_인수인계.md` §2 C-14) 2026-08-11 B2 부분승인의 "부가세·원천징수 자동계산 제외" 결정 중 **부가세 부분만 개정**했다 — 계산은 화면에서만, 서버는 검증만, 세율은 운영 전표 실측 근거(A·B·L 10% 자동, K·C 0%, I·H 제외). 원천징수는 이번에도 범위 밖(U-1 부가세 부분 해소·원천징수 부분 미결). 이 개정은 파일럿 입력화면(09) 대상이며, 본 문서(v2 템플릿 F4 세액 관계식 검증)까지 자동 확장되는지는 **별도 판단 필요**로 명시만 하고 결정하지 않았다. §8 B2 행 갱신 | 최동혁(+Claude) |
| 2026-08-12 | v1.4 — **P1·P3 구현 완료(관리자 지시 "P1 나머지 + P3 화면 구현 진행")**. ①DDL: 마이그레이션 `gl_template_v2` — `gl_draft_item_ctrl`(관리항목+`invoice_seq`)·`gl_template_item_ctrl`(fixed/input/date_shift 규칙)·`gl_template_usage`(회차 4상태)·월반복 속성 컬럼(recur_cycle·title_pattern·month_offset·vat_mode·src_family_key·amt_locked)·RPC `gl_ctrl_master_get`(service_role 전용). 정본 `이관/sql/24_gl_template_v2.sql`. ②**Edge Function `jeil-gl-draft` v4 배포** — 신규 op `tpl_recur_list`(월반복 카드)·`tpl_apply_prev`(본인 확정 초안 프리필)·`tpl_seed_bulk`(관리자 한정 시드 등재, 감사 기록) + save 확장(관리항목 검증: 필수 플래그 Y는 신형 화면 하드·구화면 경고, V6=BP 정합, V2 이상치 경고 — 세액 자동계산·검증은 B2 결정대로 없음) + usage 상태 동기. **구화면 하위호환 유지**(기존 op 동작 불변, ctrls 미전송 저장이 관리항목을 지우지 않음). ③**화면 v2** — 월반복 홈(F1: 배지 4상태·작성자·중복 경고·검색 정렬)·전월 프리필(F2: 본인 최근 확정 초안 + 날짜 +1개월 월말클램프 제안값 배지·적요 토큰 치환)·관리항목 자동 패널(F5: 계정×차대 필수* 표시·ERP seq 정렬 요약·TSV 복사)·제출 전 요약(F3: 전월 差 리스트)·행 복사/삭제 관리항목 동반(F6)·템플릿 편집기 월반복 속성·승격 시 관리항목 규칙 변환(F7). ④**3관점 코드 리뷰 지적 16건 반영** — 치명 1(수동 행 관리항목 유실) 포함: KST 회차 경계, 월반복 상태 오염(CURRENT_TPL 초기화), 민감 관리항목(EM·BA 등) 부서/전사 템플릿 고정값 저장 차단, 타인 미제출 초안 열람 차단(list/get), tpl_use 권한 확인, post 중복검사 다건 보정, usage 위조 방지(템플릿 가시성 검증·±2개월 제한). 잔여: 화면 라이브 반영은 git 커밋·gh-pages 배포 후(관리자) | 최동혁(+Claude) |
| 2026-08-12 | v1.3 — **B1 실적재 완료(관리자 지시)**. `ctrl_item` 52/52 · `acct_ctrl_assn` 1,002/1,002(필수 플래그 Y 377행), batch_run success·유실 0. `erp_load_scope` 2건 `loaded` 등재, 연동 현황 뷰 14→16종 확장(마이그레이션 `erp_sync_overview_ctrl_master` — fail-closed 규약에 따라 batch_run 기반 count). §6.8 P2 완료 처리. 이로써 P3(화면 구현) 착수의 데이터 선행 조건이 모두 충족됨 | 최동혁(+Claude) |
| 2026-08-11 | v1.2 — **승인 게이트 관리자 결정 반영·집행**. ①B1 승인 → 미러 2종(`ctrl_item_s`·`acct_ctrl_assn_s`)+RPC 확장(마이그레이션 `gl_ctrl_master_v1`, 정본 23번), ETL job 2종 신설, dry-run 통과(52·1,001행), 실적재는 관리자 실행 대기. ②B2 부분 승인 — **부가세·원천징수 자동계산 규칙 제외**(템플릿 등재는 진행, 세액은 수기 입력+전월값 프리필, F4 범위 축소). ③B2-1 — ERP 월마감 원장 `C_CLOSE_STATUS` 실측(2026-06까지 마감 Y) 연동, `gl_period_lock` 33개월 등재 완료(마감월 소급 등록 관행 주석 포함). ④B3 — 파일럿 이훈희·최동혁 확정: `accounting` 모듈 카탈로그 신설(sensitive)+이훈희 개인 예외 부여·감사 기록, 허용/차단 실측 검증. §6.8 단계 상태 갱신 | 최동혁(+Claude) |
| 2026-08-11 | v1.1 — **4관점 교차검증(수치·회계·UX·규칙, 지적 36건) 반영 개정**. 수치 정정(월1건형 39→36, 반복 계정 40→88/핵심 44, §4.1 필수/선택 구분·패밀리 기준 재집계, T5·T7 재분류, 김해 TX2 주장 삭제). 회계 규칙 전면 보강(V8 10%는 제안값·soft 검증·계산서 그룹 단위 `invoice_seq`·단수차 허용, V4 6종 실측·1차 A/K 한정, 원천징수 절사식, 적요 당월형 84% → `month_offset` 실측 저장, 날짜 상시 변동·월말 클램프). UX 보강(배지 4상태·초안 생성 시점 기록·중복 경고, 프리필 정본 이원화·본인 초안 한정, 잠금 해제 발견성·차이 리스트, F1 검색·정렬·카운터, 회계 대기열·TSV 복사). 거버넌스 정합(A5 대체 주장 축소, B2에 U-1·C-2 개정 명시, B2-1 마감 기준 신설, `tpl_seed_bulk` 단일 경로, F7 승격 user 고정, L3 등록자 전제). **분석 도구 v1.1 — 민감 관리항목(EM/BA/D1/CP/NN) 값 미기록·출력 OneDrive 밖 이동·유형/월1건형/오프셋 판정 저장** | 최동혁(+Claude) |
| 2026-08-10 | 최초 작성(v1.0) — 2026년 수기 결의전표 2,287건 전수 반복 패턴 분석 + 템플릿 플랫폼 v2(월반복 자동화) 기획 | 최동혁(+Claude) |
