# 00. ERP DB 연계 — 현재상태 스냅샷

> **기준일: 2026-08-18**(v0.28, 이전 v0.27 · 2026-08-12 / v0.26 · 2026-08-12 / v0.25 · 2026-08-11 / v0.24 · 2026-08-11 / v0.23 · 2026-08-11 / v0.22 · 2026-08-11 / v0.21 · 2026-08-10 / v0.20 · 2026-08-03 / v0.19 · 2026-08-03 / v0.18 · 2026-07-31 / v0.17 · 2026-07-24 / v0.16 · 2026-07-22 / v0.15 · 2026-07-22 / v0.14 · 2026-07-22 / v0.13 · 2026-07-21 / v0.12 · 2026-07-21 / v0.11 · 2026-07-20 / v0.10 · 2026-07-13 / v0.9 · 2026-07-08 / v0.8 · 2026-07-08 / v0.7 · 2026-07-08 / v0.6 · 2026-07-08 / v0.5 · 2026-07-08 / v0.4 · 2026-07-08 / v0.3 · 2026-07-08 / v0.2 · 2026-07-07) · 관리: 최동혁 · 전담 에이전트: `erp-db-link-manager`
> ERP(UNIERP) DB와 AI 포털·사내 타부서 간 연계의 **현황 대장(단일 출처)**. 상태가 바뀌면 여기를 먼저 고치고 §7 변경 이력에 한 줄 남긴다.
> 비밀값(접속 IP/ID/비밀번호)은 기록 금지 — 서버 지칭은 별칭 **"ERP운영DB"** 만 사용.

---

## 1. 한 줄 요약

ERP운영DB(UNIERP, 사외 IDC MS SQL Server 2022)와 AI 포털의 **중간 DB(D1-a안, Supabase) 1차 5종 실적재는 2026-07-07 완료**됐고, **2026-07-08에는 P4(포털·챗봇 연결)가 착수·부분 완료**됐다. `erp_ro`(품목·영업·구매·재고·발주+테이블사전) 위에 사내 전용 노출 뷰 6종(`v_erp_*`, `security_invoker`)을 신설해 REST 직접 노출 없이 안전하게 열었고, RLS 격리 실측 테스트(JWT 시뮬레이션)로 사내만 통과·협력사/anon 차단을 확인했다. 이 위에서 부서 대시보드 1개 화면(구매 매입집계) 파일럿 실연동과 챗봇(`jeil-chat`) ERP Tool 4종 배포(version 7 ACTIVE)까지 이뤄졌다. 다만 유니포인트 협의(P1, 읽기전용 계정·뷰 스펙)는 여전히 대기 중이고, 현재 적재분은 초안 SQL 기반이라 협의 후 재적재 가능성이 있다. 오늘 재확인한 ERP 원천 dry-run 건수(발주 2,702·품목 58,249·매출 10·매입 63·재고 659)는 중간DB 적재분(2,699/58,246/10/63/656)보다 소폭 늘어 있어, 최신 재적재를 관리자가 `!`로 직접 실행하기로 결정했다(명령 준비, 미실행). 같은 날 **사용자-부서-사원 매핑·대사 계층을 신설·실적재까지 완료**했다 — ERP `Z_USR_MAST_REC`(사용자 마스터, `usr_id`=MS 이메일 SSOT)와 `B_ACCT_DEPT`(부서 마스터)를 대사해 이메일 기준 사용자↔부서↔사원 관계를 조회할 수 있는 스키마·뷰·ETL job을 구축한 뒤, 관리자 직접 실행으로 **실제 ERP 데이터 적재까지 완료**했다: `erp_ro.dept_master_s` 351건, `erp_ro.usr_master_s` 99건. 대사 결과 **정상 매핑 89건**, **불일치 10건**(부서불일치 6·상태이상 3·테스트 1)이며, **불일치는 정상 매핑에서 자동 제외**하는 정책이 확정됐다(마이그레이션 `erp_user_dept_exclude_unmatched`). 이전 결정 대기였던 부서명 처리방침은 이로써 해소됐고, 상태이상 3건(활성계정인데 (퇴사)/(휴직) 표기)만 현업 확인 권고로 남았다(전용 관리 문서 `05_사용자부서_매핑대사.md` 참조). 같은 날 이 사용자-부서 매핑 위에 **신규 ETL job `usr_erp_module`**을 추가해 ERP 역할·메뉴 권한(`Z_USR_MAST_REC_USR_ROLE_ASSO`→`Z_USR_ROLE_MNU_AUTHZTN_ASSO`→`Z_CO_MAST_MNU`)을 근거로 한 **부서별 ERP 모듈 '제안값(참고)'**을 만들었다 — `erp_ro.usr_erp_module_s` 397건 실적재 완료, 뷰 `v_dept_erp_suggest`/`public.v_erp_dept_erp_suggest`로 노출해 관리자 콘솔(04) '권한 상세'의 부서별 ERP 모듈 매트릭스에 ◆ 참고표시로만 반영했다(자동 덮어쓰기 없음, 실질 통제선은 여전히 편집형 `dept_erp_scope`). 실측 결과 이 조직은 ERP 읽기권한이 광범위(사용자당 평균 17개 역할)해 부서 과반 임계로도 거의 전 모듈이 제안돼 **ERP-역할 신호의 부서 변별력이 약함**을 확인했고, 이는 '참고 제안으로만 쓴다'는 결정의 타당성을 뒷받침한다. 같은 날 **P5(민감 영역) 선행 정지작업도 착수**됐다 — 급여를 민감 모듈 `payroll`로 신설해 `dept_erp_scope`를 인사팀 전용으로 한정하고 인사 페이지를 이 모듈로 서버 게이트했으며(비인사팀 직접 접근 차단), 인사 데모 페이지의 개인별 실급여는 샘플값+마스킹으로 안전화했다. 민감 분리 스키마 `erp_secure`(`hr_payroll_m` 집계 테이블·`hr_access_log` 감사)도 **승인-준비 빈 구조**로 만들었지만 **실 데이터는 적재하지 않았다** — 실 적재는 경영 승인·유니포인트 View 협의·(권장) `AI-Portal-HR` Entra 그룹 신설이 선행돼야 한다. 같은 날 저녁 **챗봇(jeil-chat) ERP 프로세스(발주·구매요청·매입·입고) 응답 품질 개선**도 실적용·배포됐다. 중간DB 노출 뷰 3종을 보강해 `v_erp_po_pr_link`·`v_erp_pur_req`(둘 다 앞서 같은 날 `M_PUR_REQ` 연동으로 신설된 뷰, 정본 `10_erp_pur_req.sql`)에 발주 입고수량·구매요청 입고/매입수량·외주구분·납기경과(미입고) 플래그·연결 수주번호·요청부서(공란이면 요청자 이메일→부서 매핑으로 보완) 컬럼을 append했고(기존 컬럼 순서·타입 보존, 행수 2,703 불변 검증), 발주번호 단위 총액을 올바로 집계하는 신규 뷰 `v_erp_pur_top_po`를 추가했다(정본 SQL `실제구축준비 자료/이관/sql/15_erp_process_link_enrich.sql`). 같은 배포에서 **[치명적 버그 수정]** 챗봇 도구 호출 오케스트레이션을 1라운드→상한 멀티라운드(`MAX_ROUNDS=4`)로 교체했다 — 기존엔 1차 스트림에서 도구를 실행한 뒤 2차 스트림에 도구를 다시 안 줘서, 모델이 특정 발주에 포털 도구를 먼저 골라 허탕치면 "ERP 도구로 재조회하라"는 안내를 받고도 재호출을 못 해 안내문만 되풀이하는 사용자 루프가 있었는데, 이제 라운드마다 도구 수집→실행→누적하고 마지막 라운드만 도구 없이 최종답변하도록 해 해소했다(무한루프 3중 차단: 라운드 상한 + 직전 동일호출 반복 중단 + 마지막 라운드 강제 무도구). 상태코드 RQ(요청)→CF(확정)→PO(발주완료·입고전)→GR(입고완료)→IV(매입완료) 한글 해석을 도구 출력·프롬프트에 주입하고, 신규 도구 2종(`get_erp_receipt_pending` 미입고·납기경과 발주, `get_erp_pur_req` 상태·부서별 구매요청)을 추가해 `jeil-chat` **version 14 ACTIVE**로 배포했다. 실측 과정에서 중간DB 데이터 갭도 추가로 드러났다 — `inventory_d.in_qty` 전량 0·`stock_qty` 전량 null(출고만 유효), `sales_orders_m.order_amt`·`collect_amt` 전월 0(수주·수금 미매핑), `pur_order_s.subcontra_flg` 전건 'N'(외주 발주 0건) — 원천 ETL 매핑 점검이 필요해 §6에 결정대기로 정리했다. **같은 날 인사 급여 집계도 승인-준비 단계를 넘어 실적재까지 완료됐다** — `erp_secure.hr_payroll_m`에 ETL job `hr_payroll`(`HDF070T` 월×부서 집계·`HGA070T` 월×전사 집계)로 **143행**(급여 월×부서 137 + 퇴직 월×전사 6, 2026.01~06 6개월분)을 관리자 직접 실행으로 적재했다 — 컬럼은 `ym·dept_nm·headcount·pay_tot_amt·retire_amt`뿐이고 **개인별·이름·주민번호·계좌는 여전히 포함하지 않는다**(집계만). 이 위에 게이트 API **`jeil-hr`(Edge Function) v1을 배포**해 Entra 검증 → `dept='인사팀'`/`portal_admin`만 통과(그 외 403) → 감사 로그(`hr_access_log`) → 집계 반환 흐름을 갖췄고, 인사 페이지는 인사팀 권한자에게 이 실 집계를 보여준다(비권한·데모는 마스킹·샘플 유지). 자금(F 모듈) 실적재, `AI-Portal-HR` Entra 보안그룹 신설, 유니포인트와의 재직상태 정의 확인은 여전히 미착수·잔여 항목이다. **2026-07-13에는 재고 입출고(inventory) ETL의 데이터 커버리지 결함을 발견·해소했다** — 1차 5종 ETL 중 `inventory` job(`M_PUR_GOODS_MVMT`)만 다른 job(발주·매출·매입)과 달리 롤링 31일(`daily_start`) 창으로 남아 있어 최근 6·7월 데이터만 적재돼 있었는데, 이를 발주·매출·매입 job과 동일한 `WHERE MVMT_DT >= ? AND MVMT_DT < ?` + `year_start`/`year_end` 파라미터로 수정(커밋 `811cbd8`, main 배포)하고 관리자 직접 실행으로 재적재했다. 결과 `erp_ro.inventory_d`는 2026년 1~7월 전체 **1,810행**(월별 01:93·02:134·03:149·04:260·05:469·06:650·07:55)으로 늘었고 `etl_meta.batch_run`에 성공 이력(rows_read=1810·rows_upserted=1810)이 남았다. 참고로 발주현황 보드의 입고·매입 상태 표시는 `pur_order_s`(po_sts·rcpt_qty) 기반이라 이번 작업 전에도 이미 연중 전체가 반영돼 있었고, 이번 백필은 재고 입출고 일집계(`inventory_d`)에 한정된 보완이다.

**2026-07-20에는 P4(포털·챗봇 연결)에서 그동안 비어 있던 '챗봇↔권한체계' 연결고리를 메웠다.** 발단은 관리자 계정(dh.choi@jeilm.co.kr, `portal_admin`)이 챗봇(jeil-chat)에 "2026년 인원현황"·"권한 확인"을 물었을 때 오답이 나온 문제였다. 조사 결과 권한 체계(`jeil-me`/`jeil-hr`/`dept_erp_scope`/`portal_page`)와 인원 데이터(`erp_secure.hr_payroll_m`, 2026-01~06 월별 전사 총원 113·119·101·100·94·98명)는 이미 정상 구현·적재 완료 상태였고, 원인은 **챗봇(jeil-chat)이 이 체계에 연결돼 있지 않았던 것**이었다. 이를 메우기 위해 `jeil-chat` Edge Function을 **version 18 → version 19**로 배포했다. 신규 도구 3종: ①`get_my_access` — 로그인 본인의 역할(관리자/부서관리자/일반)·소속부서·열람가능 ERP 모듈·접근가능/불가 페이지·권한요청 방법을 반환(호출자 UPN 고정으로 타인 권한 조회 불가, 전 직원 사용 가능). ②`get_hr_headcount` — 인원현황. 전 직원에게는 월별 전사 총원만, 부서별 인원 분포는 `payroll` 권한자(인사팀·관리자)에게만 반환(급여 금액 미포함). ③`get_hr_payroll` — 급여 집계(인사팀·관리자 전용). `erp_secure`는 REST 미노출이라 기존 `jeil-hr`와 동일하게 service_role RPC `hr_payroll_get` 경유. 인사 도구(`get_hr_headcount`·`get_hr_payroll`) 접근은 허용·거부 모두 `hr_access_log`에 감사 기록된다(`jeil-hr`와 동일 원장 재사용). SYSTEM_PROMPT도 보강했다 — 권한 질의는 반드시 `get_my_access`로 실조회하도록, 권한 밖 수치 추정·역산 금지, 인원은 급여대장(HDF070T) 기준 마감 전 값임을 명시.

**같은 배포에서 소급 발견된 권한 구멍도 차단했다(중요)** — 기존 포털 도구 3종 `get_order_summary`·`get_order_detail`·`get_inspection_pending`이 그동안 `ERP_TOOL_MODULE` 매핑이 없어 권한 강제 없이 **전 직원에게 협력사 발주번호·금액·납기를 노출**하고 있었다. 이번에 `pur_order` 모듈 권한으로 강제 적용해 차단했다 — 2026-07-08 v14에서 "ERP 도구 8종을 `pur_order`로 매핑"한 작업(§4 챗봇 ERP Tool)은 ERP 도구 쪽만 대상이었고, 포털 도구 3종은 그 매핑에서 빠져 있었다는 뜻이다(즉 이번 취약점은 그날 이후 계속 실재했다). 배포는 `verify_jwt=false` 유지, 배포본이 로컬 소스와 문자 단위 일치 검증됐다.

**관리자 결정(2026-07-20)**: ①인원현황 공개 등급 — 전사 총원은 전 직원 조회 가능, 부서별 인원 분포·급여액은 인사팀·관리자 전용. ②협력사 발주·검사 챗봇 조회 권한은 신규 모듈 신설 대신 기존 `pur_order` 모듈 권한으로 강제. **경영 판단 보류(이번엔 변경 안 함)**: `dept_erp_scope`가 23개 부서 전부 sales·purchase·inventory·item·pur_order를 동일 보유해 부서별 데이터 차등이 사실상 없음(payroll만 인사팀 단독 차등) — 구조는 차등을 지원하므로 정책 결정만 필요한 상태다(§6 결정대기 참조). 소스 파일(`supabase/functions/jeil-chat/index.ts`)은 수정·배포 완료 상태이나 git 커밋은 관리자가 별도로 진행한다(본 에이전트는 커밋하지 않음).

**2026-07-21에는 P4(포털·챗봇 연결)에서 그동안 문서화되지 않았던 '연동 현황 가시성' 공백을 메웠다.** 발단은 연동 현황 확인용 뷰 `public.v_erp_sync_overview`(및 이를 쓰는 `app/erp-status.html` 화면)를 점검하는 과정이었다 — 이 뷰·화면 자체는 이미 가동 중이었으나 지금까지 본 관제 문서(`10_ERP_DB연계/`)에는 등재되지 않았던 것이 드러났다(소급 등재 필요). 더 큰 문제는 그 뷰가 **1차 5종(발주·품목·매출·매입·재고)만 노출**하고 있었는데, 실제 `erp_ro`/`erp_secure`에는 이미 **10종**이 적재돼 있었다는 점이다 — 구매요청(`pur_req_s`, §4.5~4.8에서 이미 실적재됐던 것들: 부서마스터·사용자마스터·부서별ERP모듈·급여집계)이 전부 이 현황 화면에서는 보이지 않는 '숨은 연동'이었다. 이를 마이그레이션 `erp_sync_overview_all_sources`로 해소했다 — 뷰를 **10종 전부**(pur_order·item_master·sales·purchase·inventory + 신규노출 pur_req·dept_master·usr_master·usr_erp_module·hr_payroll)로 확장하고, `sensitive`(민감표시)·`sort`(정렬) 컬럼을 추가했다. 급여(`hr_payroll`)는 `erp_secure` 민감 스키마라 **실테이블을 count하지 않고 `etl_meta.batch_run.rows_upserted`만 사용**해 금액·명세를 노출하지 않는다. 발주 원천 표기도 실제 조인 기준(`M_PUR_ORD_HDR + M_PUR_ORD_DTL`)으로 정정했다. `security_invoker=true`는 유지돼 사내 세션만 조회 가능하다(변화 없음). 같은 날 **관리자 콘솔에도 이 현황을 반영**했다 — `jeil-chat-admin`을 **version 9 → 10**으로 배포해 `v_erp_sync_overview`를 service_role로 조회한 결과를 응답 `erp_sync`로 반환하게 했고, `04_챗봇_포털_데모UI.html` 관리자 콘솔 'API' 탭에 실데이터 표를 신설했다(기존엔 '사내 운영 페이지' 카드 링크로만 접근 가능했음). `app/erp-status.html`에는 **연결 정보 표**를 추가해 원천(UNIERP MS SQL, 사외 IDC, DB `JEILMNS`)·접속 방식(ODBC 읽기전용, `SELECT … WITH (NOLOCK)`, 화이트리스트 SQL, DDL/DML 없음)·적재 경로(`10_ERP_DB연계/etl/etl_run.py` → RPC `erp_etl_upsert`/민감분 `erp_secure_upsert` → `erp_ro`/`erp_secure`)·실행 방식(**수동 실행**, 자동 스케줄러 미도입, 이력은 `etl_meta.batch_run`)·접속정보 보관 위치(실행환경 `.env`에만, 소스·문서 미기재)·데이터 격리(RLS, 협력사·anon 0행)를 명시했다. 오늘 다시 조회한 실측 적재 건수는 §4 표를 참조.

**같은 날(2026-07-21) 오후에는 오전에 스키마·ETL·뷰만 준비해 두었던(§7 오전 변경이력 참조) 구매요청 상세 확충과 매입 상세(`iv_dtl`) 신규 연동을 관리자가 직접 실행해 실적재까지 완료했다**(`batch_run` 7건 성공·0건 실패). 신규 연동 `iv_dtl`(매입 상세, `M_IV_DTL`)은 2026-01-05~07-20 구간 **1,981행**을 0.9초에 적재했다 — 송장 213건·발주 432건, 매입금액 합계 **35.6억원**, **발주 연결(`po_no`) 100%**. `pur_req`(구매요청)는 오전에 준비해 둔 상세 14컬럼(조달구분·확정/릴리즈수량·구매그룹/조직·저장위치·수주라인·도면번호·추적번호·변경차수·MRP오더·생성/수정자)을 채우는 백필로 **4,063행**을 `--full` 재적재했다(2.1초) — 조달구분 채움 4,063/4,064(99.98%, 미채움 1건 `PR202607070034`는 ERP 원천 `PROCURE_TYPE` NULL), 도면번호 보유 856건.

매입 상세를 새로 적재한 김에 기존 월집계(`purchase_m`, 2026-07 문서화 당시 7/8자 기준)와 대사해 보니 **6월 +19.2억원·7월 +1.5억원 차이**가 드러났다(1~5월은 완전 일치). 조사 결과 데이터 오류가 아니라 **적재 시점 차이**(7/8일 이후 ERP에 새로 등록된 매입 건이 월집계에 미반영)로 확인돼, 관리자가 관련 5종을 재적재했다 — `purchase --full` 151행 · `pur_order`(증분) 512행 · `sales` 37행 · `item_master`(증분) 415행 · `inventory` 1,847행(전부 성공). **재적재 후 매입 대사 차이는 전월(2026-01~07) 0으로 해소**돼, 매핑·로직이 정상 동작함이 검증됐다.

참고로 남긴 것(임의 조치 없음): ①**발주 미연결 매입 라인 36건**(`iv_dtl` 1,981행 중 1.8%, 발주 18건, 2026-01-05~06-30 구간) — 발주 원장이 2026년 범위·증분 조건에 걸리지 않은 과거 발주로 추정, 데이터 품질 참고 항목으로만 기록. ②`pur_req_s` 4,064건(§4.9 대사 당시 기준, 07-21 오전) 대비 이번 추출 4,063건의 1건 차이는 연도 범위 밖 레거시 잔존행. ③`hr_payroll`(민감)은 이번 재적재 대상에서 제외했다(별도 승인 사항, 2026-07-08 143행 그대로 유지).

**2026-07-22에는 챗봇 ERP 조회 정합성 이슈를 사용자 제보로 접수하고 원인분석(RCA)·조치까지 완료했다.** 제보 내용: 품목코드 `S3041-00065`의 발주·구매요청을 챗봇에 물으면, 실제 ERP·중간DB에는 데이터가 있는데도 챗봇이 "없다"고 답하거나 품목코드를 발주번호로 혼동해 답하는 문제가 발견됐다(제보자는 "supabase 데이터가 ERP와 안 맞게 연결된 것 아닌가"라는 우려도 제기했다). **원인분석 결론(SQL·코드 실측): 데이터·매핑 문제 아님, 모델 이슈도 아님 — 챗봇 도구 설계 공백이 원인이다.** 데이터·매핑은 정상이었다 — `erp_ro`에 해당 품목의 발주 `PO202607210001`(2026-07-21·(주)대구렌탈·341.6만원·상태 PO·연결 `PR202607020013`)과 구매요청 `PR202607020013`(2026-07-02·사업운영팀·수량1)이 품목코드·품목명·po_no·pr_no 매핑까지 정확히 적재돼 있었다. **주원인**은 품목코드로 발주/구매요청을 역조회하는 챗봇 도구가 아예 없었던 것이다 — `v_erp_po_pr_link`에 item_code 컬럼이 있는데도 기존 도구(`get_erp_po_pr`·`get_erp_receipt_pending`)는 po_no/pr_no/po_sts로만 필터했고, `get_erp_item`은 품목 마스터만 반환(발주·구매요청 미반환)했다. **부원인**은 SYSTEM_PROMPT가 "PO/PR 번호"로만 조회를 안내하고 품목코드→발주 이력 조회 시나리오를 지정하지 않은 것이다. **혼동 유발 요인**은 `get_erp_po_pr`가 품목코드를 po_no 자리에 넣어도 걸러내지 않고 0건을 조용히 반환해 "없음"·발주번호 오인을 낳은 것이다. **조치(완료, `jeil-chat` version 26 배포)**: 신규 도구 `get_erp_item_orders(item)` — 품목코드/품목명으로 그 품목의 구매요청·발주·매입 이력(요청→발주→입고→매입 추적)을 반환(`v_erp_pur_req`·`v_erp_pur_order`·`v_erp_iv_dtl`를 item_code로 조회, `pur_order` 모듈 권한 강제, list 뷰). SYSTEM_PROMPT 보강(PO…/PR…/품목코드 문자-숫자 형식 구분 + 품목코드 이력 질의는 `get_erp_item_orders` 지정). `get_erp_po_pr` 입력 가드 추가 — PO/PR 형식이 아니면(품목코드 오인) 0건 대신 재안내(재시도 도구로 `get_erp_item_orders` 명시). SQL 실측으로 `S3041-00065`·`AACZ15-00002`(구매요청 12건·발주 11건) 등 정상 반환을 확인했다. **데이터 범위 한계(변경 없음, 명시만)**: `erp_ro`는 2026년만 적재(연도 필터)돼 있어 2025년 이전 발주·구매요청은 조회 불가하다 — 이전 연도 확장은 별도 결정 사항으로 남겨둔다(임의 확장 안 함). 상세는 `02_진행상태.md` §3 작업로그·§4 이슈·메모 참조.

**같은 날(2026-07-22) 후속으로, 위 RCA 과정에서 나온 사용자 우려("Supabase 중간DB가 실제 ERP와 제대로 안 맞게 연결된 것 아닌가")에 답하기 위해 중간DB(`erp_ro`) 적재 정합성을 SQL로 실측 검증했다.** **결론: 적재 정합성 이상 없음, 구조적 오류·누락 미발견.** 적재 현황(행수/기간/고유품목): `pur_order_s` 3,088행(2026-01-07~07-21·2,798품목) · `pur_req_s` 4,064행(2026-01-06~07-21·3,300품목) · `iv_dtl_s` 1,981행(2026-01-05~07-20·1,855품목) · `item_master_s` 58,533행(item_code 고유 58,533). 점검 항목 전건 이상 없음 — item_code NULL/공백 0건(3종 전부), 품목마스터 고아(발주·구매요청·매입→item_master_s 미존재) 각 0건, 번호형식 위반(발주 po_no/pr_no·구매요청 pr_no 정규식) 0건(100% 준수), PK 중복(발주 po_no+po_seq·품목마스터 item_code) 0건. `etl_meta.batch_run` 최근 12건 전부 success·전 잡 rows_read=rows_upserted(누락 0), 최종 적재 2026-07-21 08:09 UTC. **발견 2건(둘 다 오류 아님, 알려진 한계로 확인)**: ①발주→구매요청 고아 49건 — 전부 2025년 PR(`PR2025…`)을 참조하는 연초 발주(발주일 2026-01-07~01-19)로, `erp_ro`가 2026년만 적재하는 범위 한계의 정상 귀결(매핑 오류 아님). ②(`po_no`+`item_code`) 중복 31건 — 동일 PO 안에서 `po_seq`가 다른 별개 발주 라인이 각각 다른 구매요청(`pr_no`)에서 유래한 것(예: `PO202601300002`/`S20111-00008`이 `po_seq` 2·4로 분리)으로, ERP 원본 구조를 정확히 보존한 결과(중복 적재 아님). 진짜 PK(`po_no`+`po_seq`) 중복은 0건. `S3041-00065` 재확인: 발주 1라인·구매요청 1라인 정상 존재, `get_erp_item_orders`(v26)가 정확 반환함을 라이브 재검증했다. **한계·잔여 과제**: ①데이터 최신성 — 최종 적재 2026-07-21이라 당일(2026-07-22) 발생분은 미반영(ETL 수동 실행 방식이라 실시간 아님, 사용자 안내 필요). ②범위 한계 — `erp_ro`는 2026년만 적재(2025년 이전 발주·구매요청 조회 불가, 기존 한계 재확인). ③**미실행(잔여 과제)** — ERP 원본(`M_PUR_ORD`/`M_PUR_REQ`) 직접 대사는 이번엔 수행하지 못했다(ODBC 접속 설정 `.env` 읽기 권한 거부로 중단) — 관리자가 `10_ERP_DB연계/etl/`에서 `python etl_run.py --job pur_order --dry-run` 등을 직접 실행해 원본 건수와 대사할 것을 §6·`02_진행상태.md` §4에 잔여 항목으로 등재한다. 상세는 `02_진행상태.md` §3 작업로그·§4 이슈·메모 참조.

**같은 날(2026-07-22) 오후에는 사용자 제보(협력사 정보 화면 `app/admin-vendors.html`에서 발주거래처가 보이지 않음)를 접수해 협력사 거래처 마스터 정합화 작업을 완료했다.** 원인은 `public.vendor_master`가 초기 데모 시드 **17개사**에 머물러 있어 ERP 실제 발주거래처 **103개사 중 87개사가 미등록**이었던 것이다. 추가로 기존 17건의 사업자번호는 전부 **거래처코드 뒤3자리-81-임의5자리 패턴으로 만들어진 가짜 값**(실제 사업자등록번호 아님)이었다. 조치: ①`vendor_master`를 발주·매입·매입상세 합집합 기준 **109개사**로 재적재(거래처명 5건을 ERP 최신 실적 기준으로 교정, 발급 계정 4건 무영향·고아 0건). ②가짜 사업자번호 17건을 제거(NULL 처리, ERP 거래처마스터 연계 전까지 공란 유지). ③신규 스키마 **`erp_ro.bp_master_s`**를 신설했다 — ERP `B_BIZ_PARTNER`(거래처마스터, 4,621건) 복제용, RLS internal+authenticated SELECT(기존 erp_ro 규약과 동일). **대표자주민등록번호·은행계좌번호는 개인정보·금융정보 최소수집 원칙에 따라 적재 대상에서 의도적으로 제외**했다. `erp_etl_upsert` RPC 화이트리스트에 `bp_master_s` 분기를 추가하고 `10_ERP_DB연계/etl/etl_run.py`에 신규 ETL job `bp_master`(전량, 증분 `UPDT_DT` 기준)를 준비했으나 **ODBC 접속이 필요해 미실행**이다(관리자가 `etl_run.py --job bp_master` 직접 실행 시 실제 사업자등록번호·대표자명·연락처까지 채워짐). ④관리자 전용 RPC **`public.vendor_master_sync()`**를 신설했다(비관리자 42501 차단 검증, 멱등성 검증 완료) — 원천 우선순위는 ERP 거래처마스터 → 발주·매입 실적이며, 관리자 화면에 **"🔄 거래처 동기화" 버튼**을 추가했다. 부수 효과로 `vendor_master`를 공유하는 협력사 모바일 포털(`pages/협력사_모바일_포털.html`)·계정발급 Edge Function(vendor-provision·approve-vendor)에서도 87개사 거래처명이 정상 표기된다. 라이브 검증(ai.jeilm.co.kr) 완료 — 109개사·계정 4건 표시, 거래처명 교정 반영 확인(커밋 `c11d632`).

**2026-07-24에는 야간배치를 관리자가 수동으로 실행해 최종배치(07-21/07-22) 이후 3~4일치 누적 미실행분을 일괄 반영했다(전체 12개 ETL job 전량 성공, 실패 0건).** 결과 — `usr_master` 7/7 · `dept_master` 351/351 · `usr_erp_module` 397/397 · `hr_payroll`(민감) 165/165 · `pur_order` 118/118 · `pur_req` 252/252 · `item_master` 80/80 · `sales` 37/37 · `iv_dtl` 6/6 · `purchase` 152/152 · `inventory` 1,871/1,871 · `bp_master` 3/3(추출=적재, 유실 0). 최종 적재 후 주요 소스 현재 건수(증가분): `pur_order` 3,137건(+49) · `pur_req` 4,136건(+72) · `item_master` 58,589건(+56) · `iv_dtl` 1,984건(+3) · `inventory` 1,871건(+24) · `hr_payroll`(민감) 165행(+22) · `purchase` 152건(+1) · `usr_master` 99건(값 갱신 7건) · `sales` 37건·`dept_master` 351건·`usr_erp_module` 397건은 변동 없음 · `bp_master`는 재적재 3건 반영(2026-07-22 최초 4,649건 위에 증분). **`bp_master` job은 처음 HTTP 400(PostgreSQL 42601 "INSERT has more target columns than expressions")으로 실패했다** — 원인은 중간DB 함수 `public.erp_etl_upsert`의 `bp_master_s` 분기에서, 주민등록번호 형태 차단용 case식 뒤에 붙인 인라인 `--` 주석이 같은 줄에 있던 `x.repre_nm, x.bp_type, x.bp_group` 3개 값을 통째로 주석 처리해 INSERT 대상 컬럼 20개 vs 값 17개가 어긋난 것이었다(2026-07-22 최초 4,649행 적재는 이 마스킹 코드 추가 **이전** 버전이라 성공했고, 이후 마스킹 코드가 회귀 결함을 만든 것). 조치로 주석을 세 컬럼 뒤(줄 끝)로 옮겨 함수를 재생성했다(마스킹 기능은 동일, DB 현재 정의를 읽어 깨진 한 줄만 치환하는 방식, 로컬 기록 `실제구축준비 자료/이관/sql/20_erp_etl_upsert_bp_master_fix.sql` 신설) — 재실행 결과 `bp_master` 3/3 정상 적재로 해소됐다. 부수 개선으로 `10_ERP_DB연계/etl/etl_run.py`의 `rpc()`가 HTTP 오류 응답 본문(PostgREST 상세 메시지)을 예외·`batch_run.error_msg`에 실어 향후 진단이 쉬워지도록 개선했다. 아울러 **기존 문서가 `bp_master`를 "ODBC 미실행·잔여과제"로 기록하고 있던 것은 소급 정정**한다 — 실제로는 2026-07-22에 이미 4,649행이 최초 적재됐고(§7 변경이력에는 "적재 대기"로만 남아 있었음), 2026-07-24에 증분 3행까지 반영된 상태다. **후속 조사 필요 이슈 2건(임의 조치 없이 등재만)**: ①연동 현황 뷰 `v_erp_data_asof.rows_upserted`가 최신 배치를 반영하지 못하는 정황을 확인했다(일부 job에서 07-21과 07-24 조회값이 동일하게 고정돼 있었음) — 뷰 정의 결함 가능성이 있어 조사가 필요하며, 이번 배치 성공 검증은 `last_success` 타임스탬프로 대체 확인했다. ②`erp_etl_upsert` 함수 전체 정의와 `bp_master_s` 분기 DDL이 번호 마이그레이션 파일로 관리되지 않고 DB에만 존재하던 소스 정합성 갭을 확인했다 — 이번엔 `bp_master` 픽스 한 건만 20번 파일로 기록했고, 함수 전체를 번호 파일로 승격하는 것은 별도 후속 과제다. 상세는 `02_진행상태.md` §3 작업로그·§4 이슈·메모 참조.

**2026-07-31에는 회계(A 모듈) 영역에 처음 손을 댔다 — 결의전표 입력 파일럿의 기반을 구축했다(코드·스키마 완료, 실적재·권한부여는 승인 대기).** 배경은 "ERP 결의전표를 전 직원이 각자 ERP 화면에서 입력하는데, 케이스 1개(일반 경비 지출결의)를 사내 포털에서 ERP와 동일한 형태로 입력받게 하자"는 관리자 요청이다. **먼저 ERP 스키마를 실측해 구조를 확정했다** — `A_TEMP_GL`(결의전표 헤더 28,219행: `TEMP_GL_NO` 결의전표번호·`TEMP_GL_DT` 결의일자·`CONF_FG` 승인여부·`GL_NO` 승인 시 채워지는 정식 전표번호)과 `A_TEMP_GL_ITEM`(라인 294,043행: `ACCT_CD`·`DR_CR_FG`·`ITEM_AMT`·`ITEM_DESC`·`VAT_TYPE`·`BP_CD`·`COST_CD`·`PROJECT_NO`)이 정확히 "결의전표"이며, 승인되면 `A_GL`/`A_GL_ITEM`으로 전기되는 흐름임을 `table_dict`로 확인했다. **아키텍처는 "초안은 포털, 확정은 ERP"로 확정했다(관리자 결정)** — 포털은 ERP에 쓰지 않는다(CLAUDE.md §1.2·§1.6). 포털이 검증된 초안을 만들고 회계 담당자가 ERP 화면에서 최종 저장한 뒤 전표번호(`TEMP_GL_NO`)를 되돌려 기입해야 초안이 닫힌다 — 이 회기입이 이중 입력을 막는 유일한 장치라 DB 제약(`gl_draft_posted_needs_no`)으로 강제했다. **등록자 매핑 문제는 실측으로 해소됐다** — 그룹웨어 전자결재가 ERP 전표등록자 기준으로 이뤄지므로 등록자가 MS 계정과 동일해야 하는데, `erp_ro.usr_master_s` 99명 **전원이 `@jeilm.co.kr` 이메일 형식 USR_ID**라 `lower(usr_id)=lower(upn)` 비교로 자동 매핑된다(대문자 포함 1건 있어 `lower()` 필수). 매핑 실패 시 초안 저장을 **거부**해 ERP 계정 없는 사람 명의의 전표 생성을 차단한다. **구축물**: ①ETL job 2종 신설(`acct_master`←`A_ACCT`·`cost_center`←`B_COST_CENTER`) — **금액·잔액이 아닌 코드 마스터만** 추출하며, 전표·분개 라인은 적재 대상이 아니다. `--dry-run` 검증 완료(계정과목 **691행**·코스트센터 **133행**), 실적재는 회계 데이터 접근 승인(A1) 후 관리자 직접 실행 대기. 기존 13KB짜리 `erp_etl_upsert`를 건드리지 않도록 마스터 전용 RPC `erp_master_upsert`를 분리 신설했다(`hr_payroll`의 `erp_secure_upsert` 선례와 동일). ②포털 DB 4종(`public.gl_draft`·`gl_draft_item`·`gl_draft_log` 감사·`gl_period_lock` 회계기간 마감) + `erp_ro.acct_master_s`·`cost_center_s` — **전부 RLS ON·정책 0개·anon/authenticated 회수**로 Edge Function(service_role) 단일 경로(금액이 담기는 첫 포털 쓰기 기능이라 뷰 직접 노출을 하지 않는다). ③Edge Function **`jeil-gl-draft` v1 배포**(`verify_jwt=false`, Graph `/me` 재검증 + `perm_effective` 재판정) — op 9종(bootstrap·save·mine·get·submit·void·bp·list·post). **서버 재검증**: 차변합=대변합, 계정코드 유효성(`acct_master_s` 대조), 금액>0 정수, 라인 2줄 이상, `PROJECT_FG='Y'` 계정의 프로젝트 필수, 마감월 차단, 등록자 UPN 매핑 — 합계는 화면 값을 믿지 않고 라인에서 재계산해 저장한다. ④입력 화면 `pages/자금_결의전표_입력_2026.html`(`PAGE_KEY='gl_draft'` + `_access-gate.js`) — ERP 결의전표와 동일 배치(헤더 폼 + 표준 그리드 `app/lib/grid.js` 차/대변 라인 + 실시간 차대 합계바)에 회계 담당자 탭(제출 목록·ERP 입력용 요약·전표번호 회기입)을 더했다. ⑤`portal_page`에 `gl_draft` 등재(`erp_module='accounting'`). **권한 격리 실측**: 관리자만 allowed=true, 비관리자 3명 전원 false(accounting 모듈 미부여 상태라 파일럿 안전). 인증 게이트 실측: 무토큰·위조토큰 모두 401. **잔여(전부 사람 결정)**: A1 회계 마스터 조회 승인 → 실적재, A2 `A_TEMP_GL` 샘플 조회 승인(등록자 ID 형식·`GL_TYPE`/`GL_INPUT_TYPE` 실코드 확인 — 확인 전까지 전표형태 선택지는 잠정), A3 `accounting` 모듈 신설·부여, A4 파일럿 대상자 지정, A5 회계 규칙 확정(경비 계정 후보·기본 상대계정·부가세 처리·마감 기준).

**2026-08-10~11에는 관리자 지시("erp db 연결 확인해서 erp-db-connector 스킬 활용하여 현재 미적용된 회계계정정보 동기화 작업 진행")를 수행했는데, 회계계정정보 자체는 이미 최신이라 동기화할 차이가 없었고 대신 서로 다른 두 가지 사실이 드러났다 — 오래전부터 있었던 "연동 현황 뷰의 사각지대"(즉시 발견·수정 완료)와 "12개 job의 실제 노후도"(관리자 실행 대기).** **① 회계 마스터 대사 — 실측 결과 차이 0**: `acct_master --dry-run` 691행·`cost_center --dry-run` 133행을 오늘 다시 추출해 2026-08-03 적재분(691·133)과 대사한 결과 **신규 0건·소멸 0건·필드변경 0건**이었다. ERP 원천에서 `UPDT_DT >= 2026-08-03`인 계정도 **0건**이라, "미적용"이 아니라 **원천 자체가 그 이후 변경되지 않은 것**으로 확인됐다. `A_ACCT` 773행 중 화이트리스트 WHERE절(`ISNULL(DEL_FG,'N') <> 'Y'`)로 제외되는 82행은 전부 `DEL_FG='Y'`(삭제표시) 계정임을 로직으로 재확인해, 누락이 아니라 설계대로 동작함을 확정했다. **`project_fg` 전량 NULL도 오늘 독립 재추출한 691행에서 동일하게 재현돼, 미러·ETL 결함이 아니라 ERP 원천 `A_ACCT.PROJECT_FG`가 비어 있는 것으로 확정**됐다(2026-08-03엔 "가능성"으로만 기록했던 것이 실측으로 승격) — 서버 재검증 5번(프로젝트 필수 계정 강제)은 계속 무력 상태이며 A5·유니포인트 확인 대상으로 유지한다(신규 조치 없음). **(2026-08-11 갱신 — 어제 "차이 0" 결론 자체는 정확했고, 그 다음 새 계정이 실재로 등록됐다)** 관리자가 실제 재동기화(`etl_run.py --job all`, 08-11 11:12 KST)를 실행한 결과 `acct_master`가 691 → **692**로 늘었다 — 2026-08-11 09:53에 ERP에 신규 계정 1건(`55004101` 종속기업투자주식손상차손, `bal_fg=DR`·`bs_pl_fg=PL`)이 등록돼 이번 실행에 반영됐다. 관리자가 지칭한 "미적용 회계계정정보"는 결과적으로 **실재했고 이번에 해소**됐다. `project_fg`는 신규 계정을 포함해 **692행 전량 NULL 유지**로, ERP 원천 문제라는 판단이 재확인됐다(A5·유니포인트 대상 그대로). **② 연동 현황 뷰 사각지대 — 발견·수정 완료**: 조사 과정에서 `bp_master`(거래처마스터, `B_BIZ_PARTNER`→`erp_ro.bp_master_s`) job이 2026-07-24에 이미 실적재돼 **4,650행**이 정상 가동 중인데도 `public.v_erp_sync_overview` 정의에서 누락돼 연동 현황 화면에 표시되지 않고 있었음을 확인했다(사각지대). 마이그레이션 **`erp_sync_overview_add_bp_master`**를 적용해 뷰를 **13종 → 14종**(`bp_master`·'거래처 마스터'·`B_BIZ_PARTNER`, sort=35, sensitive=false)으로 확장했다(`security_invoker=true` 유지). 적용 전 `bp_master_s`가 `authenticated` SELECT GRANT + internal 전용 RLS(`internal_select_bp_master_s`)를 이미 보유해 `item_master_s`와 동일 패턴임을 확인한 뒤 실테이블 `count(*)` 방식을 적용했다(회계 마스터 2종은 GRANT 없는 fail-closed 구성이라 여전히 `batch_run` 건수 방식 유지 — 2026-07-21 `iv_dtl` GRANT 누락형 403 재발 방지 원칙 계승). 권한 격리도 실측 재확인했다 — 사내(internal) `bp_master` **4,650** / 협력사(vendor) **0** / 회계 마스터는 vendor에게 null, permission denied 없음(화면 403 없음). **같은 조사에서 정본 SQL 드리프트도 시정했다** — `실제구축준비 자료/이관/sql/08_erp_pub_reporting_views.sql`의 뷰 정의가 5종짜리 옛 버전으로 방치돼 라이브(14종)와 어긋나 있던 것을 최신 정의로 교체하고, 확장 이력(`erp_sync_overview_v2` → `erp_sync_overview_accounting_master` → `erp_sync_overview_add_bp_master`)과 **row_count 산출이 두 갈래인 이유**(GRANT 있는 테이블은 실테이블 `count(*)` / GRANT 없는 fail-closed 테이블은 `batch_run` 최근 성공 건수)를 주석으로 명시했다. **정본 파일이 라이브 정의를 못 따라가는 이 드리프트 자체가 재발 가능한 관리 이슈**다 — 이번엔 발견해 바로잡았지만, 앞으로 뷰를 확장할 때마다 정본 SQL 파일도 같이 갱신하는 습관이 필요하다(재발 방지 메모, §6·§7 등재). **③ 적재 노후도 — 관리자 인식과 정반대**: `etl_meta.batch_run` 실측 결과 회계 2종(`acct_master`·`cost_center`)이 **2026-08-03**(8일 경과)으로 오히려 가장 최신이었고, 나머지 **12개 job**(`usr_master`·`dept_master`·`usr_erp_module`·`hr_payroll`·`pur_order`·`pur_req`·`item_master`·`sales`·`iv_dtl`·`purchase`·`inventory`·`bp_master`)은 전부 **2026-07-24**(18일 경과)에 멈춰 있었다 — "회계가 미적용"이라는 관리자의 최초 인식과 정반대다. **(2026-08-11 갱신)** 관리자가 실제로 재동기화를 실행했다(`etl_run.py --job all`, 11:12 KST) — 결과는 **7 성공 / 7 실패**였다: 성공 `usr_master` 16·`dept_master` 351·`usr_erp_module` 389·`item_master` 216·`acct_master` **692**·`cost_center` 133·`bp_master` 15(추출=적재, 유실 0), 실패 `pur_order`·`pur_req`·`sales`·`purchase`·`iv_dtl`·`inventory`·`hr_payroll` 7종 전부 동일 오류 `HYC00 [Microsoft][ODBC SQL Server Driver] 선택 기능을 구현하지 않았습니다 (SQLBindParameter)`(rows_read=0). **원인**: 이 PC엔 **레거시 `SQL Server` ODBC 드라이버**만 설치돼 있고 17/18이 없다(어제 커넥터가 이미 확인해 둔 사항이 이번 실행에서 실제로 발현) — 이 드라이버는 **`SQL_TYPE_DATE` 파라미터 바인딩을 구현하지 않으며**, 실패한 7개는 전부 `year_start`/`year_end` 등 `datetime.date` 파라미터를 넘기는 job이고 성공한 7개는 파라미터가 없거나 `datetime`(watermark)만 쓰는 job이라 실패/성공이 이 기준으로 정확히 갈렸다. **함정**: 절반이 성공해 로그가 정상처럼 보인다 — `--job all`의 종료코드·부분성공만 신뢰하면 놓친다. **조치(완료)**: `etl_run.py`의 `run_job()`에 `date` 파라미터를 `datetime`(자정)으로 승격하는 변환을 추가했다(경계비교가 `>= 시작 AND < 종료`라 결과는 완전히 동일, 드라이버 교체 후에도 안전장치로 유지). 실패했던 7개 전부 `--dry-run`으로 재검증해 정상 확인했다 — `pur_order` 354·`pur_req` 1,261·`sales` 40·`purchase` 196·`iv_dtl` 660·`inventory` 2,271·`hr_payroll` 167행(`CONVERT(date,…)` 결과 컬럼은 레거시 드라이버에서도 정상이라 파라미터 바인딩만이 원인임이 확정됨). `etl/README.md`에 "실행 환경(ODBC 드라이버)" 절을 신설해 함정·회피책·근본해결(`ODBC Driver 18 for SQL Server` 설치 권장)을 기재했다. **잔여(당시 기준)**: 실패한 7개 job의 실적재는 미실행 — 관리자 `!` 직접 실행 대기였다(이후 **2026-08-11 15:35에 완료**, 아래 신규 단락 참조). **④ 결의전표 참조 마스터 대조**: 화면·서버가 필수로 요구하는 마스터(계정 691·코스트센터 133·부서 351·거래처)는 전부 적재 완료돼 기능 차단 요인이 없다. `project_no`·`vat_type`은 미적재이나 애초에 마스터 검증 로직이 구현돼 있지 않아 "동기화 누락"이 아니라 "미구현 검증 기능"으로 분류하고, 후속 후보(부가세유형 `A_TSA_VAT_TYPE_MAPPING` 13행, 프로젝트 마스터 `PMS_PROJECT` — 스키마 확인 필요)만 등재했다(신설 없음). **⑤ 환경 이슈(기록만)**: 관리자 PC에 `pyodbc`가 미설치 상태였던 것을 확인해 설치(5.3.0)로 해소했다 — 과거 적재가 다른 환경에서 이뤄졌을 가능성이 있어 재현성 관점에서 기록해 둔다. Supabase MCP(`supabase-jeilax`)가 일시적으로 인증 오류(`password authentication failed for user "postgres"`)를 낸 뒤 자연 복구된 사례도 있었다(원인 미상, 재발 시 REST RPC 경로 우회 가능함을 기록). **⑥ `gl_slip` 현행 유지 재확인**: 결의전표 원장(`A_TEMP_GL`)은 `erp_load_scope`에서 회계 모듈 중 유일한 비적재(`none`) 항목이며, 관리자 결정으로 **현행 유지(의도적 미연계, 확대는 경영 승인 별건)**임을 2026-08-10 자로 재확인했다(변경 없음). **⑦ 신규 이슈 — `timestamptz` 라벨 9시간 오차(2026-08-11 발견, 미조치)**: 위 신규 계정의 `src_updated`가 `2026-08-11 09:53:41.923+00`으로 저장됐는데, 이를 UTC로 해석하면 KST 18:53(미래 시각)이라 성립하지 않는다 — ERP의 naive datetime(사실은 KST)이 `timestamptz` 컬럼에 **UTC로 잘못 라벨링**되어 실제 시각과 9시간 어긋난다. 영향: 증분 watermark는 `LOOKBACK_DAYS=2` 버퍼 덕에 현재 유실은 없으나, 화면 "데이터 기준 시각"(`etl_meta.v_last_success`·`v_erp_data_asof`) 표시가 9시간 틀어져 보인다. 수정 범위가 ETL 적재 전반·기존 적재분 소급까지 걸쳐 영향 평가가 필요해 **이번엔 손대지 않았다** — 별건 과제로 §6에 등재만 한다.

**2026-08-11 15:35에는 위 '실패 7개 재실행 대기' 항목이 실제로 완료됐다 — 관리자가 "직접작업진행"을 지시해 어시스턴트가 직접 실행했다(기존 `!` 직접 실행 게이트를 관리자가 이번 건에 한해 명시적으로 해제).** 이 지시에 `hr_payroll`도 포함돼 함께 적재됐고, 여전히 **집계만**(월×부서 인원·급여총액, 월×전사 퇴직 — 개인 행·이름·주민번호·계좌 없음) 추출했다. **7개 job 전량 success, 추출=적재, 유실 0**(`etl_meta.batch_run` 실측): `pur_order` 367·`pur_req` 1,267·`sales` 40·`purchase` 196·`iv_dtl` 660·`inventory` 2,276·`hr_payroll` 167. 이로써 **date→datetime 승격 수정이 실적재에서도 정상 동작함이 확정**됐다(앞서는 `--dry-run`으로만 검증했었다) — 레거시 ODBC 드라이버의 `SQL_TYPE_DATE` 파라미터 바인딩 미구현 이슈는 코드 레벨에서 해소됐다(근본해결인 드라이버 교체는 여전히 선택 권장 사항으로 남음, §6 유지). 실행 후 `v_erp_sync_overview` **14종 전부가 2026-08-11 동기화**로 갱신됐다: `pur_order` 3,137→**3,422**(2026-01-07~08-11) · `pur_req` 4,136→**4,566**(2026-01-06~08-11) · `item_master` 58,589→**58,751** · `bp_master` 4,650→**4,660** · `sales` 37→**40**(2026-01~08) · `purchase` 152→**196**(2026-01~08) · `iv_dtl` 1,984→**2,638**(2026-01-05~08-07) · `inventory` 1,871→**2,277**(2026-01-05~08-11) · `usr_master` 99→**100** · `hr_payroll`(민감·건수만) 165→**167** · `acct_master` 691→**692**(08-11 오전 갱신분 유지) · `dept_master` 351·`usr_erp_module` 397·`cost_center` 133은 변동 없음. **18일 적재 공백(07-24→08-11)이 완전히 해소**됐고, 발주·구매요청·재고의 최신 데이터 시점(`period_max`)이 당일(08-11)까지 따라잡혔다. **미해소로 남은 것(변경 없음)**: ①근본해결(`ODBC Driver 18 for SQL Server` 설치)은 여전히 미설치·선택사항 ②`A_ACCT.PROJECT_FG` 692행 전량 NULL(A5·유니포인트 확인 대상) ③`timestamptz` 9시간 라벨 오차(등재만, 미조치) ④`gl_slip`(결의전표 원장) 의도적 미연계(현행 유지) ⑤미러의 v0.18~v0.20 버전 공백은 소급 복원 불가(2026-08-10 재발방지 기록 그대로).

**2026-08-10~11에는 결의전표 파일럿의 다음 트랙으로 '반복전표 자동화'를 분석·기획했다(관리자 지시, 신규 문서 10 신설 — 중간DB 변경 없음).** 관리자가 매달 반복 작성되는 결의전표를 자동화하는 방안을 요청해, ERP운영DB의 2026년 수기 결의전표(`GL_INPUT_TYPE='TG'`)를 읽기전용으로 전수 분석했다 — `A_TEMP_GL`/`A_TEMP_GL_ITEM`/`A_TEMP_GL_DTL`(관리항목)을 `WITH (NOLOCK)`·파라미터 바인딩으로 조회(DDL/DML 없음, A2 승인 근거 위에서 수행): 헤더 **2,287건**·라인 **10,858건**·관리항목 **27,634건** + 마스터 4종(`A_CTRL_ITEM` 52행·`A_ACCT_CTRL_ASSN` 997행·`A_ACCT` 691행·`B_BIZ_PARTNER` 4,660행). **분석 결과**: 동일 부서+계정조합+거래처가 3개월 이상 반복되는 **반복 패밀리 156개 = 964건(전체의 42.2%)**, 이 중 금액까지 완전고정 34개, 6개월+·월1건형 **36개(242건)**를 1차 시드 등재 후보로 확정했다. 유형(T1 세금계산서 고정형~T7 기타)과 계정별 관리항목 체계(어느 계정에 어떤 관리항목이 필수인지는 ERP 설정 `A_ACCT_CTRL_ASSN`이 정의 — **현 `gl_draft` 파일럿에는 이 구조가 없음을 확인**)까지 실측했다. 신규 문서 `10_반복전표_자동화_기획.md`(템플릿 플랫폼 v2 — 월반복 프리필·관리항목 자동화 F1~F9 기능, DDL 스케치, Edge Function op 확장, 신규 ETL job 2종 계획)를 작성(v1.0)한 뒤 4관점(수치·회계·UX·규칙) 교차검증(지적 36건)을 반영해 **v1.1로 개정**했다. 재실행 가능한 분석 도구 `10_ERP_DB연계/etl/analyze_repeat_gl.py`도 함께 만들었다(민감 관리항목 EM/BA/D1/CP/NN은 값 미기록·개수만 저장, 출력 JSON은 OneDrive 밖 로컬 전용, 커밋·중간DB 적재 금지). **이번 작업은 분석·기획뿐이며, 중간DB 실적재·DDL 적용·Edge Function 배포는 하지 않았다** — 승인 게이트 **B1**(관리항목 마스터 2종 적재)·**B2**(시드 검수 + 부가세·원천징수 규칙 확정 = 기존 미결정 U-1 해소·확정 결정 C-2 범위 개정)·**B2-1**(회계기간 마감 기준)·**B3**(파일럿 대상)이 §6에 신규 등재됐다.

**2026-08-11 오후에는 반복전표 자동화의 승인 게이트(B1~B3)가 관리자 결정으로 집행됐다** — 정본 SQL(`실제구축준비 자료/이관/sql/23_erp_ctrl_master.sql`)·`etl_run.py` job 신설·10번 기획서 v1.2 개정·`00_관리체계/03_변경관리_CHANGELOG` 반영은 이 결정과 함께 이미 이뤄져 있었고, 이번 작업은 관제 문서(00·02) 반영을 완료한 것이다. **관리자 결정**: **B1 승인** / **B2 부분 승인**(부가세·원천징수 자동계산 규칙은 범위 제외 — 시드 등재는 진행하되 세액은 수기 입력 + 전월값 프리필만 제공) / **B2-1 ERP 회계마감 연동 적용** / **B3 파일럿 대상 = 이훈희·최동혁**. **B1**: 마이그레이션 `gl_ctrl_master_v1`을 적용해 `erp_ro.ctrl_item_s`(관리항목 마스터)·`erp_ro.acct_ctrl_assn_s`(계정별 관리항목 요건)를 신설했다(둘 다 RLS ON·정책 0개로 fail-closed, anon/authenticated 회수 — 기존 회계 마스터와 동일 규약), `erp_master_upsert` RPC에 2분기를 추가했다. `etl_run.py`에 신규 job `ctrl_item`·`acct_ctrl_assn`을 추가해 **dry-run으로 52행·1,001행을 확인**했다(테이블 사전 대비 997행보다 4행 많은 것은 그 사이 증가분). **실적재는 아직 미실행 — 관리자 직접 실행 대기**(`python 10_ERP_DB연계/etl/etl_run.py --job ctrl_item` / `--job acct_ctrl_assn`). **B2-1**: ERP 월마감 원장이 `C_CLOSE_STATUS`(작업년월 YYYYMM·`CLOSE_FLAG`·`CLOSED_DATE`, 마감 33행)임을 실측으로 확정했다 — **2023-10~2026-06은 전월 마감(Y), 2026-07부터는 미마감**. 이 실측을 근거로 `public.gl_period_lock`에 **33개월을 `locked=true`로 등재 완료**했다(등재 근거는 각 행의 note에 기록). 이로써 **09번 문서 §5.2에 남아 있던 "마감월 통제 부재"가 처음으로 해소됐다**(09 원문은 이번엔 수정하지 않고 이력으로 보존, 해소 사실은 본 문서에만 기록). 다만 주의할 점 2가지가 함께 확인됐다 — ①ERP 실측상 마감월에도 결의전표 소급 등록이 관찰된다(예: 1월 귀속 전표가 8월에 등록) — 즉 **포털 잠금이 ERP 운영관행보다 더 엄격**해지는 셈이라, 예외가 필요하면 회계 담당자가 해당 월 lock을 해제하는 방식으로 처리한다. ②`gl_period_lock`은 자동 동기 job이 없어 **새 달이 마감될 때마다 수동으로 행을 추가**해야 한다(후속 과제로 남김, 임의 자동화하지 않음). **B3**: `perm_module_catalog`에 `accounting`(회계/결의전표 입력, `sensitive=true`) 모듈을 신설했다 — 기존 A3 "모듈 신설" 결정의 실제 집행이다. `perm_grant`로 **이훈희**(`hh.lee2@jeilm.co.kr`, 총무팀)에게 `erp_module`/`accounting` allow 권한을 부여했고(`granted_by=dh.choi`), `perm_audit`에 부여 이력이 함께 기록됐다. 실측 검증 — 이훈희는 `gl_draft` `allowed=true`(전사 공개 페이지 + 모듈 보유 조건 충족), 비대상자(`hk.yang`)는 `false`(사유 "ERP 모듈 권한 없음(accounting)"), 최동혁은 전체관리자 자격으로 기존과 같이 허용. **부서 단위 부여는 계속 하지 않는다**(C-8 유지, 개인 예외는 이훈희·최동혁 2명뿐). **B2는 부분 승인이라 계속 미결로 남는다** — 시드 36건(→73건)의 계정 구성 검수(회계 담당 확인 후 등재)만 잔여이며, 파생 자동계산 규칙은 범위에서 제외됐으므로 U-1은 미해소·C-2는 개정 없음. **B4**(L3 자동 초안 생성 여부)는 여전히 미결로, 파일럿 회고 후 재판단한다. 상세는 `10_반복전표_자동화_기획.md §8`(v1.2).

**2026-08-12에는 관리자 지시("실적재 진행")로 반복전표 자동화의 B1(관리항목 마스터 2종 적재)이 실적재까지 완료됐다.** ETL 2종을 실행해 `erp_ro.ctrl_item_s` 52/52·`acct_ctrl_assn_s` 1,002/1,002를 적재했다(추출=적재, 유실 0, `batch_run` success 2건, 2026-08-11 23:33 UTC 완료 — dry-run 1,001행 대비 +1은 그 사이 원천 증가분). 계정별 필수 관리항목(DR_FG/CR_FG='Y') 377행이 확보돼 결의전표 화면의 관리항목 자동 생성·필수 검증 데이터 준비가 끝났다. `erp_load_scope`에 accounting/`ctrl_item`·`acct_ctrl_assn` 'loaded' 2건을 등재하고, 연동 현황 뷰 `v_erp_sync_overview`를 14→**16종**으로 확장했다(마이그레이션 `erp_sync_overview_ctrl_master`) — 회계 마스터 선례와 동일하게 fail-closed 규약에 따라 신규 2종도 실테이블이 아닌 `batch_run` 기반 count를 쓴다. 반복전표 자동화 §6.8 기준 **P2가 완료**됐고, 잔여는 P1 나머지(초안·템플릿 관리항목 DDL·Edge Function 확장)·P3 화면·P4 시드(계정 구성 검수 후)다. 10번 기획서(v1.3)·CHANGELOG·정본 SQL(`이관/sql/23_erp_ctrl_master.sql`)은 이 작업과 별도로 이미 반영돼 있다.

**2026-08-12 오후에는 관리자 지시("P1 나머지 + P3 화면 구현 진행")로 반복전표 자동화의 P1(구조)·P3(화면)이 구현까지 완료됐다(10번 기획서 v1.4·CHANGELOG·정본 SQL 24번·HTML 재빌드는 이 작업과 별도로 이미 반영돼 있어, 이번 갱신은 관제 문서(00·02) 반영만 수행한다).** **①DDL**: 마이그레이션 `gl_template_v2`(정본 `실제구축준비 자료/이관/sql/24_gl_template_v2.sql`) — 초안 라인 관리항목·계산서 그룹(`invoice_seq`)을 담는 `gl_draft_item_ctrl`, 템플릿 관리항목 규칙(fixed/input/date_shift)을 담는 `gl_template_item_ctrl`, 회차별 4상태 이력을 담는 `gl_template_usage`를 신설하고, `gl_template`에 월반복 속성 5컬럼(recur_cycle·title_pattern·month_offset·vat_mode·src_family_key)과 `gl_template_item.amt_locked`를 추가했다. service_role 전용 RPC `gl_ctrl_master_get`도 신설했다. 전부 C-6 규약대로 **RLS ON·정책 0개·anon/authenticated 회수**. **②Edge Function**: `jeil-gl-draft`를 **v4로 배포**(플랫폼 배포 카운터 4, ACTIVE, `verify_jwt=false` 유지) — 신규 op `tpl_recur_list`(월반복 카드 목록)·`tpl_apply_prev`(전월 프리필)·`tpl_seed_bulk`(전체관리자 한정 시드 일괄 등재, 감사 기록) + 기존 save/get/submit/void/post에 관리항목 저장·검증과 usage 상태 동기를 확장했다. 필수 관리항목 검증은 신형(ctrl_aware) 화면에만 하드 적용하고 구화면은 경고만 띄워 **구화면 하위호환을 유지**했다. **세액 자동계산·검증은 여전히 없다**(B2 결정 준수). 무토큰 호출 401을 재확인했다. **③화면**: `pages/자금_결의전표_입력_2026.html`을 v2로 갱신했다 — 월반복 홈(배지 4상태·작성자 표시·중복 작성 경고·이어서 작성·미작성 우선 정렬), 전월 프리필(본인 최근 확정 초안 기준, 날짜 관리항목 +1개월 월말클램프 제안값 배지, 적요 토큰 치환), 관리항목 자동 패널(계정×차대 필수 * 표시), 제출 전 요약(전월 대비 달라진 점 리스트), 회계 담당자용 관리항목 표(ERP 입력 순서 정렬)+TSV 복사, 템플릿 편집기의 월반복 속성 3필드·프리필 잠금 컬럼을 추가했다. **④3관점 코드 리뷰**(정확성·보안 2관점 완료, UX 리뷰어는 API 연결 오류로 실패) **지적 16건 전건 반영 후 재배포**했다 — 치명 1건(수동 추가 행의 관리항목이 그리드 얕은복사 참조 문제로 유실되던 것), 구화면 저장이 관리항목을 지우던 문제, 구화면의 템플릿 수정이 월반복 속성·관리항목 규칙을 지우던 문제, KST 기준 회차 경계(매월 1일 00~09시) 처리, 일반 템플릿 적용·타 초안 로드 시 월반복 상태(CURRENT_TPL)가 오염되던 문제, 민감 관리항목(EM 사번·BA 계좌·D1·CP·NN)을 부서/전사 템플릿 고정값으로 저장하는 것 차단, 타인의 미제출(draft) 초안 열람 차단(list·get 403 — 최소권한), tpl_use 가시성 확인(통계 오염 방지), post 중복검사를 다건 대응으로 보정(`maybeSingle`→`limit` 목록), usage 기록 위조 방지(템플릿 가시성 검증 + `use_ym` ±2개월 제한), `tpl_recur_list` 응답 최소화(타인 초안번호 미반환·작성자는 이메일 local-part만)를 반영했다. **잔여(사람 결정/실행)**: ①화면 라이브 반영은 관리자의 git 커밋·gh-pages 배포 이후다 — **현재 라이브는 여전히 구화면**이며, Edge Function v4는 하위호환 설계라 안전하다. ②P4 시드 36건 등재는 회계 검수(B2 잔여인 계정 구성 검수) 완료 후 `tpl_seed_bulk`로 일괄 처리한다. ③코드 리뷰 후속 과제 1건 — 구화면에서 저장된(관리항목 미검증) 초안을 회계 담당자 화면에서 구별 표시하는 배지 신설(서버 로그에는 ctrl_aware 여부 기록이 이미 시작됨, 화면 반영은 후속).

**2026-08-18에는 결의전표 파일럿에 「전표복사」 기능이 신설·배포되고, 그 기반으로 결의전표 미러(`A_TEMP_GL` 3종)를 중간DB에 적재하는 결정(C-10)이 내려졌다(관리자 지시).** 배경은 회계 담당자가 본인이 ERP에 입력한 결의전표를 기간·검색으로 조회해 라인·관리항목을 미리 보고, 계정·금액·적요·거래처·코스트센터·프로젝트·관리항목을 그대로 복사해 새 초안을 시작할 수 있게 하는 것이다(`pages/자금_결의전표_입력_2026.html` 전표복사 탭 — ERP 전표복사 메뉴와 동일 흐름, 복사 시 결의일자는 오늘로 새로 채우고 참조번호는 미복사, 마스터에 없는 계정은 비우고 경고. 이후 수정·저장·제출은 기존 초안 흐름 그대로, 서버 재검증 통과 필요). **관리자 결정 C-10**은 기존 **C-7("코드 마스터만 적재 — 전표는 적재하지 않는다")을 확장**해 결의전표 원장(헤더·라인·관리항목)까지 중간DB에 미러링하기로 한 것으로, 통제는 3중이다 — ①`erp_ro` RLS 전면차단(정책 0) + RPC(service_role) 단일 경로 ②조회 RPC가 소유 필터(`insrt_user_id`=JWT 매핑 본인 ERP ID, C-3 1:1 매핑 근거)를 내부에서 강제 ③민감 관리항목(EM 사번·BA 계좌·D1·CP 신용/구매카드·NN 어음)은 ETL 추출 SQL에서 값 자체를 NULL 마스킹(중간DB에 값을 두지 않음). **구축물**: ①Supabase 마이그레이션 `gl_slip_mirror_v1`+`gl_slip_synced_at_v1` 적용 — `erp_ro.gl_slip_s`(헤더)·`gl_slip_item_s`(라인)·`gl_slip_ctrl_s`(관리항목) 3종 신설, `erp_master_upsert` RPC를 3분기 확장(총 7분기), 신규 RPC `gl_slip_list`(본인 전표 목록, limit 300)·`gl_slip_get`(단건 헤더+라인+관리항목, 계정/거래처명 조인)·`gl_slip_synced_at`(동기 시점) — 전부 service_role 전용. ②정본 DDL `실제구축준비 자료/이관/sql/25_gl_slip_mirror.sql` 신설. ③`10_ERP_DB연계/etl/etl_run.py`에 신규 job 3종(`gl_slip`·`gl_slip_item`·`gl_slip_ctrl`, 2026년 전체·전 입력경로, `h.UPDT_DT` 증분 지원, rpc=`erp_master_upsert`). ④Edge Function `jeil-gl-draft` **v5 배포**(ACTIVE) — op `slip_list`·`slip_get` 추가(`canInput` 필요, `erpUsrId` 미매핑 시 409, 전 접근을 `gl_draft_log`에 slip_list/slip_view/denied로 기록). ⑤화면 전표복사 탭(조회 필터: 기간 기본 최근 3개월·적요/전표번호 검색·입력경로 필터, 목록·미리보기, 미러 동기 시점 표시). **검증**: ERP운영DB dry-run 3종 성공 — `gl_slip` 3,224행·`gl_slip_item` 16,865행·`gl_slip_ctrl` 42,371행(2026-08-18). 화면 JS 구문검증·`etl_run.py` 컴파일 통과, RPC 빈상태 응답 정상(`[]`/`null`). **잔여(관리자 직접 실행 대기)**: **실적재 미실행** — `python "10_ERP_DB연계/etl/etl_run.py" --job gl_slip` → `--job gl_slip_item` → `--job gl_slip_ctrl` 순으로 관리자 직접 실행 필요, 실적재 후 확인 2건(①`batch_run` 3건 success ②민감 관리항목 EM/BA/D1/CP/NN `ctrl_val` NULL 마스킹 실측 확인), 야간 배치 스케줄에 3개 job 편입(현재 수동). **알려진 한계**: upsert 적재라 ERP에서 삭제된 전표는 미러에 잔존 가능(복사 원천 용도로는 허용), 야간 미러라 실시간이 아님(화면에 동기 시점 표시로 안내). 이로써 **앞선 단락(§1 2026-08-10~11)에서 "`gl_slip` 현행 유지(의도적 미연계)"로 기록됐던 방침은 이번 C-10 결정으로 대체**된다 — 과거 기록은 그 시점의 결정으로 그대로 보존하고, 변경 시점만 이번 단락에 명시한다.

현재 위치는 **P0(정책 수립) 마무리 → P1(유니포인트 협의) 진입 대기 + P3(중간DB) 내부 준비 계속(재고 ETL 연 전체 적재 전환 + **매입 상세(`iv_dtl`) 신규 연동·구매요청 상세 백필·매입 대사 재적재 5종 실적재 완료(2026-07-21)** + **중간DB(erp_ro) 정합성 SQL 검증 완료 — 이상 없음(2026-07-22)** + **협력사 거래처마스터(`vendor_master`) 재적재·정합화 + ERP 거래처마스터 원천 연동(`bp_master`) 스키마·ETL 준비(2026-07-22)** + **야간배치 수동 실행 — 12개 job 전량 성공, `bp_master` 적재버그 RCA·수정·재성공 포함(2026-07-24)** + **회계 코드 마스터 실적재·A2 실측 확정(2026-08-03)** + **회계 마스터 대사 검증(차이 0)·연동 현황 뷰 사각지대 수정(bp_master 13→14종)·12개 job 재동기화 완료(2026-08-10 노후도 실측 → 08-11 실행 7성공/7실패 → 원인수정 후 08-11 재실행 전량 성공)** 포함) + P4(포털·챗봇 연결) 착수(부분 완료, 사용자-부서 매핑·부서별 ERP모듈 제안값 실적재·가동 + 챗봇 ERP 프로세스 응답 품질 개선 + 챗봇 권한체계 연결(get_my_access, v19)·포털 도구 권한구멍 차단 + 연동 현황 뷰 10종 전체 노출·관리자 콘솔 반영(2026-07-21) + **챗봇 ERP 조회 정합성 RCA·품목코드 역조회 도구 `get_erp_item_orders` 신설(v26, 2026-07-22)** + **협력사 거래처 정보 정합화(2026-07-22)** 포함) + P5(민감 영역) 인사 급여 집계 실적재 완료(자금 미착수, Entra HR 그룹 미신설) + 챗봇 인사 조회 게이팅(get_hr_headcount·get_hr_payroll, v19) + **결의전표 초안 파일럿(회계 A 모듈, 전체관리자 한정 테스트 중, A5 승인 대기)** + **반복전표 자동화 — 분석·기획(10번 문서 v1.4, 2026-08-10~12) + 승인 게이트 B1·B2-1·B3 집행 완료(2026-08-11 오후) + B1 실적재 완료(2026-08-12, 연동 현황 16종) + **P1(구조)·P3(화면) 구현 완료(2026-08-12 오후, 코드 리뷰 16건 반영 후 재배포, 라이브는 커밋·배포 대기)**, P4 시드(계정 구성 검수 잔여)·B2 부분승인·B4만 미결** + **결의전표 파일럿 — 전표복사 기능 신설·배포 + `gl_slip` 미러 3종(헤더·라인·관리항목) 구축·dry-run 검증 완료, 실적재는 관리자 직접 실행 대기(C-10 결정, 2026-08-18)** 가동**다.

---

## 2. 원천 시스템(ERP운영DB) 현황

| 항목 | 값 | 출처 |
|---|---|---|
| DB | ERP운영DB (JEILMNS, **MS SQL Server 2022**) — 사외 IDC | 마스터플랜 v2.0 §2.1 |
| 구성 | **단일 노드** — 읽기 복제본·리포팅 사본 없음 | 〃 |
| 규모 | **2,658 테이블 · 약 22.16M 행 · FK 1,361개** | 〃 |
| 대용량 모듈 | 원가 3.8M행 · 회계/자산 3.5M행 · 인터페이스/기타 13.5M행 | 〃 |
| 가동 인터페이스 | 그룹웨어 전자결재 `INTERFACE_KO174`(약 49,000건) — **쓰기형, 조회연동과 별개** | 〃 |
| 관리 주체 | 유니포인트(ERP 벤더) — 스키마 접근·뷰 생성은 협의 필요 | `CLAUDE.md §4.6` |

## 3. 접근·분석 자산 현황 (이미 가동 중)

| 자산 | 상태 | 비고 |
|---|---|---|
| 읽기전용 조회 체계 | **가동** — 코드레벨 READ-ONLY 가드(`erp_chat/lib/db.js`, `query.ps1`) | SELECT만 허용, 수정 SQL 실행 금지 |
| 자격증명 격리 | **가동** — DPAPI 암호화(`%USERPROFILE%\.erp\`, OneDrive 밖) | 호스트/ID·비밀번호·포트 분리 보관 |
| 스키마 사전 | **구축 완료** — `dictionary.json`(2,658테이블·컬럼·FK·행수), 기준일 2026-04-15 | dictionary-first 원칙(DB 왕복 최소화) |
| 전담 서브에이전트 | **가동** — `erp-db-analyst` / `oz-report-manager` / `business-question-manager` | ERP_DB 작업환경(사내 OneDrive) 소속 |
| 작업 로그 | **가동** — 모든 DB 작업 `doc/db_log_*.md` 기록 | ERP_DB `CLAUDE.md` 규칙 |

## 4. 포털(JEIL AX) 측 연계 현황

| 항목 | 현재 |
|---|---|
| ERP 실연계 | **부분 연계 착수(2026-07-08)** — 중간DB 경유로 부서 대시보드 1개 화면(구매 매입집계) + 챗봇 ERP Tool 4종이 실데이터 연동됐고, 나머지 `pages/` 화면은 여전히 추출 샘플(마스킹) 데이터로 동작 |
| 포털 데이터화 | Vercel + Supabase 가동(협력사 발주 포털·계정발급) — **ERP 데이터는 노출 뷰(P4) 경유로 부분 반영 시작**(§4 P4 행 참조) |
| 중간 DB — 스키마 | **적용 완료**(2026-07-07) — 기존 Supabase 프로젝트(`03_중간DB_구축실행기획` D1-a안)에 `erp_ro`(1차 4종: 품목·영업월·구매월·재고일 + `pur_order_s`(발주) + `table_dict` ERP 테이블 사전) · `etl_meta`(`batch_run`+`v_last_success`) 스키마와 RLS(사내만 SELECT, anon·vendor 차단) 마이그레이션 적용 |
| 중간 DB — 적재 경로 | **적용 완료** — service_role 전용 RPC(`erp_etl_upsert`/`erp_etl_batch`)로 `erp_ro`를 REST에 노출하지 않고 적재. **1차 5종 실적재 완료**(2026-07-07, 관리자 직접 실행): `pur_order_s` 2,699 / `item_master_s` 58,246 / `sales_orders_m` 10 / `purchase_m` 63 / `inventory_d` 656 / `table_dict` 2,658행. 2026-07-08 dry-run 재확인 결과 ERP 원천이 소폭 늘어(발주 2,702·품목 58,249·매출 10·매입 63·재고 659) 최신 재적재 명령 준비(관리자 `!` 직접 실행 예정, 미실행) |
| 중간 DB — ETL 스크립트 | `10_ERP_DB연계/etl/`: `load_table_dict.py`·`etl_run.py`(1차 5종, `--dry-run` 지원) **실행 완료**(2026-07-07). 야간배치 정기 가동(스케줄러)은 아직 미구성 |
| **중간 DB — 노출 계층(P4)** | **신설(2026-07-08), 같은 날 2차례 확장** — public 스키마 사내 전용 `security_invoker` 뷰. ①최초 6종: `v_erp_sales_monthly`·`v_erp_purchase_monthly`·`v_erp_inventory_daily`·`v_erp_item`·`v_erp_pur_order`·`v_erp_data_asof`(job별 최신 성공시각, 정본 `08_erp_pub_reporting_views.sql`). ②구매요청(`M_PUR_REQ`) 연동으로 `v_erp_pur_req`·`v_erp_po_pr_link`(발주↔구매요청 연결) 신설(정본 `10_erp_pur_req.sql`). ③**프로세스 응답 품질 개선**으로 `v_erp_po_pr_link`·`v_erp_pur_req`에 입고/매입 진행수량·외주구분·납기경과 플래그·연결수주·요청부서(이메일 해석) 컬럼 append + 발주 총액집계 신규 `v_erp_pur_top_po` 추가(정본 `15_erp_process_link_enrich.sql`). `erp_ro`/`etl_meta`는 REST 비노출 유지, 뷰는 기저 테이블 RLS(`internal_select_*`)를 그대로 상속해 사내 인증 사용자만 통과(`v_erp_pur_top_po`는 anon에도 GRANT했으나 기저 테이블 권한이 없어 실접근은 차단). `etl_meta.batch_run`에 `internal_select_batch_run` 정책 추가. 챗봇 Edge Function은 service_role 전용 GRANT로 서버측에서만 조회 |
| **RLS 격리 실측 테스트** | **통과(2026-07-08)** — JWT 시뮬레이션: 사내(internal) 정상조회(매출10·매입63·재고656·품목58,246·발주2,699·기준시각5행), 협력사(vendor) 전부 0행, anon 미부여로 차단 |
| **부서 대시보드 실연동** | **파일럿 1건(2026-07-08)** — `app/lib/api.js`에 `erpApi`(salesMonthly·purchaseMonthly·inventoryDaily·items·purOrders·dataAsof, 사내 세션 전용) 추가, `pages/구매_거래처별매입집계_2026.html`에서 사내 로그인 시 실데이터 표시(비로그인·협력사는 목업 폴백) |
| **챗봇 ERP Tool** | **가동, 2026-07-08 중 3차 확장(version 14 ACTIVE)** — `jeil-chat` Edge Function ERP Tool: 최초 4종(`get_erp_sales_monthly`·`get_erp_purchase_monthly`·`get_erp_inventory_status`·`get_erp_item`, version 7) → 발주↔구매요청 연결 2종(`get_erp_po_pr`·`get_erp_pur_top`, version 10) → **프로세스 응답 품질 개선**으로 신규 2종(`get_erp_receipt_pending` 미입고·납기경과 발주, `get_erp_pur_req` 상태·부서별 구매요청) 추가, 총 8종. **[치명적 버그 수정]** 도구 호출 오케스트레이션을 1라운드→상한 멀티라운드(`MAX_ROUNDS=4`)로 교체 — 특정 PO에 포털 도구를 먼저 골라 헛짚으면 재조회가 막혀 안내문만 반복되던 루프 해소(무한루프 3중 차단: 라운드 상한 + 직전 동일호출 반복 중단 + 마지막 라운드 강제 무도구). 상태코드 RQ→CF→PO→GR→IV 한글 해석을 도구 출력·프롬프트에 주입 |
| **사용자-부서-사원 매핑(대사)** | **실적재 완료·가동(2026-07-08)** — `Z_USR_MAST_REC`(이메일 SSOT)↔`B_ACCT_DEPT`(부서) 대사 계층: 원천 미러 2종 실적재 완료(`usr_master_s` 99건·`dept_master_s` 351건) + 파싱·대사 뷰 5종(erp_ro) + 노출 뷰 3종(`public.v_erp_user_dept`·`v_erp_user_dept_recon`·`v_erp_dept_roster`, security_invoker) + ETL job 2종. 대사 결과 **정상 매핑 89건 · 불일치 10건**(부서불일치 6·상태이상 3·테스트 1) — 불일치는 정상 매핑에서 **자동 제외** 정책 확정. 잔여: 상태이상 3건 현업 확인 권고. 상세는 `05_사용자부서_매핑대사.md` |
| **부서별 ERP 모듈 제안값(참고)** | **신규·실적재 완료(2026-07-08)** — ETL job `usr_erp_module`: `Z_USR_MAST_REC_USR_ROLE_ASSO`→`Z_USR_ROLE_MNU_AUTHZTN_ASSO`(MNU_USE_YN='Y')→`Z_CO_MAST_MNU`(ModuleInitial) 조인, ModuleInitial∈(SD/MM/IM/MDM) 추출 → `erp_ro.usr_erp_module_s` **397건** 적재. 뷰 `erp_ro.v_dept_erp_suggest`(부서 과반 접근 모듈, `v_user_dept` 이메일→부서 매핑 재사용) + `public.v_erp_dept_erp_suggest`로 노출, 관리자 콘솔(04) '권한 상세' ERP 모듈 매트릭스에 ◆ 참고표시로만 반영(자동 덮어쓰기 안 함). **실측 소견**: ERP 읽기권한이 광범위(사용자당 평균 17역할)해 부서 과반 임계로도 거의 전 모듈이 제안됨 → ERP-역할 신호는 부서 변별력이 약함, 실질 통제선은 편집형 `dept_erp_scope`. 정본 SQL `실제구축준비 자료/이관/sql/12_erp_dept_module_suggest.sql` |
| **급여 민감권한·인사 급여 집계 실적재** | **실적재 완료·가동(2026-07-08)** — 급여 민감 모듈 `payroll` 신설·`dept_erp_scope` 인사팀 전용 부여·인사 페이지(`portal_page.hr_2026`) 서버 게이트(비인사팀 차단), 관리자 콘솔 급여 ⚠ 민감표시(일괄/제안 제외), 인사 데모 페이지 개인급여 샘플화+마스킹은 라이브 유지. 민감 분리 스키마 `erp_secure`(`hr_payroll_m` 집계·`hr_access_log` 감사)에 이어 **같은 날 관리자 직접 실행으로 실적재까지 완료** — ETL job `hr_payroll`(`HDF070T` 월×부서·`HGA070T` 월×전사)로 `erp_secure.hr_payroll_m` **143행**(급여 137+퇴직 6, 2026.01~06) 적재. 컬럼은 `ym·dept_nm·headcount·pay_tot_amt·retire_amt`뿐 — **개인별·이름·주민번호·계좌 미포함**(집계만). 게이트 API **`jeil-hr`(Edge Function) v1 배포**(Entra 검증→`dept='인사팀'`/`portal_admin`만→감사→`hr_payroll_get()` 집계 반환, 그 외 403), service_role 전용 RPC 3종(`erp_secure_upsert`·`hr_payroll_get`·`hr_access_log_add`) 신설. 인사 페이지는 인사팀 권한자에게 실 집계 표시(비권한·데모는 마스킹·샘플 유지). **잔여**: 자금(F 모듈) 경영 승인·실적재는 별개 미착수, `AI-Portal-HR` Entra 보안그룹 미신설(현재는 `dept='인사팀'`/`portal_admin` 판정 대체), 유니포인트와 재직상태(퇴직 구분) 정의 확인. 정본 SQL `이관/sql/13_payroll_module.sql`·`14_erp_secure_hr.sql` |
| **재고 입출고(inventory) ETL — 롤링 31일 → 2026년 전체 적재 전환** | **완료(2026-07-13)** — `etl_run.py`의 `inventory` job(`M_PUR_GOODS_MVMT`)만 롤링 31일(`daily_start`) 창이라 6·7월만 적재돼 있던 결함을, 발주·매출·매입 job과 동일한 `WHERE MVMT_DT >= ? AND MVMT_DT < ?` + `year_start`/`year_end`로 수정(커밋 `811cbd8`). 관리자 직접 실행으로 재적재 — `erp_ro.inventory_d` 2026-01~07 전체 **1,810행**(월별 01:93·02:134·03:149·04:260·05:469·06:650·07:55, 이전엔 06·07월만 659행). `etl_meta.batch_run`: job_name=inventory, status=success, rows_read=1810, rows_upserted=1810(2026-07-13 07:54 UTC). 발주현황 보드의 입고·매입 상태(`pur_order_s` 기반)는 이 백필과 무관하게 이미 연중 전체 반영 |
| **챗봇 권한·인사 조회 도구(신규, 2026-07-20)** | **가동, `jeil-chat` version 19** — 신규 도구 3종: `get_my_access`(호출자 본인의 역할·소속부서·열람가능 ERP모듈·접근가능/불가 페이지 반환, UPN 고정으로 타인 조회 불가, 전 직원 사용) · `get_hr_headcount`(인원현황 — 전 직원은 월별 전사 총원만, 부서별 분포는 `payroll` 권한자(인사팀·관리자)만) · `get_hr_payroll`(급여 집계, 인사팀·관리자 전용, `hr_payroll_get` RPC 경유). 인사 도구 접근은 허용·거부 모두 `hr_access_log` 감사 기록(`jeil-hr`와 원장 재사용) |
| **챗봇 포털 도구 권한 구멍 차단(보정, 2026-07-20)** | **차단 완료** — 기존 포털 도구 3종(`get_order_summary`·`get_order_detail`·`get_inspection_pending`)이 `ERP_TOOL_MODULE` 매핑 누락으로 전 직원에게 협력사 발주번호·금액·납기를 무강제 노출하던 것을 확인 → `pur_order` 모듈 권한 강제 적용으로 차단(v19). 2026-07-08 v14의 "ERP 도구 8종 `pur_order` 매핑"에는 이 포털 도구 3종이 빠져 있었음(소급 취약점) |
| **연동 현황 뷰·화면(소급 등재 + 10종 전체 노출, 2026-07-21)** | **확장 완료** — 연동 현황 화면 `app/erp-status.html` + 원천 뷰 `public.v_erp_sync_overview`는 이미 가동 중이었으나 이번에 본 문서에 처음 등재됐다(소급). 뷰가 5종(발주·품목·매출·매입·재고)만 노출하던 것을 마이그레이션 `erp_sync_overview_all_sources`로 **10종 전체**로 확장 — 신규 노출: 구매요청 `pur_req`(`M_PUR_REQ`)·부서마스터 `dept_master`(`B_ACCT_DEPT`)·사용자마스터 `usr_master`(`Z_USR_MAST_REC`)·ERP 메뉴권한 `usr_erp_module`(`Z_USR_ROLE_MNU_AUTHZTN_ASSO` 외)·급여집계 `hr_payroll`(`HDF070T`+`HGA070T`, 민감). `sensitive`·`sort` 컬럼 추가, `security_invoker=true` 유지(사내만). 급여는 실테이블 미count·`etl_meta.batch_run.rows_upserted`만 사용(금액·명세 미노출). 발주 원천 표기를 실제 조인 기준(`M_PUR_ORD_HDR + M_PUR_ORD_DTL`)으로 정정. **관리자 콘솔 반영**: `jeil-chat-admin` version 9→10이 이 뷰를 service_role로 조회해 `erp_sync`로 반환, `04_챗봇_포털_데모UI.html` 관리자 콘솔 'API' 탭에 실데이터 표 신설(기존엔 '사내 운영 페이지' 카드 링크로만 접근 가능). `app/erp-status.html`에 **연결 정보 표** 추가(원천·접속방식·적재경로·실행방식·접속정보 보관위치·데이터 격리 — 비밀값은 여전히 미기재, "실행환경 `.env`에만" 표현만 사용) |
| **매입 상세(`iv_dtl`) 신규 연동 — 실적재 완료(2026-07-21)** | 오전에 준비만 해 둔 스키마(`erp_ro.iv_dtl_s`, RLS ON)·뷰(`v_erp_iv_dtl`·`v_erp_iv_dtl_monthly`)·ETL job(`iv_dtl`)을 관리자가 직접 실행해 **실적재 완료** — 2026-01-05~07-20 구간 **1,981행**(0.9초), 송장 213건·발주 432건, 매입금액 합계 **35.6억원**, 발주 연결(`po_no`) **100%**. 참고: 발주 미연결 매입 라인 36건(1.8%, 발주 18건, 과거 발주로 추정) — 데이터 품질 참고 항목, 임의 조치 없음 |
| **구매요청(`pur_req`) 상세 컬럼 백필 — 실적재 완료(2026-07-21)** | 오전에 준비한 상세 14컬럼(조달구분·확정/릴리즈수량·구매그룹/조직·저장위치·수주라인·도면번호·추적번호·변경차수·MRP오더·생성/수정자)을 채우는 `--full` 재적재로 **4,063행** 갱신(2.1초) — 조달구분 채움 4,063/4,064(99.98%, 미채움 1건은 ERP 원천 `PROCURE_TYPE` NULL), 도면번호 보유 856건. §4.9 대사 당시(4,064건) 대비 1건 차이는 연도 범위 밖 레거시 잔존행 |
| **매입 대사 점검 + 관련 5종 재적재(2026-07-21)** | `iv_dtl` 신규 적재분을 기존 `purchase_m` 월집계와 대사한 결과 6월 +19.2억원·7월 +1.5억원 차이 발견(1~5월은 일치) — 데이터 오류가 아니라 **적재 시점 차이**(7/8 이후 ERP 신규 등록분 미반영)로 확인. 관리자가 `purchase --full` 151행 · `pur_order`(증분) 512행 · `sales` 37행 · `item_master`(증분) 415행 · `inventory` 1,847행을 재적재(전부 성공) → **재적재 후 매입 대사 차이 전월 0으로 해소**, 매핑·로직 정상 검증. `hr_payroll`(민감)은 이번 대상에서 제외(별도 승인 사항) |
| **챗봇 ERP 조회 정합성 개선 — RCA + `get_erp_item_orders` 신설(2026-07-22)** | 사용자 제보(품목코드 `S3041-00065` 발주·구매요청 조회 실패/오인) 조사 결과 **데이터·매핑 문제 아님·모델 이슈 아님 — 챗봇 도구 설계 공백이 원인**(품목코드로 발주·구매요청을 역조회하는 도구 부재). 신규 도구 `get_erp_item_orders(item)` — 품목코드/품목명→구매요청·발주·매입 이력(요청→발주→입고→매입) 반환, `pur_order` 모듈 권한 강제. SYSTEM_PROMPT에 번호 형식(PO…/PR…/품목코드) 구분·도구 지정 보강, `get_erp_po_pr` 입력 가드(PO/PR 아니면 재안내). `jeil-chat` **version 26** 배포. 범위 한계: `erp_ro`는 2026년만 적재(2025년 이전 미조회) |
| **중간DB(erp_ro) 정합성 SQL 검증(2026-07-22)** | **실측 완료 — 이상 없음**: item_code NULL·품목마스터 고아·번호형식 위반·PK 중복 전건 0건(발주 3,088·구매요청 4,064·매입상세 1,981·품목 58,533행 대상). 발견 2건(발주→구매요청 고아 49건=2025년 PR 참조·범위한계, po_no+item_code 중복 31건=po_seq 분리 정상)은 오류 아님으로 확인. **잔여**: ERP 원본 직접 대사는 ODBC 설정 접근 거부로 미실행(관리자 `--dry-run` 실행 필요) |
| **협력사 거래처마스터(`vendor_master`) 재적재·정합화(2026-07-22)** | **완료** — 사용자 제보(발주거래처 미표시) 조사 결과 초기 데모 시드 17개사에 머물러 ERP 실제 발주거래처 103개사 중 87개사가 미등록이었던 것을 확인, 발주·매입·매입상세 합집합 기준 **109개사**로 재적재(거래처명 5건 교정, 발급 계정 4건 무영향·고아 0건). 기존 17건의 가짜 사업자번호(거래처코드 기반 임의 생성값)는 제거(NULL). 관리자 전용 RPC `public.vendor_master_sync()` 신설(비관리자 차단·멱등성 검증), 관리자 화면에 "거래처 동기화" 버튼 추가. 부수 효과: 협력사 모바일 포털·계정발급 Edge Function 거래처명도 정상화. 라이브 검증 완료(ai.jeilm.co.kr, 커밋 `c11d632`) |
| **ERP 거래처마스터 원천 연동(`bp_master`) — 실적재 완료(2026-07-24)** | **완료** — ERP `B_BIZ_PARTNER` 복제용 `erp_ro.bp_master_s`(2026-07-22 신설, 최초 4,649행 적재) 위에 관리자가 `etl_run.py --job bp_master`를 직접 실행해 **증분 3행**을 반영했다. 최초 실행은 HTTP 400(PostgreSQL 42601 "INSERT has more target columns than expressions")으로 **실패** — 원인은 `erp_etl_upsert`의 `bp_master_s` 분기에서 주민번호 마스킹 case식 뒤 인라인 `--` 주석이 같은 줄의 `x.repre_nm, x.bp_type, x.bp_group` 3개 값을 통째로 주석 처리해 대상 컬럼 20 vs 값 17 불일치가 생긴 것(2026-07-22 최초 적재는 이 마스킹 코드 이전 버전이라 성공, 이후 회귀). 주석을 세 컬럼 뒤로 옮겨 함수 재생성(마스킹 기능 동일, 로컬 기록 `실제구축준비 자료/이관/sql/20_erp_etl_upsert_bp_master_fix.sql`) 후 재실행해 3/3 성공. **대표자주민등록번호·은행계좌번호는 여전히 개인정보·금융정보 최소수집으로 적재 대상에서 제외**. 부수개선: `etl_run.py`의 `rpc()`가 HTTP 오류 응답 본문을 예외·`batch_run.error_msg`에 실어 진단 가능하게 개선 |
| **현재 적재 실측(2026-07-24 최종, 야간배치 수동 실행 12개 job 전량 성공)** | `pur_order` 3,137(+49, 2026-01-07~07-24) · `pur_req` 4,136(+72) · `item_master` 58,589(+56) · `sales` 37(변동없음) · `purchase` 152(+1) · `iv_dtl` 1,984(+3) · `inventory` 1,871(+24) · `dept_master` 351(변동없음) · `usr_master` 99(값 갱신 7건, 건수 변동없음) · `usr_erp_module` 397(변동없음) · `hr_payroll`(민감) 165행(+22, 배치 적재건수만) · `bp_master` 재적재 3건 반영(2026-07-22 최초 4,649행 위 증분). 연동일 표기는 각 소스의 최종 배치 성공일(07-24) |
| **결의전표 초안(회계 A 모듈 첫 개방) — 코드 마스터 실적재 완료(2026-08-03, A1 승인)·권한은 전체관리자 한정** | **A1 승인으로 계정과목 691행·코스트센터 133행 실적재 완료(유실 0). `accounting` 모듈은 어느 부서에도 부여하지 않아 전체관리자만 접근(A3 = 테스트 단계 한정 결정). 잔여 승인 대기** — ERP 결의전표(`A_TEMP_GL` 28,219행 + `A_TEMP_GL_ITEM` 294,043행, 승인 시 `A_GL`로 전기)를 포털에서 **초안**으로 입력받고 회계 담당자가 ERP에 최종 저장하는 구조. **포털은 ERP에 쓰지 않는다** — ERP 확정 후 `TEMP_GL_NO` 회기입이 이중입력 방지 장치이며 DB 제약(`gl_draft_posted_needs_no`)으로 강제. 구축물: ①ETL job 2종 `acct_master`(`A_ACCT`)·`cost_center`(`B_COST_CENTER`) — **코드 마스터만**(전표·분개 라인 미적재), dry-run 검증 완료(**691행**·**133행**), 실적재는 A1 승인 후 관리자 실행. 마스터 전용 RPC `erp_master_upsert` 분리 신설(기존 `erp_etl_upsert` 무수정). ②포털 DB `gl_draft`·`gl_draft_item`·`gl_draft_log`(감사)·`gl_period_lock` + `erp_ro.acct_master_s`·`cost_center_s` — 전부 RLS ON·정책 0개·anon/authenticated 회수(Edge Function 단일 경로). ③Edge Function **`jeil-gl-draft` v1**(op 9종, 서버 재검증: 차대일치·계정유효·금액>0 정수·라인 2줄↑·프로젝트 필수계정·마감월·등록자 매핑, 합계는 라인에서 재계산). ④화면 `pages/자금_결의전표_입력_2026.html`(표준 그리드 + 차대 합계바 + 회계 담당자 탭). ⑤`portal_page.gl_draft`(`erp_module='accounting'`). **등록자 매핑**: `usr_master_s` 99명 전원 이메일형 USR_ID → `lower()` 비교로 MS UPN 자동 매핑(대문자 1건 존재), 매핑 실패 시 저장 거부. 실측: 권한격리(관리자만 allowed, 비관리자 3명 전원 차단)·인증게이트(무토큰·위조토큰 401) 통과. 정본 SQL `이관/sql/21_gl_draft.sql`(마이그레이션 `gl_draft_v1`·`gl_draft_master_rpc`) |
| **연동 현황 뷰 사각지대 수정 — `bp_master` 13→14종(2026-08-10)** | **완료** — `bp_master`(거래처마스터, 2026-07-24 실적재·4,650행 가동 중)가 `public.v_erp_sync_overview` 정의에서 누락돼 연동 현황 화면에 표시되지 않던 사각지대를 발견, 마이그레이션 `erp_sync_overview_add_bp_master`로 뷰를 13종→**14종**으로 확장(sort=35·sensitive=false, `security_invoker=true` 유지). `bp_master_s`는 `item_master_s`와 동일하게 GRANT+RLS 보유라 실테이블 `count(*)` 방식 적용, 회계 마스터 2종은 fail-closed 구성이라 기존 `batch_run` 건수 방식 그대로 유지. 권한 격리 실측: 사내 4,650 / 협력사 0, permission denied 없음. **부수 시정**: 정본 SQL `실제구축준비 자료/이관/sql/08_erp_pub_reporting_views.sql`이 5종짜리 옛 버전으로 방치돼 라이브(14종)와 어긋나 있던 드리프트를 최신 정의로 교체 + 확장 이력·row_count 산출 이원화 사유를 주석으로 명시(재발 방지) |
| **적재 노후도 실측(2026-08-10) → 재동기화 실행(2026-08-11 11:12, 7 성공/7 실패) → 원인 수정 → 재실행 완료(2026-08-11 15:35, 관리자 직접작업진행 지시)** | `etl_meta.batch_run` 실측(08-10)으로 회계 2종이 12개 job보다 최신(18일 경과)임을 확인한 뒤, **관리자가 실제로 재동기화를 실행**했다(08-11 11:12 KST, `etl_run.py --job all`). 1차 결과 **7 성공**(`usr_master` 16·`dept_master` 351·`usr_erp_module` 389·`item_master` 216·`acct_master` **692**·`cost_center` 133·`bp_master` 15) / **7 실패**(`pur_order`·`pur_req`·`sales`·`purchase`·`iv_dtl`·`inventory`·`hr_payroll`, 전부 `HYC00`…`SQLBindParameter` 오류·rows_read=0). **원인**: 이 PC의 레거시 ODBC 드라이버가 `SQL_TYPE_DATE` 파라미터 바인딩 미구현 — `datetime.date` 파라미터를 넘기는 job만 실패. **조치**: `run_job()`에 date→datetime 자정 승격 변환 추가, 실패 7개 전부 `--dry-run` 재검증 정상(`pur_order` 354·`pur_req` 1,261·`sales` 40·`purchase` 196·`iv_dtl` 660·`inventory` 2,271·`hr_payroll` 167). `etl/README.md`에 ODBC 드라이버 절 신설. **(2026-08-11 15:35 갱신 — 완료)** 관리자가 "직접작업진행"을 지시해 어시스턴트가 직접 실행(기존 `!` 게이트를 이번 건에 한해 명시적으로 해제) — 실패했던 7개 job(`hr_payroll` 포함) 전량 success, 추출=적재, 유실 0: `pur_order` 367·`pur_req` 1,267·`sales` 40·`purchase` 196·`iv_dtl` 660·`inventory` 2,276·`hr_payroll` 167(집계만, PII 없음). date→datetime 승격 수정이 실적재에서도 정상 동작함을 확정. 실행 후 `v_erp_sync_overview` 14종 전부 2026-08-11 동기화(§4 표, §1 상세). 18일 적재 공백 해소. 미해소 유지: ODBC Driver 18 미설치(코드 우회만)·`project_fg` 692행 전량 NULL·`timestamptz` 9시간 오차·`gl_slip` 의도적 미연계(상세 §1·§7) |
| **반복전표 자동화 — ERP 읽기전용 분석·기획(2026-08-10~11)** | 결의전표 파일럿 위에 얹는 **v2 확장**(분석·기획만, 중간DB 실적재·DDL·Edge Function 배포 없음). ERP운영DB 2026년 수기 결의전표(TG) 전수 읽기전용 분석(`WITH NOLOCK`·파라미터 바인딩) — 헤더 2,287건·라인 10,858건·관리항목 27,634건 + 마스터 4종(`A_CTRL_ITEM` 52·`A_ACCT_CTRL_ASSN` 997·`A_ACCT` 691·`B_BIZ_PARTNER` 4,660). **반복 패밀리 156개=964건(42.2%)**, 1차 시드 후보 36개(242건). 신규 문서 `10_반복전표_자동화_기획.md`(v1.1, 4관점 교차검증 36건 반영) + 재실행 가능 분석 도구 `etl/analyze_repeat_gl.py`(민감 관리항목 값 미기록, 출력 로컬 전용). 승인 게이트 **B1**(관리항목 마스터 2종 적재)·**B2**(시드 검수+회계규칙 확정)·**B2-1**(마감 기준)·**B3**(파일럿 대상)는 §6 |
| **반복전표 자동화 — B1(관리항목 마스터) 실적재 완료(2026-08-12)** | 관리자 지시("실적재 진행")로 ETL 2종 실행 — `ctrl_item` 52/52·`acct_ctrl_assn` 1,002/1,002(추출=적재, 유실 0, `batch_run` success 2건, 2026-08-11 23:33 UTC 완료. dry-run 1,001 대비 +1은 원천 증가분). 계정별 필수 관리항목(DR_FG/CR_FG='Y') 377행 확보 — 결의전표 화면의 관리항목 자동 생성·필수 검증 데이터 준비 완료. `erp_load_scope`에 2건 loaded 등재, 연동 현황 뷰 `v_erp_sync_overview` **14→16종** 확장(마이그레이션 `erp_sync_overview_ctrl_master`, fail-closed 규약 유지 — 실테이블 아닌 `batch_run` 기반 count). §6.8 기준 **P2 완료** — 잔여 P1 나머지(초안·템플릿 관리항목 DDL·Edge Function 확장)·P3 화면·P4 시드(계정 구성 검수 후) |
| **반복전표 자동화 — P1(구조)·P3(화면) 구현 완료(2026-08-12 오후)** | 관리자 지시("P1 나머지 + P3 화면 구현 진행")로 구현 완료. **P1**: 마이그레이션 `gl_template_v2`(정본 `이관/sql/24_gl_template_v2.sql`) — `gl_draft_item_ctrl`(초안 라인 관리항목·invoice_seq)·`gl_template_item_ctrl`(fixed/input/date_shift 규칙)·`gl_template_usage`(회차 4상태) 신설 + `gl_template` 월반복 속성 5컬럼·`gl_template_item.amt_locked` + RPC `gl_ctrl_master_get`(전부 RLS ON·정책 0개). Edge Function **`jeil-gl-draft` v4 배포**(신규 op `tpl_recur_list`·`tpl_apply_prev`·`tpl_seed_bulk` + save/get/submit/void/post 확장, 필수 관리항목 검증은 신형 화면만 하드·구화면은 경고만 — **구화면 하위호환 유지**, 세액 자동계산 없음). **P3**: `pages/자금_결의전표_입력_2026.html` v2 — 월반복 홈(배지 4상태·중복 경고·이어서 작성)·전월 프리필(본인 확정 초안+날짜 제안값 배지·적요 토큰 치환)·관리항목 자동 패널(필수* 표시)·제출 전 요약(전월 差)·회계 담당자 관리항목 표+TSV 복사·템플릿 편집기 월반복 속성. **3관점 코드 리뷰(정확성·보안 2관점 완료, UX는 API 오류로 실패) 지적 16건 전건 반영 후 재배포** — 치명 1건(수동 추가 행 관리항목 그리드 얕은복사 유실), 구화면 저장·템플릿 수정의 관리항목/월반복 속성 삭제 버그, KST 회차 경계, 월반복 상태(CURRENT_TPL) 오염, 민감 관리항목(EM·BA·D1·CP·NN) 템플릿 고정값 저장 차단, 타인 미제출 초안 열람 차단(403), tpl_use 가시성·post 다건 보정·usage 위조 방지·`tpl_recur_list` 응답 최소화 등. **잔여**: ①화면 라이브 반영은 관리자 git 커밋·gh-pages 배포 후(현재 라이브는 구화면, v4는 하위호환이라 안전) ②P4 시드 36건 등재는 계정 구성 검수(B2 잔여) 후 `tpl_seed_bulk`로 ③구화면 저장분(관리항목 미검증) 구별 배지는 후속 과제(서버 로그 ctrl_aware 기록은 시작됨) |
| **결의전표 파일럿 — 전표복사 기능 + `gl_slip` 미러 3종(2026-08-18, C-10)** | **신설·배포(실적재는 관리자 직접 실행 대기)** — `pages/자금_결의전표_입력_2026.html`에 전표복사 탭 신설(ERP 전표복사 메뉴와 동일 흐름, 본인 결의전표를 기간·검색으로 조회→라인·관리항목 미리보기→복사 시 전표 작성 탭에 채움, 결의일자는 오늘로 새로·참조번호 미복사·미매핑 계정은 공란+경고). 관리자 결정 **C-10**(C-7 확장)으로 결의전표 원장(헤더·라인·관리항목) 자체를 중간DB에 미러링하기로 결정 — 통제 3중(RLS 전면차단+RPC 단일경로 / 조회 RPC의 소유 필터 내부강제 / 민감 관리항목 EM·BA·D1·CP·NN ETL 단계 NULL 마스킹). 마이그레이션 `gl_slip_mirror_v1`+`gl_slip_synced_at_v1`로 `erp_ro.gl_slip_s`(헤더)·`gl_slip_item_s`(라인)·`gl_slip_ctrl_s`(관리항목) 신설(전부 RLS ON·정책 0), `erp_master_upsert` 3분기 확장, RPC `gl_slip_list`·`gl_slip_get`·`gl_slip_synced_at`(service_role 전용). `etl_run.py` job 3종(`gl_slip`·`gl_slip_item`·`gl_slip_ctrl`) 신설, 정본 DDL `이관/sql/25_gl_slip_mirror.sql`. `jeil-gl-draft` **v5 배포**(op `slip_list`·`slip_get`, `gl_draft_log` 감사 기록). **검증**: ERP운영DB dry-run 3종 성공(`gl_slip` 3,224·`gl_slip_item` 16,865·`gl_slip_ctrl` 42,371행). **잔여**: 실적재 미실행(`--job gl_slip`→`gl_slip_item`→`gl_slip_ctrl` 순 관리자 직접 실행), 실적재 후 확인 2건(batch_run success·민감 관리항목 NULL 마스킹 실측), 야간배치 편입(현재 수동). 한계: upsert라 ERP 삭제 전표 잔존 가능(복사 원천 용도로 허용), 야간 미러라 비실시간(화면에 동기시점 표시) |
| ERP 매핑(가설) | `M_PUR_REQ`·`M_PUR_ORD`·`M_CONFIG_PROCESS`·`M_PUR_GOODS_MVMT`·`Q_INSPECTION`·`M_IV` 등 — 재고 입출고 분류 등 세부는 여전히 유니포인트 협의 전 가설 |
| 원칙 | 운영 DB **읽기 전용 + 중간 DB 경유**, 프론트 직결 금지(`CLAUDE.md §4`) |

## 5. 정책·기획 문서 현황

| 문서 | 버전·일자 | 상태 | 위치 |
|---|---|---|---|
| ERP DB 접근정책안 | v0.1 · 2026-07-02 | **초안** | 사내 OneDrive `ERP_DB/정책관리/ERP_DB접근정책/` |
| ERP 데이터연동 마스터플랜 | v2.0 · 2026-07-01 | **승인요청**(경영 결정 대기) | 사내 OneDrive `ERP_DB/데이터연동방안/` |
| ERP·챗봇 중간DB 연계설계 | 2026-06-25 | 설계 완료(구현 전) | `실제구축준비 자료/07_*.md` |
| 본 연계 관제 문서군 | v0.28 · 2026-08-18 | **갱신**(결의전표 파일럿 **전표복사 기능 신설·배포 + `gl_slip` 미러 3종(헤더·라인·관리항목) 구축**(C-10 결정, C-7 확장) — 마이그레이션·ETL job 3종·`jeil-gl-draft` v5·화면 전표복사 탭, dry-run 검증 완료(3,224·16,865·42,371행), 실적재는 관리자 직접 실행 대기) | `10_ERP_DB연계/` + 정책관리 미러 |
| 사용자-부서 매핑·대사 (신규) | v1.1 · 2026-07-08 | **갱신** — 실적재 완료(89 정상/10 불일치), 불일치 자동제외 정책 확정 | `10_ERP_DB연계/05_사용자부서_매핑대사.md` |
| 중간DB 구축 실행기획 | v0.3 · 2026-07-08 | 초안(D1~D3 결정 대기) — §7-C 내부 구축 진행 중(RLS 격리 테스트 완료) | `10_ERP_DB연계/03_*` |

## 6. 결정 대기·차단 요인

| 항목 | 내용 | 결정 주체 |
|---|---|---|
| 리포팅 계층 티어 | Tier A(동일 인스턴스 내 리포팅 DB, 즉시) vs Tier B(별도 서버) — 마스터플랜 v2.0 §11 | 경영층 |
| 유니포인트 협의 | 읽기전용 계정(`jeilax_ro`)·뷰 생성·방화벽/VPN | 최동혁 ↔ 유니포인트 |
| 민감 영역 개방 | 자금일보(F 모듈)는 여전히 경영 승인 + Entra 그룹 신설 대기(미착수). 인사 급여는 관리자 직접 실행으로 **집계 실적재 완료**(2026-07-08, 143행) — 자금 대비 진행 상태가 다르다는 점에 유의 | 경영층(자금) |
| 인사 급여 — Entra 그룹·재직상태 정의 잔여 | `erp_secure.hr_payroll_m` 실적재·게이트 API(`jeil-hr`)는 가동됐으나, 현재는 `dept='인사팀'`/`portal_admin` 판정으로 대체 게이트 중이라 ①(권장) `AI-Portal-HR` Entra 보안그룹 신설·배정 ②유니포인트와 재직상태(퇴직 구분 등) 정의 확인이 잔여 항목 | 최동혁 + 유니포인트(재직상태) |
| 네트워크 | 파일럿 IP 허용 → 운영 VPN 전환 시점 | 인프라 결정과 연동 |
| 중간DB 실행 결정(D1~D3) | 중간DB 위치·ETL 러너·추출 원천 — `03_중간DB_구축실행기획 §2` | 경영층+최동혁 |
| ETL 실적재 착수 | 루트 `.env`의 `ERP_DB_CONN` 값 미입력(`SUPABASE_SERVICE_ROLE_KEY`는 2026-07-07 입력 완료) — 값 입력 전까지 `etl_run.py`(1차 4종) 실적재 불가 | 최동혁(관리자) |
| 발주 데이터(`M_PUR_REQ`·`M_PUR_ORD`) 2026년도 우선연동 | 실적재 완료(2026-07-07, `erp_ro.pur_order_s` 2,699행). 남은 것: 유니포인트 뷰 스펙 협의(추출 SQL 초안 확정), `M_PUR_REQ`(발주요청)는 미포함 | 최동혁(관리자) + 유니포인트 |
| 전용 읽기계정(`jeilax_ro`) 전환 | 현재 ETL은 관리자 개인 계정 접속(DPAPI 폴백) — 읽기전용 전용계정 발급·교체 필요(P1 협의 항목) | 유니포인트 |
| ERP 원천 최신 재적재 | dry-run 재확인(2026-07-08)으로 원천 건수 소폭 증가 확인(§1) — 최신 재적재 명령 준비됐으나 관리자 `!` 직접 실행 대기(본 에이전트는 실행 불가). **2026-07-21 갱신**: 오늘 재확인한 `item_master_s`(58,256)·`pur_order_s`(2,703)·`purchase_m`(140)·`sales_orders_m`(32)이 2026-07-07~08 문서화 당시 건수보다 늘어 있어 그 사이 재적재가 있었던 것으로 보이나, 해당 재적재 실행 이벤트가 `02_진행상태` 작업로그에 남아 있지 않다 — **관리자에게 확인 요청**: 언제·누가 재적재했는지(또는 별도 증분 job이 도입됐는지) 확인해 주시면 §7 변경이력·작업로그에 소급 반영하겠다. **2026-07-21 오후 후속**: 같은 날 추가로 실행된 5종 재적재(purchase·pur_order·sales·item_master·inventory)는 원인(매입 대사 점검)과 실행 사실이 §7·`02_진행상태` §3 작업로그에 정상 기록됐다 — 다만 이는 오늘 오후 건에 한하며, 위 07-08→07-21 오전 사이의 최초 소폭 증가 원인은 여전히 미상으로 남아 있다(재발방지: 앞으로 재적재 시 즉시 작업로그에 기록) | 최동혁(관리자) |
| 사내 Entra→Supabase 세션 브리지 | 부서 대시보드 실데이터가 일반 사용자에게 노출되려면 Entra 로그인 → 사내 Supabase 세션 자동 발급 브리지가 필요. 현재는 사내 Supabase 세션 보유자만 실데이터 확인 가능 | 최동혁 + 개발 |
| 사용자-부서 매핑 — 상태이상 3건 현업 확인 | 김해품질팀·영업팀·자재물류파트 소속 각 1명이 활성계정(`USE_YN='Y'`)인데 이름에 `(퇴사)`/`(휴직)` 표기가 남아 있음(ERP 원천 정합 이상 추정). 현업(인사/해당 부서) 확인 후 ERP측(`Z_USR_MAST_REC`) 정리 권고. 확인 전까지 자동제외로 안전 처리(연동 영향 없음). 상세 `05_사용자부서_매핑대사.md §3·§8` | 최동혁(+인사/해당 부서 확인) |
| 중간DB 데이터 갭(ETL 매핑 점검) | 실측 확인(2026-07-08): `erp_ro.inventory_d.in_qty` 전량 0·`stock_qty` 전량 null(출고 `out_qty`만 유효 — 원천 입고·재고 매핑 미완) / `erp_ro.sales_orders_m.order_amt`·`collect_amt` 전월 0(수주·수금 미매핑, 매출액만 유효) / `erp_ro.pur_order_s.subcontra_flg` 전건 'N'(외주 발주 0건 — `CLAUDE.md §4.4`의 `M_CONFIG_PROCESS` SUBCONTRA_FLG와 다른 신호일 가능성). 원천 추출 SQL(`M_STOCK` 등) 재점검 필요. **(2026-07-13 갱신)** 이 중 재고 커버리지 문제(롤링 31일이라 6·7월만 적재)는 연 전체 적재 전환으로 **해소**(`inventory_d` 1,810행). 단 `in_qty=0`/`stock_qty=null` **원천 매핑 결함은 여전히 미해결**(별개 이슈) | 유니포인트 ↔ 최동혁(ETL 재점검) |
| 부서별 ERP 모듈 차등 정책 | `dept_erp_scope`가 23개 부서 전부 sales·purchase·inventory·item·pur_order를 동일 보유 → payroll(인사팀 단독)만 차등되고 나머지는 부서별 데이터 차등이 사실상 없음. 구조(`dept_erp_scope` 편집형)는 부서별 차등을 지원하므로 값만 바꾸면 되는 상태 — 2026-07-20 챗봇 권한 정비(v19) 과정에서 재확인, **경영 판단 보류로 이번엔 변경 안 함** | 경영층 |
| ERP 원본 직접 대사(잔여, 2026-07-22) | 중간DB(erp_ro) 정합성 SQL 검증(고아·중복·형식)은 완료·이상 없음이나, ERP 원본(`M_PUR_ORD`/`M_PUR_REQ`) 테이블과의 직접 건수 대사는 ODBC 접속 설정(`.env`) 읽기 권한 거부로 미실행. `10_ERP_DB연계/etl/`에서 `python etl_run.py --job pur_order --dry-run`(및 `pur_req` 등) 직접 실행 필요 | 최동혁(관리자) |
| ~~ERP 거래처마스터(`bp_master`) 실적재~~ → **해소(2026-07-24)** | `bp_master` 3/3 정상 적재 완료(적재버그 RCA·수정 포함). 잔여는 관리자 화면 "🔄 거래처 동기화" 버튼으로 `public.vendor_master`에 실제 사업자등록번호·대표자명·연락처를 반영할지 여부(선택 사항) | 최동혁(관리자) |
| `v_erp_data_asof.rows_upserted` 최신 배치 미반영 정황(신규, 2026-07-24) | 야간배치 재실행 검증 중 일부 job에서 07-21과 07-24 조회값이 동일 고정돼 있는 정황을 확인 — 뷰 정의 결함 가능성. 배치 성공 검증은 이번엔 `last_success` 타임스탬프로 대체 확인했다. 뷰 SQL 재점검 필요 | 최동혁(관리자, 조사 후 결정) |
| ~~결의전표 파일럿 — 회계 데이터 접근 승인(A1)~~ → **해소(2026-08-03)** | 관리자 승인으로 계정과목(`A_ACCT`) **691행**·코스트센터(`B_COST_CENTER`) **133행** 실적재 완료(추출=적재, 유실 0). 금액·잔액·전표는 여전히 미적재(코드 목록만). 연동 현황 뷰에 2종 추가(13종), `erp_load_scope`의 `accounting/acct_master`·`cost_center`를 `loaded`로 기록 | — (완료) |
| ~~결의전표 파일럿 — `A_TEMP_GL` 샘플 조회 승인(A2)~~ → **해소(2026-08-03)** | 승인 후 읽기 전용 1회성 조회로 실측 확정 — **등록자 `INSRT_USER_ID` 2026년 2,947건 전량 이메일형**(고유 76명, 비이메일형 0건)이라 자동매핑이 **추정에서 확정으로** 승격 · **`GL_TYPE` 전량 `03` 단일값** · `GL_INPUT_TYPE`은 수기 결의가 `TG`(2,177건·74%) · `CONF_FG`는 등록 직후 `U`→승인 시 `C`(C 2,719/U 228) · **`TEMP_GL_NO` = `TG`+YYYYMMDD+4자리 순번(14자) 전량 동일**. 금액·적요는 최소수집 원칙으로 조회하지 않았다 | — (완료) |
| **결의전표 파일럿 — 회계 규칙 확정(A5) · 부서 부여(A3~A4)는 보류 결정(2026-08-03)** | ③④**관리자 결정(2026-08-03): 테스트 단계이므로 `accounting` 모듈을 어느 부서에도 부여하지 않고 전체관리자만 사용한다** — `dept_erp_scope` 무변경, 실측으로 비관리자 2명 `gl_draft` 접근 `false`·관리자만 `true` 확인. 부서 부여·파일럿 대상자 지정은 테스트 통과 후 재판단. ⑤회계 규칙 확정 — 경비 지출결의 계정과목 후보, 기본 상대계정(미지급금 vs 보통예금), 부가세 처리 여부, 회계기간 마감 기준(`gl_period_lock`에 등재). ⑤가 없으면 계정 드롭다운을 실무에 맞게 좁힐 수 없다 | 최동혁(관리자) + 회계 담당 |
| `erp_etl_upsert` 함수 전체 정의 — 번호 마이그레이션 미승격(신규, 2026-07-24) | 함수 전체 정의와 `bp_master_s` DDL이 번호 마이그레이션 파일로 관리되지 않고 DB에만 존재하던 소스 정합성 갭을 확인. 이번엔 `bp_master` 픽스 한 건만 20번 파일(`20_erp_etl_upsert_bp_master_fix.sql`)로 기록했고, 함수 전체를 번호 파일로 승격하는 것은 별도 후속 과제 | 최동혁(관리자, 후속 결정) |
| ~~야간배치 12개 job 재동기화 — 실행됨(7 성공/7 실패) → 원인 수정, 실패 7개 재실행~~ → **해소(2026-08-11 15:35)** | 08-11 11:12 KST 1차 실행(`etl_run.py --job all`)은 **7 성공**(usr_master·dept_master·usr_erp_module·item_master·acct_master·cost_center·bp_master) / **7 실패**(pur_order·pur_req·sales·purchase·iv_dtl·inventory·hr_payroll, 레거시 ODBC 드라이버의 `SQL_TYPE_DATE` 파라미터 바인딩 미구현). 원인 수정(`etl_run.py` date→datetime 승격) 후 관리자가 "직접작업진행"을 지시해 어시스턴트가 직접 실행(기존 `!` 게이트를 이번 건에 한해 명시적으로 해제) — 실패 7개(hr_payroll 포함) 전량 success, 추출=적재, 유실 0(`pur_order` 367·`pur_req` 1,267·`sales` 40·`purchase` 196·`iv_dtl` 660·`inventory` 2,276·`hr_payroll` 167, 집계만). date→datetime 승격 수정이 실적재에서도 정상 동작함을 확정. **미해소로 남는 것**: 근본해결(ODBC Driver 18 설치)은 여전히 선택 권장 사항(아래 행), `project_fg`·`timestamptz`·`gl_slip`은 각각 별도 행·§1에서 유지 | 최동혁(관리자) |
| **레거시 ODBC 드라이버 — `SQL_TYPE_DATE` 파라미터 바인딩 미구현(신규, 2026-08-11, 코드 수정 완료·드라이버 교체는 잔여)** | 이 PC엔 레거시 `SQL Server` ODBC 드라이버만 설치돼 있고 17/18이 없음(어제 커넥터가 이미 확인해 둔 사항). `datetime.date` 파라미터를 바인딩하는 job 7종이 전부 `HYC00`…`SQLBindParameter` 오류로 실패하는 함정(부분성공이라 로그가 정상처럼 보임)을 발견, `etl_run.py`에 date→datetime 승격 변환으로 우회했다(안전장치로 계속 유지). **근본해결 — `ODBC Driver 18 for SQL Server` 설치는 여전히 권장 사항으로 남음**(급하지 않음, 우회책이 검증됨) | 최동혁(관리자, 근본해결은 선택) |
| **`timestamptz` 라벨 9시간 오차(신규, 2026-08-11, 미조치)** | ERP의 naive datetime(KST)이 `timestamptz` 컬럼에 UTC로 잘못 라벨링돼 실제 시각과 9시간 어긋남(신규 계정 `src_updated`가 미래 시각으로 보이는 현상으로 발견). 증분 watermark는 `LOOKBACK_DAYS=2` 버퍼로 현재 유실 없음, 화면 "데이터 기준 시각" 표시만 9시간 틀어짐. 수정 범위가 ETL 적재 전반·기존 적재분 소급까지 걸려 영향 평가 필요 — 이번엔 손대지 않음(별건 과제) | 최동혁(관리자, 후속 결정) |
| ~~연동 현황 뷰 정본 SQL 드리프트(`08_erp_pub_reporting_views.sql` 5종 vs 라이브 14종)~~ → **해소(2026-08-10)** | 정본 SQL 파일이 마이그레이션 확장(5→10→13→14종)을 따라가지 못하고 옛 버전으로 방치돼 있던 것을 발견해 최신 정의로 교체하고 확장 이력·row_count 산출 이원화 사유를 주석으로 명시. **재발 방지 메모**: 앞으로 `v_erp_sync_overview`를 확장할 때는 마이그레이션 적용과 정본 SQL 파일 갱신을 같은 작업으로 묶을 것 | — (완료, 재발방지 습관화는 계속 유의) |
| `A_ACCT.PROJECT_FG` 전량 NULL — **확정(2026-08-10, 신규 계정 포함 692행으로 재확인 2026-08-11)** | 2026-08-03엔 "가능성"으로 기록했던 것을, 08-10 독립 재추출한 691행에서 동일 재현돼 **ERP 원천 결함으로 확정**했고, 08-11 재동기화로 신설 계정 1건이 추가된 692행에서도 여전히 전량 NULL(신규 계정도 NULL)임을 재확인했다. 서버 재검증 5번(프로젝트 필수 계정 강제)은 계속 무력 상태 — A5(회계 규칙)·유니포인트 확인 대상으로 유지(신규 조치 없음) | 최동혁(관리자) + 유니포인트 |
| **반복전표 자동화 — 승인 게이트: B1 실적재까지 완료(2026-08-12), B2-1·B3 집행 완료, B2 부분승인(계정 구성 검수 잔여), B4만 미결** | **B1(완료)** 마이그레이션 `gl_ctrl_master_v1` 적용(관리항목 마스터 2종 스키마·`erp_master_upsert` RPC 확장·ETL job 신설, dry-run 52행·1,001행 통과, 2026-08-11 오후) → **2026-08-12 관리자 지시로 실적재까지 완료** — `erp_ro.ctrl_item_s` 52/52·`acct_ctrl_assn_s` 1,002/1,002(추출=적재, 유실 0), 연동 현황 뷰 14→16종 확장. DR_FG/CR_FG 의미는 유니포인트 확인 계속 병행. **B2(부분승인)** 부가세·원천징수 자동계산 규칙은 범위 제외(세액은 수기 입력 + 전월값 프리필만 제공, U-1 미해소·C-2 무개정) — **잔여: 시드 36건(→73건)의 계정 구성 검수**(회계 담당 확인 후 등재). **B2-1(적용 완료)** ERP 월마감 원장 `C_CLOSE_STATUS` 실측 연동(2026-06까지 마감 Y·2026-07부터 미마감), `public.gl_period_lock`에 33개월(2023-10~2026-06) 등재 완료 — 09 §5.2 "마감월 통제 부재" 해소. 마감월 소급 등록 관찰(예외는 회계 담당이 lock 해제)·신규 마감월 수동 추가 필요는 등재만(`02_진행상태.md` §4 참조). **B3(확정)** `perm_module_catalog`에 `accounting`(sensitive) 신설(A3 실행) + 이훈희(`hh.lee2@jeilm.co.kr`, 총무팀)에 `perm_grant` 개인 예외 부여(`perm_audit` 감사 동반), 실측 검증 완료(이훈희 허용·`hk.yang` 차단·관리자 기허용). 부서 부여는 계속 없음(C-8 유지). **B4(미결)** L3 자동 초안 생성 여부 — 파일럿 회고 후 재판단. 상세는 `10_반복전표_자동화_기획.md §8`(v1.3) | B1: 완료 / B2 잔여: 회계 담당 + 최동혁 / B2-1·B3: 완료 / B4: 경영/재무 + 최동혁(파일럿 회고 후) |
| **결의전표 파일럿 — `gl_slip` 미러 3종 실적재(신규, 2026-08-18, C-10)** | 전표복사 기능·미러 스키마(`gl_slip_s`·`gl_slip_item_s`·`gl_slip_ctrl_s`)·ETL job 3종·Edge Function v5·화면은 배포 완료, ERP운영DB dry-run 3종 성공(`gl_slip` 3,224·`gl_slip_item` 16,865·`gl_slip_ctrl` 42,371행)이나 **실적재는 미실행** — `etl_run.py --job gl_slip` → `--job gl_slip_item` → `--job gl_slip_ctrl` 순 관리자 직접 실행 필요. 완료 후 확인 2건 잔여: ①`batch_run` 3건 success ②민감 관리항목(EM/BA/D1/CP/NN) `ctrl_val` NULL 마스킹 실측. 야간배치 스케줄 편입(현재 수동)도 별도 과제 | 최동혁(관리자) |

---

## 7. 변경 이력

| 일자 | 변경 내용 | 작성 |
|---|---|---|
| 2026-08-18 | **결의전표 파일럿 — 「전표복사」 기능 신설·배포 + `gl_slip` 미러 3종 구축(v0.28, 관리자 결정 C-10)** — 회계 담당자가 본인이 ERP에 입력한 결의전표를 기간·검색으로 조회→라인·관리항목 미리보기→복사해 전표 작성 탭을 채우는 「전표복사」 탭을 `pages/자금_결의전표_입력_2026.html`에 신설했다(ERP 전표복사 메뉴와 동일 흐름, 결의일자는 오늘로 새로·참조번호 미복사, 이후 수정·저장·제출은 기존 초안 흐름). 이를 뒷받침하기 위해 **관리자 결정 C-10**으로 기존 **C-7(코드 마스터만 적재)을 확장** — 결의전표 원장(`A_TEMP_GL` 3종: 헤더·라인·관리항목) 자체를 중간DB에 미러링하기로 했다. **통제 3중**: ①`erp_ro` RLS 전면차단(정책 0) + RPC(service_role) 단일 경로 ②조회 RPC의 소유 필터(`insrt_user_id`=JWT 매핑 본인 ERP ID, C-3 근거) 내부 강제 ③민감 관리항목(EM 사번·BA 계좌·D1·CP 신용/구매카드·NN 어음)은 ETL 추출 SQL에서 값 자체를 NULL 마스킹. **구축물**: 마이그레이션 `gl_slip_mirror_v1`+`gl_slip_synced_at_v1`(`erp_ro.gl_slip_s`·`gl_slip_item_s`·`gl_slip_ctrl_s` 신설, `erp_master_upsert` 3분기 확장, RPC `gl_slip_list`·`gl_slip_get`·`gl_slip_synced_at`) + 정본 DDL `이관/sql/25_gl_slip_mirror.sql` + `etl_run.py` job 3종(`gl_slip`·`gl_slip_item`·`gl_slip_ctrl`) + `jeil-gl-draft` **v5 배포**(op `slip_list`·`slip_get`, `gl_draft_log` 감사) + 화면 전표복사 탭(기간·검색 필터, 미러 동기시점 표시). **검증**: ERP운영DB dry-run 3종 성공(`gl_slip` 3,224·`gl_slip_item` 16,865·`gl_slip_ctrl` 42,371행), 화면 JS·`etl_run.py` 컴파일 통과, RPC 빈상태 응답 정상. **잔여(관리자 직접 실행 대기)**: 실적재 미실행(`--job gl_slip`→`gl_slip_item`→`gl_slip_ctrl` 순), 실적재 후 확인 2건(batch_run success·민감 관리항목 NULL 마스킹 실측), 야간배치 스케줄 편입(현재 수동). **한계**: upsert 적재라 ERP 삭제 전표가 미러에 잔존 가능(복사 원천 용도로 허용), 야간 미러라 비실시간(화면 동기시점 표시로 안내). 앞선 "`gl_slip` 현행 유지(의도적 미연계)" 기록은 이번 C-10 결정으로 대체(과거 기록은 그대로 보존). §1·§4·§5·§6·§7 갱신, 미러 동기화(버전 0.28·20260818) | 최동혁(+Claude) |
| 2026-08-12 | **반복전표 자동화 — P1(구조)·P3(화면) 구현 완료(v0.27, 관리자 지시 "P1 나머지 + P3 화면 구현 진행", 오후)** — 10번 기획서(v1.4)·CHANGELOG·정본 SQL(`이관/sql/24_gl_template_v2.sql`)·HTML 재빌드는 이 작업과 별도로 이미 반영돼 있어, 이번 갱신은 관제 문서(00·02) 반영만 수행했다. **P1(DDL+Edge Function)**: 마이그레이션 `gl_template_v2` — `gl_draft_item_ctrl`(초안 라인 관리항목·invoice_seq)·`gl_template_item_ctrl`(fixed/input/date_shift 규칙)·`gl_template_usage`(회차 4상태) 신설 + `gl_template` 월반복 속성 5컬럼·`gl_template_item.amt_locked` + RPC `gl_ctrl_master_get`(전부 RLS ON·정책 0개·anon/authenticated 회수). `jeil-gl-draft` **v4 배포**(신규 op `tpl_recur_list`·`tpl_apply_prev`·`tpl_seed_bulk` + save/get/submit/void/post 확장, 필수 관리항목 검증은 신형 화면만 하드·구화면은 경고만 — 구화면 하위호환 유지, 세액 자동계산 없음·B2 결정 준수). **P3(화면)**: `pages/자금_결의전표_입력_2026.html` v2 — 월반복 홈·전월 프리필(날짜 제안값 배지·적요 토큰 치환)·관리항목 자동 패널·제출 전 요약·회계 담당자 관리항목 표+TSV 복사·템플릿 편집기 월반복 속성. **3관점 코드 리뷰(정확성·보안 2관점 완료, UX는 API 연결 오류로 실패) 지적 16건 전건 반영 후 재배포** — 치명 1건(수동 추가 행 관리항목 그리드 얕은복사 유실) 포함, 구화면 저장·템플릿 수정의 관리항목/월반복 속성 삭제 버그, KST 회차 경계, 월반복 상태 오염(CURRENT_TPL), 민감 관리항목(EM·BA·D1·CP·NN) 템플릿 고정값 저장 차단, 타인 미제출 초안 열람 차단(403), tpl_use 가시성·post 다건 보정·usage 위조 방지·`tpl_recur_list` 응답 최소화. **잔여**: ①화면 라이브 반영은 관리자 git 커밋·gh-pages 배포 후(현재 라이브는 구화면, v4는 하위호환이라 안전) ②P4 시드 36건 등재는 계정 구성 검수(B2 잔여) 후 `tpl_seed_bulk`로 ③구화면 저장분(관리항목 미검증) 구별 배지 신설은 후속 과제. §1·§4·§5·§7 갱신, 미러 동기화(버전 0.27·20260812) | 최동혁(+Claude) |
| 2026-08-12 | **반복전표 자동화 — B1(관리항목 마스터) 실적재 완료(v0.26, 관리자 지시)** — 관리자 지시("실적재 진행")로 ETL 2종 실행 — `ctrl_item` 52/52·`acct_ctrl_assn` 1,002/1,002(추출=적재, 유실 0, `batch_run` success 2건, 2026-08-11 23:33 UTC 완료, dry-run 1,001 대비 +1은 원천 증가분). 계정별 필수 관리항목(DR_FG/CR_FG='Y') 377행 확보 — 결의전표 화면의 관리항목 자동 생성·필수 검증 데이터 준비 완료. `erp_load_scope`에 accounting/`ctrl_item`·`acct_ctrl_assn` 'loaded' 2건 등재, 연동 현황 뷰 `v_erp_sync_overview` 14→**16종** 확장(마이그레이션 `erp_sync_overview_ctrl_master`, fail-closed 규약 유지 — batch_run 기반 count). 반복전표 자동화 §6.8 기준 **P2 완료** — 잔여 P1 나머지(초안·템플릿 관리항목 DDL·Edge Function 확장)·P3 화면·P4 시드(계정 구성 검수 후). 10번 기획서(v1.3)·CHANGELOG·정본 SQL(`이관/sql/23_erp_ctrl_master.sql`)은 이 작업과 별도로 이미 반영됨. §1·§4·§5·§6·§7 갱신, 미러 동기화(버전 0.26·20260812) | 최동혁(+Claude) |
| 2026-08-11 | **반복전표 자동화 — 승인 게이트 B1·B2-1·B3 집행 완료, B2 부분승인·B4 미결(v0.25, 관리자 결정 오후)** — 정본 SQL(`이관/sql/23_erp_ctrl_master.sql`)·`etl_run.py` job 신설·10번 기획서 v1.2·CHANGELOG는 이 결정과 함께 별도로 이미 반영돼 있었고, 이번 작업은 관제 문서(00·02) 반영이다. **B1 승인**: 마이그레이션 `gl_ctrl_master_v1` 적용 — `erp_ro.ctrl_item_s`(관리항목 마스터)·`erp_ro.acct_ctrl_assn_s`(계정별 관리항목 요건) 신설(둘 다 RLS ON·정책 0개 fail-closed, anon/authenticated 회수), `erp_master_upsert` RPC 2분기 추가, `etl_run.py`에 job `ctrl_item`·`acct_ctrl_assn` 신설, **dry-run 통과(52행·1,001행)**. **실적재는 미실행 — 관리자 직접 실행 대기**(`--job ctrl_item` / `--job acct_ctrl_assn`). **B2 부분승인**: 부가세·원천징수 자동계산 규칙은 범위 제외(세액 수기 입력+전월값 프리필만, U-1 미해소·C-2 무개정) — **잔여: 시드 36건(→73건) 계정 구성 검수**. **B2-1 적용 완료**: ERP 월마감 원장이 `C_CLOSE_STATUS`(작업년월·`CLOSE_FLAG`·`CLOSED_DATE`, 마감 33행)임을 실측 확정(2023-10~2026-06 마감 Y, 2026-07부터 미마감), `public.gl_period_lock`에 **33개월 `locked=true` 등재 완료** — **09 §5.2 "마감월 통제 부재"가 처음으로 해소**됐다(09 원문은 이번엔 수정하지 않음). 주의 2건(등재만): ①ERP 실측상 마감월에도 결의전표 소급 등록이 관찰돼 포털 잠금이 ERP 운영관행보다 엄격해짐(예외는 회계 담당이 lock 해제) ②`gl_period_lock`은 자동 동기 job이 없어 새 달 마감 시 수동 추가 필요(후속 과제). **B3 확정**: `perm_module_catalog`에 `accounting`(sensitive) 신설(A3 실행) + 이훈희(`hh.lee2@jeilm.co.kr`, 총무팀)에 `perm_grant` 개인 예외 부여(`perm_audit` 감사 동반), 실측 검증(이훈희 허용·`hk.yang` 차단·관리자 기허용). 부서 부여는 계속 없음(C-8 유지, 개인 예외 2명뿐). **B4는 미결**(파일럿 회고 후 재판단). §1·§5·§6·§7 갱신, 미러 동기화(버전 0.25·20260811) | 최동혁(+Claude) |
| 2026-08-11 | **야간배치 실패 7개 job — 관리자 직접작업진행 지시로 실적재 완료(v0.24, 정정 마무리)** — 위 v0.23에서 "실패 7개 재실행 대기"로 남겨뒀던 항목이 완료됐다. 관리자가 "직접작업진행"을 지시해 어시스턴트가 직접 실행했다(기존 `!` 직접 실행 게이트를 관리자가 이번 건에 한해 명시적으로 해제, 2026-08-11 15:35 KST). `hr_payroll`도 이 지시에 포함돼 함께 적재했고, 여전히 **집계만**(월×부서 인원·급여총액, 월×전사 퇴직 — 개인 행·이름·주민번호·계좌 없음) 추출했다. **7개 job 전량 success, 추출=적재, 유실 0**(`etl_meta.batch_run` 실측): `pur_order` 367·`pur_req` 1,267·`sales` 40·`purchase` 196·`iv_dtl` 660·`inventory` 2,276·`hr_payroll` 167. **date→datetime 승격 수정이 실적재에서도 정상 동작함이 확정**됐고(dry-run 뿐 아니라 실적재까지 검증 완료), 레거시 ODBC 드라이버 이슈는 코드 레벨에서 해소됐다. **연동현황 14종 전체가 2026-08-11 동기화로 갱신**: `pur_order` 3,137→**3,422** · `pur_req` 4,136→**4,566** · `item_master` 58,589→**58,751** · `bp_master` 4,650→**4,660** · `sales` 37→**40** · `purchase` 152→**196** · `iv_dtl` 1,984→**2,638** · `inventory` 1,871→**2,277** · `usr_master` 99→**100** · `hr_payroll`(민감·건수만) 165→**167** · `acct_master` 691→**692**(08-11 오전 갱신 유지) · `dept_master`·`usr_erp_module`·`cost_center` 변동 없음. **18일 적재 공백(07-24→08-11) 완전 해소**, 발주·구매요청·재고 `period_max`가 당일(08-11)까지 따라잡힘. **미해소 유지(변경 없음)**: ①`ODBC Driver 18` 미설치(코드 우회만, 근본 미해결) ②`A_ACCT.PROJECT_FG` 692행 전량 NULL(A5·유니포인트) ③`timestamptz` 9시간 라벨 오차(등재만) ④`gl_slip` 의도적 미연계 ⑤미러 버전 공백 v0.18~v0.20 소급 불가. §1·§4·§5·§6·§7 갱신, 미러 동기화 | 최동혁(+Claude, 관리자 직접작업진행 지시 수행) |
| 2026-08-11 | **야간배치 12개 job 재동기화 — 실제 실행(7 성공/7 실패) → 레거시 ODBC 드라이버 원인 확정·수정 → 실패 7개 재실행 대기(v0.23, 정정)** — 08-10에 "명령 준비·미실행"으로 남겨뒀던 12개 job 재동기화를 관리자가 실제로 실행했다(`etl_run.py --job all`, 11:12 KST). **결과 7 성공/7 실패**: 성공 `usr_master` 16·`dept_master` 351·`usr_erp_module` 389·`item_master` 216·`acct_master` **692**·`cost_center` 133·`bp_master` 15(추출=적재, 유실 0), 실패 `pur_order`·`pur_req`·`sales`·`purchase`·`iv_dtl`·`inventory`·`hr_payroll` 전부 `HYC00 [Microsoft][ODBC SQL Server Driver] 선택 기능을 구현하지 않았습니다 (SQLBindParameter)`(rows_read=0). **원인**: 이 PC엔 레거시 `SQL Server` ODBC 드라이버만 설치돼 있고 17/18이 없음(어제 커넥터가 이미 확인해 둔 사항이 이번 실행에서 실제로 발현) — 이 드라이버는 `SQL_TYPE_DATE` 파라미터 바인딩을 구현하지 않아 `year_start`/`year_end` 등 `datetime.date` 파라미터를 넘기는 job만 실패하고, 파라미터가 없거나 `datetime`(watermark)만 쓰는 job은 성공해 실패/성공이 정확히 이 기준으로 갈렸다. **함정**: 절반이 성공해 로그가 정상처럼 보이므로 `--job all`의 종료코드·부분성공만 신뢰하면 놓친다. **조치(완료)**: `etl_run.py`의 `run_job()`에 `date` 파라미터를 `datetime`(자정)으로 승격하는 변환을 추가(경계비교가 `>= 시작 AND < 종료`라 결과는 완전히 동일, 드라이버 교체 후에도 안전장치로 유지). 실패했던 7개 전부 `--dry-run`으로 재검증해 정상 확인 — `pur_order` 354·`pur_req` 1,261·`sales` 40·`purchase` 196·`iv_dtl` 660·`inventory` 2,271·`hr_payroll` 167행(`CONVERT(date,…)` 결과 컬럼은 레거시 드라이버에서도 정상이라 파라미터 바인딩만이 원인임이 확정됨). `etl/README.md`에 "실행 환경(ODBC 드라이버)" 절 신설(함정·회피책·근본해결 `ODBC Driver 18 for SQL Server` 설치 권장 기재). **잔여: 실패한 7개 job의 실적재는 아직 미실행 — 관리자 `!` 직접 실행 대기**(명령은 제시됨). **회계 마스터 691→692 정정**: 08-10 대사(차이 0)는 정확했으나, 08-11 09:53에 신규 계정 1건(`55004101` 종속기업투자주식손상차손, `bal_fg=DR`·`bs_pl_fg=PL`)이 ERP에 등록돼 이번 실행에 반영됐다 — 관리자가 지칭한 "미적용 회계계정정보"는 결과적으로 실재했고 이번에 해소됐다. `project_fg`는 692행 전량 NULL 유지로 ERP 원천 문제 판단이 재확인됐다(A5·유니포인트 대상 그대로). **신규 이슈 등재(미조치)**: 신규 계정의 `src_updated`가 `2026-08-11 09:53:41.923+00`으로 저장돼 UTC로 해석하면 미래 시각이 되는 `timestamptz` 라벨 9시간 오차를 발견 — ERP naive datetime(KST)이 UTC로 잘못 라벨링된 것으로 추정, 증분 watermark는 `LOOKBACK_DAYS=2` 버퍼로 현재 유실 없으나 "데이터 기준 시각" 표시가 9시간 틀어짐. 수정 범위가 넓어 이번엔 손대지 않고 별건 과제로만 등재. §1·§4·§5·§6·§7 갱신, 미러 동기화 | 최동혁(+Claude) |
| 2026-08-11 | **반복전표 자동화 — ERP 읽기전용 분석·기획 완료(v0.22)** — 관리자 지시로 결의전표 파일럿의 다음 트랙(반복전표 자동화)을 분석·기획했다. ERP운영DB 2026년 수기 결의전표(TG) 전수를 읽기전용 조회(`WITH NOLOCK`·파라미터 바인딩, DDL/DML 없음, A2 승인 근거)해 헤더 2,287건·라인 10,858건·관리항목 27,634건 + 마스터 4종(`A_CTRL_ITEM` 52·`A_ACCT_CTRL_ASSN` 997·`A_ACCT` 691·`B_BIZ_PARTNER` 4,660)을 분석했다. **반복 패밀리 156개=964건(42.2%)**, 금액 완전고정 34개, 1차 시드 후보 36개(242건)를 확정하고, 유형(T1~T7)·관리항목 체계(계정별 필수 항목은 `A_ACCT_CTRL_ASSN`이 정의, 현 파일럿엔 이 구조 부재)를 실측했다. 신규 문서 `10_반복전표_자동화_기획.md`(v1.0 작성 → 4관점 교차검증 36건 반영 → **v1.1** 개정) + 재실행 가능 분석 도구 `etl/analyze_repeat_gl.py`(민감 관리항목 값 미기록, 출력 로컬 전용, `.gitignore` 안전망 추가) 신설. **중간DB 실적재·DDL·Edge Function 배포는 하지 않았다** — 승인 게이트 B1(마스터 적재)·B2(시드 검수+회계규칙 확정)·B2-1(마감 기준)·B3(파일럿 대상) 신규 등재. §1·§4·§5·§6·§7 갱신, 미러 동기화 | 최동혁(+Claude) |
| 2026-08-10 | **회계 마스터 대사 검증(차이 0) + 연동 현황 뷰 사각지대 수정(`bp_master` 13→14종) + 정본 SQL 드리프트 시정 + 적재 노후도 실측(v0.21)** — 관리자 지시("회계계정정보 동기화 진행")를 erp-db-connector로 수행한 결과 **회계계정정보는 이미 최신이라 동기화할 차이가 없었다**: `acct_master`·`cost_center` dry-run(691·133)이 2026-08-03 적재분과 신규 0·소멸 0·필드변경 0으로 완전 일치, ERP 원천 `UPDT_DT >= 2026-08-03` 변경 건도 0건. 화이트리스트로 제외되는 `A_ACCT` 82행은 전부 `DEL_FG='Y'`임을 재확인(누락 아님). **`project_fg` 전량 NULL은 오늘 독립 재추출로도 재현돼 "가능성"에서 "ERP 원천 결함 확정"으로 승격**(A5·유니포인트 확인 대상 유지, 신규 조치 없음). 대신 조사 과정에서 두 가지가 드러났다. **① 연동 현황 뷰 사각지대**: `bp_master`(2026-07-24 실적재·4,650행 가동 중)가 `public.v_erp_sync_overview`에서 누락돼 있던 것을 발견, 마이그레이션 `erp_sync_overview_add_bp_master`로 13→**14종**으로 확장(GRANT+RLS 보유라 `item_master_s`와 동일하게 실테이블 count 적용, 회계 마스터는 fail-closed라 `batch_run` 건수 방식 유지). 권한 격리 실측: 사내 4,650/협력사 0. **부수로 정본 SQL 드리프트도 시정** — `이관/sql/08_erp_pub_reporting_views.sql`이 5종짜리 옛 버전으로 방치돼 라이브(14종)와 어긋나 있던 것을 최신 정의로 교체하고 확장 이력·row_count 이원화 사유를 주석으로 명시(재발 방지 메모 §6 등재). **② 적재 노후도**: `etl_meta.batch_run` 실측 결과 회계 2종(08-03, 8일 경과)이 오히려 가장 최신이고 **나머지 12개 job은 전부 2026-07-24(18일 경과)에 정지** — "회계 미적용"이라는 최초 인식과 정반대. **12개 job 재동기화는 명령만 준비, 관리자 `!` 직접 실행 대기(미실행)**. **③ 결의전표 참조 마스터**: 화면·서버 필수 마스터(계정·코스트센터·부서·거래처)는 전부 적재 완료라 기능 차단 요인 없음, `project_no`·`vat_type`은 검증 로직 자체가 미구현이라 "미구현 기능"으로 분류(후속 후보만 등재: `A_TSA_VAT_TYPE_MAPPING`·`PMS_PROJECT`). **④ 환경 이슈(기록)**: 관리자 PC `pyodbc` 미설치 발견·설치(5.3.0)로 해소(재현성 메모), Supabase MCP 일시 인증오류 후 자연복구(원인 미상, REST RPC 우회 가능 기록). **⑤ `gl_slip` 현행 유지(의도적 미연계) 재확인**(변경 없음). **⑥ 미러 경로 설정 오류 발견·수정**: `.claude/erp_db_mirror.path`가 이 PC에 존재하지 않는 Windows 로그인 프로필을 가리키고 있어 동기화 시도가 실제 OneDrive 미러가 아닌 로컬 고아 폴더에 쌓이고 있었음을 발견, 이 PC의 실제 OneDrive 로그인 프로필 경로로 정정하고 오적재분을 정리했다(경로 값 자체는 `.claude/erp_db_mirror.path`에만 두고 본 문서에는 적지 않는다). 같은 조사에서 v0.18~v0.20(2026-07-31·08-03) 버전이 애초에 미러에 동기화된 적이 없어(원본이 별도 커밋 없이 다음 버전으로 덮어써져 소급 복원 불가) 미러가 v0.17에서 곧바로 v0.22(같은 날 진행된 반복전표 자동화 개정과 병합된 버전)로 건너뛰게 됐음을 함께 기록한다(재발 방지). §1·§4·§5·§6·§7 갱신, 미러 동기화(버전 0.22·20260811, 경로 수정 후) | 최동혁(+Claude) |
| 2026-08-03 | **A2 승인·실행 — 결의전표 코드 체계 실측 확정 + 전표번호 형식 검증 신설(v0.20)** — 관리자 승인으로 `A_TEMP_GL` 샘플 조회(읽기 전용 1회성, 금액·적요는 최소수집으로 미조회)를 수행해 06 추진계획 **S2를 완료**했다. **① 등록자 형식 — 확정**: 2026년 결의전표 **2,947건 전량 이메일형 `INSRT_USER_ID`**(고유 76명, 비이메일형 0건). 기존 `usr_master_s` 기반 자동매핑이 **추정에서 실측 확정으로** 승격됐고 매핑 로직 보정은 불필요했다. **② `GL_TYPE` — 사실상 상수**: 2026년 전량 **`03`** 단일값이라 선택지가 아니다. 화면 드롭다운을 `03 · 일반전표` 단일 옵션으로 확정하고 서버 기본값 `GL_TYPE_DEFAULT='03'`을 두었다(실측 근거 없는 코드는 임의로 추가하지 않는다). **③ `GL_INPUT_TYPE` — 입력경로 구분**: `TG` 2,177(74%)·`UX` 426·`AP` 245·`AR` 82·`FN` 9·`DP` 6·`AC` 2. 수기 결의 경로가 `TG`이며 나머지는 타 모듈 자동생성분이다(포털 초안이 대응하는 것은 `TG`). **④ `CONF_FG`**: 등록 직후 `U`(미승인) → 회계 승인 시 `C`. 2026년 C 2,719 / U 228이고 최근 샘플 10건은 전부 `U`였다. **⑤ `TEMP_GL_NO` 형식 — 검증 신설**: `TG`+YYYYMMDD+4자리 순번 **14자 고정**(2026년 전량 동일, 예 `TG202608030001`). 이 실측을 근거로 Edge Function `jeil-gl-draft`에 **형식 검증(`erp_no_format` 400)을 신설·배포(v1→v2 ACTIVE)** 했다 — 직전 테스트에서 전표번호로 `"1"` 같은 임의값이 그대로 확정 처리되던 공백을 막는 조치다(기존엔 빈 값 여부와 타 초안 중복만 검사). 부수 확인: 부서코드 4자리 체계, 사업장 `BA1` 단일. **잔여**: A5(회계 규칙 — 경비 계정 후보·상대계정·부가세·마감 기준) · S5 E2E 테스트 1건 + 서버 검증 우회 시도 · 실사용 직전 `/security-review`. §4·§6·§7 갱신 | 최동혁(+Claude) |
| 2026-08-03 | **회계 코드 마스터 실적재 완료 + 연동 현황 반영 + 권한 전체관리자 한정(v0.19)** — 관리자 승인(A1)으로 결의전표 파일럿의 최대 차단 요인이 해소됐다. ① **실적재**: `acct_master` **691/691**·`cost_center` **133/133**(추출=적재, 유실 0, `etl_meta.batch_run` success 2건). dry-run 수치·테이블 사전 수치와 정확히 일치. 적재 범위는 **코드 목록뿐** — 금액·잔액·전표·분개 라인은 여전히 미적재다. ② **연동 현황 반영**: `public.v_erp_sync_overview`를 11→**13종**으로 확장해 회계 마스터 2종을 등재했다(마이그레이션 `erp_sync_overview_accounting_master`). **주의점 — 실테이블 `count(*)`를 쓰지 않고 `batch_run` 성공 건수로 표시**했다: 이 뷰는 `security_invoker`인데 회계 마스터 2종은 설계상 `authenticated` GRANT가 없는 fail-closed 구성이라, 실테이블을 count하면 사내 사용자에게 permission denied가 나 **연동현황 화면 전체가 403**이 된다(2026-07-21 `iv_dtl` 사고와 동일 유형 — 급여 집계 선례를 따라 회피). 권한을 열어 우회하지 않았다. ③ **권한(A3·A4) 관리자 결정**: 테스트 단계이므로 **`accounting` 모듈을 어느 부서에도 부여하지 않고 전체관리자만 사용**한다 — `dept_erp_scope` 무변경. 실측 재확인: 비관리자 2명 `gl_draft` `allowed=false`·`accounting` 미보유, 관리자만 `allowed=true`(사유 "전체 관리자"). ④ `erp_load_scope`의 `accounting/acct_master`·`cost_center`를 `loaded`로 기록(챗봇 결측 배지·데이터 요청 완료 게이트의 판정 근거). ⑤ **검증**: `gl_master_get()` 계정 691·코스트센터 133·부서 107 정상 반환 → 화면의 "마스터 미적재" 배너·`master_empty` 503이 해소됐다. 계정 구성은 BS 268·PL 235·MC(제조원가) 184·미분류 4, 차대구분 DR 507·CR 184로 화면의 차대 자동제안이 동작한다. **발견 1건(임의 조치 없음)**: `project_fg`가 **691행 전량 NULL** — 서버 검증 7종 중 5번(프로젝트 필수 계정 강제)이 현재 어떤 계정에도 걸리지 않는 무력 상태다. ERP 원천 `A_ACCT.PROJECT_FG`가 비어 있거나 프로젝트 필수 여부가 다른 컬럼·다른 체계로 관리될 가능성이 있어 A5(회계 규칙)·유니포인트 확인 대상으로 등재한다. §1·§4·§5·§6·§7 갱신 | 최동혁(+Claude) |
| 2026-07-31 | **결의전표 초안 입력 파일럿 — 회계(A 모듈) 첫 개방, 기반 구축 완료(v0.18)** — "ERP 결의전표를 전 직원이 각자 입력하는데 케이스 1개를 사내 포털에서 동일 형태로 입력받게 하자"는 관리자 요청을 받아 기반을 구축했다(**실적재·권한부여는 승인 대기**). ERP 스키마 실측으로 `A_TEMP_GL`(결의전표 헤더 28,219행)·`A_TEMP_GL_ITEM`(라인 294,043행)이 결의전표이며 승인 시 `A_GL`로 전기됨을 확정. **아키텍처 "초안은 포털, 확정은 ERP"(관리자 결정)** — 포털은 ERP에 쓰지 않고(CLAUDE.md §1.2·§1.6), 회계 담당자가 ERP 저장 후 `TEMP_GL_NO`를 회기입해야 초안이 닫힌다(DB 제약 `gl_draft_posted_needs_no`로 강제 — 이중입력 방지). **등록자 매핑**: 그룹웨어 전자결재가 전표등록자 기준이라 MS 계정과 일치해야 하는데, `usr_master_s` 99명 전원 이메일형 USR_ID로 확인돼 `lower()` 비교 자동매핑 성립(대문자 1건 존재), 매핑 실패 시 저장 거부. **구축물**: ①ETL job `acct_master`(`A_ACCT`)·`cost_center`(`B_COST_CENTER`) — 코드 마스터만(전표·분개 라인 미적재), dry-run 691행·133행 검증 완료, 마스터 전용 RPC `erp_master_upsert` 분리 신설(기존 `erp_etl_upsert` 무수정). ②포털 DB `gl_draft`·`gl_draft_item`·`gl_draft_log`·`gl_period_lock` + `erp_ro.acct_master_s`·`cost_center_s`, 전부 RLS ON·정책 0개·anon/authenticated 회수. ③Edge Function `jeil-gl-draft` v1(op 9종, 서버 재검증 7종). ④화면 `pages/자금_결의전표_입력_2026.html`(표준 그리드 + 차대 합계바 + 회계 담당자 탭). ⑤`portal_page.gl_draft`(`erp_module='accounting'`). **실측**: 권한격리(관리자만 허용·비관리자 3명 전원 차단)·인증게이트(무토큰/위조토큰 401) 통과. **잔여(전부 사람 결정)**: A1 회계 마스터 조회 승인→실적재 / A2 `A_TEMP_GL` 샘플 조회 승인(등록자 ID 형식·`GL_TYPE` 실코드) / A3 `accounting` 모듈 신설·부여 / A4 파일럿 대상자 / A5 회계 규칙(경비 계정·상대계정·부가세·마감 기준). 정본 SQL `실제구축준비 자료/이관/sql/21_gl_draft.sql`(마이그레이션 `gl_draft_v1`·`gl_draft_master_rpc`). §1·§4·§5·§6·§7 갱신 | 최동혁(+Claude) |
| 2026-07-24 | **야간배치 수동 실행 — 12개 job 전량 성공 + `bp_master` 적재버그 RCA·수정·재성공(v0.17)** — 최종배치(07-21/07-22) 이후 누적된 3~4일치 미실행분을 관리자 직접 실행으로 일괄 반영했다. 결과: usr_master 7/7·dept_master 351/351·usr_erp_module 397/397·hr_payroll(민감) 165/165·pur_order 118/118·pur_req 252/252·item_master 80/80·sales 37/37·iv_dtl 6/6·purchase 152/152·inventory 1,871/1,871·bp_master 3/3(전량 성공, 실패 0). 최종 건수(증가분): pur_order 3,137(+49)·pur_req 4,136(+72)·item_master 58,589(+56)·iv_dtl 1,984(+3)·inventory 1,871(+24)·hr_payroll 165(+22)·purchase 152(+1)·usr_master 99(값 7건 갱신)·나머지(sales·dept_master·usr_erp_module) 변동없음. **`bp_master`는 최초 HTTP 400(PostgreSQL 42601 컬럼-값 개수 불일치)으로 실패** — 원인은 `erp_etl_upsert`의 `bp_master_s` 분기에서 주민번호 마스킹 case식 뒤 인라인 `--` 주석이 같은 줄의 `x.repre_nm, x.bp_type, x.bp_group` 값 3개를 통째로 주석 처리해 대상 컬럼 20 vs 값 17 불일치가 생긴 것(2026-07-22 최초 4,649행 적재는 이 마스킹 코드 이전 버전이라 성공, 이후 회귀). 주석을 세 컬럼 뒤로 이동해 함수 재생성(마스킹 기능 동일, DB 현재 정의를 읽어 깨진 한 줄만 치환) 후 재실행해 3/3 성공, 로컬 기록 `실제구축준비 자료/이관/sql/20_erp_etl_upsert_bp_master_fix.sql` 신설. 부수개선: `10_ERP_DB연계/etl/etl_run.py`의 `rpc()`가 HTTP 오류 응답 본문을 예외·`batch_run.error_msg`에 실어 진단 가능하게 개선. 기존 문서의 "bp_master ODBC 미실행" 기록은 실제로는 07-22 최초 적재(4,649행) 후 07-24 증분(3행)까지 반영된 것으로 **소급 정정**. **후속 조사 필요 이슈 2건 등재**: ①`v_erp_data_asof.rows_upserted`가 최신 배치를 반영 못하는 정황(뷰 정의 결함 가능성, 조사 필요) ②`erp_etl_upsert` 함수 전체 정의·`bp_master_s` DDL이 번호 마이그레이션으로 미승격(이번엔 픽스 한 건만 20번 파일 기록, 전체 승격은 후속 과제). §1·§4·§5·§6·§7 갱신 | 최동혁(+Claude) |
| 2026-07-22 | **협력사 거래처마스터(`vendor_master`) 재적재·정합화 + ERP 거래처마스터 원천 연동(`bp_master`) 준비(v0.16)** — 사용자 제보(협력사 정보 화면에 발주거래처 미표시)를 조사한 결과 `vendor_master`가 초기 데모 시드 17개사에 머물러 ERP 실제 발주거래처 103개사 중 87개사가 미등록이었음을 확인, 발주·매입·매입상세 합집합 기준 **109개사**로 재적재(거래처명 5건 교정, 발급 계정 4건 무영향·고아 0건). 기존 17건의 사업자번호는 거래처코드 기반 임의 생성값(가짜)이었음을 확인해 제거(NULL). 신규 스키마 `erp_ro.bp_master_s`(ERP `B_BIZ_PARTNER` 4,621건 복제용, RLS internal+authenticated SELECT)를 신설했으나 **대표자주민등록번호·은행계좌번호는 개인정보·금융정보 최소수집으로 적재 제외**, ETL job `bp_master` 준비했으나 ODBC 접속 필요로 **미실행**(관리자 실행 대기). 관리자 전용 RPC `public.vendor_master_sync()` 신설(비관리자 차단·멱등성 검증) + 관리자 화면 "거래처 동기화" 버튼 추가. 부수 효과로 협력사 모바일 포털·계정발급 Edge Function 거래처명도 정상화. 라이브 검증 완료(커밋 `c11d632`). §1·§4·§5·§7 갱신 | 최동혁(+Claude) |
| 2026-07-22 | **중간DB(erp_ro) 정합성 SQL 검증(v0.15)** — S3041-00065 챗봇 오답 이슈의 후속으로, 사용자 우려("Supabase 중간DB가 실제 ERP와 안 맞게 연결된 것 아닌가")에 SQL 실측으로 답했다. **결론: 적재 정합성 이상 없음.** item_code NULL·품목마스터 고아·번호형식 위반·PK 중복 모두 전건 0건(발주 3,088·구매요청 4,064·매입상세 1,981·품목 58,533행 대상). 발견 2건(발주→구매요청 고아 49건=2025년 PR 참조로 범위한계의 정상 귀결, po_no+item_code 중복 31건=동일 PO 내 po_seq 분리로 ERP 원본 구조 보존)은 조사 결과 오류 아님으로 확인. `etl_meta.batch_run` 최근 12건 전부 success·rows_read=rows_upserted(누락 0). **잔여 과제**: ERP 원본(M_PUR_ORD/M_PUR_REQ) 직접 대사는 ODBC 접속 설정 읽기 권한 거부로 미실행 — 관리자 `--dry-run` 직접 실행 필요. 데이터 최신성(최종 적재 07-21, 당일 미반영)·범위 한계(2026년만 적재) 재확인. §1·§4·§5·§6·§7 갱신 | 최동혁(+Claude) |
| 2026-07-22 | **챗봇 ERP 조회 정합성 RCA + `get_erp_item_orders` 신설(v0.14)** — 사용자 제보(품목코드 `S3041-00065` 발주·구매요청 조회 실패/오인, "supabase 데이터가 ERP와 안 맞는 것 아닌가" 우려) 접수·조사. SQL·코드 실측 결론: **데이터·매핑 정상, 모델 이슈 아님 — 챗봇 도구 설계 공백이 원인**. `erp_ro`에 해당 품목의 발주·구매요청이 매핑까지 정확히 적재돼 있었으나, 품목코드로 발주/구매요청을 역조회하는 도구가 없어(`v_erp_po_pr_link`에 item_code 컬럼은 있으나 기존 도구가 po_no/pr_no로만 필터, `get_erp_item`은 품목 마스터만 반환) "없음"·발주번호 오인 답변이 나온 것으로 확인. 조치: 신규 도구 `get_erp_item_orders(item)`(품목코드/명→구매요청·발주·매입 이력, pur_order 모듈 권한 강제) 배포 + SYSTEM_PROMPT 번호형식 구분·`get_erp_po_pr` 입력가드 보강, `jeil-chat` **version 26**. 데이터 범위 한계(erp_ro 2026년만 적재) 명시. §1·§4·§5·§7 갱신 | 최동혁(+Claude) |
| 2026-07-21 | **매입 상세(`iv_dtl`) 신규 연동 + 구매요청 상세 백필 — 실적재 완료 + 매입 대사 점검·5종 재적재(v0.13)** — 같은 날 오전(v0.12)에 스키마·ETL·뷰만 준비해 뒀던 구매요청 상세 확충과 매입 상세 신규 연동을, 관리자가 직접 실행해 실적재까지 완료했다(`batch_run` 7건 성공·0건 실패). 신규 `iv_dtl`(`M_IV_DTL`): 2026-01-05~07-20 **1,981행**(0.9초), 송장 213건·발주 432건, 매입금액 합계 35.6억원, 발주 연결(`po_no`) 100%(발주 미연결 36건은 데이터 품질 참고 항목). `pur_req` 상세 14컬럼 백필: `--full` **4,063행**(2.1초), 조달구분 채움 99.98%(미채움 1건은 ERP 원천 NULL), 도면번호 856건. 매입 상세 적재 후 기존 월집계(`purchase_m`)와 대사해 6월 +19.2억·7월 +1.5억 차이 발견(1~5월 일치) — **원인은 적재 시점 차이**(7/8 이후 ERP 신규 등록분 미반영)로 확인, `purchase --full` 151행·`pur_order`(증분) 512행·`sales` 37행·`item_master`(증분) 415행·`inventory` 1,847행 재적재로 **매입 대사 차이 전월 0 해소**(매핑·로직 정상 검증). `hr_payroll`(민감)은 이번 대상에서 제외. §1·§4·§5·§6·§7 갱신 | 최동혁(+Claude) |
| 2026-07-21 | **연동 현황 뷰 `v_erp_sync_overview` 5→10종 전체 노출 + 관리자 콘솔 반영(v0.12)** — 연동 현황 화면(`app/erp-status.html`)·원천 뷰(`public.v_erp_sync_overview`)는 이미 가동 중이었으나 본 문서에 소급 등재(문서화 공백 해소). 뷰가 1차 5종(발주·품목·매출·매입·재고)만 노출하던 것을 마이그레이션 `erp_sync_overview_all_sources`로 **10종 전체**(신규: 구매요청 `pur_req`·부서마스터 `dept_master`·사용자마스터 `usr_master`·ERP메뉴권한 `usr_erp_module`·급여집계 `hr_payroll`)로 확장, `sensitive`·`sort` 컬럼 추가(급여는 실테이블 미count·배치 적재건수만 노출, `security_invoker=true` 유지). 발주 원천 표기를 `M_PUR_ORD_HDR + M_PUR_ORD_DTL`로 정정. `jeil-chat-admin` **version 9 → 10** 배포로 관리자 콘솔 'API' 탭에 실데이터 표 신설(`04_챗봇_포털_데모UI.html`), `app/erp-status.html`에 연결 정보 표(원천·접속방식·적재경로·실행방식·접속정보 보관위치·데이터 격리, 비밀값 미기재) 추가. 현재 적재 실측(2026-07-21): erp_ro 10종(`dept_master_s` 351·`inventory_d` 1,810·`item_master_s` 58,256·`pur_order_s` 2,703·`pur_req_s` 3,784·`purchase_m` 140·`sales_orders_m` 32·`table_dict` 2,658·`usr_erp_module_s` 397·`usr_master_s` 99), erp_secure(`hr_payroll_m` 143·`hr_access_log` 20). 일부 소스(품목·발주·매입·매출)는 2026-07-07~08 문서화 당시보다 건수가 늘어 있으나 개별 재적재 이벤트가 작업로그에 없어 §6에 관리자 확인 요청으로 등재. §1·§4·§5·§6·§7 갱신 | 최동혁(+Claude) |
| 2026-07-20 | **챗봇(jeil-chat) 권한체계 연결 + 포털 도구 권한 구멍 차단 — version 18 → 19 배포**(v0.11) — 관리자 계정이 "2026년 인원현황"·"권한 확인"을 챗봇에 물었을 때 오답이 나온 원인 조사: 권한 체계(`jeil-me`/`jeil-hr`/`dept_erp_scope`/`portal_page`)·인원 데이터(`erp_secure.hr_payroll_m` 2026-01~06)는 이미 정상 구현·적재 완료였고, **챗봇이 이 체계에 연결돼 있지 않았던 것**이 원인. 신규 도구 3종 배포: `get_my_access`(본인 역할·부서·ERP모듈·접근페이지 반환, 전 직원, UPN 고정) · `get_hr_headcount`(전사 총원은 전 직원, 부서별 분포는 payroll 권한자만) · `get_hr_payroll`(급여 집계, 인사팀·관리자 전용, `hr_payroll_get` RPC 경유, 감사는 `hr_access_log` 재사용). **소급 발견 권한 구멍 차단**: 기존 포털 도구 3종(`get_order_summary`·`get_order_detail`·`get_inspection_pending`)이 `ERP_TOOL_MODULE` 매핑 누락으로 전 직원에게 협력사 발주정보를 무강제 노출 중이던 것을 `pur_order` 모듈 권한 강제로 차단(2026-07-08 v14의 ERP 도구 8종 매핑에는 이 3종이 빠져 있었음). SYSTEM_PROMPT 보강(권한질의는 반드시 `get_my_access` 실조회, 추정·역산 금지). 관리자 결정: 전사총원 전 직원 공개·부서분포는 인사팀·관리자 전용, 협력사 발주 챗봇 조회는 기존 `pur_order` 모듈로 통합. 경영 판단 보류 항목(부서별 ERP 모듈 차등 미실시) §6에 등재. 배포는 `verify_jwt=false` 유지, 소스-배포본 일치 검증. 소스(`supabase/functions/jeil-chat/index.ts`)는 수정·배포 완료, git 커밋은 관리자 별도 진행(미완료). §1·§4·§5·§6·§7 갱신 | 최동혁(+Claude) |
| 2026-07-13 | **재고 입출고(inventory) ETL — 롤링 31일 → 2026년 전체 적재 전환 + 재적재 완료**(v0.10) — 1차 5종 ETL 중 `inventory` job(`M_PUR_GOODS_MVMT`)만 롤링 31일(`daily_start`) 창으로 남아 있어 최근 6·7월만 적재돼 있던 결함을 발견, 발주·매출·매입 job과 동일한 `WHERE MVMT_DT >= ? AND MVMT_DT < ?` + `year_start`/`year_end` 파라미터로 수정(커밋 `811cbd8`, main 배포). 관리자 직접 실행으로 재적재 완료 — `erp_ro.inventory_d` 2026-01~07 전체 **1,810행**(월별 01:93·02:134·03:149·04:260·05:469·06:650·07:55, 이전엔 06·07월만 659행). `etl_meta.batch_run`: job_name=inventory, status=success, rows_read=1810, rows_upserted=1810(2026-07-13 07:54 UTC). 참고: 발주현황 보드 입고/매입 상태는 `pur_order_s`(po_sts·rcpt_qty) 기반이라 이미 연중 전체 반영돼 있었음 — 이번은 재고 일집계(`inventory_d`) 백필. §1·§4·§5·§6·§7 갱신 | 최동혁(+Claude) |
| 2026-07-08 | **인사 급여 집계 실적재 완료 + 게이트 API `jeil-hr` 배포**(v0.9, 관리자 직접 실행) — 승인-준비 상태였던 `erp_secure.hr_payroll_m`에 실제 데이터를 적재했다: ETL job `hr_payroll`(`HDF070T` 월×부서·`HGA070T` 월×전사) 확정·가동 → **143행**(급여 137+퇴직 6, 2026.01~06). 컬럼은 `ym·dept_nm·headcount·pay_tot_amt·retire_amt`뿐 — 개인별·이름·주민번호·계좌 미포함(집계만). 게이트 API `jeil-hr`(Edge Function) v1 배포(Entra 검증→`dept='인사팀'`/`portal_admin`만→감사→집계 반환, 그 외 403), service_role 전용 RPC 3종(`erp_secure_upsert`·`hr_payroll_get`·`hr_access_log_add`) 신설. 인사 페이지는 인사팀 권한자에게 실 집계 표시. 잔여: 자금(F 모듈) 경영 승인·실적재 미착수, `AI-Portal-HR` Entra 보안그룹 미신설, 유니포인트 재직상태 정의 확인. 정본 SQL은 기존 `이관/sql/14_erp_secure_hr.sql` 재사용(ETL job만 추가). §1·§4·§5·§6·§7 갱신 | 최동혁(+Claude) |
| 2026-07-08 | **챗봇 ERP 프로세스(발주·구매요청·매입·입고) 응답 품질 개선 — 실적용·배포**(v0.8) — 중간DB 노출 뷰 3종 보강: `v_erp_po_pr_link`·`v_erp_pur_req`에 발주 입고수량·구매요청 입고/매입수량·외주구분·납기경과(미입고) 플래그·연결수주·요청부서(요청자 이메일→부서 매핑으로 보완) 컬럼 append(기존 컬럼 순서·타입 보존, 행수 2,703 불변 검증), 발주번호 단위 총액집계 신규 뷰 `v_erp_pur_top_po` 추가(정본 SQL `이관/sql/15_erp_process_link_enrich.sql`). `jeil-chat` Edge Function은 **[치명적 버그 수정]** 도구 호출 오케스트레이션을 1라운드→상한 멀티라운드(`MAX_ROUNDS=4`)로 교체(포털 도구 헛짚음 후 재조회 불가로 안내문만 반복되던 사용자 루프 해소, 무한루프 3중 차단), 상태코드(RQ/CF/PO/GR/IV) 한글 해석 주입, 신규 도구 2종(`get_erp_receipt_pending`·`get_erp_pur_req`) 추가 배포(**version 14 ACTIVE**). 실측으로 재고 입고·재고량 미적재/매출 수주·수금 미매핑/발주 외주구분 전건 'N' 등 ETL 데이터 갭 확인(§6 결정대기 추가). §1·§4·§5·§6·§7 갱신 | 최동혁(+Claude) |
| 2026-07-08 | **급여(민감) 권한 강화 + `erp_secure` 착수(승인-준비)**(v0.7) — 급여를 민감 모듈 `payroll`로 신설, `dept_erp_scope` 인사팀 전용 부여·인사 페이지 서버 게이트(비인사팀 직접 접근 차단), 관리자 콘솔 급여 ⚠ 민감표시(일괄/제안 제외). 인사 데모 페이지 개인급여 샘플화+마스킹(인사팀 권한자만 해제). 민감 분리 스키마 `erp_secure`(`hr_payroll_m` 집계·`hr_access_log` 감사) 신설(REST 비노출·service_role 전용) — **승인-준비 빈 구조, 실 데이터 미적재**. 커넥터 검증으로 인사 테이블(`HAA010T`·`HDF070T`·`HGA070T`·`TH101BA1`) 실존 확인, 컬럼 스펙은 유니포인트 확인 필요. 정본 SQL `이관/sql/13_payroll_module.sql`·`14_erp_secure_hr.sql`. §1·§4·§5·§6·§7 갱신 | 최동혁(+Claude) |
| 2026-07-08 | **부서별 ERP 모듈 제안값(참고) 신설·실적재 완료**(v0.6) — 신규 ETL job `usr_erp_module`로 ERP 역할·메뉴 권한(`Z_USR_MAST_REC_USR_ROLE_ASSO`→`Z_USR_ROLE_MNU_AUTHZTN_ASSO`→`Z_CO_MAST_MNU`)을 근거로 `erp_ro.usr_erp_module_s` 397건 실적재, 뷰 `v_dept_erp_suggest`/`v_erp_dept_erp_suggest`로 관리자 콘솔(04) '권한 상세' ERP 모듈 매트릭스에 ◆ 참고표시 반영(관리자 결정: 자동 덮어쓰기 안 함, 실질 통제선은 편집형 `dept_erp_scope`). 실측 결과 ERP-역할 신호는 부서 변별력이 약함(광범위 권한)을 확인 — 참고 제안 결정의 타당성 입증. 정본 SQL `이관/sql/12_erp_dept_module_suggest.sql`. §1·§4·§5·§7 갱신 | 최동혁(+Claude) |
| 2026-07-08 | **사용자-부서-사원 매핑·대사 실적재 완료 + 정책 확정**(v0.5) — 관리자 직접 실행으로 `erp_ro.dept_master_s` 351건·`erp_ro.usr_master_s` 99건 실적재 완료. 대사 결과 정상 매핑 89건·불일치 10건(부서불일치 6·상태이상 3·테스트 1) 확정. **불일치 자동제외 정책 확정**(`v_erp_user_dept`에 부서일치 조건 추가, 마이그레이션 `erp_user_dept_exclude_unmatched`, 정본 SQL `09_erp_user_dept_mapping.sql §3-2`) — 이전 "부서명 4개 처리방침" 결정 대기는 해소, 상태이상 3건 현업확인만 잔류. §1·§4·§5·§6·§7 갱신, `05_사용자부서_매핑대사.md` §3·§7·§8·§9 갱신 | 최동혁(+Claude) |
| 2026-07-08 | **사용자-부서-사원 매핑·대사 계층 신설**(v0.4) — `Z_USR_MAST_REC`(이메일 SSOT)↔`B_ACCT_DEPT`(부서) 대사: 원천 미러 2종·파싱/대사 뷰 5종·노출 뷰 3종(`v_erp_user_dept`·`v_erp_user_dept_recon`·`v_erp_dept_roster`)·ETL job 2종(`usr_master`·`dept_master`) 구축(정본 SQL `이관/sql/09`, 마이그레이션 `erp_user_dept_mapping`). 대상 99건 중 부서명 25/29 일치, 불일치 4개(해외법인·외부회계·재무 세분화·admin) 처리방침 결정 대기. 실적재는 미실행(0행). 전용 관리 문서 `05_사용자부서_매핑대사.md` 신설. §1·§4·§5·§6 갱신 | 최동혁(+Claude) |
| 2026-07-08 | **P4(포털·챗봇 연결) 착수·부분 완료**(v0.3) — public 스키마에 사내 전용 노출 뷰 6종(`v_erp_*`, `security_invoker`) 신설(erp_ro/etl_meta는 REST 비노출 유지) + RLS 격리 실측 테스트(JWT 시뮬레이션) 통과(사내만 정상, 협력사·anon 차단). 이 위에서 `app/lib/api.js`(`erpApi`)로 `pages/구매_거래처별매입집계_2026.html` 1건 파일럿 실연동, 챗봇 `jeil-chat`에 ERP Tool 4종 배포(version 7 ACTIVE). §1·§4·§5·§6 갱신, `01_연계기획 §4.4`·`03_중간DB_구축실행기획` §7-C에도 반영 | 최동혁(+Claude) |
| 2026-07-08 | ERP→중간DB dry-run 재확인 — 원천 5종 최신 건수(발주 2,702·품목 58,249·매출 10·매입 63·재고 659)가 2026-07-07 적재분(2,699/58,246/10/63/656)보다 소폭 증가한 것을 확인. 최신 재적재는 관리자가 `!`로 직접 실행하기로 결정(명령 준비, 미실행) | 최동혁(+Claude) |
| 2026-07-07 | **연계 담당 에이전트 2종 체계 확립** — `erp-db-link-manager`(문서·미러 관제, 기존)와 `erp-db-connector`(ERP운영DB 연결·연결테스트·`table_dict`·ETL dry-run/실적재·`batch_run` 점검, 신규) 역할 분리. 배포킷 `10_ERP_DB연계/agent_kit/` 신설로 ERP_DB 작업환경에도 connector 설치 가능 | 최동혁(+Claude) |
| 2026-07-07 | 발주(`M_PUR_REQ`·`M_PUR_ORD`) 2026년도 데이터 우선연동 요청 접수 반영(§4 매핑 비고·§6 결정대기 추가). 실행은 유니포인트 협의·`ERP_DB_CONN` 입력·ETL 개발 이후로, 본 관제 에이전트는 문서화·요청 목록 제시까지만 수행 | 최동혁(+Claude) |
| 2026-07-07 | `SUPABASE_SERVICE_ROLE_KEY` 입력 완료 → `load_table_dict.py` 실행 성공, `erp_ro.table_dict`에 ERP 테이블 사전 2,658건 적재(컬럼 문서화 452개·11개 모듈) 완료 반영. `ERP_DB_CONN`은 여전히 미입력이라 1차 4종 실적재(`etl_run.py`)는 대기. §1·§4·§6 갱신 | 최동혁(+Claude) |
| 2026-07-07 | 중간DB 스키마 적용 완료 반영(v0.2) — Supabase에 `erp_ro`(1차 4종+`table_dict`)·`etl_meta` 스키마·RLS·적재 RPC 적용, ETL 스크립트(`10_ERP_DB연계/etl/`) 작성. 실적재는 `.env` 키 입력 대기로 미실행. §4·§5·§6 갱신 | 최동혁(+Claude) |
| 2026-07-03 | 현재상태 스냅샷 최초 작성(v0.1) — 원천·접근자산·포털측·정책문서·결정대기 정리. `10_ERP_DB연계/` 관제 체계 + 정책관리 미러 신설. | 최동혁(+Claude) |
| 2026-07-03 | 중간DB 구축 실행기획 v0.1 신설 — D1(중간DB 위치)·D2(ETL 러너)·D3(추출 원천) 결정안, DDL 4종+`etl_meta`, ETL 처리방법, 준비사항 체크리스트, 6주 일정 정리. §5 문서 현황·§6 결정 대기 추가. | 최동혁(+Claude) |
