# 03. 이관 진행 상태 (추적표)

> **최종 갱신: 2026-06-29** · 관리: 최동혁
> 이 문서는 **작업할 때마다 갱신하는 진행 로그**다. 단계 완료 시 체크하고, §3 작업로그에 날짜·내용을 남긴다. 시스템 상태가 바뀌면 `00_현황스냅샷`도 함께 갱신한다.

---

## 1. 현재 위치

```
[1] GitHub Pages ───►███ [2] Vercel+Supabase ───────► [3] Azure 운영
   (정적 데모)          데이터화 (진행 중)              (미착수)
```

- **현재 페이즈: [2] 데이터화 — 문서/계획 수립 완료, 구현 착수 전**
- 계정: Vercel ✅ 생성 · Supabase ✅ 생성 / 프로젝트 ⬜ 미생성

---

## 2. 단계별 체크리스트

### 사전 (문서·전략)
- [x] 배포 전략 확정(`08/03_실구축기획/07`)
- [x] 이관 관제 문서 세트 작성(본 `이관/` 폴더)
- [x] 현황 대장 작성(`00_현황스냅샷`)
- [x] 실행 가이드 작성(`01_실행가이드`)
- [x] Azure 계획 작성(`02_Azure이관계획`)

### A. 저장소 준비 (`01 §A`)
- [x] `.gitignore` 보강(.env/키/node_modules/.vercel) — 2026-06-29
- [x] `.env.example` 추가(키 이름만, Supabase URL 주석) — 2026-06-29
- [ ] (선택) 디렉터리 분리 demo/app/api/docs

### B. Supabase (`01 §B`)
- [x] 프로젝트 생성(Free) — `dvzohdqtjzocgcclgwro` (URL: https://dvzohdqtjzocgcclgwro.supabase.co)
- [x] 마이그레이션 SQL 작성 — `sql/01_schema_rls.sql` (테이블·헬퍼·RLS·Realtime·Storage 일괄)
- [x] 스키마/RLS **적용** — 대시보드 SQL Editor 수동 (헬퍼 함수 `auth`→`public` 수정 후 성공) 2026-06-29
- [x] 검증(`sql/02_verify.sql`) — 객체 생성 확인

### C. 인증 (`01 §C`)
- [x] **RLS 격리 테스트 [A]~[E] 전부 통과** (MCP 직접 실행) — 2026-06-29
- [x] 보안 어드바이저 **클린** — 함수 search_path 고정 + `rls_auto_enable` 실행권한 회수
- [x] Custom Access Token Hook **함수 생성**(`sql/04_auth_hook.sql`)
- [ ] Hook **대시보드 활성화** — Authentication → Hooks → Customize Access Token (사용자 작업)
- [ ] 협력사 Supabase Auth(이메일/매직링크) + 테스트 계정 app_metadata
- [~] **사내 Entra OIDC → Supabase 브리지** — **프론트 코드 적용 완료**(`admin-vendors.html` Azure SSO 로그인 버튼 + `supabaseClient.js` PKCE/detectSessionInUrl) 2026-06-29. **대기(관리자/대시보드)**: ① Entra 앱에 웹 redirect `https://dvzohdqtjzocgcclgwro.supabase.co/auth/v1/callback` + client secret VALUE 확인/발급 ② Supabase Authentication→Providers→Azure 활성화(client id `ffb54d0e…`, tenant `c877a817…`, secret) ③ URL Configuration Site/Redirect URLs 등록

### C′. 협력사 온보딩 (이메일 가입 → 승인) ★신규
- [x] RLS 전수 재점검 — 7→**10 테이블** 모두 RLS ON + 정책(어드바이저 클린)
- [x] `vendor_master`(ERP 협력사 마스터) + `vendor_application`(가입신청) 스키마 + RLS
- [x] ERP `B_BIZ_PARTNER` **17개사 데이터화**(상호 부분마스킹·사업자번호 데모값)
- [x] 가입 시 신청서 자동생성 트리거(`handle_new_vendor_signup`)
- [x] 승인 Edge Function `approve-vendor` 배포(service_role, 관리자 JWT 검증)
- [x] 페이지: `app/vendor-login.html`(협력사 가입/로그인), `app/admin-vendors.html`(관리자 승인) — 대시보드와 별개
- [x] Hook 활성화 + 관리자 계정 `dh.choi@jeilm.co.kr` 생성 — Hook이 role=internal 자동주입 검증 완료
- [x] 페이지 권한 판정을 JWT 디코드 방식으로 보강(getUser 대신 access_token 클레임)
- [x] **사내 포털(`04_챗봇_포털_데모UI.html`) 운영페이지에 카드 통합** — "협력사 계정 관리(관리자)" + "협력사 가입·로그인(신규)", 대시보드·협력사 모바일 포털 옆에 배치
- [x] **임시 권한정책(권한분리 전): 관리자페이지 프론트 게이트 완화** — `admin-vendors.html` `role===internal` 차단 제거, 로그인 사용자 전체 기능 노출. **백엔드 RLS·`approve-vendor` Edge Function은 `role=internal` 검증 유지(안전망)**. 04 카드 문구도 "사내 로그인(임시 전체)"으로 갱신 — 2026-06-29 (TODO: 권한분리 시 게이트 복원)
- [ ] E2E 검증: 협력사 가입 → 인증 → 관리자 승인 → 협력사 발주 접근
- [x] **가입신청 미표시 원인 규명·조치(2026-06-29)**: `vendor_application`=0건. 원인 = Auth `mailer_autoconfirm=false` + `smtp_host=none` → 외부 협력사 인증메일 발송 불가로 signUp 실패. RLS·트리거는 정상(`vendor_master`=17건, `on_auth_vendor_signup` 설치됨). **조치 완료: 데모용 이메일 자동확인 ON(`mailer_autoconfirm=true`, 사용자 위임 승인) + vendor-login 안내문구 자동확인 전제로 수정.** ⚠️운영 전환 시 SMTP 연결 후 이메일 인증 복원 필수
- [ ] gh-pages 배포 시 `app/` 동반 필요(F단계) — 사내포털에서 카드 링크 동작
- [ ] **사내 Entra→Supabase OIDC 브리지**(C절) — 미연결 시 관리자페이지는 별도 Supabase 로그인 필요(현재 사내 계정은 dh.choi만 존재). SSO 사용자 전원 무로그인 진입은 이 브리지 후 가능

### C″. 관리자 주도 등록 모델 전환 (2026-06-29) ★ 현행
> 자유가입+승인 → **ERP 거래처 기반 관리자 발급**으로 전환. (자유가입 SMTP 오류 회피 + ERP 원천 일치)
- [x] 스키마 `sql/06_vendor_provisioning.sql`: `portal_admin`(권한자)·`vendor_account`(거래처↔계정)·`vendor_account_log`(감사로그) + `is_vendor_admin()` 헬퍼, 자유가입 트리거(`on_auth_vendor_signup`)·함수 제거. 부트스트랩 권한자=dh.choi
- [x] Edge Function 배포: **`vendor-provision`**(거래처+이메일→createUser+app_metadata+vendor_account+log), **`vendor-reset-password`**(비번 재발급+log). 둘 다 관리자 JWT(role=internal + portal_admin) 검증
- [x] `admin-vendors.html` 전면개편: ① ERP 거래처 발급 ② 비번 초기화 ③ 처리 로그(처리자·시각) ④ 관리자 권한 지정 ⑤ **Graph `/me/sendMail`**(SSO `provider_token`)로 계정 안내 메일 + 임시비번 화면표시 폴백. OAuth scope에 `Mail.Send` 추가
- [x] `vendor-login.html` 자유가입 제거 → **로그인 전용**(관리자 발급 안내). 04 포털 카드 2종 문구 갱신
- [x] 보안 어드바이저: `is_vendor_admin` invoker로 정정해 경고 해소(남은 1건 `leaked_password_protection`은 전역 Auth 설정, 데모 수용)
- [ ] **E2E**: SSO 로그인→거래처 계정발급→협력사 로그인(RLS 자기거래처)→비번초기화→로그 확인
- [ ] **선행(관리자)**: Entra 앱에 Graph `Mail.Send`(위임) 권한 + 관리자 동의 — 메일 자동발송용(없으면 화면표시 폴백)

### D. api.js (`01 §D`)
- [x] `app/config.js`·`app/lib/supabaseClient.js`·`app/lib/api.js` 작성 (supabase/mock 어댑터)
- [x] 데모용 발주헤더 `sp_order_header` 스키마 보강 + RLS + Realtime
- [x] **협력사 포털·대시보드 인라인 JS → `portalApi` 치환** — 2026-06-29. 협력사 포털: 자체 로그인 제거→세션 게이트, 자기 거래처 발주(RLS)·사진/메시지/검수요청 쓰기. 사내 대시보드: BASE mock 제거→getOrders 전체 발주, 검수판정→judge, 자동 SSO(internal), Realtime
- [x] **시드 후 브라우저 실동작 검증** — 대시보드 8건 표시 확인(dh.choi 세션). 협력사 포털 세션 게이트 동작 확인
- [x] getOrders 보강(vendor_name/proj/amt/inspection — 대시보드용)

### E. 데이터 시드 (`01 §E`)
- [x] **마스킹 샘플 발주 시드** — `sql/07_seed_orders.sql`: 02128(3)·00206(2)·00209/01768/4521(각1) 총 8건 + 상태·메시지·검수요청. RLS 테스트 잔여 정리. 2026-06-29

### F. 배포
- [x] **임시: GitHub Pages(gh-pages) 배포** — `ai.jeilm.co.kr`에 `app/` 포함 반영(2026-06-29, `705b8d9`). https라 Entra 2차인증·Supabase 연동 동작
- [ ] (Supabase Auth) URL Configuration — Site URL `https://ai.jeilm.co.kr`, Redirect URLs 등록(이메일 인증 링크용)
- [ ] 본 데모는 Vercel 권장(Hobby 비상업 유의) — 트래픽 붙으면 이전

### F-Vercel. Vercel 배포 (`01 §F`, 선택/후속)
- [ ] GitHub Import·자동배포
- [ ] 환경변수 등록(anon 프론트/service 서버)
- [ ] 데모 도메인 결정·연결
- [ ] HTTPS 동작

### G. 검증 (`01 §G` — 보안 게이트)
- [ ] RLS 격리 테스트 5종 통과
- [ ] `/security-review` 셀프 점검
- [ ] **관리자: Entra redirect URI 추가**(데모 호스트)

---

## 3. 작업 로그

| 일자 | 작업 | 결과 | 다음 |
|---|---|---|---|
| 2026-06-29 | 배포 전략 결정(Vercel+Supabase→Azure), 인증 분리(사내 Entra/협력사 Supabase) | `08/03/07` 신규 문서 + 요구사항 이력 ⑫ | 이관 관제 폴더 구축 |
| 2026-06-29 | `이관/` 관제 센터 구축 — 00 현황·01 가이드·02 Azure·03 진행·index 허브 | 문서 세트 완료 | A 저장소 준비 착수 |
| 2026-06-29 | A 저장소 준비(.gitignore·.env.example) + B 마이그레이션 SQL 작성(`sql/01_schema_rls.sql`) | A 완료 / B SQL 준비 | MCP 권한 블로커 → 적용 경로 결정 |
| 2026-06-29 | B 스키마/RLS 대시보드 수동 적용 성공(헬퍼 `auth`→`public` 수정). 데모 도메인=Vercel 기본으로 결정 | B 완료 | C 인증·RLS 격리 검증 |
| 2026-06-29 | MCP 로컬 연결(`.mcp.json` supabase-jeilax, project-ref 고정) + RLS 격리테스트 [A]~[E] 통과 + 보안 어드바이저 클린 | C 검증 완료 | Hook 활성화·협력사계정 → D api.js |
| 2026-06-29 | D 착수 — `sp_order_header` 보강 + `app/` 모듈(config·supabaseClient·api.js, supabase/mock 어댑터) 작성 | D 모듈 완료 | 화면 치환·시드·로그인 검증 |
| 2026-06-29 | C′ 협력사 온보딩 — vendor_master/application+RLS+트리거, ERP 17개사 시드, approve-vendor Edge Function, 가입/관리자 페이지 2종 | 온보딩 구축 완료 | 사용자: Hook·관리자계정·SMTP → E2E |
| 2026-06-29 | Hook 활성화·관리자계정(dh.choi) 검증, JWT 디코드 보강, 사내포털 04에 협력사 관리/가입 카드 통합 | 포털 통합 완료 | E2E 검증 / F 배포(app 동반) |
| 2026-06-29 | git 커밋·main/gh-pages 배포(`705b8d9`) — 협력사 온보딩·`app/`·이관문서 실서버(ai.jeilm.co.kr) 반영 | 라이브 배포 완료 | https에서 2차인증·협력사 온보딩 E2E |
| 2026-06-29 | Vercel 점검(.env VERCEL_TOKEN+REST API) — jeil-ax 프로젝트 main 자동배포 READY. **도메인 분리 확정: ai=GitHub Pages, ax=Vercel**(서버헤더) | 호스팅 분리 확인 | 데모=ax 사용 / ax Entra redirect 등록은 관리자 |
| 2026-06-29 | 임시 권한정책 — 협력사 관리페이지(`admin-vendors.html`) 프론트 게이트 완화(role 차단 제거, 로그인 사용자 전체 노출). 백엔드 RLS·Edge Function role=internal 유지. 04 카드 문구 갱신 | 권한분리 전 임시 적용(`17500be` 커밋) | Entra→Supabase 브리지 / 추후 권한분리 복원 |
| 2026-06-29 | **관리자 주도 등록 모델 전환** — 스키마 06(portal_admin/vendor_account/vendor_account_log), Edge Function 2종(vendor-provision/reset-password) 배포, admin 전면개편(발급·초기화·로그·권한지정·Graph메일), 자유가입 제거. autoconfirm ON(위임). 보안 어드바이저 클린 | 구현 완료 | E2E + Entra Mail.Send 동의(관리자) |
| 2026-06-29 | **발주 포털·대시보드 Supabase 연동** — 발주 샘플 8건 시드(`sql/07`), 협력사 포털(자체로그인 제거·세션게이트·자기거래처 RLS·쓰기), 사내 대시보드(BASE 제거·getOrders 전체·검수판정·자동SSO·Realtime), getOrders 보강. Email provider 재활성. 대시보드 8건 표시 검증. **ai 도메인도 Vercel 전환 확인** | 포털·대시보드 연동 완료 | 협력사 로그인 발주표시(사용자) |
| 2026-06-29 | **E2E 디버깅·검증(브라우저 직접)** — ①Custom Access Token Hook **비활성** 발견→재활성(role 주입 복구) ②Edge Function 권한판정을 `getUser` role(=DB meta, 비어있음)→**이메일+portal_admin** 기반으로 수정·재배포 ③프론트 JWT **base64url 디코드** 버그 수정 ④세션 없으면 **자동 SSO**. **계정발급·비번초기화·감사로그·권한게이트 전부 통과**(test@naver.com / 00206) | E2E 통과 | 운영 전 테스트계정 정리 + Entra Mail.Send 동의(메일발송) |
| 2026-06-29 | Entra→Supabase 브리지 **프론트 코드** — admin Azure SSO 버튼 + 핸들러, supabaseClient PKCE/콜백 자동세션. `.mcp.json` 손상복구(vercel-jeil 제거) | 코드 준비 완료(로컬) | 관리자/대시보드 설정 3종 → SSO 진입 검증 |

---

## 4. 이슈·블로커

| 상태 | 항목 | 비고 |
|---|---|---|
| 🟢 해소 | Supabase MCP 권한 | **프로젝트 로컬 `.mcp.json`(supabase-jeilax, PAT, project-ref 고정)로 연결 완료.** 이제 Claude가 직접 SQL 실행 가능 |
| 🟢 결정됨 | 데모 도메인 | **Vercel 기본 도메인**으로 진행. 추후 `ai-dev.jeilm.co.kr` 교체 가능 |
| 🟡 대기 | Entra redirect URI 추가(관리자) | 데모 호스트 확정 후 |
| ⚪ 예정 | Entra Client Secret 갱신 | 2026-12-09 만료 |

---

## 5. 의사결정 대기 (경영진/관리자)

- **D-도메인**: 데모 도메인 정책.
- **D1(협력사 인증)**: 운영 전환 시 Entra B2B vs 전용(`08/03/03`). 데모는 Supabase로 확정.
- **D-호스팅**: 운영 Azure 구성(`실제구축준비 자료/01`).
- **D-비용**: Azure 전환 예산 승인 시점.

---

## 6. 관리 규칙 (이 폴더 운영)

1. 시스템/배포/데이터/계정이 바뀌면 → **`00_현황스냅샷` 갱신** + §9 이력.
2. 작업을 하면 → **본 문서 체크리스트 체크 + §3 작업로그** 기록.
3. 새 결정/구조 변경 → 해당 가이드(`01`/`02`) 반영, 필요 시 `08/03/07`·`CLAUDE.md` 동기화.
4. 문서 수정 후 → `python _build_html.py`로 HTML 재생성.
5. 비밀값은 어떤 문서에도 적지 않는다(`CLAUDE.md §1`).
